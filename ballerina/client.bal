// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2026, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;
import ballerina/mime;

# GitLab REST API used to interact with a GitLab installation.
public isolated client class Client {
    final http:Client clientEp;
    final readonly & ApiKeysConfig? apiKeyConfig;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://gitlab.com/api/v4") returns error? {
        http:ClientConfiguration httpClientConfig = {httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        if config.auth is ApiKeysConfig {
            self.apiKeyConfig = (<ApiKeysConfig>config.auth).cloneReadOnly();
        } else {
            httpClientConfig.auth = <http:BearerTokenConfig|OAuth2RefreshTokenGrantConfig>config.auth;
            self.apiKeyConfig = ();
        }
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Retrieve namespace subscription
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + return - The retrieved namespace subscription 
    remote isolated function getNamespaceSubscription(IdOrPath id, map<string|string[]> headers = {}) returns GitlabSubscription|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/gitlab_subscription`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update the subscription for the namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the subscription for the namespace 
    # + return - The updated namespace subscription 
    # 
    # # Deprecated
    @deprecated
    remote isolated function updateNamespaceSubscription(IdOrPath id, UpdateNamespaceSubscriptionRequest payload, map<string|string[]> headers = {}) returns GitlabSubscription|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/gitlab_subscription`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create a subscription for the namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a subscription for the namespace 
    # + return - Created 
    # 
    # # Deprecated
    @deprecated
    remote isolated function createNamespaceSubscription(IdOrPath id, CreateNamespaceSubscriptionRequest payload, map<string|string[]> headers = {}) returns GitlabSubscription|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/gitlab_subscription`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a compute minutes purchase record for the namespace
    #
    # + id - The ID of a namespace
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a compute minutes purchase record for the namespace 
    # + return - Created 
    # 
    # # Deprecated
    @deprecated
    remote isolated function createComputeMinutesPurchase(string id, CreateComputeMinutesPurchaseRequest payload, map<string|string[]> headers = {}) returns CiMinutesAdditionalPack|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/minutes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Transfer purchased compute minutes packs to another namespace
    #
    # + id - The ID of the namespace to transfer from
    # + targetId - The ID of the namespace for the packs to transfer to
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    # 
    # # Deprecated
    @deprecated
    remote isolated function transferComputeMinutesPacks(string id, string targetId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/minutes/move/${getEncodedUri(targetId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns an add-on purchase for the namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + addOnName - The name of the add-on
    # + headers - Headers to be sent with the request 
    # + return - The retrieved add on purchase 
    # 
    # # Deprecated
    @deprecated
    remote isolated function getAddOnPurchase(IdOrPath id, string addOnName, map<string|string[]> headers = {}) returns GitlabSubscriptionsAddOnPurchase|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/subscription_add_on_purchase/${getEncodedUri(addOnName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update the add-on purchase for the namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + addOnName - The name of the add-on
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the add-on purchase for the namespace 
    # + return - The updated add on purchase 
    # 
    # # Deprecated
    @deprecated
    remote isolated function updateAddOnPurchase(IdOrPath id, string addOnName, UpdateAddOnPurchaseRequest payload, map<string|string[]> headers = {}) returns GitlabSubscriptionsAddOnPurchase|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/subscription_add_on_purchase/${getEncodedUri(addOnName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create an add-on purchase for the namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + addOnName - The name of the add-on
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an add-on purchase for the namespace 
    # + return - Created 
    # 
    # # Deprecated
    @deprecated
    remote isolated function createAddOnPurchase(IdOrPath id, string addOnName, CreateAddOnPurchaseRequest payload, map<string|string[]> headers = {}) returns GitlabSubscriptionsAddOnPurchase|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/subscription_add_on_purchase/${getEncodedUri(addOnName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Returns the projects of a namespace (including subgroups), with license data included
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved subscription namespace projects 
    remote isolated function listSubscriptionNamespaceProjects(IdOrPath id, map<string|string[]> headers = {}, *ListSubscriptionNamespaceProjectsQueries queries) returns InternalProjectWithLicense[]|error {
        string resourcePath = string `/internal/gitlab_subscriptions/namespaces/${getEncodedUri(id)}/projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Ingest a rollout workflow event
    #
    # + id - The ID of the rollout
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to ingest a rollout workflow event 
    # + return - Accepted 
    remote isolated function ingestRolloutWorkflowEvent(int id, IngestRolloutWorkflowEventRequest payload, map<string|string[]> headers = {}) returns CdRollout|error {
        string resourcePath = string `/rollouts/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Execute a query
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to execute a query 
    # + return - Success 
    remote isolated function executeOrbitQuery(ExecuteOrbitQueryRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/orbit/query`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Execute a named query
    #
    # + name - Named query identifier
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to execute a named query 
    # + return - Success 
    remote isolated function executeOrbitNamedQuery(string name, ExecuteOrbitNamedQueryRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/orbit/query/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List named query templates
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listOrbitQueryTemplates(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/orbit/query/templates`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the schema
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getOrbitSchema(map<string|string[]> headers = {}, *GetOrbitSchemaQueries queries) returns error? {
        string resourcePath = string `/orbit/schema`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve access and cluster health
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getOrbitStatus(map<string|string[]> headers = {}, *GetOrbitStatusQueries queries) returns error? {
        string resourcePath = string `/orbit/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all tools
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listOrbitTools(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/orbit/tools`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Authorize SBOM file upload
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to authorize SBOM file upload 
    # + return - SBOM Upload allowed 
    remote isolated function authorizeSbomFileUpload(int id, AuthorizeSbomFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/sbom_scans/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Upload an SBOM file
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload an SBOM file 
    # + return - Created 
    remote isolated function uploadSbomFile(int id, UploadSbomFileRequest payload, map<string|string[]> headers = {}) returns SecurityVulnerabilityScanningSbomScan|error {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/sbom_scans`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download an SBOM scan result
    #
    # + id - ID of the job
    # + sbomDigest - SBOM Scan's ID
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadSbomScanResult(int id, int sbomDigest, map<string|string[]> headers = {}) returns SecurityVulnerabilityScanningSbomScan|error? {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/sbom_scans/${getEncodedUri(sbomDigest)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Reuse SBOM scan results
    #
    # + id - ID of the job
    # + sbomDigest - The sbom digest that identifies an sbom scan uniquely
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to reuse SBOM scan results 
    # + return - Created 
    remote isolated function reuseSbomScanResults(int id, string sbomDigest, ReuseSbomScanResultsRequest payload, map<string|string[]> headers = {}) returns SecurityVulnerabilityScanningSbomScan|error {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/sbom_scans/${getEncodedUri(sbomDigest)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Set status of an external control
    #
    # + id - The ID or URL-encoded path of the project
    # + controlId - The ID of the control
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set status of an external control 
    # + return - Success 
    remote isolated function setExternalControlStatus(IdOrPath id, int controlId, SetExternalControlStatusRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/compliance_external_controls/${getEncodedUri(controlId)}/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Scan a file for vulnerabilities
    #
    # + id - The ID or URL-encoded path of the project
    # + sastEndpoint - The name of the SAST scanner service endpoint to call
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to scan a file for vulnerabilities 
    # + return - Success 
    remote isolated function scanProjectFile(IdOrPath id, string sastEndpoint, ScanProjectFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/security_scans/sast/${getEncodedUri(sastEndpoint)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Evaluate code review
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to evaluate code review 
    # + return - Created 
    remote isolated function evaluateCodeReview(EvaluateCodeReviewRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/duo_code_review/evaluations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Generate a Chat response
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to generate a Chat response 
    # + return - Created 
    remote isolated function generateChatResponse(GenerateChatResponseRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/chat/completions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Issue a short-lived JWT for a single modular-service audience
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to issue a short-lived JWT for a single modular-service audience 
    # + return - Created 
    remote isolated function issueShortLivedJwt(IssueShortLivedJwtRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/token_exchange`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Generate code completions
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to generate code completions 
    # + return - Success 
    remote isolated function generateCodeCompletions(GenerateCodeCompletionsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/code_suggestions/completions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve direct connection details for the AI Gateway
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to retrieve direct connection details for the AI Gateway 
    # + return - Created 
    remote isolated function getCodeSuggestionsDirectAccess(GetCodeSuggestionsDirectAccessRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/code_suggestions/direct_access`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Validate status of Code Suggestions
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to validate status of Code Suggestions 
    # + return - Success 
    remote isolated function validateCodeSuggestionsEnabled(ValidateCodeSuggestionsEnabledRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/code_suggestions/enabled`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve connection details
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function getCodeSuggestionsConnection(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/code_suggestions/connection_details`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Search project code using natural language
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchProjectCode(IdOrPath id, map<string|string[]> headers = {}, *SearchProjectCodeQueries queries) returns AiSemanticCodeResponse|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/-/search/semantic`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get connection details for third party agents
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function getThirdPartyAgentConnection(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/third_party_agents/direct_access`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List external agent sessions for a project.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved external agent sessions 
    remote isolated function listExternalAgentSessions(IdOrPath id, map<string|string[]> headers = {}, *ListExternalAgentSessionsQueries queries) returns AiExternalAgentsSession[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ai_agent/sessions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an external agent session.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an external agent session 
    # + return - Created 
    remote isolated function createExternalAgentSession(IdOrPath id, CreateExternalAgentSessionRequest payload, map<string|string[]> headers = {}) returns AiExternalAgentsSession|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ai_agent/sessions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Complete or fail an external agent session.
    #
    # + sessionId - The session ID
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to complete or fail an external agent session 
    # + return - Success 
    remote isolated function completeOrFailExternalAgentSession(int sessionId, IdOrPath id, CompleteOrFailExternalAgentSessionRequest payload, map<string|string[]> headers = {}) returns AiExternalAgentsSession|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ai_agent/sessions/${getEncodedUri(sessionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Register an external agent identity.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to register an external agent identity 
    # + return - Created 
    remote isolated function registerExternalAgentIdentity(IdOrPath id, RegisterExternalAgentIdentityRequest payload, map<string|string[]> headers = {}) returns AiExternalAgentsIdentity|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ai_agent/identities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Ingest external agent audit events.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to ingest external agent audit events 
    # + return - Accepted 
    remote isolated function ingestExternalAgentAuditEvents(IdOrPath id, IngestExternalAgentAuditEventsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ai_agent/audit_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Ingest AI audit events from Duo Workflow Service
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to ingest AI audit events from Duo Workflow Service 
    # + return - Created 
    remote isolated function ingestAiAuditEvents(int id, IngestAiAuditEventsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/audit_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Submit risk classification results for a merge request
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to submit risk classification results for a merge request 
    # + return - No Content 
    remote isolated function submitRiskClassificationResults(SubmitRiskClassificationResultsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/tools/risk_classification/results`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Add code review comments to a merge request
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add code review comments to a merge request 
    # + return - Success 
    remote isolated function addCodeReviewComments(AddCodeReviewCommentsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/code_review/add_comments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Fetch the Duo Code Review custom instructions for a merge request
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getCodeReviewCustomInstructions(map<string|string[]> headers = {}, *GetCodeReviewCustomInstructionsQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/code_review/custom_instructions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create workflow persistence (agent-initiated, limited privileges)
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create workflow persistence (agent-initiated, limited privileges) 
    # + return - Created 
    remote isolated function createWorkflowPersistence(CreateWorkflowPersistenceRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/agent_workflows`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Revoke ai_workflows token
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to revoke ai_workflows token 
    # + return - Success 
    remote isolated function revokeAiWorkflowsToken(RevokeAiWorkflowsTokenRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/revoke_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get workflow details
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getWorkflow(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update the workflow status
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the workflow status 
    # + return - Success 
    remote isolated function updateWorkflowStatus(int id, UpdateWorkflowStatusRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all workflow checkpoints
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listWorkflowCheckpoints(int id, map<string|string[]> headers = {}, *ListWorkflowCheckpointsQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/checkpoints`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create workflow checkpoint
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create workflow checkpoint 
    # + return - Created 
    remote isolated function createWorkflowCheckpoint(int id, CreateWorkflowCheckpointRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/checkpoints`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get a workflow checkpoint reconstructed from incremental blobs
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getWorkflowCheckpointByThread(int id, map<string|string[]> headers = {}, *GetWorkflowCheckpointByThreadQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/checkpoints/by_thread_ts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get details on a workflow checkpoint
    #
    # + id - The ID of the workflow
    # + checkpointId - The ID of the checkpoint
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getWorkflowCheckpoint(int id, int checkpointId, map<string|string[]> headers = {}, *GetWorkflowCheckpointQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/checkpoints/${getEncodedUri(checkpointId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create multiple workflow checkpoint writes
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create multiple workflow checkpoint writes 
    # + return - Created 
    remote isolated function createWorkflowCheckpointWrites(int id, CreateWorkflowCheckpointWritesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/checkpoint_writes_batch`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all workflow events
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listWorkflowEvents(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create workflow event
    #
    # + id - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create workflow event 
    # + return - Created 
    remote isolated function createWorkflowEvent(int id, CreateWorkflowEventRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update workflow event
    #
    # + id - The ID of the workflow
    # + eventId - The ID of the event
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update workflow event 
    # + return - Success 
    remote isolated function updateWorkflowEvent(int id, int eventId, UpdateWorkflowEventRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(id)}/events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Connection details for accessing Duo Agent Platform Service directly
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to get connection details for direct Duo Agent Platform Service access 
    # + return - Created 
    remote isolated function getDuoAgentPlatformConnection(GetDuoAgentPlatformConnectionRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/direct_access`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List Duo Agent Platform tools
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listDuoAgentPlatformTools(map<string|string[]> headers = {}, *ListDuoAgentPlatformToolsQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/list_tools`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get Duo Workflows WebSocket connection details
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getWorkflowWebSocketConnection(map<string|string[]> headers = {}, *GetWorkflowWebSocketConnectionQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/ws`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get workflow trace as JSONL
    #
    # + workflowId - The ID of the workflow
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getWorkflowTrace(int workflowId, map<string|string[]> headers = {}, *GetWorkflowTraceQueries queries) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(workflowId)}/trace.jsonl`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a flow
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a flow 
    # + return - Success 
    remote isolated function createFlow(CreateFlowRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all agent privileges
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listAgentPrivileges(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/agent_privileges`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Request Duo Workflows server-side execution connection details
    #
    # + workflowId - The ID of the workflow to execute
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for request Duo Workflows server-side execution connection details 
    # + return - Success 
    remote isolated function getWorkflowExecutionConnection(string workflowId, GetWorkflowExecutionConnectionRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(workflowId)}/execute`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Resume a paused workflow
    #
    # + workflowId - The ID of the workflow to resume
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to resume a paused workflow 
    # + return - Created 
    remote isolated function resumeWorkflow(string workflowId, ResumeWorkflowRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(workflowId)}/resume`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Restart a failed or stopped workflow as a new session
    #
    # + workflowId - The ID of the workflow to restart
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function restartWorkflow(string workflowId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/duo_workflows/workflows/${getEncodedUri(workflowId)}/restart`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Generate Git commands from natural text
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to generate Git commands from natural text 
    # + return - Created 
    remote isolated function generateGitCommands(GenerateGitCommandsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/ai/llm/git_command`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all enterprise users
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved enterprise users 
    remote isolated function listEnterpriseUsers(IdOrPath id, map<string|string[]> headers = {}, *ListEnterpriseUsersQueries queries) returns UserPublic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/enterprise_users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an enterprise user
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - ID of user account
    # + headers - Headers to be sent with the request 
    # + return - The retrieved enterprise user 
    remote isolated function getEnterpriseUser(IdOrPath id, int userId, map<string|string[]> headers = {}) returns UserPublic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/enterprise_users/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an enterprise user
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - ID of user account
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteEnterpriseUser(IdOrPath id, int userId, map<string|string[]> headers = {}, *DeleteEnterpriseUserQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/enterprise_users/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update an enterprise user
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - ID of user account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an enterprise user 
    # + return - The updated enterprise user 
    remote isolated function updateEnterpriseUser(IdOrPath id, int userId, UpdateEnterpriseUserRequest payload, map<string|string[]> headers = {}) returns UserPublic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/enterprise_users/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Disable two-factor authentication for an enterprise user
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - ID of user account
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function disableEnterpriseUserTwoFactor(IdOrPath id, int userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/enterprise_users/${getEncodedUri(userId)}/disable_two_factor`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Create a dependency list export
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a dependency list export 
    # + return - Created 
    remote isolated function createProjectDependencyListExport(IdOrPath id, CreateProjectDependencyListExportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/dependency_list_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a dependency list export
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a dependency list export 
    # + return - Created 
    remote isolated function createGroupDependencyListExport(IdOrPath id, CreateGroupDependencyListExportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/dependency_list_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a dependency list export
    #
    # + id - The ID of the pipeline
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a dependency list export 
    # + return - Created 
    remote isolated function createPipelineDependencyListExport(IdOrPath id, CreatePipelineDependencyListExportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/pipelines/${getEncodedUri(id)}/dependency_list_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a single dependency list export
    #
    # + exportId - The ID of the dependency list export
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getDependencyListExport(IdOrPath exportId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/dependency_list_exports/${getEncodedUri(exportId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download dependency list export
    #
    # + exportId - The ID of the dependency list export
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadDependencyListExport(IdOrPath exportId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/dependency_list_exports/${getEncodedUri(exportId)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all protected branches
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group protected branches 
    remote isolated function listGroupProtectedBranches(string id, map<string|string[]> headers = {}, *ListGroupProtectedBranchesQueries queries) returns GroupProtectedBranch[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Protect a repository branch
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to protect a repository branch 
    # + return - Created 
    remote isolated function protectGroupBranch(string id, ProtectBranchRequest payload, map<string|string[]> headers = {}) returns GroupProtectedBranch|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a protected branch
    #
    # + id - The ID or URL-encoded path of a group
    # + name - The name of the branch or wildcard
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group protected branch 
    remote isolated function getGroupProtectedBranch(string id, string name, map<string|string[]> headers = {}) returns GroupProtectedBranch|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_branches/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Unprotect repository branches
    #
    # + id - The ID or URL-encoded path of a group
    # + name - The name of the protected branch
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function unprotectGroupBranch(string id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_branches/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a protected branch
    #
    # + id - The ID or URL-encoded path of a group
    # + name - The name of the branch
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a protected branch 
    # + return - The updated group protected branch 
    remote isolated function updateGroupProtectedBranch(string id, string name, UpdateProtectedBranchRequest payload, map<string|string[]> headers = {}) returns GroupProtectedBranch|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_branches/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all group repository storage moves
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all group storage moves 
    remote isolated function listAllGroupStorageMoves(map<string|string[]> headers = {}, *ListAllGroupStorageMovesQueries queries) returns GroupsRepositoryStorageMove[]|error {
        string resourcePath = string `/group_repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create group repository storage moves for a storage shard
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create group repository storage moves for a storage shard 
    # + return - 202 Accepted 
    remote isolated function scheduleGroupStorageMovesForShard(ScheduleStorageMovesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/group_repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group repository storage move
    #
    # + repositoryStorageMoveId - The ID of a group repository storage move
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group storage move 
    remote isolated function getGroupStorageMove(int repositoryStorageMoveId, map<string|string[]> headers = {}) returns GroupsRepositoryStorageMove|error {
        string resourcePath = string `/group_repository_storage_moves/${getEncodedUri(repositoryStorageMoveId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all repository storage moves for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group repository storage moves 
    remote isolated function listGroupRepositoryStorageMoves(string id, map<string|string[]> headers = {}, *ListGroupRepositoryStorageMovesQueries queries) returns GroupsRepositoryStorageMove[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group repository storage move
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group repository storage move 
    # + return - Created 
    remote isolated function createGroupRepositoryStorageMove(string id, CreateStorageMoveRequest payload, map<string|string[]> headers = {}) returns GroupsRepositoryStorageMove|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a repository storage move for a group
    #
    # + id - The ID of a group
    # + repositoryStorageMoveId - The ID of a group repository storage move
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group repository storage move 
    remote isolated function getGroupRepositoryStorageMove(string id, int repositoryStorageMoveId, map<string|string[]> headers = {}) returns GroupsRepositoryStorageMove|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/repository_storage_moves/${getEncodedUri(repositoryStorageMoveId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project iterations
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project iterations 
    remote isolated function listProjectIterations(IdOrPath id, map<string|string[]> headers = {}, *ListProjectIterationsQueries queries) returns Iteration[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/iterations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"in": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group iterations
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group iterations 
    remote isolated function listGroupIterations(string id, map<string|string[]> headers = {}, *ListGroupIterationsQueries queries) returns Iteration[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/iterations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"in": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get vulnerabilities
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listOccurrenceVulnerabilities(map<string|string[]> headers = {}, *ListOccurrenceVulnerabilitiesQueries queries) returns error? {
        string resourcePath = string `/occurrences/vulnerabilities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Lists SAML group links
    #
    # + id - ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved saml group links 
    remote isolated function listSamlGroupLinks(IdOrPath id, map<string|string[]> headers = {}) returns SamlGroupLink[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml_group_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add SAML group link
    #
    # + id - ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add SAML group link 
    # + return - Created 
    remote isolated function addSamlGroupLink(IdOrPath id, AddSamlGroupLinkRequest payload, map<string|string[]> headers = {}) returns SamlGroupLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml_group_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get SAML group link
    #
    # + id - ID or URL-encoded path of the group
    # + samlGroupName - Name of an SAML group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved saml group link 
    remote isolated function getSamlGroupLink(IdOrPath id, string samlGroupName, map<string|string[]> headers = {}, *GetSamlGroupLinkQueries queries) returns SamlGroupLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml_group_links/${getEncodedUri(samlGroupName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete SAML group link
    #
    # + id - ID or URL-encoded path of the group
    # + samlGroupName - Name of a SAML group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteSamlGroupLink(IdOrPath id, string samlGroupName, map<string|string[]> headers = {}, *DeleteSamlGroupLinkQueries queries) returns SamlGroupLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml_group_links/${getEncodedUri(samlGroupName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all issue iteration events
    #
    # + id - The ID or URL-encoded path the project
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue iteration events 
    remote isolated function listIssueIterationEvents(IdOrPath id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListIssueIterationEventsQueries queries) returns ResourceIterationEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_iteration_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an issue iteration event
    #
    # + id - The ID or URL-encoded path the project
    # + eventId - The ID of a resource iteration event
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue iteration event 
    remote isolated function getIssueIterationEvent(IdOrPath id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceIterationEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_iteration_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project issue weight events
    #
    # + id - The ID or URL-encoded path of the project
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue weight events 
    remote isolated function listIssueWeightEvents(IdOrPath id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListIssueWeightEventsQueries queries) returns ResourceWeightEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_weight_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve single issue weight event
    #
    # + id - The ID or URL-encoded path of the project
    # + eventId - The ID of a resource weight event
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue weight event 
    remote isolated function getIssueWeightEvent(IdOrPath id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceWeightEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_weight_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all protected environments for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project protected environments 
    remote isolated function listProjectProtectedEnvironments(IdOrPath id, map<string|string[]> headers = {}, *ListProjectProtectedEnvironmentsQueries queries) returns ProtectedEnvironment[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_environments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Protect an environment for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to protect an environment for a project 
    # + return - Created 
    remote isolated function protectProjectEnvironment(IdOrPath id, ProtectProjectEnvironmentRequest payload, map<string|string[]> headers = {}) returns ProtectedEnvironment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_environments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a protected environment for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + name - The name of the protected environment
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project protected environment 
    remote isolated function getProjectProtectedEnvironment(IdOrPath id, string name, map<string|string[]> headers = {}) returns ProtectedEnvironment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_environments/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a protected environment for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + name - The name of the environment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a protected environment for a project 
    # + return - The updated project protected environment 
    remote isolated function updateProjectProtectedEnvironment(IdOrPath id, string name, UpdateProjectProtectedEnvironmentRequest payload, map<string|string[]> headers = {}) returns ProtectedEnvironment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_environments/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Unprotect a protected environment for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + name - The name of the protected environment
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function unprotectProjectEnvironment(IdOrPath id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_environments/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all protected environments for a group
    #
    # + id - The ID or URL-encoded path of the group maintained by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group protected environments 
    remote isolated function listGroupProtectedEnvironments(IdOrPath id, map<string|string[]> headers = {}, *ListGroupProtectedEnvironmentsQueries queries) returns ProtectedEnvironment[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_environments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Protect an environment for a group
    #
    # + id - The ID or URL-encoded path of the group maintained by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to protect an environment for a group 
    # + return - Created 
    remote isolated function protectGroupEnvironment(IdOrPath id, ProtectGroupEnvironmentRequest payload, map<string|string[]> headers = {}) returns ProtectedEnvironment|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_environments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a protected environment for a group
    #
    # + id - The ID or URL-encoded path of the group maintained by the authenticated user
    # + name - The deployment tier of the protected environment
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group protected environment 
    remote isolated function getGroupProtectedEnvironment(IdOrPath id, "production"|"staging"|"testing"|"development"|"other" name, map<string|string[]> headers = {}) returns ProtectedEnvironment|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_environments/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a protected environment for a group
    #
    # + id - The ID or URL-encoded path of the group maintained by the authenticated user
    # + name - The deployment tier of the protected environment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a protected environment for a group 
    # + return - The updated group protected environment 
    remote isolated function updateGroupProtectedEnvironment(IdOrPath id, "production"|"staging"|"testing"|"development"|"other" name, UpdateGroupProtectedEnvironmentRequest payload, map<string|string[]> headers = {}) returns ProtectedEnvironment|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_environments/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Unprotect a group environment
    #
    # + id - The ID or URL-encoded path of the group maintained by the authenticated user
    # + name - The tier name of the protected environment
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function unprotectGroupEnvironment(IdOrPath id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/protected_environments/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List deployment frequencies for the project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project deployment frequencies 
    remote isolated function listProjectDeploymentFrequencies(IdOrPath id, map<string|string[]> headers = {}, *ListProjectDeploymentFrequenciesQueries queries) returns AnalyticsDeploymentFrequency[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/analytics/deployment_frequency`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve count of recently created issues for a group
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The number of group recent issues 
    remote isolated function countGroupRecentIssues(map<string|string[]> headers = {}, *CountGroupRecentIssuesQueries queries) returns AnalyticsGroupActivityIssuesCount|error {
        string resourcePath = string `/analytics/group_activity/issues_count`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve count of recently created merge requests for a group
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The number of group recent merge requests 
    remote isolated function countGroupRecentMergeRequests(map<string|string[]> headers = {}, *CountGroupRecentMergeRequestsQueries queries) returns AnalyticsGroupActivityMergeRequestsCount|error {
        string resourcePath = string `/analytics/group_activity/merge_requests_count`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve count of members recently added to a group
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The number of group new members 
    remote isolated function countGroupNewMembers(map<string|string[]> headers = {}, *CountGroupNewMembersQueries queries) returns AnalyticsGroupActivityNewMembersCount|error {
        string resourcePath = string `/analytics/group_activity/new_members_count`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List code review information about project
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved code review analytics 
    remote isolated function listCodeReviewAnalytics(map<string|string[]> headers = {}, *ListCodeReviewAnalyticsQueries queries) returns AnalyticsCodeReviewMergeRequest[]|error {
        string resourcePath = string `/analytics/code_review`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"label_name": {style: FORM, explode: false}, "not[label_name]": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project dependencies
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project dependencies 
    remote isolated function listProjectDependencies(IdOrPath id, map<string|string[]> headers = {}, *ListProjectDependenciesQueries queries) returns Dependency[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/dependencies`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"package_manager": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project aliases
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project aliases 
    remote isolated function listProjectAliases(map<string|string[]> headers = {}, *ListProjectAliasesQueries queries) returns ProjectAlias[]|error {
        string resourcePath = string `/project_aliases`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project alias
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project alias 
    # + return - Created 
    remote isolated function createProjectAlias(CreateProjectAliasRequest payload, map<string|string[]> headers = {}) returns ProjectAlias|error {
        string resourcePath = string `/project_aliases`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a project alias
    #
    # + name - The alias of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project alias 
    remote isolated function getProjectAlias(string name, map<string|string[]> headers = {}) returns ProjectAlias|error {
        string resourcePath = string `/project_aliases/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a project alias
    #
    # + name - The alias of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectAlias(string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/project_aliases/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all approval rules for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request approval rules 
    remote isolated function listMergeRequestApprovalRules(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestApprovalRulesQueries queries) returns MergeRequestApprovalRule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approval_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an approval rule for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an approval rule for a merge request 
    # + return - Created 
    remote isolated function createMergeRequestApprovalRule(IdOrPath id, int mergeRequestIid, CreateMergeRequestApprovalRuleRequest payload, map<string|string[]> headers = {}) returns MergeRequestApprovalRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approval_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an approval rule for a specific merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + approvalRuleId - The ID of a merge request approval rule
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request approval rule 
    remote isolated function getMergeRequestApprovalRule(IdOrPath id, int mergeRequestIid, int approvalRuleId, map<string|string[]> headers = {}) returns MergeRequestApprovalRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an approval rule for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + approvalRuleId - The ID of a merge request approval rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an approval rule for a merge request 
    # + return - The updated merge request approval rule 
    remote isolated function updateMergeRequestApprovalRule(IdOrPath id, int mergeRequestIid, int approvalRuleId, UpdateMergeRequestApprovalRuleRequest payload, map<string|string[]> headers = {}) returns MergeRequestApprovalRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an approval rule for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + approvalRuleId - The ID of a merge request approval rule
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteMergeRequestApprovalRule(IdOrPath id, int mergeRequestIid, int approvalRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Create a project-level vulnerability export
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project-level vulnerability export 
    # + return - Created 
    remote isolated function createProjectVulnerabilityExport(IdOrPath id, CreateProjectVulnerabilityExportRequest payload, map<string|string[]> headers = {}) returns VulnerabilityExport|error {
        string resourcePath = string `/security/projects/${getEncodedUri(id)}/vulnerability_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a group-level vulnerability export
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group-level vulnerability export 
    # + return - Created 
    remote isolated function createGroupVulnerabilityExport(string id, CreateGroupVulnerabilityExportRequest payload, map<string|string[]> headers = {}) returns VulnerabilityExport|error {
        string resourcePath = string `/security/groups/${getEncodedUri(id)}/vulnerability_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create an instance-level vulnerability export
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an instance-level vulnerability export 
    # + return - Created 
    remote isolated function createInstanceVulnerabilityExport(CreateInstanceVulnerabilityExportRequest payload, map<string|string[]> headers = {}) returns VulnerabilityExport|error {
        string resourcePath = string `/security/vulnerability_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a vulnerability export
    #
    # + id - The ID of the vulnerability export
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vulnerability export 
    remote isolated function getVulnerabilityExport(int id, map<string|string[]> headers = {}) returns VulnerabilityExport|error {
        string resourcePath = string `/security/vulnerability_exports/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a vulnerability export
    #
    # + id - The ID of the vulnerability export
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadVulnerabilityExport(int id, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/security/vulnerability_exports/${getEncodedUri(id)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an export for archived vulnerabilities
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an export for archived vulnerabilities 
    # + return - Created 
    remote isolated function createArchivedVulnerabilityExport(IdOrPath id, CreateArchivedVulnerabilityExportRequest payload, map<string|string[]> headers = {}) returns VulnerabilityArchiveExport|error {
        string resourcePath = string `/security/projects/${getEncodedUri(id)}/vulnerability_archive_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a vulnerability archive export
    #
    # + id - The ID of the vulnerability archive export
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vulnerability archive export 
    remote isolated function getVulnerabilityArchiveExport(int id, map<string|string[]> headers = {}) returns VulnerabilityArchiveExport|error {
        string resourcePath = string `/security/vulnerability_archive_exports/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a vulnerability archive export
    #
    # + id - The ID of the vulnerability archive export
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadVulnerabilityArchiveExport(int id, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/security/vulnerability_archive_exports/${getEncodedUri(id)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get related issues for a vulnerability
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vulnerability issue links 
    remote isolated function listVulnerabilityIssueLinks(int id, map<string|string[]> headers = {}) returns VulnerabilityRelatedIssue[]|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/issue_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Relate an issue to a vulnerability
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to relate an issue to a vulnerability 
    # + return - Created 
    remote isolated function createVulnerabilityIssueLink(int id, CreateVulnerabilityIssueLinkRequest payload, map<string|string[]> headers = {}) returns VulnerabilityIssueLink|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/issue_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a link between an issue and a vulnerability
    #
    # + id - The ID of a vulnerability
    # + issueLinkId - The ID of a vulnerability-issue-link to delete
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteVulnerabilityIssueLink(int id, int issueLinkId, map<string|string[]> headers = {}) returns VulnerabilityIssueLink|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/issue_links/${getEncodedUri(issueLinkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all project vulnerability findings
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project vulnerability findings 
    remote isolated function listProjectVulnerabilityFindings(IdOrPath id, map<string|string[]> headers = {}, *ListProjectVulnerabilityFindingsQueries queries) returns VulnerabilityFinding[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerability_findings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"report_type": {style: FORM, explode: false}, "severity": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update AI False Positive Detection results
    #
    # + vulnerabilityId - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update AI False Positive Detection results 
    # + return - Success 
    remote isolated function updateAiFalsePositiveDetectionResults(string vulnerabilityId, UpdateAiFalsePositiveDetectionResultsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(vulnerabilityId)}/flags/ai_detection`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a vulnerability
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vulnerability 
    remote isolated function getVulnerability(string id, map<string|string[]> headers = {}) returns Vulnerability|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Resolve a vulnerability
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to resolve a vulnerability 
    # + return - Created 
    remote isolated function resolveVulnerability(string id, ResolveVulnerabilityRequest payload, map<string|string[]> headers = {}) returns Vulnerability|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/resolve`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Dismiss a vulnerability
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function dismissVulnerability(string id, map<string|string[]> headers = {}) returns Vulnerability|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/dismiss`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Confirm a vulnerability
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to confirm a vulnerability 
    # + return - Created 
    remote isolated function confirmVulnerability(string id, VulnerabilityStateRequest payload, map<string|string[]> headers = {}) returns Vulnerability|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/confirm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Revert a vulnerability to detected state
    #
    # + id - The ID of a vulnerability
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to revert a vulnerability to detected state 
    # + return - Created 
    remote isolated function revertVulnerability(string id, VulnerabilityStateRequest payload, map<string|string[]> headers = {}) returns Vulnerability|error {
        string resourcePath = string `/vulnerabilities/${getEncodedUri(id)}/revert`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project vulnerabilities
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project vulnerabilities 
    remote isolated function listProjectVulnerabilities(IdOrPath id, map<string|string[]> headers = {}, *ListProjectVulnerabilitiesQueries queries) returns Vulnerability[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a new vulnerability
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a new vulnerability 
    # + return - Created 
    remote isolated function createVulnerability(IdOrPath id, CreateVulnerabilityRequest payload, map<string|string[]> headers = {}) returns Vulnerability|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve setup script for Google Cloud integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved google cloud setup script 
    remote isolated function getGoogleCloudSetupScript(IdOrPath id, map<string|string[]> headers = {}, *GetGoogleCloudSetupScriptQueries queries) returns string|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/google_cloud/setup/integrations.sh`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve configuration script for Google Cloud runner provisioning
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved google cloud runner script 
    remote isolated function getGoogleCloudRunnerScript(IdOrPath id, map<string|string[]> headers = {}, *GetGoogleCloudRunnerScriptQueries queries) returns string|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/google_cloud/setup/runner_deployment_project.sh`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve approval configuration for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project approval config 
    remote isolated function getProjectApprovalConfig(IdOrPath id, map<string|string[]> headers = {}) returns ApprovalSettings|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approvals`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update approval configuration for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update approval configuration for a project 
    # + return - Created 
    remote isolated function setProjectApprovalConfig(IdOrPath id, SetProjectApprovalConfigRequest payload, map<string|string[]> headers = {}) returns ApprovalSettings|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approvals`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all software license policies
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved software license policies 
    remote isolated function listSoftwareLicensePolicies(IdOrPath id, map<string|string[]> headers = {}, *ListSoftwareLicensePoliciesQueries queries) returns ManagedLicense[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/managed_licenses`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a software license policy
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a software license policy 
    # + return - Created 
    remote isolated function createSoftwareLicensePolicy(IdOrPath id, CreateSoftwareLicensePolicyRequest payload, map<string|string[]> headers = {}) returns ManagedLicense|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/managed_licenses`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a software license policy
    #
    # + id - The ID or URL-encoded path of the project
    # + managedLicenseId - The ID of a managed license
    # + headers - Headers to be sent with the request 
    # + return - The retrieved software license policy 
    remote isolated function getSoftwareLicensePolicy(IdOrPath id, string managedLicenseId, map<string|string[]> headers = {}) returns ManagedLicense|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/managed_licenses/${getEncodedUri(managedLicenseId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a software license policy
    #
    # + id - The ID or URL-encoded path of the project
    # + managedLicenseId - The ID of a managed license
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteSoftwareLicensePolicy(IdOrPath id, string managedLicenseId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/managed_licenses/${getEncodedUri(managedLicenseId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a software license policy
    #
    # + id - The ID or URL-encoded path of the project
    # + managedLicenseId - The ID of a managed license
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a software license policy 
    # + return - The updated software license policy 
    remote isolated function updateSoftwareLicensePolicy(IdOrPath id, string managedLicenseId, UpdateSoftwareLicensePolicyRequest payload, map<string|string[]> headers = {}) returns ManagedLicense|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/managed_licenses/${getEncodedUri(managedLicenseId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all personal access tokens for a group
    #
    # + id - ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listGroupPersonalAccessTokens(string id, map<string|string[]> headers = {}, *ListGroupPersonalAccessTokensQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a personal access token for an enterprise user
    #
    # + id - ID or URL-encoded path of the group
    # + patId - The ID of the personal access token
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeEnterprisePersonalAccessToken(string id, int patId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/personal_access_tokens/${getEncodedUri(patId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a personal access token for an enterprise user
    #
    # + id - ID or URL-encoded path of the group
    # + patId - The ID of the personal access token
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a personal access token for an enterprise user 
    # + return - Created 
    remote isolated function rotateEnterprisePersonalAccessToken(string id, int patId, RotateTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/personal_access_tokens/${getEncodedUri(patId)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group and project access tokens for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listGroupResourceAccessTokens(IdOrPath id, map<string|string[]> headers = {}, *ListGroupResourceAccessTokensQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/resource_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a group or project access token for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + pratId - The ID of the resource access token
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function revokeGroupResourceAccessToken(IdOrPath id, int pratId, map<string|string[]> headers = {}, *RevokeGroupResourceAccessTokenQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/resource_access_tokens/${getEncodedUri(pratId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a group or project access token for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + pratId - The ID of the resource access token
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a group or project access token for a group 
    # + return - No Content 
    remote isolated function rotateGroupResourceAccessToken(IdOrPath id, int pratId, RotateTokenRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/resource_access_tokens/${getEncodedUri(pratId)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all SSH keys for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group ssh keys 
    remote isolated function listGroupSshKeys(IdOrPath id, map<string|string[]> headers = {}, *ListGroupSshKeysQueries queries) returns SshKeyWithUserId[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/ssh_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an SSH key for an enterprise user
    #
    # + id - The ID or URL-encoded path of the group
    # + keyId - The ID of the SSH key
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteEnterpriseSshKey(IdOrPath id, int keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/manage/ssh_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all dependencies for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request dependencies 
    remote isolated function listMergeRequestDependencies(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestDependenciesQueries queries) returns MergeRequestDependency[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/blocks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a merge request dependency
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal IID of the blocked merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a merge request dependency 
    # + return - Created 
    remote isolated function createMergeRequestDependency(IdOrPath id, int mergeRequestIid, CreateMergeRequestDependencyRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/blocks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a merge request dependency
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + blockId - The ID of the merge request dependency
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getMergeRequestDependency(IdOrPath id, int mergeRequestIid, int blockId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/blocks/${getEncodedUri(blockId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Remove a merge request dependency
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + blockId - The ID of the merge request dependency
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function removeMergeRequestDependency(IdOrPath id, int mergeRequestIid, int blockId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/blocks/${getEncodedUri(blockId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve blocked merge requests
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request blockees 
    remote isolated function listMergeRequestBlockees(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestBlockeesQueries queries) returns MergeRequestDependency[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/blockees`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve MR approval settings for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project mr approval settings 
    remote isolated function getProjectMrApprovalSettings(IdOrPath id, map<string|string[]> headers = {}) returns MergeRequestApprovalSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_request_approval_setting`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update MR approval settings for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update MR approval settings for a project 
    # + return - The updated project mr approval settings 
    remote isolated function updateProjectMrApprovalSettings(IdOrPath id, UpdateMrApprovalSettingsRequest payload, map<string|string[]> headers = {}) returns MergeRequestApprovalSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_request_approval_setting`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve MR approval settings for a group
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group mr approval settings 
    remote isolated function getGroupMrApprovalSettings(string id, map<string|string[]> headers = {}) returns MergeRequestApprovalSetting|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/merge_request_approval_setting`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update MR approval settings for a group
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update MR approval settings for a group 
    # + return - The updated group mr approval settings 
    remote isolated function updateGroupMrApprovalSettings(string id, UpdateMrApprovalSettingsRequest payload, map<string|string[]> headers = {}) returns MergeRequestApprovalSetting|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/merge_request_approval_setting`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all group hooks
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group hooks 
    remote isolated function listGroupHooks(IdOrPath id, map<string|string[]> headers = {}, *ListGroupHooksQueries queries) returns GroupHook[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group hook
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group hook 
    # + return - Created 
    remote isolated function createGroupHook(IdOrPath id, CreateGroupHookRequest payload, map<string|string[]> headers = {}) returns GroupHook|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group hook
    #
    # + id - The ID or URL-encoded path of the group
    # + hookId - The ID of a group hook
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group hook 
    remote isolated function getGroupHook(IdOrPath id, int hookId, map<string|string[]> headers = {}) returns GroupHook|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group hook
    #
    # + id - The ID or URL-encoded path of the group
    # + hookId - The ID of the group hook
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group hook 
    # + return - The updated group hook 
    remote isolated function updateGroupHook(IdOrPath id, int hookId, UpdateGroupHookRequest payload, map<string|string[]> headers = {}) returns GroupHook|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group hook
    #
    # + id - The ID or URL-encoded path of the group
    # + hookId - The ID of the group hook
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupHook(IdOrPath id, int hookId, map<string|string[]> headers = {}) returns GroupHook|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all events
    #
    # + id - The ID or URL-encoded path of the group
    # + hookId - The ID of a group hook
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listGroupHookEvents(IdOrPath id, int hookId, map<string|string[]> headers = {}, *ListGroupHookEventsQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"status": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a URL variable
    #
    # + hookId - The ID of the hook
    # + 'key - The key of the variable
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a URL variable 
    # + return - Success 
    remote isolated function updateGroupHookUrlVariable(int hookId, string 'key, IdOrPath id, UpdateHookUrlVariableRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/url_variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a URL variable
    #
    # + hookId - The ID of the hook
    # + 'key - The key of the variable
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupHookUrlVariable(int hookId, string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/url_variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a custom header
    #
    # + hookId - The ID of the hook
    # + 'key - The name of the custom header
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a custom header 
    # + return - Success 
    remote isolated function updateGroupHookCustomHeader(int hookId, string 'key, IdOrPath id, UpdateHookCustomHeaderRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/custom_headers/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a custom header
    #
    # + hookId - The ID of the hook
    # + 'key - The name of the custom header
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupHookCustomHeader(int hookId, string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/custom_headers/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Trigger a test webhook
    #
    # + hookId - The ID of the hook
    # + trigger - The type of trigger hook
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function testGroupHook(int hookId, "confidential_issues_events"|"confidential_note_events"|"deployment_events"|"emoji_events"|"feature_flag_events"|"issues_events"|"job_events"|"merge_requests_events"|"milestone_events"|"note_events"|"pipeline_events"|"push_events"|"releases_events"|"resource_access_token_events"|"resource_deploy_token_events"|"tag_push_events"|"wiki_page_events"|"vulnerability_events" trigger, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/test/${getEncodedUri(trigger)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Resend a webhook event
    #
    # + hookId - The ID of the hook
    # + hookLogId - The ID of the hook log entry
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resendGroupHookEvent(int hookId, int hookLogId, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/events/${getEncodedUri(hookLogId)}/resend`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve SAML identities for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group saml identities 
    remote isolated function listGroupSamlIdentities(string id, map<string|string[]> headers = {}) returns IdentityDetail[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml/identities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a SAML identity
    #
    # + id - The ID of a group
    # + uid - External UID of the user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved saml identity 
    remote isolated function getSamlIdentity(string id, string uid, map<string|string[]> headers = {}) returns IdentityDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml/${getEncodedUri(uid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a SAML identity
    #
    # + id - The ID of a group
    # + uid - Current external UID of the user
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSamlIdentity(string id, string uid, map<string|string[]> headers = {}) returns IdentityDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml/${getEncodedUri(uid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update `extern_uid` field for a SAML identity
    #
    # + id - The ID of a group
    # + uid - Current external UID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update `extern_uid` field for a SAML identity 
    # + return - The updated saml identity 
    remote isolated function updateSamlIdentity(string id, string uid, UpdateIdentityRequest payload, map<string|string[]> headers = {}) returns IdentityDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml/${getEncodedUri(uid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Retrieve SCIM identities for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group scim identities 
    remote isolated function listGroupScimIdentities(string id, map<string|string[]> headers = {}) returns IdentityDetail[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/scim/identities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a SCIM identity
    #
    # + id - The ID of a group
    # + uid - External UID of the user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved scim identity 
    remote isolated function getScimIdentity(string id, string uid, map<string|string[]> headers = {}) returns IdentityDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/scim/${getEncodedUri(uid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a SCIM identity
    #
    # + id - The ID of a group
    # + uid - Current external UID of the user
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteScimIdentity(string id, string uid, map<string|string[]> headers = {}) returns IdentityDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/scim/${getEncodedUri(uid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update `extern_uid` field for a SCIM identity
    #
    # + id - The ID of a group
    # + uid - Current external UID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update `extern_uid` field for a SCIM identity 
    # + return - The updated scim identity 
    remote isolated function updateScimIdentity(string id, string uid, UpdateIdentityRequest payload, map<string|string[]> headers = {}) returns IdentityDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/scim/${getEncodedUri(uid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all group member roles
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group member roles 
    remote isolated function listGroupMemberRoles(IdOrPath id, map<string|string[]> headers = {}) returns MemberRole[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/member_roles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a member role to a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a member role to a group 
    # + return - Created 
    remote isolated function addGroupMemberRole(IdOrPath id, CreateMemberRoleRequest payload, map<string|string[]> headers = {}) returns MemberRole|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/member_roles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove member role of a group
    #
    # + id - The ID or URL-encoded path of the group
    # + memberRoleId - The ID of the Group-Member Role to be deleted
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function removeGroupMemberRole(IdOrPath id, int memberRoleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/member_roles/${getEncodedUri(memberRoleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all instance member roles
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved instance member roles 
    remote isolated function listInstanceMemberRoles(map<string|string[]> headers = {}) returns MemberRole[]|error {
        string resourcePath = string `/member_roles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an instance member role
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an instance member role 
    # + return - Created 
    remote isolated function createInstanceMemberRole(CreateMemberRoleRequest payload, map<string|string[]> headers = {}) returns MemberRole|error {
        string resourcePath = string `/member_roles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete an instance member role
    #
    # + memberRoleId - ID of the member role
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteInstanceMemberRole(int memberRoleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/member_roles/${getEncodedUri(memberRoleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all merge trains for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge trains 
    remote isolated function listMergeTrains(IdOrPath id, map<string|string[]> headers = {}, *ListMergeTrainsQueries queries) returns MergeTrainsCar[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_trains`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge requests in a merge train
    #
    # + id - The ID or URL-encoded path of the project
    # + targetBranch - The target branch of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge train merge requests 
    remote isolated function listMergeTrainMergeRequests(IdOrPath id, string targetBranch, map<string|string[]> headers = {}, *ListMergeTrainMergeRequestsQueries queries) returns MergeTrainsCar[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_trains/${getEncodedUri(targetBranch)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve merge train status
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge train merge request 
    remote isolated function getMergeTrainMergeRequest(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeTrainsCar|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_trains/merge_requests/${getEncodedUri(mergeRequestIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a merge request to a merge train
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of the merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a merge request to a merge train 
    # + return - Created 
    remote isolated function addMergeRequestToMergeTrain(IdOrPath id, int mergeRequestIid, AddMergeRequestToMergeTrainRequest payload, map<string|string[]> headers = {}) returns MergeTrainsCar|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_trains/merge_requests/${getEncodedUri(mergeRequestIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the push rules of a group
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group push rules 
    remote isolated function getGroupPushRules(string id, map<string|string[]> headers = {}) returns GroupPushRule|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update push rules of a group
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update push rules of a group 
    # + return - The updated group push rules 
    remote isolated function updateGroupPushRules(string id, GroupPushRulesRequest payload, map<string|string[]> headers = {}) returns GroupPushRule|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Add push rules to a group
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add push rules to a group 
    # + return - Created 
    remote isolated function addGroupPushRules(string id, GroupPushRulesRequest payload, map<string|string[]> headers = {}) returns GroupPushRule|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete the push rules of a group
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupPushRules(string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Gets agentw info
    #
    # + headers - Headers to be sent with the request 
    # + return - Agentw info retrieved successfully 
    remote isolated function getAgentwInfo(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/internal/agents/agentw/agent_info`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # authorize_user_access
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - User access authorization info retrieved successfully 
    remote isolated function authorizeAgentwUserAccess(map<string|string[]> headers = {}, *AuthorizeAgentwUserAccessQueries queries) returns error? {
        string resourcePath = string `/internal/agents/agentw/authorize_user_access`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # server_config
    #
    # + headers - Headers to be sent with the request 
    # + return - Server config retrieved successfully 
    remote isolated function getAgentwServerConfig(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/internal/agents/agentw/server_config`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the push rules of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project push rules 
    remote isolated function getProjectPushRules(IdOrPath id, map<string|string[]> headers = {}) returns ProjectPushRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update push rules of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update push rules of a project 
    # + return - The updated project push rules 
    remote isolated function updateProjectPushRules(IdOrPath id, ProjectPushRulesRequest payload, map<string|string[]> headers = {}) returns ProjectPushRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Add push rules to a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add push rules to a project 
    # + return - Created 
    remote isolated function addProjectPushRules(IdOrPath id, ProjectPushRulesRequest payload, map<string|string[]> headers = {}) returns ProjectPushRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete the push rules of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectPushRules(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/push_rule`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve project pull mirror details
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project pull mirror 
    remote isolated function getProjectPullMirror(IdOrPath id, map<string|string[]> headers = {}) returns PullMirror|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/mirror/pull`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update project pull mirroring settings
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update project pull mirroring settings 
    # + return - The updated project pull mirror 
    remote isolated function updateProjectPullMirror(IdOrPath id, UpdateProjectPullMirrorRequest payload, map<string|string[]> headers = {}) returns PullMirror|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/mirror/pull`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Start the pull mirroring process for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to start the pull mirroring process for a project 
    # + return - Success 
    remote isolated function startProjectPullMirror(IdOrPath id, StartProjectPullMirrorRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/mirror/pull`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve license information
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved license 
    remote isolated function getLicense(map<string|string[]> headers = {}) returns GitlabLicenseWithActiveUsers|error {
        string resourcePath = string `/license`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a license
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a license 
    # + return - Created 
    remote isolated function createLicense(CreateLicenseRequest payload, map<string|string[]> headers = {}) returns GitlabLicenseWithActiveUsers|error {
        string resourcePath = string `/license`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download license usage data
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadLicenseUsageData(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/license/usage_export`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a license
    #
    # + id - ID of the GitLab license
    # + headers - Headers to be sent with the request 
    # + return - The retrieved license by id 
    remote isolated function getLicenseById(int id, map<string|string[]> headers = {}) returns GitlabLicenseWithActiveUsers|error {
        string resourcePath = string `/license/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a license
    #
    # + id - ID of the GitLab license
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteLicense(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/license/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Trigger recalculation of billable users
    #
    # + id - ID of the GitLab license
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function refreshLicenseBillableUsers(int id, map<string|string[]> headers = {}) returns BasicSuccess|error {
        string resourcePath = string `/license/${getEncodedUri(id)}/refresh_billable_users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all licenses
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved licenses 
    remote isolated function listLicenses(map<string|string[]> headers = {}) returns GitlabLicense[]|error {
        string resourcePath = string `/licenses`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all LDAP group links
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group ldap links 
    remote isolated function listGroupLdapLinks(IdOrPath id, map<string|string[]> headers = {}) returns LdapGroupLink[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ldap_group_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an LDAP group link with CN or filter
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an LDAP group link with CN or filter 
    # + return - Created 
    remote isolated function addGroupLdapLink(IdOrPath id, AddGroupLdapLinkRequest payload, map<string|string[]> headers = {}) returns LdapGroupLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ldap_group_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete an LDAP group link with CN or filter
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteGroupLdapLinkByFilter(IdOrPath id, map<string|string[]> headers = {}, *DeleteGroupLdapLinkByFilterQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ldap_group_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Delete LDAP group link
    #
    # + id - The ID or URL-encoded path of the group
    # + cn - The CN of a LDAP group
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteGroupLdapLink(IdOrPath id, string cn, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ldap_group_links/${getEncodedUri(cn)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Delete LDAP group link
    #
    # + id - The ID or URL-encoded path of the group
    # + cn - The CN of a LDAP group
    # + provider - LDAP provider for the LDAP group link
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteGroupLdapLinkForProvider(IdOrPath id, string cn, string provider, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ldap_group_links/${getEncodedUri(provider)}/${getEncodedUri(cn)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all LDAP groups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved ldap groups 
    remote isolated function listLdapGroups(map<string|string[]> headers = {}, *ListLdapGroupsQueries queries) returns LdapGroup[]|error {
        string resourcePath = string `/ldap/groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all LDAP groups by provider
    #
    # + provider - The LDAP provider name
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved ldap provider groups 
    remote isolated function listLdapProviderGroups(string provider, map<string|string[]> headers = {}, *ListLdapProviderGroupsQueries queries) returns LdapGroup[]|error {
        string resourcePath = string `/ldap/${getEncodedUri(provider)}/groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Geo sites
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved geo sites 
    remote isolated function listGeoSites(map<string|string[]> headers = {}, *ListGeoSitesQueries queries) returns GeoSite[]|error {
        string resourcePath = string `/geo_sites`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a Geo site
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Geo site 
    # + return - The created geo site 
    remote isolated function createGeoSite(CreateGeoSiteRequest payload, map<string|string[]> headers = {}) returns GeoSite|error {
        string resourcePath = string `/geo_sites`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all Geo site statuses
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved geo site statuses 
    remote isolated function listGeoSiteStatuses(map<string|string[]> headers = {}, *ListGeoSiteStatusesQueries queries) returns GeoSiteStatus[]|error {
        string resourcePath = string `/geo_sites/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a Geo site
    #
    # + id - The ID of the site
    # + headers - Headers to be sent with the request 
    # + return - The retrieved geo site 
    remote isolated function getGeoSite(int id, map<string|string[]> headers = {}) returns GeoSite|error {
        string resourcePath = string `/geo_sites/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a Geo site
    #
    # + id - The ID of the site
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Geo site 
    # + return - The updated geo site 
    remote isolated function updateGeoSite(int id, UpdateGeoSiteRequest payload, map<string|string[]> headers = {}) returns GeoSite|error {
        string resourcePath = string `/geo_sites/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a Geo site
    #
    # + id - The ID of the site
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteGeoSite(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo_sites/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a Geo site status
    #
    # + id - The ID of the site
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved geo site status 
    remote isolated function getGeoSiteStatus(int id, map<string|string[]> headers = {}, *GetGeoSiteStatusQueries queries) returns GeoSiteStatus|error {
        string resourcePath = string `/geo_sites/${getEncodedUri(id)}/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Repair a Geo site
    #
    # + id - The ID of the site
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function repairGeoSite(int id, map<string|string[]> headers = {}) returns GeoSiteStatus|error {
        string resourcePath = string `/geo_sites/${getEncodedUri(id)}/repair`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all Geo nodes
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved geo nodes 
    remote isolated function listGeoNodes(map<string|string[]> headers = {}, *ListGeoNodesQueries queries) returns GeoNode[]|error {
        string resourcePath = string `/geo_nodes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a Geo node
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Geo node 
    # + return - The created geo node 
    remote isolated function createGeoNode(CreateGeoNodeRequest payload, map<string|string[]> headers = {}) returns GeoNode|error {
        string resourcePath = string `/geo_nodes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all Geo node statuses
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved geo node statuses 
    remote isolated function listGeoNodeStatuses(map<string|string[]> headers = {}, *ListGeoNodeStatusesQueries queries) returns GeoNodeStatus[]|error {
        string resourcePath = string `/geo_nodes/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a single Geo node
    #
    # + id - The ID of the node
    # + headers - Headers to be sent with the request 
    # + return - The retrieved geo node 
    remote isolated function getGeoNode(int id, map<string|string[]> headers = {}) returns GeoNode|error {
        string resourcePath = string `/geo_nodes/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a Geo node
    #
    # + id - The ID of the node
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Geo node 
    # + return - The updated geo node 
    remote isolated function updateGeoNode(int id, UpdateGeoNodeRequest payload, map<string|string[]> headers = {}) returns GeoNode|error {
        string resourcePath = string `/geo_nodes/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a Geo node
    #
    # + id - The ID of the node
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteGeoNode(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo_nodes/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve metrics for a Geo node
    #
    # + id - The ID of the node
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved geo node status 
    remote isolated function getGeoNodeStatus(int id, map<string|string[]> headers = {}, *GetGeoNodeStatusQueries queries) returns GeoNodeStatus|error {
        string resourcePath = string `/geo_nodes/${getEncodedUri(id)}/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Repair authentication of the Geo node
    #
    # + id - The ID of the node
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function repairGeoNode(int id, map<string|string[]> headers = {}) returns GeoNodeStatus|error {
        string resourcePath = string `/geo_nodes/${getEncodedUri(id)}/repair`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all experiments
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved experiments 
    remote isolated function listExperiments(map<string|string[]> headers = {}) returns Experiment[]|error {
        string resourcePath = string `/experiments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete cached assignments for an experiment
    #
    # + name - Experiment cache key
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteExperimentCache(string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/experiments/${getEncodedUri(name)}/cache`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get current variant assignment for an experiment context
    #
    # + experimentName - The experiment name
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved variant assignment 
    remote isolated function getVariantAssignment(string experimentName, map<string|string[]> headers = {}, *GetVariantAssignmentQueries queries) returns ExperimentsAssignment|error {
        string resourcePath = string `/experiments/${getEncodedUri(experimentName)}/assignments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Force a variant assignment for an experiment context
    #
    # + experimentName - The experiment name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to force a variant assignment for an experiment context 
    # + return - Success 
    remote isolated function forceVariantAssignment(string experimentName, ForceVariantAssignmentRequest payload, map<string|string[]> headers = {}) returns ExperimentsAssignment|error {
        string resourcePath = string `/experiments/${getEncodedUri(experimentName)}/assignments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Clear a variant assignment for an experiment context
    #
    # + experimentName - The experiment name
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function clearVariantAssignment(string experimentName, map<string|string[]> headers = {}, *ClearVariantAssignmentQueries queries) returns error? {
        string resourcePath = string `/experiments/${getEncodedUri(experimentName)}/assignments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rollout namespaces to be indexed up to n%
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to roll out namespace indexing up to a percentage 
    # + return - Success 
    remote isolated function rolloutElasticsearchNamespaces(ElasticsearchNamespacesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/elasticsearch_indexed_namespaces/rollout`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Rollback namespaces to be indexed down to n%
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rollback namespaces to be indexed down to n% 
    # + return - Success 
    remote isolated function rollbackElasticsearchNamespaces(ElasticsearchNamespacesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/elasticsearch_indexed_namespaces/rollback`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all related epic links for a group
    #
    # + id - ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved related epic links 
    remote isolated function listRelatedEpicLinks(string id, map<string|string[]> headers = {}, *ListRelatedEpicLinksQueries queries) returns RelatedEpic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/related_epic_links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all linked epics for an epic
    #
    # + id - ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved linked epics 
    remote isolated function listLinkedEpics(string id, int epicIid, map<string|string[]> headers = {}) returns RelatedEpic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/related_epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a related epic link
    #
    # + id - ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a related epic link 
    # + return - Created 
    remote isolated function createRelatedEpicLink(string id, int epicIid, CreateRelatedEpicLinkRequest payload, map<string|string[]> headers = {}) returns RelatedEpicLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/related_epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a related epic link
    #
    # + id - ID or URL-encoded path of the group
    # + relatedEpicLinkId - Internal ID of a related epic link
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRelatedEpicLink(string id, int relatedEpicLinkId, int epicIid, map<string|string[]> headers = {}) returns RelatedEpicLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/related_epics/${getEncodedUri(relatedEpicLinkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Proxy the download of a NPM package tarball
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function proxyNpmTarballDownload(IdOrPath id, string packageName, string fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/dependency_proxy/packages/npm/${getEncodedUri(packageName)}/-/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Proxy the download of a maven package file at a project level
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Package file name
    # + path - Package path
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function proxyMavenPackageDownload(IdOrPath id, string fileName, string path, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/dependency_proxy/packages/maven/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all epic boards in a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group epic boards 
    remote isolated function listGroupEpicBoards(string id, map<string|string[]> headers = {}, *ListGroupEpicBoardsQueries queries) returns EpicBoard[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epic_boards`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a group epic board
    #
    # + id - The ID of a group
    # + boardId - The ID of an epic board
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group epic board 
    remote isolated function getGroupEpicBoard(string id, int boardId, map<string|string[]> headers = {}) returns EpicBoard|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epic_boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group epic board lists
    #
    # + id - The ID of a group
    # + boardId - The ID of an epic board
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group epic board lists 
    remote isolated function listGroupEpicBoardLists(string id, int boardId, map<string|string[]> headers = {}, *ListGroupEpicBoardListsQueries queries) returns EpicBoardsList[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epic_boards/${getEncodedUri(boardId)}/lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a group epic board list
    #
    # + id - The ID of a group
    # + boardId - The ID of an epic board
    # + listId - The ID of a list
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group epic board list 
    remote isolated function getGroupEpicBoardList(string id, int boardId, int listId, map<string|string[]> headers = {}) returns EpicBoardsList|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epic_boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group epics
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group epics 
    remote isolated function listGroupEpics(string id, map<string|string[]> headers = {}, *ListGroupEpicsQueries queries) returns Epic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "not[labels]": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group epics
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved epics 
    remote isolated function listEpics(string id, map<string|string[]> headers = {}, *ListEpicsQueries queries) returns Epic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "not[labels]": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a new epic
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a new epic 
    # + return - Created 
    remote isolated function createEpic(string id, CreateEpicRequest payload, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get details of an epic
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group epic 
    remote isolated function getGroupEpic(string id, int epicIid, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get details of an epic
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic 
    remote isolated function getEpic(string id, int epicIid, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an epic
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an epic 
    # + return - The updated epic 
    remote isolated function updateEpic(string id, int epicIid, UpdateEpicRequest payload, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Destroy an epic
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteEpic(string id, int epicIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get related epics
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved child epics 
    remote isolated function listChildEpics(string id, int epicIid, map<string|string[]> headers = {}) returns Epic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create and relate epic to a parent
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create and relate epic to a parent 
    # + return - Created 
    remote isolated function createChildEpic(string id, int epicIid, CreateChildEpicRequest payload, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/epics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reorder child epics
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + childEpicId - The global ID of the child epic. Internal ID can't be used because they can conflict with epics from other groups
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to reorder child epics 
    # + return - Success 
    remote isolated function reorderChildEpic(string id, int epicIid, int childEpicId, ReorderChildEpicRequest payload, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/epics/${getEncodedUri(childEpicId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Relate epics
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + childEpicId - The global ID of the child epic. Internal ID can't be used because they can conflict with epics from other groups
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function assignChildEpic(string id, int epicIid, int childEpicId, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/epics/${getEncodedUri(childEpicId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove epics relation
    #
    # + id - The ID of a group
    # + epicIid - The internal ID of an epic
    # + childEpicId - The global ID of the child epic. Internal ID can't be used because they can conflict with epics from other groups
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function removeChildEpic(string id, int epicIid, int childEpicId, map<string|string[]> headers = {}) returns Epic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/epics/${getEncodedUri(childEpicId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a related epic link
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + epicIid - The internal ID of the epic
    # + epicIssueId - The ID of the epic-issue association to update
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a related epic link 
    # + return - The updated epic issue 
    remote isolated function updateEpicIssue(IdOrPath id, IdOrPath epicIid, IdOrPath epicIssueId, UpdateEpicIssueRequest payload, map<string|string[]> headers = {}) returns EpicIssue|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/issues/${getEncodedUri(epicIssueId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Add an issue to an epic
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + epicIid - The internal ID of the epic
    # + epicIssueId - The ID of the issue to assign to the epic
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function addEpicIssue(IdOrPath id, IdOrPath epicIid, IdOrPath epicIssueId, map<string|string[]> headers = {}) returns EpicIssueLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/issues/${getEncodedUri(epicIssueId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove an issue from the epic
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + epicIid - The internal ID of the epic
    # + epicIssueId - The ID of the association
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function removeEpicIssue(IdOrPath id, IdOrPath epicIid, IdOrPath epicIssueId, map<string|string[]> headers = {}) returns EpicIssueLink|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/issues/${getEncodedUri(epicIssueId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all issues for an epic
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + epicIid - The internal ID of the epic
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group epic issues 
    remote isolated function listGroupEpicIssues(IdOrPath id, IdOrPath epicIid, map<string|string[]> headers = {}, *ListGroupEpicIssuesQueries queries) returns EpicIssue[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all issues for an epic
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + epicIid - The internal ID of the epic
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved epic issues 
    remote isolated function listEpicIssues(IdOrPath id, IdOrPath epicIid, map<string|string[]> headers = {}, *ListEpicIssuesQueries queries) returns EpicIssue[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/epics/${getEncodedUri(epicIid)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve project-level DORA metrics
    #
    # + id - The ID or URL-encoded path of the project can be accessed by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - successful operation 
    remote isolated function getProjectDoraMetrics(IdOrPath id, map<string|string[]> headers = {}, *GetProjectDoraMetricsQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/dora/metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"environment_tiers": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve group-level DORA metrics
    #
    # + id - The ID of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - successful operation 
    remote isolated function getGroupDoraMetrics(string id, map<string|string[]> headers = {}, *GetGroupDoraMetricsQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/dora/metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"environment_tiers": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get all project approval rules
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project approval settings 
    remote isolated function getProjectApprovalSettings(IdOrPath id, map<string|string[]> headers = {}, *GetProjectApprovalSettingsQueries queries) returns ProjectApprovalSettings|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create new approval rule
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create new approval rule 
    # + return - Created 
    remote isolated function createApprovalSettingsRule(IdOrPath id, CreateApprovalRuleRequest payload, map<string|string[]> headers = {}) returns ProjectApprovalSettingRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_settings/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update approval rule
    #
    # + id - The ID or URL-encoded path of the project
    # + approvalRuleId - The ID of an approval_rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update approval rule 
    # + return - The updated approval settings rule 
    remote isolated function updateApprovalSettingsRule(IdOrPath id, int approvalRuleId, UpdateApprovalRuleRequest payload, map<string|string[]> headers = {}) returns ProjectApprovalSettingRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_settings/rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an approval rule
    #
    # + id - The ID or URL-encoded path of the project
    # + approvalRuleId - The ID of an approval_rule
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteApprovalSettingsRule(IdOrPath id, int approvalRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_settings/rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve project external status check services
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved external status checks 
    remote isolated function listExternalStatusChecks(IdOrPath id, map<string|string[]> headers = {}, *ListExternalStatusChecksQueries queries) returns ExternalStatusCheck[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/external_status_checks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create external status check service
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create external status check service 
    # + return - Created 
    remote isolated function createExternalStatusCheck(IdOrPath id, CreateExternalStatusCheckRequest payload, map<string|string[]> headers = {}) returns ExternalStatusCheck|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/external_status_checks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update external status check service
    #
    # + id - The ID or URL-encoded path of the project
    # + checkId - ID of an external status check
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update external status check service 
    # + return - The updated external status check 
    remote isolated function updateExternalStatusCheck(IdOrPath id, int checkId, UpdateExternalStatusCheckRequest payload, map<string|string[]> headers = {}) returns ExternalStatusCheck|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/external_status_checks/${getEncodedUri(checkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete external status check service
    #
    # + id - The ID or URL-encoded path of the project
    # + checkId - ID of an external status check
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteExternalStatusCheck(IdOrPath id, int checkId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/external_status_checks/${getEncodedUri(checkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update status of an external status check
    #
    # + id - ID of a project
    # + mergeRequestIid - IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update status of an external status check 
    # + return - Created 
    remote isolated function setStatusCheckResponse(string id, int mergeRequestIid, SetStatusCheckResponseRequest payload, map<string|string[]> headers = {}) returns MergeRequestsStatusCheckResponse|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/status_check_responses`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all status checks for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request status checks 
    remote isolated function listMergeRequestStatusChecks(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeRequestsStatusCheck[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/status_checks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retry failed status check for a merge request
    #
    # + id - ID of a project
    # + mergeRequestIid - IID of a merge request
    # + externalStatusCheckId - ID of a failed external status check
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function retryMergeRequestStatusCheck(string id, int mergeRequestIid, int externalStatusCheckId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/status_checks/${getEncodedUri(externalStatusCheckId)}/retry`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all approval rules for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group approval rules 
    remote isolated function listGroupApprovalRules(string id, map<string|string[]> headers = {}, *ListGroupApprovalRulesQueries queries) returns GroupApprovalRule[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/approval_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an approval rule for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an approval rule for a group 
    # + return - Created 
    remote isolated function createGroupApprovalRule(string id, CreateGroupApprovalRuleRequest payload, map<string|string[]> headers = {}) returns GroupApprovalRule|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/approval_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update group approval rule
    #
    # + id - The ID of a group
    # + approvalRuleId - The ID of an approval rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update group approval rule 
    # + return - The updated group approval rule 
    remote isolated function updateGroupApprovalRule(string id, int approvalRuleId, UpdateGroupApprovalRuleRequest payload, map<string|string[]> headers = {}) returns GroupApprovalRule|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Update group security settings
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update group security settings 
    # + return - Success 
    remote isolated function updateGroupSecuritySettings(IdOrPath id, UpdateGroupSecuritySettingsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/security_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all project security settings
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectSecuritySettings(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/security_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update project security settings
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update project security settings 
    # + return - Success 
    remote isolated function updateProjectSecuritySettings(IdOrPath id, UpdateProjectSecuritySettingsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/security_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all approval rules for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project approval rules 
    remote isolated function listProjectApprovalRules(IdOrPath id, map<string|string[]> headers = {}, *ListProjectApprovalRulesQueries queries) returns ProjectApprovalRule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an approval rule for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an approval rule for a project 
    # + return - Created 
    remote isolated function createProjectApprovalRule(IdOrPath id, CreateApprovalRuleRequest payload, map<string|string[]> headers = {}) returns ProjectApprovalRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an approval rule for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + approvalRuleId - The ID of an approval rule
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project approval rule 
    remote isolated function getProjectApprovalRule(IdOrPath id, int approvalRuleId, map<string|string[]> headers = {}) returns ProjectApprovalRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an approval rule for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + approvalRuleId - The ID of an approval_rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an approval rule for a project 
    # + return - The updated project approval rule 
    remote isolated function updateProjectApprovalRule(IdOrPath id, int approvalRuleId, UpdateApprovalRuleRequest payload, map<string|string[]> headers = {}) returns ProjectApprovalRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an approval rule for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + approvalRuleId - The ID of an approval_rule
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectApprovalRule(IdOrPath id, int approvalRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/approval_rules/${getEncodedUri(approvalRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all agent URL configurations
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + headers - Headers to be sent with the request 
    # + return - The retrieved agent url configurations 
    remote isolated function listAgentUrlConfigurations(IdOrPath id, int agentId, map<string|string[]> headers = {}) returns ClustersAgentUrlConfiguration[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/url_configurations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an agent URL configuration
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an agent URL configuration 
    # + return - Created 
    remote isolated function createAgentUrlConfiguration(IdOrPath id, int agentId, CreateAgentUrlConfigurationRequest payload, map<string|string[]> headers = {}) returns ClustersAgentUrlConfiguration|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/url_configurations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an agent URL configuration
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + urlConfigurationId - The ID of the agent url configuration
    # + headers - Headers to be sent with the request 
    # + return - The retrieved agent url configuration 
    remote isolated function getAgentUrlConfiguration(IdOrPath id, int agentId, int urlConfigurationId, map<string|string[]> headers = {}) returns ClustersAgentUrlConfiguration|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/url_configurations/${getEncodedUri(urlConfigurationId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an agent URL configuration
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + urlConfigurationId - The ID of the agent url configuration
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteAgentUrlConfiguration(IdOrPath id, int agentId, int urlConfigurationId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/url_configurations/${getEncodedUri(urlConfigurationId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all runner controller tokens
    #
    # + runnerControllerId - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved runner controller tokens 
    remote isolated function listRunnerControllerTokens(int runnerControllerId, map<string|string[]> headers = {}, *ListRunnerControllerTokensQueries queries) returns CiRunnerControllerToken[]|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(runnerControllerId)}/tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a runner controller token
    #
    # + runnerControllerId - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a runner controller token 
    # + return - Created 
    remote isolated function createRunnerControllerToken(int runnerControllerId, CreateRunnerControllerTokenRequest payload, map<string|string[]> headers = {}) returns CiRunnerControllerToken|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(runnerControllerId)}/tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a single runner controller token
    #
    # + runnerControllerId - ID of the runner controller
    # + id - ID of the runner controller token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved runner controller token 
    remote isolated function getRunnerControllerToken(int runnerControllerId, int id, map<string|string[]> headers = {}) returns CiRunnerControllerToken|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(runnerControllerId)}/tokens/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a runner controller token
    #
    # + runnerControllerId - ID of the runner controller
    # + id - ID of the runner controller token
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeRunnerControllerToken(int runnerControllerId, int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/runner_controllers/${getEncodedUri(runnerControllerId)}/tokens/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a runner controller token
    #
    # + runnerControllerId - ID of the runner controller
    # + id - ID of the runner controller token
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function rotateRunnerControllerToken(int runnerControllerId, int id, map<string|string[]> headers = {}) returns CiRunnerControllerTokenWithToken|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(runnerControllerId)}/tokens/${getEncodedUri(id)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all scopes for a runner controller
    #
    # + id - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listRunnerControllerScopes(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}/scopes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add instance scope
    #
    # + id - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function addRunnerControllerInstanceScope(int id, map<string|string[]> headers = {}) returns CiRunnerControllerInstanceLevelScoping|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}/scopes/instance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove instance scope
    #
    # + id - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function removeRunnerControllerInstanceScope(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}/scopes/instance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Add runner scope
    #
    # + id - ID of the runner controller
    # + runnerId - ID of the runner
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function addRunnerControllerRunnerScope(int id, int runnerId, map<string|string[]> headers = {}) returns CiRunnerControllerRunnerLevelScoping|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}/scopes/runners/${getEncodedUri(runnerId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove runner scope
    #
    # + id - ID of the runner controller
    # + runnerId - ID of the runner
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function removeRunnerControllerRunnerScope(int id, int runnerId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}/scopes/runners/${getEncodedUri(runnerId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all runner controllers
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved runner controllers 
    remote isolated function listRunnerControllers(map<string|string[]> headers = {}, *ListRunnerControllersQueries queries) returns CiRunnerController[]|error {
        string resourcePath = string `/runner_controllers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Register a runner controller
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to register a runner controller 
    # + return - Created 
    remote isolated function registerRunnerController(RegisterRunnerControllerRequest payload, map<string|string[]> headers = {}) returns CiRunnerController|error {
        string resourcePath = string `/runner_controllers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a single runner controller
    #
    # + id - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + return - The retrieved runner controller 
    remote isolated function getRunnerController(int id, map<string|string[]> headers = {}) returns CiRunnerControllerDetail|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a runner controller
    #
    # + id - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a runner controller 
    # + return - The updated runner controller 
    remote isolated function updateRunnerController(int id, UpdateRunnerControllerRequest payload, map<string|string[]> headers = {}) returns CiRunnerController|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a runner controller
    #
    # + id - ID of the runner controller
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRunnerController(int id, map<string|string[]> headers = {}) returns CiRunnerController|error {
        string resourcePath = string `/runner_controllers/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all instance audit events
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved instance audit events 
    remote isolated function listInstanceAuditEvents(map<string|string[]> headers = {}, *ListInstanceAuditEventsQueries queries) returns AuditEvent[]|error {
        string resourcePath = string `/audit_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an instance audit event
    #
    # + id - The ID of audit event
    # + headers - Headers to be sent with the request 
    # + return - The retrieved instance audit event 
    remote isolated function getInstanceAuditEvent(int id, map<string|string[]> headers = {}) returns AuditEvent|error {
        string resourcePath = string `/audit_events/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Set the AI-recommended reviewers for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set the AI-recommended reviewers for a merge request 
    # + return - Created 
    remote isolated function setSuggestedReviewers(IdOrPath id, int mergeRequestIid, SetSuggestedReviewersRequest payload, map<string|string[]> headers = {}) returns AiSuggestedReviewer|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/suggested_reviewers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve information about a model record
    #
    # + modelName - See https://docs.gitlab.com/administration/admin_area/#data-management for the list of allowed values
    # + recordIdentifier - The identifier of the model record
    # + headers - Headers to be sent with the request 
    # + return - The retrieved model record 
    remote isolated function getModelRecord(string modelName, IdOrPath recordIdentifier, map<string|string[]> headers = {}) returns AdminModel|error {
        string resourcePath = string `/admin/data_management/${getEncodedUri(modelName)}/${getEncodedUri(recordIdentifier)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Recalculate the checksum of a model record
    #
    # + modelName - See https://docs.gitlab.com/administration/admin_area/#data-management for the list of allowed values
    # + recordIdentifier - The identifier of the model record
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function recalculateModelRecordChecksum(string modelName, int recordIdentifier, map<string|string[]> headers = {}) returns AdminModel|error {
        string resourcePath = string `/admin/data_management/${getEncodedUri(modelName)}/${getEncodedUri(recordIdentifier)}/checksum`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve model information
    #
    # + modelName - See https://docs.gitlab.com/administration/admin_area/#data-management for the list of allowed values
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved data management model 
    remote isolated function getDataManagementModel(string modelName, map<string|string[]> headers = {}, *GetDataManagementModelQueries queries) returns AdminModel|error {
        string resourcePath = string `/admin/data_management/${getEncodedUri(modelName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"identifiers": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Recalculate checksums for model records
    #
    # + modelName - See https://docs.gitlab.com/administration/admin_area/#data-management for the list of allowed values
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to recalculate checksums for model records 
    # + return - Success 
    remote isolated function recalculateModelChecksums(string modelName, RecalculateModelChecksumsRequest payload, map<string|string[]> headers = {}) returns AdminModel|error {
        string resourcePath = string `/admin/data_management/${getEncodedUri(modelName)}/checksum`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve security policy settings
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved security policy settings 
    remote isolated function getSecurityPolicySettings(map<string|string[]> headers = {}) returns AdminSecurityPolicySetting|error {
        string resourcePath = string `/admin/security/compliance_policy_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update security policy settings
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update security policy settings 
    # + return - The updated security policy settings 
    remote isolated function updateSecurityPolicySettings(UpdateSecurityPolicySettingsRequest payload, map<string|string[]> headers = {}) returns AdminSecurityPolicySetting|error {
        string resourcePath = string `/admin/security/compliance_policy_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Get all enabled namespaces for Knowledge Graph
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved knowledge graph namespaces 
    remote isolated function listKnowledgeGraphNamespaces(map<string|string[]> headers = {}) returns AnalyticsKnowledgeGraphEnabledNamespace[]|error {
        string resourcePath = string `/admin/knowledge_graph/namespaces`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Enable a namespace for Knowledge Graph
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function enableKnowledgeGraphNamespace(IdOrPath id, map<string|string[]> headers = {}) returns AnalyticsKnowledgeGraphEnabledNamespace|error {
        string resourcePath = string `/admin/knowledge_graph/namespaces/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Disable a namespace for Knowledge Graph
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function disableKnowledgeGraphNamespace(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin/knowledge_graph/namespaces/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all advanced search migrations
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listAdvancedSearchMigrations(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin/search/migrations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an advanced search migration
    #
    # + migrationId - The version or name of the search migration
    # + headers - Headers to be sent with the request 
    # + return - The retrieved advanced search migration 
    remote isolated function getAdvancedSearchMigration(IdOrPath migrationId, map<string|string[]> headers = {}) returns SearchMigration|error {
        string resourcePath = string `/admin/search/migrations/${getEncodedUri(migrationId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Triggers indexing for the specified project
    #
    # + projectId - The id of the project you want to index
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function triggerZoektProjectIndexing(int projectId, map<string|string[]> headers = {}) returns SearchZoektProjectIndexSuccess|error {
        string resourcePath = string `/admin/zoekt/projects/${getEncodedUri(projectId)}/index`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Update the number of replicas override for an enabled namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the number of replicas override for an enabled namespace 
    # + return - The updated zoekt namespace replicas 
    remote isolated function updateZoektNamespaceReplicas(IdOrPath id, UpdateZoektNamespaceReplicasRequest payload, map<string|string[]> headers = {}) returns SearchZoektIndexedNamespace|error {
        string resourcePath = string `/admin/zoekt/namespaces/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get all the Zoekt nodes
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved zoekt nodes 
    remote isolated function listZoektNodes(map<string|string[]> headers = {}) returns SearchZoektNode[]|error {
        string resourcePath = string `/admin/zoekt/shards`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get all the indexed namespaces for this node
    #
    # + nodeId - The id of the Search::Zoekt::Node
    # + headers - Headers to be sent with the request 
    # + return - The retrieved zoekt node namespaces 
    remote isolated function listZoektNodeNamespaces(int nodeId, map<string|string[]> headers = {}) returns SearchZoektIndexedNamespace[]|error {
        string resourcePath = string `/admin/zoekt/shards/${getEncodedUri(nodeId)}/indexed_namespaces`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a namespace to a node for Zoekt indexing
    #
    # + nodeId - The id of the Search::Zoekt::Node
    # + namespaceId - The id of the namespace you want to index in this node
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a namespace to a node for Zoekt indexing 
    # + return - The created zoekt node namespace 
    remote isolated function addZoektNodeNamespace(int nodeId, int namespaceId, AddZoektNodeNamespaceRequest payload, map<string|string[]> headers = {}) returns SearchZoektIndexedNamespace|error {
        string resourcePath = string `/admin/zoekt/shards/${getEncodedUri(nodeId)}/indexed_namespaces/${getEncodedUri(namespaceId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Remove a namespace from a node for Zoekt indexing
    #
    # + nodeId - The id of the Search::Zoekt::Node
    # + namespaceId - The id of the namespace you want to remove from this node
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function removeZoektNodeNamespace(int nodeId, int namespaceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin/zoekt/shards/${getEncodedUri(nodeId)}/indexed_namespaces/${getEncodedUri(namespaceId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get all ActiveContext connections
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved active context connections 
    remote isolated function listActiveContextConnections(map<string|string[]> headers = {}) returns AiActiveContextConnection[]|error {
        string resourcePath = string `/admin/active_context/connections`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Activate an ActiveContext connection
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to activate an ActiveContext connection 
    # + return - Success 
    remote isolated function activateActiveContextConnection(ActivateActiveContextConnectionRequest payload, map<string|string[]> headers = {}) returns AiActiveContextConnection|error {
        string resourcePath = string `/admin/active_context/connections/activate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Deactivate an ActiveContext connection
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to deactivate an ActiveContext connection 
    # + return - Success 
    remote isolated function deactivateActiveContextConnection(DeactivateActiveContextConnectionRequest payload, map<string|string[]> headers = {}) returns AiActiveContextConnection|error {
        string resourcePath = string `/admin/active_context/connections/deactivate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Update collection options
    #
    # + id - Collection name or ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update collection options 
    # + return - The updated collection options 
    remote isolated function updateCollectionOptions(IdOrPath id, UpdateCollectionOptionsRequest payload, map<string|string[]> headers = {}) returns AiActiveContextCollection|error {
        string resourcePath = string `/admin/active_context/collections/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Clear the ActiveContext dead queue
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function clearActiveContextDeadQueue(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin/active_context/dead_queue`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Replay items from the dead queue into another queue
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to replay items from the dead queue into another queue 
    # + return - Created 
    remote isolated function replayActiveContextDeadQueue(ReplayActiveContextDeadQueueRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin/active_context/dead_queue/replay`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update enabled namespace state
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update enabled namespace state 
    # + return - The updated enabled namespace state 
    remote isolated function updateEnabledNamespaceState(UpdateEnabledNamespaceStateRequest payload, map<string|string[]> headers = {}) returns AiActiveContextCodeEnabledNamespace|error {
        string resourcePath = string `/admin/active_context/code/enabled_namespaces`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Get Admin Roles for this GitLab instance
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved admin roles 
    remote isolated function listAdminRoles(map<string|string[]> headers = {}) returns MemberRole[]|error {
        string resourcePath = string `/admin_member_roles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create Admin Role on the GitLab instance
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create Admin Role on the GitLab instance 
    # + return - Created 
    remote isolated function createAdminRole(CreateAdminRoleRequest payload, map<string|string[]> headers = {}) returns MemberRole|error {
        string resourcePath = string `/admin_member_roles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete Admin Role
    #
    # + memberRoleId - ID of the member role
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteAdminRole(int memberRoleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin_member_roles/${getEncodedUri(memberRoleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all group issue boards in a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group issue boards 
    remote isolated function listGroupIssueBoards(string id, map<string|string[]> headers = {}, *ListGroupIssueBoardsQueries queries) returns Board[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group issue board
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group issue board 
    # + return - Created 
    remote isolated function createGroupIssueBoard(string id, CreateIssueBoardRequest payload, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group issue board
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group issue board 
    remote isolated function getGroupIssueBoard(string id, int boardId, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group issue board
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group issue board 
    # + return - The updated group issue board 
    remote isolated function updateGroupIssueBoard(string id, int boardId, UpdateIssueBoardRequest payload, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group issue board
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupIssueBoard(string id, int boardId, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Report an organization's current lifecycle state
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getOrgLifecycleState(map<string|string[]> headers = {}, *GetOrgLifecycleStateQueries queries) returns error? {
        string resourcePath = string `/internal/org_mover/maintenance_state`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Report cutover readiness for an organization
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getOrgCutoverReadiness(map<string|string[]> headers = {}, *GetOrgCutoverReadinessQueries queries) returns error? {
        string resourcePath = string `/internal/org_mover/maintenance_readiness`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Start maintenance on an organization (active -> maintenance_initialization)
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to start maintenance on an organization (active -> maintenance_initialization) 
    # + return - No Content 
    remote isolated function startOrgMaintenance(StartOrgMaintenanceRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/internal/org_mover/start_maintenance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Confirm maintenance on an organization (maintenance_initialization -> maintenance)
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to confirm maintenance on an organization 
    # + return - No Content 
    remote isolated function confirmOrgMaintenance(OrgMaintenanceRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/internal/org_mover/confirm_maintenance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Cancel maintenance on an organization (maintenance_initialization -> active)
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to cancel maintenance on an organization (maintenance_initialization -> active) 
    # + return - No Content 
    remote isolated function cancelOrgMaintenance(OrgMaintenanceRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/internal/org_mover/cancel_maintenance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Exit maintenance on an organization (maintenance -> active)
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for exit maintenance on an organization (maintenance -> active) 
    # + return - No Content 
    remote isolated function exitOrgMaintenance(OrgMaintenanceRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/internal/org_mover/exit_maintenance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List Gitaly object pool members
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listGitalyObjectPoolMembers(map<string|string[]> headers = {}, *ListGitalyObjectPoolMembersQueries queries) returns error? {
        string resourcePath = string `/internal/gitaly/object_pool_members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"disk_paths": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # MLflow artifact API
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listMlflowArtifacts(IdOrPath id, map<string|string[]> headers = {}, *ListMlflowArtifactsQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow-artifacts/artifacts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get MLflow artifact file
    #
    # + modelVersion - The model version
    # + id - The ID or URL-encoded path of the project
    # + filePath - Path of the artifact file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getMlflowArtifactFile(string modelVersion, IdOrPath id, string filePath, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow-artifacts/artifacts/${getEncodedUri(modelVersion)}/${getEncodedUri(filePath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates a Registered Model.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Registered Model 
    # + return - Created 
    remote isolated function createMlflowRegisteredModel(IdOrPath id, CreateMlflowRegisteredModelRequest payload, map<string|string[]> headers = {}) returns MlMlflowRegisteredModel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/create`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Fetch a Registered Model by Name
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow registered model 
    remote isolated function getMlflowRegisteredModel(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowRegisteredModelQueries queries) returns MlMlflowRegisteredModel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/get`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a Registered Model by Name
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Registered Model by Name 
    # + return - The updated mlflow registered model 
    remote isolated function updateMlflowRegisteredModel(IdOrPath id, UpdateMlflowRegisteredModelRequest payload, map<string|string[]> headers = {}) returns MlMlflowRegisteredModel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/update`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Fetch the latest Model Version for the given Registered Model Name
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to fetch the latest Model Version for the given Registered Model Name 
    # + return - Created 
    remote isolated function getMlflowLatestModelVersions(IdOrPath id, GetMlflowLatestModelVersionsRequest payload, map<string|string[]> headers = {}) returns MlMlflowModelVersion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/get-latest-versions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a Registered Model by Name
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteMlflowRegisteredModel(IdOrPath id, map<string|string[]> headers = {}, *DeleteMlflowRegisteredModelQueries queries) returns MlMlflowRegisteredModel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/delete`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Search Registered Models within a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchMlflowRegisteredModels(IdOrPath id, map<string|string[]> headers = {}, *SearchMlflowRegisteredModelsQueries queries) returns MlMlflowRegisteredModel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Gets a Model Version by alias
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getMlflowModelVersionByAlias(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowModelVersionByAliasQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/registered-models/alias`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates a Run.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Run 
    # + return - Created 
    remote isolated function createMlflowRun(IdOrPath id, CreateMlflowRunRequest payload, map<string|string[]> headers = {}) returns MlMlflowRun|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/create`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Gets an MLFlow Run, which maps to GitLab Candidates
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow run 
    remote isolated function getMlflowRun(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowRunQueries queries) returns MlMlflowRun|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/get`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Searches runs/candidates within a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to search runs/candidates within a project 
    # + return - Created 
    remote isolated function searchMlflowRuns(IdOrPath id, SearchMlflowRunsRequest payload, map<string|string[]> headers = {}) returns MlMlflowRun|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Updates a Run.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Run 
    # + return - Created 
    remote isolated function updateMlflowRun(IdOrPath id, UpdateMlflowRunRequest payload, map<string|string[]> headers = {}) returns MlMlflowUpdateRun|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/update`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Logs a metric to a run.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to log a metric to a run 
    # + return - Created 
    remote isolated function logMlflowMetric(IdOrPath id, LogMlflowMetricRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/log-metric`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Logs a parameter to a run.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to log a parameter to a run 
    # + return - Created 
    remote isolated function logMlflowParameter(IdOrPath id, LogMlflowParameterRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/log-parameter`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Sets a tag for a run.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set a tag for a run 
    # + return - Created 
    remote isolated function setMlflowRunTag(IdOrPath id, SetMlflowRunTagRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/set-tag`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Logs multiple parameters and metrics.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to log multiple parameters and metrics 
    # + return - Created 
    remote isolated function logMlflowBatch(IdOrPath id, LogMlflowBatchRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/log-batch`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a run.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to delete a run 
    # + return - Created 
    remote isolated function deleteMlflowRun(IdOrPath id, DeleteMlflowRunRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/runs/delete`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Creates a Model Version.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Model Version 
    # + return - Created 
    remote isolated function createMlflowModelVersion(IdOrPath id, CreateMlflowModelVersionRequest payload, map<string|string[]> headers = {}) returns MlMlflowModelVersion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/model-versions/create`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Fetch the download URI for the model version.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow model version download uri 
    remote isolated function getMlflowModelVersionDownloadUri(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowModelVersionDownloadUriQueries queries) returns MlMlflowGetDownload|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/model-versions/get-download-uri`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Fetch model version by name and version
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow model version 
    remote isolated function getMlflowModelVersion(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowModelVersionQueries queries) returns MlMlflowModelVersion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/model-versions/get`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Updates a Model Version.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Model Version 
    # + return - The updated mlflow model version 
    remote isolated function updateMlflowModelVersion(IdOrPath id, UpdateMlflowModelVersionRequest payload, map<string|string[]> headers = {}) returns MlMlflowModelVersion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/model-versions/update`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Gets metric history for a run
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow metric history 
    remote isolated function getMlflowMetricHistory(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowMetricHistoryQueries queries) returns MlMlflowGetMetricHistory|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/metrics/get-history`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Fetch experiment by experiment_id
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow experiment 
    remote isolated function getMlflowExperiment(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowExperimentQueries queries) returns MlMlflowGetExperiment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/get`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Fetch experiment by experiment_name
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved mlflow experiment by name 
    remote isolated function getMlflowExperimentByName(IdOrPath id, map<string|string[]> headers = {}, *GetMlflowExperimentByNameQueries queries) returns MlMlflowGetExperiment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/get-by-name`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List experiments
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved mlflow experiments 
    remote isolated function listMlflowExperiments(IdOrPath id, map<string|string[]> headers = {}) returns MlMlflowListExperiment[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/list`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search experiments
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to search experiments 
    # + return - Created 
    remote isolated function searchMlflowExperiments(IdOrPath id, SearchMlflowExperimentsRequest payload, map<string|string[]> headers = {}) returns MlMlflowListExperiment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create experiment
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create experiment 
    # + return - Created 
    remote isolated function createMlflowExperiment(IdOrPath id, CreateMlflowExperimentRequest payload, map<string|string[]> headers = {}) returns MlMlflowNewExperiment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/create`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Sets a tag for an experiment.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set a tag for an experiment 
    # + return - Created 
    remote isolated function setMlflowExperimentTag(IdOrPath id, SetMlflowExperimentTagRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/set-experiment-tag`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete an experiment.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to delete an experiment 
    # + return - Created 
    remote isolated function deleteMlflowExperiment(IdOrPath id, DeleteMlflowExperimentRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ml/mlflow/api/2.0/mlflow/experiments/delete`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get the settings manifest for Settings Sync
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vscode settings manifest 
    remote isolated function getVscodeSettingsManifest(map<string|string[]> headers = {}) returns VsCodeManifest|error {
        string resourcePath = string `/vscode/settings_sync/v1/manifest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the settings manifest for Settings Sync
    #
    # + settingsContextHash - The settings context hash
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vscode settings manifest in context 
    remote isolated function getVscodeSettingsManifestInContext(string settingsContextHash, map<string|string[]> headers = {}) returns VsCodeManifest|error {
        string resourcePath = string `/vscode/settings_sync/${getEncodedUri(settingsContextHash)}/v1/manifest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get a specific setting resource
    #
    # + resourceName - Name of the resource such as settings
    # + id - ID of the resource to retrieve
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vscode setting 
    remote isolated function getVscodeSetting("settings"|"extensions"|"globalState"|"machines"|"keybindings"|"snippets"|"tasks"|"profiles" resourceName, string id, map<string|string[]> headers = {}) returns VsCodeSetting|error? {
        string resourcePath = string `/vscode/settings_sync/v1/resource/${getEncodedUri(resourceName)}/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get a specific setting resource
    #
    # + settingsContextHash - The settings context hash
    # + resourceName - Name of the resource such as settings
    # + id - ID of the resource to retrieve
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vscode setting in context 
    remote isolated function getVscodeSettingInContext(string settingsContextHash, "settings"|"extensions"|"globalState"|"machines"|"keybindings"|"snippets"|"tasks"|"profiles" resourceName, string id, map<string|string[]> headers = {}) returns VsCodeSetting|error? {
        string resourcePath = string `/vscode/settings_sync/${getEncodedUri(settingsContextHash)}/v1/resource/${getEncodedUri(resourceName)}/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get a list of references to one or more vscode setting resources
    #
    # + resourceName - Name of the resource such as settings
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vscode setting refs 
    remote isolated function listVscodeSettingRefs("settings"|"extensions"|"globalState"|"machines"|"keybindings"|"snippets"|"tasks"|"profiles" resourceName, map<string|string[]> headers = {}) returns VsCodeSettingReference[]|error {
        string resourcePath = string `/vscode/settings_sync/v1/resource/${getEncodedUri(resourceName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates or updates a specific setting
    #
    # + resourceName - Name of the resource such as settings
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function setVscodeSetting("settings"|"extensions"|"globalState"|"machines"|"keybindings"|"snippets"|"tasks"|"profiles" resourceName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/vscode/settings_sync/v1/resource/${getEncodedUri(resourceName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get a list of references to one or more vscode setting resources
    #
    # + settingsContextHash - The settings context hash
    # + resourceName - Name of the resource such as settings
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vscode setting refs in context 
    remote isolated function listVscodeSettingRefsInContext(string settingsContextHash, "settings"|"extensions"|"globalState"|"machines"|"keybindings"|"snippets"|"tasks"|"profiles" resourceName, map<string|string[]> headers = {}) returns VsCodeSettingReference[]|error {
        string resourcePath = string `/vscode/settings_sync/${getEncodedUri(settingsContextHash)}/v1/resource/${getEncodedUri(resourceName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates or updates a specific setting
    #
    # + settingsContextHash - The settings context hash
    # + resourceName - Name of the resource such as settings
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function setVscodeSettingInContext(string settingsContextHash, "settings"|"extensions"|"globalState"|"machines"|"keybindings"|"snippets"|"tasks"|"profiles" resourceName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/vscode/settings_sync/${getEncodedUri(settingsContextHash)}/v1/resource/${getEncodedUri(resourceName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Deletes all user vscode setting resources
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteVscodeSettings(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/vscode/settings_sync/v1/collection`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Deletes all user vscode setting resources
    #
    # + settingsContextHash - The settings context hash
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteVscodeSettingsInContext(string settingsContextHash, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/vscode/settings_sync/${getEncodedUri(settingsContextHash)}/v1/collection`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all custom attributes for a user
    #
    # + id - The ID or URL-encoded path of the user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user custom attributes 
    remote isolated function listUserCustomAttributes(IdOrPath id, map<string|string[]> headers = {}) returns CustomAttribute[]|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/custom_attributes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a custom attribute for a user
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user custom attribute 
    remote isolated function getUserCustomAttribute(string 'key, IdOrPath id, map<string|string[]> headers = {}) returns CustomAttribute|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates or updates a custom attribute for a user
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or updates a custom attribute for a user 
    # + return - Success 
    remote isolated function setUserCustomAttribute(string 'key, IdOrPath id, SetCustomAttributeRequest payload, map<string|string[]> headers = {}) returns CustomAttribute|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a custom attribute for a user
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteUserCustomAttribute(string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a Support PIN for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user support pin 
    remote isolated function getUserSupportPin(int id, map<string|string[]> headers = {}) returns UserSupportPin|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/support_pin`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a Support PIN for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeUserSupportPin(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/support_pin/revoke`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all users
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved users 
    remote isolated function listUsers(map<string|string[]> headers = {}, *ListUsersQueries queries) returns UserBasic[]|error {
        string resourcePath = string `/users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a user
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a user 
    # + return - Created 
    remote isolated function createUser(CreateUserRequest payload, map<string|string[]> headers = {}) returns UserWithAdmin|error {
        string resourcePath = string `/users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a user as a regular user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user 
    remote isolated function getUser(int id, map<string|string[]> headers = {}, *GetUserQueries queries) returns UserProfile|error {
        string resourcePath = string `/users/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a user 
    # + return - The updated user 
    remote isolated function updateUser(int id, UpdateUserRequest payload, map<string|string[]> headers = {}) returns UserWithAdmin|error {
        string resourcePath = string `/users/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteUser(int id, map<string|string[]> headers = {}, *DeleteUserQueries queries) returns Email|error {
        string resourcePath = string `/users/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve the status of a user
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved status 
    remote isolated function getStatus(string userId, map<string|string[]> headers = {}) returns UserStatus|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Follow a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function followUser(int id, map<string|string[]> headers = {}) returns User|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/follow`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unfollow a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unfollowUser(int id, map<string|string[]> headers = {}) returns User|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/unfollow`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all accounts followed by a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user following 
    remote isolated function listUserFollowing(int id, map<string|string[]> headers = {}, *ListUserFollowingQueries queries) returns UserBasic[]|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/following`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all accounts that follow a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user followers 
    remote isolated function listUserFollowers(int id, map<string|string[]> headers = {}, *ListUserFollowersQueries queries) returns UserBasic[]|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/followers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Disable two-factor authentication for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function disableUserTwoFactor(int id, map<string|string[]> headers = {}) returns UserWithAdmin|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/disable_two_factor`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Delete authentication identity from a user
    #
    # + id - The ID of the user
    # + provider - The external provider
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteUserIdentity(int id, string provider, map<string|string[]> headers = {}) returns UserWithAdmin|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/identities/${getEncodedUri(provider)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all project deploy keys for a user
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user project deploy keys 
    remote isolated function listUserProjectDeployKeys(string userId, map<string|string[]> headers = {}, *ListUserProjectDeployKeysQueries queries) returns DeployKey[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/project_deploy_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all SSH keys for a user
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user ssh keys 
    remote isolated function listUserSshKeys(string userId, map<string|string[]> headers = {}, *ListUserSshKeysQueries queries) returns SSHKey[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an SSH key for a user
    #
    # + userId - The ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an SSH key for a user 
    # + return - Created 
    remote isolated function addUserSshKey(int userId, AddSshKeyRequest payload, map<string|string[]> headers = {}) returns SSHKey|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an SSH key for a user
    #
    # + id - The ID of the user
    # + keyId - The ID of the SSH key
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user ssh key 
    remote isolated function getUserSshKey(int id, int keyId, map<string|string[]> headers = {}) returns SSHKey|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an SSH key for a user
    #
    # + id - The ID of the user
    # + keyId - The ID of the SSH key
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteUserSshKey(int id, int keyId, map<string|string[]> headers = {}) returns SSHKey|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all GPG keys for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user gpg keys 
    remote isolated function listUserGpgKeys(int id, map<string|string[]> headers = {}, *ListUserGpgKeysQueries queries) returns GpgKey[]|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/gpg_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a GPG key for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a GPG key for a user 
    # + return - Created 
    remote isolated function addUserGpgKey(int id, AddGpgKeyRequest payload, map<string|string[]> headers = {}) returns GpgKey|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/gpg_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a GPG key for a user
    #
    # + id - The ID of the user
    # + keyId - The ID of the GPG key
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user gpg key 
    remote isolated function getUserGpgKey(int id, int keyId, map<string|string[]> headers = {}) returns GpgKey|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/gpg_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a GPG key for a user
    #
    # + id - The ID of the user
    # + keyId - The ID of the GPG key
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteUserGpgKey(int id, int keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/gpg_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Revoke a GPG key for a user.
    #
    # + id - The ID of the user
    # + keyId - The ID of the GPG key
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function revokeUserGpgKey(int id, int keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/gpg_keys/${getEncodedUri(keyId)}/revoke`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all email addresses for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user email addresses 
    remote isolated function listUserEmailAddresses(int id, map<string|string[]> headers = {}, *ListUserEmailAddressesQueries queries) returns Email[]|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/emails`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an email address for a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an email address for a user 
    # + return - Created 
    remote isolated function addUserEmailAddress(int id, AddUserEmailAddressRequest payload, map<string|string[]> headers = {}) returns Email|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/emails`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete an email address for a user
    #
    # + id - The ID of the user
    # + emailId - The ID of the email
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteUserEmailAddress(int id, int emailId, map<string|string[]> headers = {}) returns Email|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/emails/${getEncodedUri(emailId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Reactivate a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function activateUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/activate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Approve access to a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function approveUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/approve`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reject access to a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function rejectUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/reject`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Deactivate a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function deactivateUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/deactivate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Block access to a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function blockUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/block`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unblock access to a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unblockUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/unblock`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Ban a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function banUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/ban`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unban a user
    #
    # + id - The ID of the user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unbanUser(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(id)}/unban`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project and group memberships for a user
    #
    # + userId - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user memberships 
    remote isolated function listUserMemberships(int userId, map<string|string[]> headers = {}, *ListUserMembershipsQueries queries) returns Membership[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/memberships`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve association counts for a user
    #
    # + id - ID of the user to query
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user association counts 
    remote isolated function getUserAssociationCounts(int id, map<string|string[]> headers = {}) returns UserAssociationsCount|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/associations_count`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all impersonation tokens for a user
    #
    # + userId - The ID of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user impersonation tokens 
    remote isolated function listUserImpersonationTokens(int userId, map<string|string[]> headers = {}, *ListUserImpersonationTokensQueries queries) returns ImpersonationToken[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/impersonation_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an impersonation token
    #
    # + userId - The ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an impersonation token 
    # + return - Created 
    remote isolated function createUserImpersonationToken(int userId, CreateUserImpersonationTokenRequest payload, map<string|string[]> headers = {}) returns ImpersonationTokenWithToken|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/impersonation_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an impersonation token for a user
    #
    # + userId - The ID of the user
    # + impersonationTokenId - The ID of the impersonation token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user impersonation token 
    remote isolated function getUserImpersonationToken(int userId, int impersonationTokenId, map<string|string[]> headers = {}) returns ImpersonationToken|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/impersonation_tokens/${getEncodedUri(impersonationTokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke an impersonation token
    #
    # + userId - The ID of the user
    # + impersonationTokenId - The ID of the impersonation token
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeUserImpersonationToken(int userId, int impersonationTokenId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/users/${getEncodedUri(userId)}/impersonation_tokens/${getEncodedUri(impersonationTokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Create a personal access token for a user
    #
    # + userId - The ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a personal access token for a user 
    # + return - Created 
    remote isolated function createUserPersonalAccessToken(int userId, CreatePersonalAccessTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve current user details
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user 
    remote isolated function getCurrentUser(map<string|string[]> headers = {}) returns UserPublic|error {
        string resourcePath = string `/user`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all SSH keys
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved current user ssh keys 
    remote isolated function listCurrentUserSshKeys(map<string|string[]> headers = {}, *ListCurrentUserSshKeysQueries queries) returns SSHKey[]|error {
        string resourcePath = string `/user/keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an SSH key
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an SSH key 
    # + return - Created 
    remote isolated function addCurrentUserSshKey(AddSshKeyRequest payload, map<string|string[]> headers = {}) returns SSHKey|error {
        string resourcePath = string `/user/keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an SSH key
    #
    # + keyId - The ID of the SSH key
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user ssh key 
    remote isolated function getCurrentUserSshKey(int keyId, map<string|string[]> headers = {}) returns SSHKey|error {
        string resourcePath = string `/user/keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an SSH key
    #
    # + keyId - The ID of the SSH key
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteCurrentUserSshKey(int keyId, map<string|string[]> headers = {}) returns SSHKey|error {
        string resourcePath = string `/user/keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all GPG keys
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved current user gpg keys 
    remote isolated function listCurrentUserGpgKeys(map<string|string[]> headers = {}, *ListCurrentUserGpgKeysQueries queries) returns GpgKey[]|error {
        string resourcePath = string `/user/gpg_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a GPG key
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a GPG key 
    # + return - Created 
    remote isolated function addCurrentUserGpgKey(AddGpgKeyRequest payload, map<string|string[]> headers = {}) returns GpgKey|error {
        string resourcePath = string `/user/gpg_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a GPG key
    #
    # + keyId - The ID of the GPG key
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user gpg key 
    remote isolated function getCurrentUserGpgKey(int keyId, map<string|string[]> headers = {}) returns GpgKey|error {
        string resourcePath = string `/user/gpg_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a GPG key
    #
    # + keyId - The ID of the SSH key
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteCurrentUserGpgKey(int keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/user/gpg_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Revoke a GPG key
    #
    # + keyId - The ID of the GPG key
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function revokeCurrentUserGpgKey(int keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/user/gpg_keys/${getEncodedUri(keyId)}/revoke`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all email addresses for a user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved current user email addresses 
    remote isolated function listCurrentUserEmailAddresses(map<string|string[]> headers = {}, *ListCurrentUserEmailAddressesQueries queries) returns Email[]|error {
        string resourcePath = string `/user/emails`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an email address
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an email address 
    # + return - Created 
    remote isolated function addCurrentUserEmailAddress(AddCurrentUserEmailAddressRequest payload, map<string|string[]> headers = {}) returns Email|error {
        string resourcePath = string `/user/emails`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update a user's credit_card_validation
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a user's credit_card_validation 
    # + return - The updated user credit card validation 
    # 
    # # Deprecated
    @deprecated
    remote isolated function updateUserCreditCardValidation(string userId, UpdateUserCreditCardValidationRequest payload, map<string|string[]> headers = {}) returns UserCreditCardValidations|error {
        string resourcePath = string `/user/${getEncodedUri(userId)}/credit_card_validation`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve Support PIN
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user support pin 
    remote isolated function getCurrentUserSupportPin(map<string|string[]> headers = {}) returns UserSupportPin|error {
        string resourcePath = string `/user/support_pin`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a Support PIN
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createCurrentUserSupportPin(map<string|string[]> headers = {}) returns UserSupportPin|error {
        string resourcePath = string `/user/support_pin`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve your user preferences
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user preferences 
    remote isolated function getCurrentUserPreferences(map<string|string[]> headers = {}) returns UserPreferences|error {
        string resourcePath = string `/user/preferences`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update your user preferences
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update your user preferences 
    # + return - The updated current user preferences 
    remote isolated function updateCurrentUserPreferences(UpdateCurrentUserPreferencesRequest payload, map<string|string[]> headers = {}) returns UserPreferences|error {
        string resourcePath = string `/user/preferences`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve details on an email address
    #
    # + emailId - The ID of the email
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user email address 
    remote isolated function getCurrentUserEmailAddress(int emailId, map<string|string[]> headers = {}) returns Email|error {
        string resourcePath = string `/user/emails/${getEncodedUri(emailId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an email address
    #
    # + emailId - The ID of the email
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteCurrentUserEmailAddress(int emailId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/user/emails/${getEncodedUri(emailId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all activity for a user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user activities 
    remote isolated function listUserActivities(map<string|string[]> headers = {}, *ListUserActivitiesQueries queries) returns UserActivity[]|error {
        string resourcePath = string `/user/activities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve your user status
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user status 
    remote isolated function getCurrentUserStatus(map<string|string[]> headers = {}) returns UserStatus|error {
        string resourcePath = string `/user/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Set a user status
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set a user status 
    # + return - Success 
    remote isolated function setCurrentUserStatus(UserStatusRequest payload, map<string|string[]> headers = {}) returns UserStatus|error {
        string resourcePath = string `/user/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Update a user status
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a user status 
    # + return - The updated current user status 
    remote isolated function updateCurrentUserStatus(UserStatusRequest payload, map<string|string[]> headers = {}) returns UserStatus|error {
        string resourcePath = string `/user/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Upload an avatar
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload an avatar 
    # + return - Success 
    remote isolated function uploadCurrentUserAvatar(UploadCurrentUserAvatarRequest payload, map<string|string[]> headers = {}) returns Avatar|error {
        string resourcePath = string `/user/avatar`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create a personal access token
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a personal access token 
    # + return - Created 
    remote isolated function createCurrentUserPersonalAccessToken(CreateCurrentUserPersonalAccessTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/user/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all applications
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user applications 
    remote isolated function listCurrentUserApplications(map<string|string[]> headers = {}) returns Application[]|error {
        string resourcePath = string `/user/applications`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an application
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an application 
    # + return - Created 
    remote isolated function createCurrentUserApplication(CreateCurrentUserApplicationRequest payload, map<string|string[]> headers = {}) returns ApplicationWithSecret|error {
        string resourcePath = string `/user/applications`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an application
    #
    # + id - ID of the application. Differs from the `application_id`
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user application 
    remote isolated function getCurrentUserApplication(int id, map<string|string[]> headers = {}) returns Application|error {
        string resourcePath = string `/user/applications/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an application
    #
    # + id - ID of the application. Differs from the `application_id`
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an application 
    # + return - The updated current user application 
    remote isolated function updateCurrentUserApplication(int id, UpdateCurrentUserApplicationRequest payload, map<string|string[]> headers = {}) returns Application|error {
        string resourcePath = string `/user/applications/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an application
    #
    # + id - ID of the application. Differs from the `application_id`
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteCurrentUserApplication(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/user/applications/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Create a to-do item for an issuable
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + mergeRequestIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createMergeRequestTodo(string id, int mergeRequestIid, map<string|string[]> headers = {}) returns Todo|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/todo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a to-do item for an issuable
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createIssueTodo(string id, int issueIid, map<string|string[]> headers = {}) returns Todo|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/todo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all to-do items
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved todos 
    remote isolated function listTodos(map<string|string[]> headers = {}, *ListTodosQueries queries) returns Todo[]|error {
        string resourcePath = string `/todos`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Mark a to-do item as done
    #
    # + id - The ID of to-do item
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function markTodoAsDone(int id, map<string|string[]> headers = {}) returns Todo|error {
        string resourcePath = string `/todos/${getEncodedUri(id)}/mark_as_done`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Mark all to-do items as done
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function markAllTodosAsDone(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/todos/mark_as_done`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a to-do item for an epic
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + epicIid - The internal ID of a group’s epic
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createEpicTodo(string id, int epicIid, map<string|string[]> headers = {}) returns Todo|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/todo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all license templates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved license templates 
    remote isolated function listLicenseTemplates(map<string|string[]> headers = {}, *ListLicenseTemplatesQueries queries) returns License[]|error {
        string resourcePath = string `/templates/licenses`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a license template
    #
    # + name - The name of the license template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved license template 
    remote isolated function getLicenseTemplate(string name, map<string|string[]> headers = {}, *GetLicenseTemplateQueries queries) returns License|error {
        string resourcePath = string `/templates/licenses/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all .gitignore templates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved gitignore templates 
    remote isolated function listGitignoreTemplates(map<string|string[]> headers = {}, *ListGitignoreTemplatesQueries queries) returns TemplatesList[]|error {
        string resourcePath = string `/templates/gitignores`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a .gitignore template
    #
    # + name - The name of the .gitignore template
    # + headers - Headers to be sent with the request 
    # + return - The retrieved gitignore template 
    remote isolated function getGitignoreTemplate(string name, map<string|string[]> headers = {}) returns Template|error {
        string resourcePath = string `/templates/gitignores/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all GitLab CI/CD YAML templates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved git lab ci cd yaml templates 
    remote isolated function listGitLabCiCdYamlTemplates(map<string|string[]> headers = {}, *ListGitLabCiCdYamlTemplatesQueries queries) returns TemplatesList[]|error {
        string resourcePath = string `/templates/gitlab_ci_ymls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a GitLab CI/CD YAML template
    #
    # + name - The name of the GitLab CI/CD YAML template
    # + headers - Headers to be sent with the request 
    # + return - The retrieved git lab ci cd yaml template 
    remote isolated function getGitLabCiCdYamlTemplate(string name, map<string|string[]> headers = {}) returns Template|error {
        string resourcePath = string `/templates/gitlab_ci_ymls/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Dockerfile templates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved dockerfile templates 
    remote isolated function listDockerfileTemplates(map<string|string[]> headers = {}, *ListDockerfileTemplatesQueries queries) returns TemplatesList[]|error {
        string resourcePath = string `/templates/dockerfiles`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a Dockerfile template
    #
    # + name - The name of the Dockerfile template
    # + headers - Headers to be sent with the request 
    # + return - The retrieved dockerfile template 
    remote isolated function getDockerfileTemplate(string name, map<string|string[]> headers = {}) returns Template|error {
        string resourcePath = string `/templates/dockerfiles/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Subscribe to a merge request
    #
    # + id - The project ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function subscribeMergeRequest(string id, string subscribableId, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(subscribableId)}/subscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unsubscribe to a merge request
    #
    # + id - The project ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unsubscribeMergeRequest(string id, string subscribableId, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(subscribableId)}/unsubscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Subscribe to an issue
    #
    # + id - The project ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function subscribeIssue(string id, string subscribableId, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(subscribableId)}/subscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unsubscribe to an issue
    #
    # + id - The project ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unsubscribeIssue(string id, string subscribableId, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(subscribableId)}/unsubscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Subscribe to a project label
    #
    # + id - The project ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function subscribeProjectLabel(string id, string subscribableId, map<string|string[]> headers = {}) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/${getEncodedUri(subscribableId)}/subscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unsubscribe to a project label
    #
    # + id - The project ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unsubscribeProjectLabel(string id, string subscribableId, map<string|string[]> headers = {}) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/${getEncodedUri(subscribableId)}/unsubscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Subscribe to a group label
    #
    # + id - The group ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function subscribeGroupLabel(string id, string subscribableId, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels/${getEncodedUri(subscribableId)}/subscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unsubscribe to a group label
    #
    # + id - The group ID
    # + subscribableId - The ID of a resource
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unsubscribeGroupLabel(string id, string subscribableId, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels/${getEncodedUri(subscribableId)}/unsubscribe`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all snippet repository storage moves
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all snippet storage moves 
    remote isolated function listAllSnippetStorageMoves(map<string|string[]> headers = {}, *ListAllSnippetStorageMovesQueries queries) returns SnippetsRepositoryStorageMove[]|error {
        string resourcePath = string `/snippet_repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Schedule repository storage moves for all snippets on a storage shard
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to schedule repository storage moves for all snippets on a storage shard 
    # + return - Accepted 
    remote isolated function scheduleSnippetStorageMovesForShard(ScheduleStorageMovesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/snippet_repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a snippet repository storage move
    #
    # + repositoryStorageMoveId - The ID of a snippet repository storage move
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet storage move 
    remote isolated function getSnippetStorageMove(int repositoryStorageMoveId, map<string|string[]> headers = {}) returns SnippetsRepositoryStorageMove|error {
        string resourcePath = string `/snippet_repository_storage_moves/${getEncodedUri(repositoryStorageMoveId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all repository storage moves for a snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved snippet repository storage moves 
    remote isolated function listSnippetRepositoryStorageMoves(string id, map<string|string[]> headers = {}, *ListSnippetRepositoryStorageMovesQueries queries) returns SnippetsRepositoryStorageMove[]|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}/repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Schedule a repository storage move for a snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to schedule a repository storage move for a snippet 
    # + return - Created 
    remote isolated function createSnippetRepositoryStorageMove(string id, CreateStorageMoveRequest payload, map<string|string[]> headers = {}) returns SnippetsRepositoryStorageMove|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}/repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a repository storage move for a snippet
    #
    # + id - The ID of a snippet
    # + repositoryStorageMoveId - The ID of a snippet repository storage move
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet repository storage move 
    remote isolated function getSnippetRepositoryStorageMove(string id, int repositoryStorageMoveId, map<string|string[]> headers = {}) returns SnippetsRepositoryStorageMove|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}/repository_storage_moves/${getEncodedUri(repositoryStorageMoveId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all job queue metrics
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listSidekiqQueueMetrics(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/sidekiq/queue_metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Sidekiq processes
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listSidekiqProcesses(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/sidekiq/process_metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve job completion metrics
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getSidekiqJobStats(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/sidekiq/job_stats`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Sidekiq metrics
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listSidekiqMetrics(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/sidekiq/compound_metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve application settings
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved application settings 
    remote isolated function getApplicationSettings(map<string|string[]> headers = {}) returns ApplicationSetting|error {
        string resourcePath = string `/application/settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update application settings
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update application settings 
    # + return - The updated application settings 
    remote isolated function updateApplicationSettings(UpdateApplicationSettingsRequest payload, map<string|string[]> headers = {}) returns ApplicationSetting|error {
        string resourcePath = string `/application/settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all instance service accounts
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved instance service accounts 
    remote isolated function listInstanceServiceAccounts(map<string|string[]> headers = {}, *ListInstanceServiceAccountsQueries queries) returns ServiceAccount[]|error {
        string resourcePath = string `/service_accounts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an instance service account
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an instance service account 
    # + return - Created 
    remote isolated function createInstanceServiceAccount(ServiceAccountRequest payload, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/service_accounts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update an instance service account
    #
    # + userId - The ID of the service account user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an instance service account 
    # + return - The updated instance service account 
    remote isolated function updateInstanceServiceAccount(int userId, ServiceAccountRequest payload, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all project issue state events
    #
    # + id - The ID of a project
    # + eventableId - The IID of the issue
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue state events 
    remote isolated function listIssueStateEvents(string id, int eventableId, map<string|string[]> headers = {}, *ListIssueStateEventsQueries queries) returns ResourceStateEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_state_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an issue state event
    #
    # + id - The ID of a project
    # + eventableId - The IID of the issue
    # + eventId - The ID of a resource state event
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue state event 
    remote isolated function getIssueStateEvent(string id, int eventableId, int eventId, map<string|string[]> headers = {}) returns ResourceStateEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_state_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project merge request state events
    #
    # + id - The ID of a project
    # + eventableId - The IID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request state events 
    remote isolated function listMergeRequestStateEvents(string id, int eventableId, map<string|string[]> headers = {}, *ListMergeRequestStateEventsQueries queries) returns ResourceStateEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(eventableId)}/resource_state_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a merge request state event
    #
    # + id - The ID of a project
    # + eventableId - The IID of the merge request
    # + eventId - The ID of a resource state event
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request state event 
    remote isolated function getMergeRequestStateEvent(string id, int eventableId, int eventId, map<string|string[]> headers = {}) returns ResourceStateEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(eventableId)}/resource_state_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group epic state events
    #
    # + id - The ID of a group
    # + eventableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group epic state events 
    remote isolated function listGroupEpicStateEvents(string id, int eventableId, map<string|string[]> headers = {}, *ListGroupEpicStateEventsQueries queries) returns ResourceStateEvent[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(eventableId)}/resource_state_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an epic state event
    #
    # + id - The ID of a group
    # + eventableId - The ID of the epic
    # + eventId - The ID of a resource state event
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic state event 
    remote isolated function getEpicStateEvent(string id, int eventableId, int eventId, map<string|string[]> headers = {}) returns ResourceStateEvent|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(eventableId)}/resource_state_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project issue label events
    #
    # + id - The ID of a project
    # + eventableId - The IID of the issue
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue label events 
    remote isolated function listIssueLabelEvents(string id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListIssueLabelEventsQueries queries) returns ResourceLabelEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_label_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an issue label event
    #
    # + id - The ID of a project
    # + eventId - The ID of a resource label event
    # + eventableId - The IID of the issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue label event 
    remote isolated function getIssueLabelEvent(string id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceLabelEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_label_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project merge request label events
    #
    # + id - The ID of a project
    # + eventableId - The IID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request label events 
    remote isolated function listMergeRequestLabelEvents(string id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListMergeRequestLabelEventsQueries queries) returns ResourceLabelEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(eventableId)}/resource_label_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a merge request label event
    #
    # + id - The ID of a project
    # + eventId - The ID of a resource label event
    # + eventableId - The IID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request label event 
    remote isolated function getMergeRequestLabelEvent(string id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceLabelEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(eventableId)}/resource_label_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group epic label events
    #
    # + id - The ID of a group
    # + eventableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group epic label events 
    remote isolated function listGroupEpicLabelEvents(string id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListGroupEpicLabelEventsQueries queries) returns ResourceLabelEvent[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(eventableId)}/resource_label_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an epic label event
    #
    # + id - The ID of a group
    # + eventId - The ID of a resource label event
    # + eventableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic label event 
    remote isolated function getEpicLabelEvent(string id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceLabelEvent|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(eventableId)}/resource_label_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project service accounts
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project service accounts 
    remote isolated function listProjectServiceAccounts(IdOrPath id, map<string|string[]> headers = {}, *ListProjectServiceAccountsQueries queries) returns ServiceAccount[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project service account 
    # + return - Created 
    remote isolated function createProjectServiceAccount(IdOrPath id, ServiceAccountRequest payload, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get a single project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project service account 
    remote isolated function getProjectServiceAccount(IdOrPath id, int userId, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectServiceAccount(IdOrPath id, int userId, map<string|string[]> headers = {}, *DeleteProjectServiceAccountQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project service account 
    # + return - The updated project service account 
    remote isolated function updateProjectServiceAccount(IdOrPath id, int userId, ServiceAccountRequest payload, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all personal access tokens for a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project service account tokens 
    remote isolated function listProjectServiceAccountTokens(IdOrPath id, int userId, map<string|string[]> headers = {}, *ListProjectServiceAccountTokensQueries queries) returns PersonalAccessToken[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a personal access token for a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a personal access token for a project service account 
    # + return - Created 
    remote isolated function createProjectServiceAccountToken(IdOrPath id, int userId, CreatePersonalAccessTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Revoke a personal access token for a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + tokenId - The ID of the personal access token
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeProjectServiceAccountToken(IdOrPath id, int tokenId, int userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a personal access token for a project service account
    #
    # + id - The ID or URL-encoded path of the project
    # + tokenId - The ID of the personal access token
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a personal access token for a project service account 
    # + return - Created 
    remote isolated function rotateProjectServiceAccountToken(IdOrPath id, int tokenId, int userId, RotateTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens/${getEncodedUri(tokenId)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project repository storage moves
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all project storage moves 
    remote isolated function listAllProjectStorageMoves(map<string|string[]> headers = {}, *ListAllProjectStorageMovesQueries queries) returns ProjectsRepositoryStorageMove[]|error {
        string resourcePath = string `/project_repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create repository storage moves for all projects on a storage shard
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create repository storage moves for all projects on a storage shard 
    # + return - Accepted 
    remote isolated function scheduleProjectStorageMovesForShard(ScheduleStorageMovesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/project_repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a project repository storage move
    #
    # + repositoryStorageMoveId - The ID of a project repository storage move
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project storage move 
    remote isolated function getProjectStorageMove(int repositoryStorageMoveId, map<string|string[]> headers = {}) returns ProjectsRepositoryStorageMove|error {
        string resourcePath = string `/project_repository_storage_moves/${getEncodedUri(repositoryStorageMoveId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all repository storage moves for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project repository storage moves 
    remote isolated function listProjectRepositoryStorageMoves(IdOrPath id, map<string|string[]> headers = {}, *ListProjectRepositoryStorageMovesQueries queries) returns ProjectsRepositoryStorageMove[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a repository storage move for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a repository storage move for a project 
    # + return - Created 
    remote isolated function createProjectRepositoryStorageMove(IdOrPath id, CreateStorageMoveRequest payload, map<string|string[]> headers = {}) returns ProjectsRepositoryStorageMove|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository_storage_moves`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a repository storage move for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + repositoryStorageMoveId - The ID of a project repository storage move
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project repository storage move 
    remote isolated function getProjectRepositoryStorageMove(IdOrPath id, int repositoryStorageMoveId, map<string|string[]> headers = {}) returns ProjectsRepositoryStorageMove|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository_storage_moves/${getEncodedUri(repositoryStorageMoveId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project milestones
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project milestones 
    remote isolated function listProjectMilestones(IdOrPath id, map<string|string[]> headers = {}, *ListProjectMilestonesQueries queries) returns Milestone[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"iids": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project milestone 
    # + return - Created 
    remote isolated function createProjectMilestone(IdOrPath id, CreateMilestoneRequest payload, map<string|string[]> headers = {}) returns Milestone|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a project milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The ID of a project milestone
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project milestone 
    remote isolated function getProjectMilestone(IdOrPath id, int milestoneId, map<string|string[]> headers = {}) returns Milestone|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The milestone ID number
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project milestone 
    # + return - The updated project milestone 
    remote isolated function updateProjectMilestone(IdOrPath id, int milestoneId, UpdateMilestoneRequest payload, map<string|string[]> headers = {}) returns Milestone|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a project milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The ID of a project milestone
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectMilestone(IdOrPath id, int milestoneId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all issues for a project milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The ID of a project milestone
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project milestone issues 
    remote isolated function listProjectMilestoneIssues(IdOrPath id, int milestoneId, map<string|string[]> headers = {}, *ListProjectMilestoneIssuesQueries queries) returns IssueBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge requests for a project milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The ID of a project milestone
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project milestone merge requests 
    remote isolated function listProjectMilestoneMergeRequests(IdOrPath id, int milestoneId, map<string|string[]> headers = {}, *ListProjectMilestoneMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Promote a project milestone to a group milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The ID of a project milestone
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function promoteProjectMilestone(IdOrPath id, int milestoneId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/promote`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all burndown chart events for a milestone
    #
    # + id - The ID or URL-encoded path of the project
    # + milestoneId - The ID of a project milestone
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listProjectMilestoneBurndownEvents(IdOrPath id, int milestoneId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/burndown_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve global notification settings
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved global notification settings 
    remote isolated function getGlobalNotificationSettings(map<string|string[]> headers = {}) returns GlobalNotificationSetting|error {
        string resourcePath = string `/notification_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update global notification settings
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update global notification settings 
    # + return - The updated global notification settings 
    remote isolated function updateGlobalNotificationSettings(UpdateGlobalNotificationSettingsRequest payload, map<string|string[]> headers = {}) returns GlobalNotificationSetting|error {
        string resourcePath = string `/notification_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve notification settings for a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group notification settings 
    remote isolated function getGroupNotificationSettings(string id, map<string|string[]> headers = {}) returns NotificationSetting|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/notification_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update notification settings for a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update notification settings for a group 
    # + return - The updated group notification settings 
    remote isolated function updateGroupNotificationSettings(string id, UpdateGroupNotificationSettingsRequest payload, map<string|string[]> headers = {}) returns NotificationSetting|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/notification_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve notification settings for a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project notification settings 
    remote isolated function getProjectNotificationSettings(string id, map<string|string[]> headers = {}) returns NotificationSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/notification_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update notification settings for a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update notification settings for a project 
    # + return - The updated project notification settings 
    remote isolated function updateProjectNotificationSettings(string id, UpdateProjectNotificationSettingsRequest payload, map<string|string[]> headers = {}) returns NotificationSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/notification_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all issue notes
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue notes 
    remote isolated function listIssueNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListIssueNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an issue note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an issue note 
    # + return - Created 
    remote isolated function createIssueNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an issue note
    #
    # + id - The ID of a project
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue note 
    remote isolated function getIssueNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an issue note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an issue note 
    # + return - The updated issue note 
    remote isolated function updateIssueNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an issue note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteIssueNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all merge request notes
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request notes 
    remote isolated function listMergeRequestNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListMergeRequestNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a merge request note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a merge request note 
    # + return - Created 
    remote isolated function createMergeRequestNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a merge request note
    #
    # + id - The ID of a project
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request note 
    remote isolated function getMergeRequestNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a merge request note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a merge request note 
    # + return - The updated merge request note 
    remote isolated function updateMergeRequestNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a merge request note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteMergeRequestNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all snippet notes
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved snippet notes 
    remote isolated function listSnippetNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListSnippetNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a snippet note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a snippet note 
    # + return - Created 
    remote isolated function createSnippetNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a snippet note
    #
    # + id - The ID of a project
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet note 
    remote isolated function getSnippetNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a snippet note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a snippet note 
    # + return - The updated snippet note 
    remote isolated function updateSnippetNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a snippet note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSnippetNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all wiki page meta notes
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project wiki page meta notes 
    remote isolated function listProjectWikiPageMetaNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListProjectWikiPageMetaNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a wiki page meta note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a wiki page meta note 
    # + return - Created 
    remote isolated function createProjectWikiPageMetaNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a wiki page meta note
    #
    # + id - The ID of a project
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project wiki page meta note 
    remote isolated function getProjectWikiPageMetaNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a wiki page meta note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a wiki page meta note 
    # + return - The updated project wiki page meta note 
    remote isolated function updateProjectWikiPageMetaNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a wiki page meta note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectWikiPageMetaNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all epic notes
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved epic notes 
    remote isolated function listEpicNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListEpicNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an epic note
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an epic note 
    # + return - Created 
    remote isolated function createEpicNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an epic note
    #
    # + id - The ID of a group
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic note 
    remote isolated function getEpicNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an epic note
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an epic note 
    # + return - The updated epic note 
    remote isolated function updateEpicNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an epic note
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteEpicNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all vulnerability notes
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved vulnerability notes 
    remote isolated function listVulnerabilityNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListVulnerabilityNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a vulnerability note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a vulnerability note 
    # + return - Created 
    remote isolated function createVulnerabilityNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a vulnerability note
    #
    # + id - The ID of a project
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved vulnerability note 
    remote isolated function getVulnerabilityNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a vulnerability note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a vulnerability note 
    # + return - The updated vulnerability note 
    remote isolated function updateVulnerabilityNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a vulnerability note
    #
    # + id - The ID of a project
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteVulnerabilityNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/vulnerabilities/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all wiki page meta notes
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group wiki page meta notes 
    remote isolated function listGroupWikiPageMetaNotes(string id, int noteableId, map<string|string[]> headers = {}, *ListGroupWikiPageMetaNotesQueries queries) returns Note[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a wiki page meta note
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a wiki page meta note 
    # + return - Created 
    remote isolated function createGroupWikiPageMetaNote(string id, int noteableId, CreateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a wiki page meta note
    #
    # + id - The ID of a group
    # + noteId - The ID of a note
    # + noteableId - The ID of the noteable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group wiki page meta note 
    remote isolated function getGroupWikiPageMetaNote(string id, int noteId, int noteableId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a wiki page meta note
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a wiki page meta note 
    # + return - The updated group wiki page meta note 
    remote isolated function updateGroupWikiPageMetaNote(string id, int noteableId, int noteId, UpdateNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a wiki page meta note
    #
    # + id - The ID of a group
    # + noteableId - The ID of the noteable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupWikiPageMetaNote(string id, int noteableId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wiki_pages/${getEncodedUri(noteableId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get MCP response listener
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getMcpResponseListener(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/mcp`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create MCP request handler
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create MCP request handler 
    # + return - Created 
    remote isolated function sendMcpRequest(McpRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/mcp`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group service accounts
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group service accounts 
    remote isolated function listGroupServiceAccounts(IdOrPath id, map<string|string[]> headers = {}, *ListGroupServiceAccountsQueries queries) returns ServiceAccount[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group service account 
    # + return - Created 
    remote isolated function createGroupServiceAccount(IdOrPath id, ServiceAccountRequest payload, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get a single group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group service account 
    remote isolated function getGroupServiceAccount(IdOrPath id, int userId, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteGroupServiceAccount(IdOrPath id, int userId, map<string|string[]> headers = {}, *DeleteGroupServiceAccountQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group service account 
    # + return - The updated group service account 
    remote isolated function updateGroupServiceAccount(IdOrPath id, int userId, ServiceAccountRequest payload, map<string|string[]> headers = {}) returns ServiceAccount|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all personal access tokens for a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group service account tokens 
    remote isolated function listGroupServiceAccountTokens(IdOrPath id, int userId, map<string|string[]> headers = {}, *ListGroupServiceAccountTokensQueries queries) returns PersonalAccessToken[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a personal access token for a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a personal access token for a group service account 
    # + return - Created 
    remote isolated function createGroupServiceAccountToken(IdOrPath id, int userId, CreatePersonalAccessTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Revoke a personal access token for a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + tokenId - The ID of the personal access token
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeGroupServiceAccountToken(IdOrPath id, int tokenId, int userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a personal access token for a group service account
    #
    # + id - The ID or URL-encoded path of the group
    # + tokenId - The ID of the personal access token
    # + userId - The ID of the service account
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a personal access token for a group service account 
    # + return - Created 
    remote isolated function rotateGroupServiceAccountToken(IdOrPath id, int tokenId, int userId, RotateTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/service_accounts/${getEncodedUri(userId)}/personal_access_tokens/${getEncodedUri(tokenId)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group milestones
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group milestones 
    remote isolated function listGroupMilestones(string id, map<string|string[]> headers = {}, *ListGroupMilestonesQueries queries) returns Milestone[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"iids": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group milestone
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group milestone 
    # + return - Created 
    remote isolated function createGroupMilestone(string id, CreateMilestoneRequest payload, map<string|string[]> headers = {}) returns Milestone|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group milestone
    #
    # + id - The ID of a group
    # + milestoneId - The ID of a group milestone
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group milestone 
    remote isolated function getGroupMilestone(string id, int milestoneId, map<string|string[]> headers = {}) returns Milestone|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group milestone
    #
    # + id - The ID of a group
    # + milestoneId - The milestone ID number
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group milestone 
    # + return - The updated group milestone 
    remote isolated function updateGroupMilestone(string id, int milestoneId, UpdateMilestoneRequest payload, map<string|string[]> headers = {}) returns Milestone|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group milestone
    #
    # + id - The ID of a group
    # + milestoneId - The ID of a group milestone
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteGroupMilestone(string id, int milestoneId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all issues assigned to a single milestone
    #
    # + id - The ID of a group
    # + milestoneId - The ID of a group milestone
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group milestone issues 
    remote isolated function listGroupMilestoneIssues(string id, int milestoneId, map<string|string[]> headers = {}, *ListGroupMilestoneIssuesQueries queries) returns IssueBasic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge requests assigned to a single milestone
    #
    # + id - The ID of a group
    # + milestoneId - The ID of a group milestone
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group milestone merge requests 
    remote isolated function listGroupMilestoneMergeRequests(string id, int milestoneId, map<string|string[]> headers = {}, *ListGroupMilestoneMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all burndown chart events for a milestone
    #
    # + id - The ID of a group
    # + milestoneId - The ID of a group milestone
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listGroupMilestoneBurndownEvents(string id, int milestoneId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/milestones/${getEncodedUri(milestoneId)}/burndown_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project labels
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project labels 
    remote isolated function listProjectLabels(IdOrPath id, map<string|string[]> headers = {}, *ListProjectLabelsQueries queries) returns ProjectLabel[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an existing label. At least one optional parameter is required.
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an existing label. At least one optional parameter is required 
    # + return - The updated project label by param 
    # 
    # # Deprecated
    @deprecated
    remote isolated function updateProjectLabelByParam(IdOrPath id, UpdateProjectLabelByParamRequest payload, map<string|string[]> headers = {}) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create a project label
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project label 
    # + return - Created 
    remote isolated function createProjectLabel(IdOrPath id, CreateProjectLabelRequest payload, map<string|string[]> headers = {}) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete an existing label
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    # 
    # # Deprecated
    @deprecated
    remote isolated function deleteProjectLabelByParam(IdOrPath id, map<string|string[]> headers = {}, *DeleteProjectLabelByParamQueries queries) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a project label
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The ID or name of a label
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project label 
    remote isolated function getProjectLabel(IdOrPath id, IdOrPath name, map<string|string[]> headers = {}, *GetProjectLabelQueries queries) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project label
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name or id of the label to be updated
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project label 
    # + return - The updated project label 
    remote isolated function updateProjectLabel(IdOrPath id, string name, UpdateProjectLabelRequest payload, map<string|string[]> headers = {}) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a project label
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name or id of the label to be deleted
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectLabel(IdOrPath id, string name, map<string|string[]> headers = {}) returns ProjectLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Promote a label to a group label
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to promote a label to a group label 
    # + return - Success 
    # 
    # # Deprecated
    @deprecated
    remote isolated function promoteProjectLabelByParam(IdOrPath id, PromoteProjectLabelByParamRequest payload, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/promote`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Promote a project label to a group label
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name or id of the label to be promoted
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function promoteProjectLabel(IdOrPath id, string name, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/labels/${getEncodedUri(name)}/promote`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all group labels
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group labels 
    remote isolated function listGroupLabels(string id, map<string|string[]> headers = {}, *ListGroupLabelsQueries queries) returns GroupLabel[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group label
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group label 
    # + return - The updated group label by param 
    remote isolated function updateGroupLabelByParam(string id, UpdateGroupLabelByParamRequest payload, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create a group label
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group label 
    # + return - Created 
    remote isolated function createGroupLabel(string id, CreateGroupLabelRequest payload, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a group label
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupLabelByParam(string id, map<string|string[]> headers = {}, *DeleteGroupLabelByParamQueries queries) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a group label
    #
    # + id - The ID of a group
    # + name - The ID or name of a label
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group label 
    remote isolated function getGroupLabel(string id, IdOrPath name, map<string|string[]> headers = {}, *GetGroupLabelQueries queries) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group label
    #
    # + id - The ID of a group
    # + name - The name or id of the label to be updated
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group label 
    # + return - The updated group label 
    remote isolated function updateGroupLabel(string id, string name, UpdateGroupLabelRequest payload, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group label
    #
    # + id - The ID of a group
    # + name - The name or id of the label to be deleted
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupLabel(string id, string name, map<string|string[]> headers = {}) returns GroupLabel|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/labels/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all group issue board lists
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group issue board lists 
    remote isolated function listGroupIssueBoardLists(string id, int boardId, map<string|string[]> headers = {}, *ListGroupIssueBoardListsQueries queries) returns BoardList[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group issue board list
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group issue board list 
    # + return - Created 
    remote isolated function createGroupIssueBoardList(string id, int boardId, CreateIssueBoardListRequest payload, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group issue board list
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + listId - The ID of a list
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group issue board list 
    remote isolated function getGroupIssueBoardList(string id, int boardId, int listId, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group issue board list
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + listId - The ID of a list
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group issue board list 
    # + return - The updated group issue board list 
    remote isolated function updateGroupIssueBoardList(string id, int boardId, int listId, UpdateIssueBoardListRequest payload, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group issue board list
    #
    # + id - The ID of a group
    # + boardId - The ID of a board
    # + listId - The ID of a board list
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupIssueBoardList(string id, int boardId, int listId, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all discussions for an issue
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue discussions 
    remote isolated function listIssueDiscussions(string id, int noteableId, map<string|string[]> headers = {}, *ListIssueDiscussionsQueries queries) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an issue discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an issue discussion 
    # + return - Created 
    remote isolated function createIssueDiscussion(string id, int noteableId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a discussion for an issue
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue discussion 
    remote isolated function getIssueDiscussion(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Resolve or unresolve an issue discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to resolve or unresolve an issue discussion 
    # + return - Success 
    remote isolated function resolveIssueDiscussion(string id, int noteableId, string discussionId, ResolveDiscussionRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List comments in an issue discussion
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue discussion comments 
    remote isolated function listIssueDiscussionComments(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a comment to an issue discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a comment to an issue discussion 
    # + return - Created 
    remote isolated function addIssueDiscussionComment(string id, int noteableId, string discussionId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a comment from an issue discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue discussion comment 
    remote isolated function getIssueDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a comment in an issue discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a comment in an issue discussion 
    # + return - The updated issue discussion comment 
    remote isolated function updateIssueDiscussionComment(string id, int noteableId, string discussionId, int noteId, UpdateDiscussionCommentRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a comment from an issue discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the issue
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteIssueDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all discussions for a snippet
    #
    # + id - The ID of a project
    # + noteableId - The ID of the snippet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved snippet discussions 
    remote isolated function listSnippetDiscussions(string id, int noteableId, map<string|string[]> headers = {}, *ListSnippetDiscussionsQueries queries) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a snippet discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the snippet
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a snippet discussion 
    # + return - Created 
    remote isolated function createSnippetDiscussion(string id, int noteableId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a discussion for a snippet
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet discussion 
    remote isolated function getSnippetDiscussion(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List comments in a snippet discussion
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet discussion comments 
    remote isolated function listSnippetDiscussionComments(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a comment to a snippet discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the snippet
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a comment to a snippet discussion 
    # + return - Created 
    remote isolated function addSnippetDiscussionComment(string id, int noteableId, string discussionId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a comment from a snippet discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the snippet
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet discussion comment 
    remote isolated function getSnippetDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a comment in a snippet discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the snippet
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a comment in a snippet discussion 
    # + return - The updated snippet discussion comment 
    remote isolated function updateSnippetDiscussionComment(string id, int noteableId, string discussionId, int noteId, UpdateDiscussionCommentRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a comment from a snippet discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the snippet
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSnippetDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all discussions for a merge request
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request discussions 
    remote isolated function listMergeRequestDiscussions(string id, int noteableId, map<string|string[]> headers = {}, *ListMergeRequestDiscussionsQueries queries) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a merge request discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a merge request discussion 
    # + return - Created 
    remote isolated function createMergeRequestDiscussion(string id, int noteableId, CreateMergeRequestDiscussionRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a discussion for a merge request
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request discussion 
    remote isolated function getMergeRequestDiscussion(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Resolve or unresolve a merge request discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to resolve or unresolve a merge request discussion 
    # + return - Success 
    remote isolated function resolveMergeRequestDiscussion(string id, int noteableId, string discussionId, ResolveDiscussionRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List comments in a merge request discussion
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request discussion comments 
    remote isolated function listMergeRequestDiscussionComments(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a comment to a merge request discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a comment to a merge request discussion 
    # + return - Created 
    remote isolated function addMergeRequestDiscussionComment(string id, int noteableId, string discussionId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a comment from a merge request discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request discussion comment 
    remote isolated function getMergeRequestDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a comment in a merge request discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a comment in a merge request discussion 
    # + return - The updated merge request discussion comment 
    remote isolated function updateMergeRequestDiscussionComment(string id, int noteableId, string discussionId, int noteId, UpdateDiscussionCommentRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a comment from a merge request discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the merge request
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteMergeRequestDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all discussions for a commit
    #
    # + id - The ID of a project
    # + noteableId - The ID of the commit
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit discussions 
    remote isolated function listCommitDiscussions(string id, string noteableId, map<string|string[]> headers = {}, *ListCommitDiscussionsQueries queries) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a commit discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the commit
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a commit discussion 
    # + return - Created 
    remote isolated function createCommitDiscussion(string id, string noteableId, CreateCommitDiscussionRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a discussion for a commit
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the commit
    # + headers - Headers to be sent with the request 
    # + return - The retrieved commit discussion 
    remote isolated function getCommitDiscussion(string id, string discussionId, string noteableId, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List comments in a commit discussion
    #
    # + id - The ID of a project
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the commit
    # + headers - Headers to be sent with the request 
    # + return - The retrieved commit discussion comments 
    remote isolated function listCommitDiscussionComments(string id, string discussionId, string noteableId, map<string|string[]> headers = {}) returns Discussion[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a comment to a commit discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the commit
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a comment to a commit discussion 
    # + return - Created 
    remote isolated function addCommitDiscussionComment(string id, string noteableId, string discussionId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a comment from a commit discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the commit
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved commit discussion comment 
    remote isolated function getCommitDiscussionComment(string id, string noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a comment in a commit discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the commit
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a comment in a commit discussion 
    # + return - The updated commit discussion comment 
    remote isolated function updateCommitDiscussionComment(string id, string noteableId, string discussionId, int noteId, UpdateDiscussionCommentRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a comment from a commit discussion
    #
    # + id - The ID of a project
    # + noteableId - The ID of the commit
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteCommitDiscussionComment(string id, string noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all discussions for an epic
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved epic discussions 
    remote isolated function listEpicDiscussions(string id, int noteableId, map<string|string[]> headers = {}, *ListEpicDiscussionsQueries queries) returns Discussion[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an epic discussion
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an epic discussion 
    # + return - Created 
    remote isolated function createEpicDiscussion(string id, int noteableId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a discussion for an epic
    #
    # + id - The ID of a group
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic discussion 
    remote isolated function getEpicDiscussion(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Resolve or unresolve an epic discussion
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to resolve or unresolve an epic discussion 
    # + return - Success 
    remote isolated function resolveEpicDiscussion(string id, int noteableId, string discussionId, ResolveDiscussionRequest payload, map<string|string[]> headers = {}) returns Discussion|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List comments in an epic discussion
    #
    # + id - The ID of a group
    # + discussionId - The ID of a discussion
    # + noteableId - The ID of the epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic discussion comments 
    remote isolated function listEpicDiscussionComments(string id, string discussionId, int noteableId, map<string|string[]> headers = {}) returns Discussion[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a comment to an epic discussion
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + discussionId - The ID of a discussion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a comment to an epic discussion 
    # + return - Created 
    remote isolated function addEpicDiscussionComment(string id, int noteableId, string discussionId, DiscussionNoteRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a comment from an epic discussion
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic discussion comment 
    remote isolated function getEpicDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a comment in an epic discussion
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a comment in an epic discussion 
    # + return - The updated epic discussion comment 
    remote isolated function updateEpicDiscussionComment(string id, int noteableId, string discussionId, int noteId, UpdateDiscussionCommentRequest payload, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a comment from an epic discussion
    #
    # + id - The ID of a group
    # + noteableId - The ID of the epic
    # + discussionId - The ID of a discussion
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteEpicDiscussionComment(string id, int noteableId, string discussionId, int noteId, map<string|string[]> headers = {}) returns Note|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(noteableId)}/discussions/${getEncodedUri(discussionId)}/notes/${getEncodedUri(noteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all project issue boards
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project issue boards 
    remote isolated function listProjectIssueBoards(IdOrPath id, map<string|string[]> headers = {}, *ListProjectIssueBoardsQueries queries) returns Board[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an issue board
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an issue board 
    # + return - Created 
    remote isolated function createProjectIssueBoard(IdOrPath id, CreateIssueBoardRequest payload, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an issue board
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project issue board 
    remote isolated function getProjectIssueBoard(IdOrPath id, int boardId, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an issue board
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an issue board 
    # + return - The updated project issue board 
    remote isolated function updateProjectIssueBoard(IdOrPath id, int boardId, UpdateIssueBoardRequest payload, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an issue board
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectIssueBoard(IdOrPath id, int boardId, map<string|string[]> headers = {}) returns Board|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all board lists in an issue board
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project issue board lists 
    remote isolated function listProjectIssueBoardLists(IdOrPath id, int boardId, map<string|string[]> headers = {}, *ListProjectIssueBoardListsQueries queries) returns BoardList[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an issue board list
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an issue board list 
    # + return - Created 
    remote isolated function createProjectIssueBoardList(IdOrPath id, int boardId, CreateIssueBoardListRequest payload, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a board list
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + listId - The ID of a list
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project issue board list 
    remote isolated function getProjectIssueBoardList(IdOrPath id, int boardId, int listId, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update position of a board list
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + listId - The ID of a list
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update position of a board list 
    # + return - The updated project issue board list 
    remote isolated function updateProjectIssueBoardList(IdOrPath id, int boardId, int listId, UpdateIssueBoardListRequest payload, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a list from an issue board
    #
    # + id - The ID or URL-encoded path of the project
    # + boardId - The ID of a board
    # + listId - The ID of a board list
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectIssueBoardList(IdOrPath id, int boardId, int listId, map<string|string[]> headers = {}) returns BoardList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/boards/${getEncodedUri(boardId)}/lists/${getEncodedUri(listId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Delete jobs from a Sidekiq queue
    #
    # + queueName - The Sidekiq queue name
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function deleteSidekiqQueueJobs(string queueName, map<string|string[]> headers = {}, *DeleteSidekiqQueueJobsQueries queries) returns error? {
        string resourcePath = string `/admin/sidekiq/queues/${getEncodedUri(queueName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all wiki pages for a group
    #
    # + id - The ID or URL-encoded path of the group or project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group wiki pages 
    remote isolated function listGroupWikiPages(IdOrPath id, map<string|string[]> headers = {}, *ListGroupWikiPagesQueries queries) returns WikiPageBasic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wikis`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a wiki page for a group
    #
    # + id - The ID or URL-encoded path of the group or project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a wiki page for a group 
    # + return - Created 
    remote isolated function createGroupWikiPage(IdOrPath id, CreateWikiPageRequest payload, map<string|string[]> headers = {}) returns WikiPage|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wikis`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a wiki page for a group
    #
    # + id - The ID or URL-encoded path of the group or project
    # + slug - The slug of a wiki page
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group wiki page 
    remote isolated function getGroupWikiPage(IdOrPath id, string slug, map<string|string[]> headers = {}, *GetGroupWikiPageQueries queries) returns WikiPage|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wikis/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a wiki page for a group
    #
    # + id - The ID or URL-encoded path of the group or project
    # + slug - The slug of a wiki page
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a wiki page for a group 
    # + return - The updated group wiki page 
    remote isolated function updateGroupWikiPage(IdOrPath id, string slug, UpdateWikiPageRequest payload, map<string|string[]> headers = {}) returns WikiPage|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wikis/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a wiki page for a group
    #
    # + id - The ID or URL-encoded path of the group or project
    # + slug - The slug of a wiki page
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupWikiPage(IdOrPath id, string slug, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wikis/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Upload an attachment to a group wiki
    #
    # + id - The ID or URL-encoded path of the group or project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload an attachment to a group wiki 
    # + return - Created 
    remote isolated function uploadGroupWikiAttachment(IdOrPath id, UploadWikiAttachmentRequest payload, map<string|string[]> headers = {}) returns WikiAttachment|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/wikis/attachments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all wiki pages for a project
    #
    # + id - The ID or URL-encoded path of the group or project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project wiki pages 
    remote isolated function listProjectWikiPages(IdOrPath id, map<string|string[]> headers = {}, *ListProjectWikiPagesQueries queries) returns WikiPageBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wikis`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a wiki page for a project
    #
    # + id - The ID or URL-encoded path of the group or project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a wiki page for a project 
    # + return - Created 
    remote isolated function createProjectWikiPage(IdOrPath id, CreateWikiPageRequest payload, map<string|string[]> headers = {}) returns WikiPage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wikis`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a wiki page for a project
    #
    # + id - The ID or URL-encoded path of the group or project
    # + slug - The slug of a wiki page
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project wiki page 
    remote isolated function getProjectWikiPage(IdOrPath id, string slug, map<string|string[]> headers = {}, *GetProjectWikiPageQueries queries) returns WikiPage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wikis/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a wiki page for a project
    #
    # + id - The ID or URL-encoded path of the group or project
    # + slug - The slug of a wiki page
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a wiki page for a project 
    # + return - The updated project wiki page 
    remote isolated function updateProjectWikiPage(IdOrPath id, string slug, UpdateWikiPageRequest payload, map<string|string[]> headers = {}) returns WikiPage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wikis/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a wiki page for a project
    #
    # + id - The ID or URL-encoded path of the group or project
    # + slug - The slug of a wiki page
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectWikiPage(IdOrPath id, string slug, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wikis/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Upload an attachment to a project wiki
    #
    # + id - The ID or URL-encoded path of the group or project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload an attachment to a project wiki 
    # + return - Created 
    remote isolated function uploadProjectWikiAttachment(IdOrPath id, UploadWikiAttachmentRequest payload, map<string|string[]> headers = {}) returns WikiAttachment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/wikis/attachments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the public signing key
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getWebCommitsPublicKey(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/web_commits/public_key`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a runner owned by currently authenticated user
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a runner owned by currently authenticated user 
    # + return - Created 
    remote isolated function createCurrentUserRunner(CreateCurrentUserRunnerRequest payload, map<string|string[]> headers = {}) returns CiRunnerRegistrationDetails|error {
        string resourcePath = string `/user/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Return the user specific counts
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved current user counts 
    remote isolated function getCurrentUserCounts(map<string|string[]> headers = {}) returns UserCounts|error {
        string resourcePath = string `/user_counts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Service Ping SQL queries
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listServicePingSqlQueries(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/queries`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all non-SQL metrics
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listNonSqlMetrics(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/non_sql_metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Track usage data event
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to track usage data event 
    # + return - Success 
    remote isolated function incrementUsageCounter(UsageDataEventRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/increment_counter`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Track usage data event for the current user
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to track usage data event for the current user 
    # + return - Success 
    remote isolated function incrementUsageUniqueUsers(UsageDataEventRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/increment_unique_users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Track multiple internal GitLab events
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to track multiple internal GitLab events 
    # + return - Success 
    remote isolated function trackMultipleInternalGitLabEvents(TrackMultipleInternalGitLabEventsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/track_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download metric definitions
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadMetricDefinitions(map<string|string[]> headers = {}, *DownloadMetricDefinitionsQueries queries) returns http:Response|error {
        string resourcePath = string `/usage_data/metric_definitions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get Unleash features
    #
    # + projectId - The ID of a project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getUnleashFeatures(string projectId, map<string|string[]> headers = {}, *GetUnleashFeaturesQueries queries) returns error? {
        string resourcePath = string `/feature_flags/unleash/${getEncodedUri(projectId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get a list of features (v2 client support)
    #
    # + projectId - The ID of a project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    # 
    # # Deprecated
    @deprecated
    remote isolated function listUnleashFeaturesV2(string projectId, map<string|string[]> headers = {}, *ListUnleashFeaturesV2Queries queries) returns error? {
        string resourcePath = string `/feature_flags/unleash/${getEncodedUri(projectId)}/features`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get a list of features
    #
    # + projectId - The ID of a project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listUnleashClientFeatures(string projectId, map<string|string[]> headers = {}, *ListUnleashClientFeaturesQueries queries) returns error? {
        string resourcePath = string `/feature_flags/unleash/${getEncodedUri(projectId)}/client/features`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Register Unleash client
    #
    # + projectId - The ID of a project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to register Unleash client 
    # + return - Created 
    remote isolated function registerUnleashClient(string projectId, UnleashClientRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/feature_flags/unleash/${getEncodedUri(projectId)}/client/register`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Report Unleash client metrics
    #
    # + projectId - The ID of a project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to report Unleash client metrics 
    # + return - Created 
    remote isolated function reportUnleashClientMetrics(string projectId, UnleashClientRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/feature_flags/unleash/${getEncodedUri(projectId)}/client/metrics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all topics
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved topics 
    remote isolated function listTopics(map<string|string[]> headers = {}, *ListTopicsQueries queries) returns ProjectsTopic[]|error {
        string resourcePath = string `/topics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project topic
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project topic 
    # + return - Created 
    remote isolated function createTopic(CreateTopicRequest payload, map<string|string[]> headers = {}) returns ProjectsTopic|error {
        string resourcePath = string `/topics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a topic
    #
    # + id - ID of project topic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved topic 
    remote isolated function getTopic(int id, map<string|string[]> headers = {}) returns ProjectsTopic|error {
        string resourcePath = string `/topics/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project topic
    #
    # + id - ID of project topic
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project topic 
    # + return - The updated topic 
    remote isolated function updateTopic(int id, UpdateTopicRequest payload, map<string|string[]> headers = {}) returns ProjectsTopic|error {
        string resourcePath = string `/topics/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a project topic
    #
    # + id - ID of project topic
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteTopic(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/topics/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Merge topics
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to merge topics 
    # + return - Created 
    remote isolated function mergeTopics(MergeTopicsRequest payload, map<string|string[]> headers = {}) returns ProjectsTopic|error {
        string resourcePath = string `/topics/merge`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a Terraform state version
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + serial - The version number of the state
    # + headers - Headers to be sent with the request 
    # + return - The retrieved terraform state version 
    remote isolated function getTerraformStateVersion(IdOrPath id, string name, int serial, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}/versions/${getEncodedUri(serial)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a Terraform state version
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + serial - The version number of the state
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteTerraformStateVersion(IdOrPath id, string name, int serial, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}/versions/${getEncodedUri(serial)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all Terraform state protection rules for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved terraform state protection rules 
    remote isolated function listTerraformStateProtectionRules(IdOrPath id, map<string|string[]> headers = {}) returns TerraformStateProtectionRule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state_protection_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a Terraform state protection rule for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Terraform state protection rule for a project 
    # + return - Created 
    remote isolated function createTerraformStateProtectionRule(IdOrPath id, CreateTerraformStateProtectionRuleRequest payload, map<string|string[]> headers = {}) returns TerraformStateProtectionRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state_protection_rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a Terraform state protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + terraformStateProtectionRuleId - The ID of the Terraform state protection rule
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteTerraformStateProtectionRule(IdOrPath id, int terraformStateProtectionRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state_protection_rules/${getEncodedUri(terraformStateProtectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a Terraform state protection rule for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + terraformStateProtectionRuleId - The ID of the Terraform state protection rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Terraform state protection rule for a project 
    # + return - The updated terraform state protection rule 
    remote isolated function updateTerraformStateProtectionRule(IdOrPath id, int terraformStateProtectionRuleId, UpdateTerraformStateProtectionRuleRequest payload, map<string|string[]> headers = {}) returns TerraformStateProtectionRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state_protection_rules/${getEncodedUri(terraformStateProtectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Retrieve a Terraform state
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getTerraformState(IdOrPath id, string name, map<string|string[]> headers = {}, *GetTerraformStateQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update a Terraform state
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update a Terraform state 
    # + return - Success 
    remote isolated function setTerraformState(IdOrPath id, string name, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a Terraform state
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteTerraformState(IdOrPath id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Authorize Terraform state upload
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeTerraformStateUpload(IdOrPath id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Lock a Terraform state
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to lock a Terraform state 
    # + return - Success 
    remote isolated function lockTerraformState(IdOrPath id, string name, LockTerraformStateRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}/lock`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unlock a Terraform state
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of a Terraform state
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function unlockTerraformState(IdOrPath id, string name, map<string|string[]> headers = {}, *UnlockTerraformStateQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/terraform/state/${getEncodedUri(name)}/lock`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download the latest version of a module
    #
    # + id - The ID or full path of a project
    # + moduleName - Module name
    # + moduleSystem - Module system
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function downloadProjectTerraformModuleLatest(IdOrPath id, string moduleName, string moduleSystem, map<string|string[]> headers = {}, *DownloadProjectTerraformModuleLatestQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/terraform/modules/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a specific version of a module
    #
    # + id - The ID or full path of a project
    # + moduleName - Module name
    # + moduleSystem - Module system
    # + moduleVersion - Module version
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function downloadProjectTerraformModule(IdOrPath id, string moduleName, string moduleSystem, string moduleVersion, map<string|string[]> headers = {}, *DownloadProjectTerraformModuleQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/terraform/modules/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/${getEncodedUri(moduleVersion)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Workhorse authorize Terraform Module package file
    #
    # + id - The ID or full path of a project
    # + moduleName - Module name
    # + moduleSystem - Module system
    # + moduleVersion - Module version
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeTerraformModuleUpload(IdOrPath id, string moduleName, string moduleSystem, string moduleVersion, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/terraform/modules/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/${getEncodedUri(moduleVersion)}/file/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Upload Terraform Module package file
    #
    # + id - The ID or full path of a project
    # + moduleName - Module name
    # + moduleSystem - Module system
    # + moduleVersion - Module version
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload Terraform Module package file 
    # + return - Created 
    remote isolated function uploadTerraformModule(IdOrPath id, string moduleName, string moduleSystem, string moduleVersion, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/terraform/modules/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/${getEncodedUri(moduleVersion)}/file`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all available versions for a module
    #
    # + moduleNamespace - Group's ID or slug
    # + moduleName - The module name
    # + moduleSystem - The module system (provider)
    # + headers - Headers to be sent with the request 
    # + return - The retrieved terraform module versions 
    remote isolated function listTerraformModuleVersions(string moduleNamespace, string moduleName, string moduleSystem, map<string|string[]> headers = {}) returns TerraformModuleVersions[]|error {
        string resourcePath = string `/packages/terraform/modules/v1/${getEncodedUri(moduleNamespace)}/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/versions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve download URL for latest module version
    #
    # + moduleNamespace - Group's ID or slug
    # + moduleName - The module name
    # + moduleSystem - The module system (provider)
    # + headers - Headers to be sent with the request 
    # + return - Found 
    remote isolated function getTerraformModuleDownloadUrl(string moduleNamespace, string moduleName, string moduleSystem, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/terraform/modules/v1/${getEncodedUri(moduleNamespace)}/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve latest version for a module
    #
    # + moduleNamespace - Group's ID or slug
    # + moduleName - The module name
    # + moduleSystem - The module system (provider)
    # + headers - Headers to be sent with the request 
    # + return - The retrieved terraform module latest version 
    remote isolated function getTerraformModuleLatestVersion(string moduleNamespace, string moduleName, string moduleSystem, map<string|string[]> headers = {}) returns TerraformModuleVersion|error {
        string resourcePath = string `/packages/terraform/modules/v1/${getEncodedUri(moduleNamespace)}/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get download location for specific version of a module
    #
    # + moduleNamespace - Group's ID or slug
    # + moduleName - The module name
    # + moduleSystem - The module system (provider)
    # + moduleVersion - Version of the module
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function getTerraformModuleVersionDownload(string moduleNamespace, string moduleName, string moduleSystem, string moduleVersion, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/terraform/modules/v1/${getEncodedUri(moduleNamespace)}/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/${getEncodedUri(moduleVersion)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download specific version of a module
    #
    # + moduleNamespace - Group's ID or slug
    # + moduleName - The module name
    # + moduleSystem - The module system (provider)
    # + moduleVersion - Version of the module
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadTerraformModuleVersion(string moduleNamespace, string moduleName, string moduleSystem, string moduleVersion, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/packages/terraform/modules/v1/${getEncodedUri(moduleNamespace)}/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/${getEncodedUri(moduleVersion)}/file`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get details about specific version of a module
    #
    # + moduleNamespace - Group's ID or slug
    # + moduleName - The module name
    # + moduleSystem - The module system (provider)
    # + moduleVersion - Version of the module
    # + headers - Headers to be sent with the request 
    # + return - The retrieved terraform module version 
    remote isolated function getTerraformModuleVersion(string moduleNamespace, string moduleName, string moduleSystem, string moduleVersion, map<string|string[]> headers = {}) returns TerraformModuleVersion|error {
        string resourcePath = string `/packages/terraform/modules/v1/${getEncodedUri(moduleNamespace)}/${getEncodedUri(moduleName)}/${getEncodedUri(moduleSystem)}/${getEncodedUri(moduleVersion)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project repository tags
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project repository tags 
    remote isolated function listProjectRepositoryTags(IdOrPath id, map<string|string[]> headers = {}, *ListProjectRepositoryTagsQueries queries) returns Tag[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a tag
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a tag 
    # + return - Created 
    remote isolated function createRepositoryTag(IdOrPath id, CreateRepositoryTagRequest payload, map<string|string[]> headers = {}) returns Tag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a single repository tag
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The name of the tag
    # + headers - Headers to be sent with the request 
    # + return - The retrieved repository tag 
    remote isolated function getRepositoryTag(IdOrPath id, string tagName, map<string|string[]> headers = {}) returns Tag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/tags/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a tag
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The name of the tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteRepositoryTag(IdOrPath id, string tagName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/tags/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve X.509 signature of a tag
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The name of the tag
    # + headers - Headers to be sent with the request 
    # + return - The retrieved repository tag signature 
    remote isolated function getRepositoryTagSignature(IdOrPath id, string tagName, map<string|string[]> headers = {}) returns TagSignature|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/tags/${getEncodedUri(tagName)}/signature`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a URL variable
    #
    # + hookId - The ID of the hook
    # + 'key - The key of the variable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a URL variable 
    # + return - Success 
    remote isolated function updateSystemHookUrlVariable(int hookId, string 'key, UpdateHookUrlVariableRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}/url_variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a URL variable
    #
    # + hookId - The ID of the hook
    # + 'key - The key of the variable
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteSystemHookUrlVariable(int hookId, string 'key, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}/url_variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a custom header
    #
    # + hookId - The ID of the hook
    # + 'key - The name of the custom header
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a custom header 
    # + return - Success 
    remote isolated function updateSystemHookCustomHeader(int hookId, string 'key, UpdateHookCustomHeaderRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}/custom_headers/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a custom header
    #
    # + hookId - The ID of the hook
    # + 'key - The name of the custom header
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteSystemHookCustomHeader(int hookId, string 'key, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}/custom_headers/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all system hooks
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved system hooks 
    remote isolated function listSystemHooks(map<string|string[]> headers = {}, *ListSystemHooksQueries queries) returns Hook[]|error {
        string resourcePath = string `/hooks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a system hook
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a system hook 
    # + return - Created 
    remote isolated function createSystemHook(CreateSystemHookRequest payload, map<string|string[]> headers = {}) returns Hook|error {
        string resourcePath = string `/hooks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a system hook
    #
    # + hookId - The ID of the system hook
    # + headers - Headers to be sent with the request 
    # + return - The retrieved system hook 
    remote isolated function getSystemHook(int hookId, map<string|string[]> headers = {}) returns Hook|error {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a system hook
    #
    # + hookId - The ID of the system hook
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a system hook 
    # + return - The updated system hook 
    remote isolated function updateSystemHook(int hookId, UpdateSystemHookRequest payload, map<string|string[]> headers = {}) returns Hook|error {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create a test run
    #
    # + hookId - The ID of the hook
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function testSystemHook(int hookId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a system hook
    #
    # + hookId - The ID of the system hook
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSystemHook(int hookId, map<string|string[]> headers = {}) returns Hook|error {
        string resourcePath = string `/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all attestations for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + subjectDigest - The SHA-256 hash of the artifact
    # + headers - Headers to be sent with the request 
    # + return - The retrieved attestations 
    remote isolated function listAttestations(IdOrPath id, string subjectDigest, map<string|string[]> headers = {}) returns SupplyChainAttestation[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/attestations/${getEncodedUri(subjectDigest)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an attestation bundle
    #
    # + id - The ID or URL-encoded path of the project
    # + attestationIid - The iid of the attestation
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadAttestationBundle(IdOrPath id, IdOrPath attestationIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/attestations/${getEncodedUri(attestationIid)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Apply a suggestion to a merge request
    #
    # + id - The ID of the suggestion
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to apply a suggestion to a merge request 
    # + return - Success 
    remote isolated function applySuggestion(int id, ApplySuggestionRequest payload, map<string|string[]> headers = {}) returns Suggestion|error {
        string resourcePath = string `/suggestions/${getEncodedUri(id)}/apply`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Apply multiple suggestions to a merge request
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to apply multiple suggestions to a merge request 
    # + return - Success 
    remote isolated function applyMultipleSuggestions(ApplyMultipleSuggestionsRequest payload, map<string|string[]> headers = {}) returns Suggestion|error {
        string resourcePath = string `/suggestions/batch_apply`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Update a submodule reference
    #
    # + id - The ID or URL-encoded path of a project
    # + submodule - URL-encoded full path to submodule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a submodule reference 
    # + return - The updated submodule reference 
    remote isolated function updateSubmoduleReference(string id, string submodule, UpdateSubmoduleReferenceRequest payload, map<string|string[]> headers = {}) returns CommitDetail|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/submodules/${getEncodedUri(submodule)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve application statistics
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved application statistics 
    remote isolated function getApplicationStatistics(map<string|string[]> headers = {}) returns ApplicationStatistics|error {
        string resourcePath = string `/application/statistics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all snippets for current user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved current user snippets 
    remote isolated function listCurrentUserSnippets(map<string|string[]> headers = {}, *ListCurrentUserSnippetsQueries queries) returns Snippet[]|error {
        string resourcePath = string `/snippets`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a snippet
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a snippet 
    # + return - Created 
    remote isolated function createSnippet(CreateSnippetRequest payload, map<string|string[]> headers = {}) returns PersonalSnippet|error {
        string resourcePath = string `/snippets`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all public snippets
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved public snippets 
    remote isolated function listPublicSnippets(map<string|string[]> headers = {}, *ListPublicSnippetsQueries queries) returns PersonalSnippet[]|error {
        string resourcePath = string `/snippets/public`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all snippets
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all snippets 
    remote isolated function listAllSnippets(map<string|string[]> headers = {}, *ListAllSnippetsQueries queries) returns Snippet[]|error {
        string resourcePath = string `/snippets/all`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet 
    remote isolated function getSnippet(int id, map<string|string[]> headers = {}) returns PersonalSnippet|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update snippet 
    # + return - The updated snippet 
    remote isolated function updateSnippet(int id, UpdateSnippetRequest payload, map<string|string[]> headers = {}) returns PersonalSnippet|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteSnippet(int id, map<string|string[]> headers = {}) returns PersonalSnippet|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a raw snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getRawSnippet(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/snippets/${getEncodedUri(id)}/raw`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve snippet file content
    #
    # + ref - The name of branch, tag or commit
    # + filePath - The URL-encoded path to the file, like lib%2Fclass%2Erb
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getSnippetFileContent(string ref, string filePath, string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/snippets/${getEncodedUri(id)}/files/${getEncodedUri(ref)}/${getEncodedUri(filePath)}/raw`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve user agent details for a snippet
    #
    # + id - The ID of a snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet user agent 
    remote isolated function getSnippetUserAgent(int id, map<string|string[]> headers = {}) returns UserAgentDetail|error {
        string resourcePath = string `/snippets/${getEncodedUri(id)}/user_agent_detail`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search an instance
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchInstance(map<string|string[]> headers = {}, *SearchInstanceQueries queries) returns error? {
        string resourcePath = string `/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"type": {style: FORM, explode: false}, "fields": {style: FORM, explode: false}, "label_name": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search on GitLab within a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchGroup(IdOrPath id, map<string|string[]> headers = {}, *SearchGroupQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"type": {style: FORM, explode: false}, "fields": {style: FORM, explode: false}, "label_name": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchProject(IdOrPath id, map<string|string[]> headers = {}, *SearchProjectQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/-/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"type": {style: FORM, explode: false}, "fields": {style: FORM, explode: false}, "label_name": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download the spec index file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Spec file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadRubyGemsSpecIndex(IdOrPath id, "specs.4.8.gz"|"latest_specs.4.8.gz"|"prerelease_specs.4.8.gz" fileName, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rubygems/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a gemspec file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Gemspec file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadRubyGemspec(IdOrPath id, string fileName, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rubygems/quick/Marshal.4.8/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a gem file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadRubyGem(IdOrPath id, string fileName, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rubygems/gems/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Authorize a gem upload from workhorse
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeRubyGemUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rubygems/api/v1/gems/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Upload a gem
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a gem 
    # + return - Created 
    remote isolated function uploadRubyGem(IdOrPath id, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rubygems/api/v1/gems`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve dependencies
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved ruby gem dependencies 
    remote isolated function getRubyGemDependencies(IdOrPath id, map<string|string[]> headers = {}, *GetRubyGemDependenciesQueries queries) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rubygems/api/v1/dependencies`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"gems": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download repository metadata files
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Repository metadata file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadRpmRepositoryMetadata(IdOrPath id, string fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rpm/repodata/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download RPM package files
    #
    # + id - The ID or URL-encoded path of the project
    # + packageFileId - RPM package file id
    # + fileName - RPM package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadRpmPackageFile(IdOrPath id, int packageFileId, string fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rpm/${getEncodedUri(packageFileId)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a RPM package
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function uploadRpmPackage(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rpm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Authorize package upload from workhorse
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function authorizeRpmPackageUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/rpm/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project issue milestone events
    #
    # + id - The ID or URL-encoded path of the project
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue milestone events 
    remote isolated function listIssueMilestoneEvents(IdOrPath id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListIssueMilestoneEventsQueries queries) returns ResourceMilestoneEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_milestone_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an issue milestone event
    #
    # + id - The ID or URL-encoded path of the project
    # + eventId - The ID of a resource milestone event
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue milestone event 
    remote isolated function getIssueMilestoneEvent(IdOrPath id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceMilestoneEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(eventableId)}/resource_milestone_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project merge request milestone events
    #
    # + id - The ID or URL-encoded path of the project
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request milestone events 
    remote isolated function listMergeRequestMilestoneEvents(IdOrPath id, IdOrPath eventableId, map<string|string[]> headers = {}, *ListMergeRequestMilestoneEventsQueries queries) returns ResourceMilestoneEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(eventableId)}/resource_milestone_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a merge request milestone event
    #
    # + id - The ID or URL-encoded path of the project
    # + eventId - The ID of a resource milestone event
    # + eventableId - The ID of the eventable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request milestone event 
    remote isolated function getMergeRequestMilestoneEvent(IdOrPath id, string eventId, IdOrPath eventableId, map<string|string[]> headers = {}) returns ResourceMilestoneEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(eventableId)}/resource_milestone_events/${getEncodedUri(eventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Rotate a project access token
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a project access token 
    # + return - Success 
    remote isolated function rotateSelfProjectAccessToken(string id, RotateTokenRequest payload, map<string|string[]> headers = {}) returns ResourceAccessTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_tokens/self/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Rotate a group access token
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a group access token 
    # + return - Success 
    remote isolated function rotateSelfGroupAccessToken(string id, RotateTokenRequest payload, map<string|string[]> headers = {}) returns ResourceAccessTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_tokens/self/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project access tokens
    #
    # + id - ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project access tokens 
    remote isolated function listProjectAccessTokens(IdOrPath id, map<string|string[]> headers = {}, *ListProjectAccessTokensQueries queries) returns ResourceAccessToken[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project access token
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project access token 
    # + return - Created 
    remote isolated function createProjectAccessToken(string id, CreateProjectAccessTokenRequest payload, map<string|string[]> headers = {}) returns ResourceAccessTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve details on a project access token
    #
    # + id - ID or URL-encoded path of the project
    # + tokenId - The ID of the token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project access token 
    remote isolated function getProjectAccessToken(IdOrPath id, string tokenId, map<string|string[]> headers = {}) returns ResourceAccessToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a project access token
    #
    # + id - The project ID
    # + tokenId - The ID of the token
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeProjectAccessToken(string id, string tokenId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a project access token
    #
    # + id - The project ID
    # + tokenId - The ID of the token
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a project access token 
    # + return - Success 
    remote isolated function rotateProjectAccessToken(string id, string tokenId, RotateTokenRequest payload, map<string|string[]> headers = {}) returns ResourceAccessTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_tokens/${getEncodedUri(tokenId)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group access tokens
    #
    # + id - ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group access tokens 
    remote isolated function listGroupAccessTokens(IdOrPath id, map<string|string[]> headers = {}, *ListGroupAccessTokensQueries queries) returns ResourceAccessToken[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group access token
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group access token 
    # + return - Created 
    remote isolated function createGroupAccessToken(string id, CreateGroupAccessTokenRequest payload, map<string|string[]> headers = {}) returns ResourceAccessTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve details on a group access token
    #
    # + id - ID or URL-encoded path of the group
    # + tokenId - The ID of the token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group access token 
    remote isolated function getGroupAccessToken(IdOrPath id, string tokenId, map<string|string[]> headers = {}) returns ResourceAccessToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a group access token
    #
    # + id - The group ID
    # + tokenId - The ID of the token
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeGroupAccessToken(string id, string tokenId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a group access token
    #
    # + id - The group ID
    # + tokenId - The ID of the token
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a group access token 
    # + return - Success 
    remote isolated function rotateGroupAccessToken(string id, string tokenId, RotateTokenRequest payload, map<string|string[]> headers = {}) returns ResourceAccessTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_tokens/${getEncodedUri(tokenId)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all repository trees in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository tree 
    remote isolated function listRepositoryTree(IdOrPath id, map<string|string[]> headers = {}, *ListRepositoryTreeQueries queries) returns TreeObject[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/tree`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve raw blob content
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - The commit hash
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getRawRepositoryBlob(IdOrPath id, string sha, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/blobs/${getEncodedUri(sha)}/raw`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a blob from a repository
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - The commit hash
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getRepositoryBlob(IdOrPath id, string sha, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/blobs/${getEncodedUri(sha)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve file archive from a repository
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getRepositoryArchive(IdOrPath id, map<string|string[]> headers = {}, *GetRepositoryArchiveQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/archive`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"exclude_paths": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Compare branches, tags, or commits
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function compareRepository(IdOrPath id, map<string|string[]> headers = {}, *CompareRepositoryQueries queries) returns Compare|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/compare`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve repository health statistics
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository health statistics 
    remote isolated function getRepositoryHealthStatistics(IdOrPath id, map<string|string[]> headers = {}, *GetRepositoryHealthStatisticsQueries queries) returns RepositoryHealth|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/health`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve contributors metrics
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository contributors 
    remote isolated function listRepositoryContributors(IdOrPath id, map<string|string[]> headers = {}, *ListRepositoryContributorsQueries queries) returns Contributor[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/contributors`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a merge base
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge base 
    remote isolated function getMergeBase(IdOrPath id, map<string|string[]> headers = {}, *GetMergeBaseQueries queries) returns Commit|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/merge_base`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"refs": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get contents of multiple files in a single request
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to get contents of multiple files in a single request 
    # + return - The retrieved repository blobs batch 
    remote isolated function getRepositoryBlobsBatch(IdOrPath id, GetRepositoryBlobsBatchRequest payload, map<string|string[]> headers = {}) returns BatchBlob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/blobs/batch`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List files changed between two commits
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved changed paths 
    remote isolated function listChangedPaths(IdOrPath id, map<string|string[]> headers = {}, *ListChangedPathsQueries queries) returns ChangedPath[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/changed_paths`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve diverging commit counts between two refs
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved diverging commit counts 
    remote isolated function getDivergingCommitCounts(IdOrPath id, map<string|string[]> headers = {}, *GetDivergingCommitCountsQueries queries) returns DivergingCommitCount|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/diverging_commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get diff statistics between two commits
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository diff stats 
    remote isolated function getRepositoryDiffStats(IdOrPath id, map<string|string[]> headers = {}, *GetRepositoryDiffStatsQueries queries) returns DiffStat|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/diff_stats`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Generate changelog data
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function generateChangelog(IdOrPath id, map<string|string[]> headers = {}, *GenerateChangelogQueries queries) returns Changelog|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/changelog`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add changelog data to file
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add changelog data to file 
    # + return - Success 
    remote isolated function addChangelog(IdOrPath id, AddChangelogRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/changelog`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all remote mirrors for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved remote mirrors 
    remote isolated function listRemoteMirrors(IdOrPath id, map<string|string[]> headers = {}, *ListRemoteMirrorsQueries queries) returns RemoteMirror[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a push mirror
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a push mirror 
    # + return - Created 
    remote isolated function createRemoteMirror(IdOrPath id, CreateRemoteMirrorRequest payload, map<string|string[]> headers = {}) returns RemoteMirror|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a remote mirror for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + mirrorId - The ID of a remote mirror
    # + headers - Headers to be sent with the request 
    # + return - The retrieved remote mirror 
    remote isolated function getRemoteMirror(IdOrPath id, string mirrorId, map<string|string[]> headers = {}) returns RemoteMirror|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors/${getEncodedUri(mirrorId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a remote mirror in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + mirrorId - The ID of a remote mirror
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a remote mirror in a project 
    # + return - The updated remote mirror 
    remote isolated function updateRemoteMirror(IdOrPath id, string mirrorId, UpdateRemoteMirrorRequest payload, map<string|string[]> headers = {}) returns RemoteMirror|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors/${getEncodedUri(mirrorId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a remote mirror from a project
    #
    # + id - The ID or URL-encoded path of the project
    # + mirrorId - The ID of a remote mirror
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteRemoteMirror(IdOrPath id, string mirrorId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors/${getEncodedUri(mirrorId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Force push mirror update
    #
    # + id - The ID or URL-encoded path of the project
    # + mirrorId - The ID of a remote mirror
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function syncRemoteMirror(IdOrPath id, string mirrorId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors/${getEncodedUri(mirrorId)}/sync`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a public key for a remote mirror
    #
    # + id - The ID or URL-encoded path of the project
    # + mirrorId - The ID of a remote mirror
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getRemoteMirrorPublicKey(IdOrPath id, string mirrorId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/remote_mirrors/${getEncodedUri(mirrorId)}/public_key`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all release links
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The tag associated with the release
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved release links 
    remote isolated function listReleaseLinks(IdOrPath id, string tagName, map<string|string[]> headers = {}, *ListReleaseLinksQueries queries) returns ReleasesLink[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/assets/links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a release link
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The tag associated with the release
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a release link 
    # + return - Created 
    remote isolated function createReleaseLink(IdOrPath id, string tagName, CreateReleaseLinkRequest payload, map<string|string[]> headers = {}) returns ReleasesLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/assets/links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a release link
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The tag associated with the release
    # + linkId - The ID of the link
    # + headers - Headers to be sent with the request 
    # + return - The retrieved release link 
    remote isolated function getReleaseLink(IdOrPath id, string tagName, int linkId, map<string|string[]> headers = {}) returns ReleasesLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/assets/links/${getEncodedUri(linkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a release link
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The tag associated with the release
    # + linkId - The ID of the link
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a release link 
    # + return - The updated release link 
    remote isolated function updateReleaseLink(IdOrPath id, string tagName, int linkId, UpdateReleaseLinkRequest payload, map<string|string[]> headers = {}) returns ReleasesLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/assets/links/${getEncodedUri(linkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a release link
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The tag associated with the release
    # + linkId - The ID of the link
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteReleaseLink(IdOrPath id, string tagName, int linkId, map<string|string[]> headers = {}) returns ReleasesLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/assets/links/${getEncodedUri(linkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all releases in a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group releases 
    remote isolated function listGroupReleases(IdOrPath id, map<string|string[]> headers = {}, *ListGroupReleasesQueries queries) returns Release[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/releases`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all releases in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project releases 
    remote isolated function listProjectReleases(IdOrPath id, map<string|string[]> headers = {}, *ListProjectReleasesQueries queries) returns Release[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a release
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a release 
    # + return - Created 
    remote isolated function createRelease(IdOrPath id, CreateReleaseRequest payload, map<string|string[]> headers = {}) returns Release|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a release by tag name
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The Git tag the release is associated with
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved release 
    remote isolated function getRelease(IdOrPath id, string tagName, map<string|string[]> headers = {}, *GetReleaseQueries queries) returns Release|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a release
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The Git tag the release is associated with
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a release 
    # + return - The updated release 
    remote isolated function updateRelease(IdOrPath id, string tagName, UpdateReleaseRequest payload, map<string|string[]> headers = {}) returns Release|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a release
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The Git tag the release is associated with
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRelease(IdOrPath id, string tagName, map<string|string[]> headers = {}) returns Release|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download a project release asset file
    #
    # + id - The ID or URL-encoded path of the project
    # + tagName - The Git tag the release is associated with
    # + directAssetPath - The path to the file to download, as specified when creating the release asset
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadReleaseAsset(IdOrPath id, string tagName, string directAssetPath, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/downloads/${getEncodedUri(directAssetPath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the latest project release
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getLatestRelease(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/permalink/latest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the latest project release
    #
    # + id - The ID or URL-encoded path of the project
    # + suffixPath - The path to be suffixed to the latest release
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getLatestReleaseByPath(IdOrPath id, string suffixPath, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/permalink/latest/${getEncodedUri(suffixPath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Generate release evidence
    #
    # + tagName - The Git tag the release is associated with
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function generateReleaseEvidence(string tagName, IdOrPath id, map<string|string[]> headers = {}) returns Release|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/releases/${getEncodedUri(tagName)}/evidence`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a package file from a group
    #
    # + id - The ID or full path of the group
    # + fileIdentifier - The PyPi package file identifier
    # + sha256 - The PyPi package sha256 check sum
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupPypiPackageFile(IdOrPath id, string fileIdentifier, string sha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/pypi/files/${getEncodedUri(sha256)}/${getEncodedUri(fileIdentifier)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all packages for a group
    #
    # + id - The ID or full path of the group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listGroupPypiPackages(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/pypi/simple`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The PyPi Simple Group Package Endpoint
    #
    # + id - The ID or full path of the group
    # + packageName - The PyPi package name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupPypiPackage(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/pypi/simple/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The PyPi package download endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + fileIdentifier - The PyPi package file identifier
    # + sha256 - The PyPi package sha256 check sum
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadPypiPackageFile(IdOrPath id, string fileIdentifier, string sha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/pypi/files/${getEncodedUri(sha256)}/${getEncodedUri(fileIdentifier)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a forwarded (proxied) PyPI package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The normalized PyPI package name
    # + upstreamPath - The upstream artifact host and path, as emitted by the Simple index rewrite
    # + headers - Headers to be sent with the request 
    # + return - Found 
    remote isolated function downloadForwardedPypiPackageFile(IdOrPath id, string packageName, string upstreamPath, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/pypi/forward/${getEncodedUri(packageName)}/${getEncodedUri(upstreamPath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all packages for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listProjectPypiPackages(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/pypi/simple`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The PyPi Simple Project Package Endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The PyPi package name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectPypiPackage(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/pypi/simple/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a package
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a package 
    # + return - Created 
    remote isolated function uploadPypiPackage(IdOrPath id, UploadPypiPackageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/pypi`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Authorize the PyPi package upload from workhorse
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizePypiPackageUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/pypi/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all protected tags
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project protected tags 
    remote isolated function listProjectProtectedTags(IdOrPath id, map<string|string[]> headers = {}, *ListProjectProtectedTagsQueries queries) returns ProtectedTag[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Protect a repository tag
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to protect a repository tag 
    # + return - Created 
    remote isolated function protectProjectTag(IdOrPath id, ProtectProjectTagRequest payload, map<string|string[]> headers = {}) returns ProtectedTag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a protected tag or wildcard protected tag
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of the tag or wildcard
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project protected tag 
    remote isolated function getProjectProtectedTag(IdOrPath id, string name, map<string|string[]> headers = {}) returns ProtectedTag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_tags/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Unprotect repository tags
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of the protected tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function unprotectProjectTag(IdOrPath id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_tags/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all protected branches
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project protected branches 
    remote isolated function listProjectProtectedBranches(IdOrPath id, map<string|string[]> headers = {}, *ListProjectProtectedBranchesQueries queries) returns ProtectedBranch[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Protect repository branches
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to protect repository branches 
    # + return - Created 
    remote isolated function protectProjectBranch(IdOrPath id, ProtectBranchRequest payload, map<string|string[]> headers = {}) returns ProtectedBranch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a protected branch or wildcard protected branch
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of the branch or wildcard
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project protected branch 
    remote isolated function getProjectProtectedBranch(IdOrPath id, string name, map<string|string[]> headers = {}) returns ProtectedBranch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_branches/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Unprotect repository branches
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of the protected branch
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function unprotectProjectBranch(IdOrPath id, string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_branches/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a protected branch
    #
    # + id - The ID or URL-encoded path of the project
    # + name - The name of the branch
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a protected branch 
    # + return - The updated project protected branch 
    remote isolated function updateProjectProtectedBranch(IdOrPath id, string name, UpdateProtectedBranchRequest payload, map<string|string[]> headers = {}) returns ProtectedBranch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/protected_branches/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all personal projects for a user
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user projects 
    remote isolated function listUserProjects(string userId, map<string|string[]> headers = {}, *ListUserProjectsQueries queries) returns BasicProjectDetails[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"topic": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all projects contributions for a user
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user contributed projects 
    remote isolated function listUserContributedProjects(string userId, map<string|string[]> headers = {}, *ListUserContributedProjectsQueries queries) returns BasicProjectDetails[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/contributed_projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all projects starred by a user
    #
    # + userId - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user starred projects 
    remote isolated function listUserStarredProjects(string userId, map<string|string[]> headers = {}, *ListUserStarredProjectsQueries queries) returns BasicProjectDetails[]|error {
        string resourcePath = string `/users/${getEncodedUri(userId)}/starred_projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"topic": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all custom attributes for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project custom attributes 
    remote isolated function listProjectCustomAttributes(IdOrPath id, map<string|string[]> headers = {}) returns CustomAttribute[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/custom_attributes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a custom attribute for a project
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project custom attribute 
    remote isolated function getProjectCustomAttribute(string 'key, IdOrPath id, map<string|string[]> headers = {}) returns CustomAttribute|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates or updates a custom attribute for a project
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or updates a custom attribute for a project 
    # + return - Success 
    remote isolated function setProjectCustomAttribute(string 'key, IdOrPath id, SetCustomAttributeRequest payload, map<string|string[]> headers = {}) returns CustomAttribute|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a custom attribute for a project
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectCustomAttribute(string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Restore a project marked for deletion
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function restoreProject(IdOrPath id, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/restore`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all projects
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved projects 
    remote isolated function listProjects(map<string|string[]> headers = {}, *ListProjectsQueries queries) returns BasicProjectDetails[]|error {
        string resourcePath = string `/projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"topic": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project 
    # + return - Created 
    remote isolated function createProject(CreateProjectRequest payload, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a project for a user
    #
    # + userId - The ID of a user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project for a user 
    # + return - Created 
    remote isolated function createProjectForUser(int userId, CreateProjectForUserRequest payload, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/user/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all groups available to invite to a project
    #
    # + id - The id of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project share locations 
    remote isolated function listProjectShareLocations(int id, map<string|string[]> headers = {}, *ListProjectShareLocationsQueries queries) returns Group[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/share_locations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project 
    remote isolated function getProject(IdOrPath id, map<string|string[]> headers = {}, *GetProjectQueries queries) returns ProjectsWithAccessAndCatalogSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project 
    # + return - The updated project 
    remote isolated function updateProject(IdOrPath id, UpdateProjectRequest payload, map<string|string[]> headers = {}) returns ProjectsWithCatalogSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function deleteProject(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Create a fork of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a fork of a project 
    # + return - Created 
    remote isolated function createFork(IdOrPath id, CreateForkRequest payload, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/fork`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a fork relationship
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteForkRelationship(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/fork`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all forks of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved forks 
    remote isolated function listForks(IdOrPath id, map<string|string[]> headers = {}, *ListForksQueries queries) returns Project[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/forks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"topic": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Check pages access of this project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function checkPagesAccess(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages_access`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Archive a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function archiveProject(IdOrPath id, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/archive`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unarchive a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unarchiveProject(IdOrPath id, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/unarchive`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Star a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function starProject(IdOrPath id, map<string|string[]> headers = {}) returns Project|error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/star`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unstar a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unstarProject(IdOrPath id, map<string|string[]> headers = {}) returns Project|error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/unstar`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all users who starred a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project starrers 
    remote isolated function listProjectStarrers(IdOrPath id, map<string|string[]> headers = {}, *ListProjectStarrersQueries queries) returns UserBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/starrers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve programming language usage information
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectLanguages(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/languages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a fork relationship
    #
    # + id - The ID or URL-encoded path of the project
    # + forkedFromId - The ID of the project it was forked from
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createForkRelationship(IdOrPath id, string forkedFromId, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/fork/${getEncodedUri(forkedFromId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Share a project with a group
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to share a project with a group 
    # + return - Created 
    remote isolated function shareProjectWithGroup(IdOrPath id, ShareProjectWithGroupRequest payload, map<string|string[]> headers = {}) returns ProjectGroupLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/share`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a shared project link in a group
    #
    # + id - The ID or URL-encoded path of the project
    # + groupId - The ID of the group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function unshareProjectWithGroup(IdOrPath id, int groupId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/share/${getEncodedUri(groupId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Import members
    #
    # + id - The ID or URL-encoded path of the project
    # + projectId - The ID of the source project to import the members from
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function importProjectMembers(IdOrPath id, int projectId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/import_project_members/${getEncodedUri(projectId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all members of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project users 
    remote isolated function listProjectUsers(IdOrPath id, map<string|string[]> headers = {}, *ListProjectUsersQueries queries) returns UserBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"skip_users": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all ancestor groups
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project ancestor groups 
    remote isolated function listProjectAncestorGroups(IdOrPath id, map<string|string[]> headers = {}, *ListProjectAncestorGroupsQueries queries) returns PublicGroupDetails[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"skip_groups": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all invited groups in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project invited groups 
    remote isolated function listProjectInvitedGroups(IdOrPath id, map<string|string[]> headers = {}, *ListProjectInvitedGroupsQueries queries) returns Group[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/invited_groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"relation": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Start the housekeeping task for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to start the housekeeping task for a project 
    # + return - Created 
    remote isolated function startProjectHousekeeping(IdOrPath id, StartProjectHousekeepingRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/housekeeping`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Start a task to recalculate repository size for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function recalculateRepositorySize(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository_size`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Transfer a project to another namespace
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to transfer a project to another namespace 
    # + return - Success 
    remote isolated function transferProject(IdOrPath id, TransferProjectRequest payload, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/transfer`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all transferable namespaces for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project transfer locations 
    remote isolated function listProjectTransferLocations(IdOrPath id, map<string|string[]> headers = {}, *ListProjectTransferLocationsQueries queries) returns PublicGroupDetails[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/transfer_locations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the path to repository storage
    #
    # + id - ID of a project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project storage 
    remote isolated function getProjectStorage(string id, map<string|string[]> headers = {}) returns ProjectRepositoryStorage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/storage`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project audit events
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project audit events 
    remote isolated function listProjectAuditEvents(IdOrPath id, map<string|string[]> headers = {}, *ListProjectAuditEventsQueries queries) returns AuditEvent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/audit_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a project audit event
    #
    # + auditEventId - The ID of the audit event
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project audit event 
    remote isolated function getProjectAuditEvent(int auditEventId, IdOrPath id, map<string|string[]> headers = {}) returns AuditEvent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/audit_events/${getEncodedUri(auditEventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all templates of a particular type
    #
    # + id - The ID or URL-encoded path of the project
    # + 'type - The type (dockerfiles|gitignores|gitlab_ci_ymls|licenses|issues|merge_requests) of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project templates 
    remote isolated function listProjectTemplates(IdOrPath id, "dockerfiles"|"gitignores"|"gitlab_ci_ymls"|"licenses"|"issues"|"merge_requests" 'type, map<string|string[]> headers = {}, *ListProjectTemplatesQueries queries) returns TemplatesList[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/templates/${getEncodedUri('type)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a template of a particular type
    #
    # + id - The ID or URL-encoded path of the project
    # + 'type - The type (dockerfiles|gitignores|gitlab_ci_ymls|licenses|issues|merge_requests) of the template
    # + name - The key of the template, as obtained from the collection endpoint
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project template 
    remote isolated function getProjectTemplate(IdOrPath id, "dockerfiles"|"gitignores"|"gitlab_ci_ymls"|"licenses"|"issues"|"merge_requests" 'type, string name, map<string|string[]> headers = {}, *GetProjectTemplateQueries queries) returns License|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/templates/${getEncodedUri('type)}/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the statistics of the last 30 days
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project statistics 
    remote isolated function getProjectStatistics(IdOrPath id, map<string|string[]> headers = {}) returns ProjectDailyStatistics|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/statistics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all snippets for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project snippets 
    remote isolated function listProjectSnippets(IdOrPath id, map<string|string[]> headers = {}, *ListProjectSnippetsQueries queries) returns ProjectSnippet[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project snippet 
    # + return - Created 
    remote isolated function createProjectSnippet(IdOrPath id, CreateProjectSnippetRequest payload, map<string|string[]> headers = {}) returns ProjectSnippet|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a project snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project snippet 
    remote isolated function getProjectSnippet(IdOrPath id, int snippetId, map<string|string[]> headers = {}) returns ProjectSnippet|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project snippet 
    # + return - The updated project snippet 
    remote isolated function updateProjectSnippet(IdOrPath id, int snippetId, UpdateProjectSnippetRequest payload, map<string|string[]> headers = {}) returns ProjectSnippet|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a project snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectSnippet(IdOrPath id, int snippetId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a raw project snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved raw project snippet 
    remote isolated function getRawProjectSnippet(IdOrPath id, int snippetId, map<string|string[]> headers = {}) returns ProjectSnippet|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/raw`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve snippet repository file content
    #
    # + id - The ID or URL-encoded path of the project
    # + ref - The name of branch, tag or commit
    # + filePath - The URL-encoded path to the file, like lib%2Fclass%2Erb
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet repository file content 
    remote isolated function getSnippetRepositoryFileContent(IdOrPath id, string ref, string filePath, int snippetId, map<string|string[]> headers = {}) returns ProjectSnippet|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/files/${getEncodedUri(ref)}/${getEncodedUri(filePath)}/raw`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve user agent details for a project snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project snippet user agent 
    remote isolated function getProjectSnippetUserAgent(IdOrPath id, int snippetId, map<string|string[]> headers = {}) returns UserAgentDetail|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/user_agent_detail`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download snapshot of a Git repository
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadRepositorySnapshot(IdOrPath id, map<string|string[]> headers = {}, *DownloadRepositorySnapshotQueries queries) returns http:Response|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snapshot`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all package protection rules
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved package protection rules 
    remote isolated function listPackageProtectionRules(IdOrPath id, map<string|string[]> headers = {}) returns ProjectsPackagesProtectionRule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/protection/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a package protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a package protection rule 
    # + return - Created 
    remote isolated function createPackageProtectionRule(IdOrPath id, CreatePackageProtectionRuleRequest payload, map<string|string[]> headers = {}) returns ProjectsPackagesProtectionRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/protection/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a package protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + packageProtectionRuleId - The ID of the package protection rule
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deletePackageProtectionRule(IdOrPath id, int packageProtectionRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/protection/rules/${getEncodedUri(packageProtectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a package protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + packageProtectionRuleId - The ID of the package protection rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a package protection rule 
    # + return - The updated package protection rule 
    remote isolated function updatePackageProtectionRule(IdOrPath id, int packageProtectionRuleId, UpdatePackageProtectionRuleRequest payload, map<string|string[]> headers = {}) returns ProjectsPackagesProtectionRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/protection/rules/${getEncodedUri(packageProtectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all packages for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project packages 
    remote isolated function listProjectPackages(IdOrPath id, map<string|string[]> headers = {}, *ListProjectPackagesQueries queries) returns Package[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a project package
    #
    # + id - The ID or URL-encoded path of the project
    # + packageId - The ID of a package
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project package 
    remote isolated function getProjectPackage(IdOrPath id, int packageId, map<string|string[]> headers = {}) returns Package|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/${getEncodedUri(packageId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a project package
    #
    # + id - The ID or URL-encoded path of the project
    # + packageId - The ID of a package
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectPackage(IdOrPath id, int packageId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/${getEncodedUri(packageId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all package pipelines
    #
    # + id - The ID or URL-encoded path of the project
    # + packageId - The ID of a package
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project package pipelines 
    remote isolated function listProjectPackagePipelines(IdOrPath id, int packageId, map<string|string[]> headers = {}, *ListProjectPackagePipelinesQueries queries) returns PackagePipeline[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/${getEncodedUri(packageId)}/pipelines`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the CI/CD job token access settings for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved job token scope 
    remote isolated function getJobTokenScope(IdOrPath id, map<string|string[]> headers = {}) returns ProjectJobTokenScope|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update the CI/CD job token access settings for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the CI/CD job token access settings for a project 
    # + return - No Content 
    remote isolated function updateJobTokenScope(IdOrPath id, UpdateJobTokenScopeRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all projects in a CI/CD job token allowlist
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved job token allowlist projects 
    remote isolated function listJobTokenAllowlistProjects(IdOrPath id, map<string|string[]> headers = {}, *ListJobTokenAllowlistProjectsQueries queries) returns BasicProjectDetails[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope/allowlist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a project to a CI/CD job token allowlist
    #
    # + id - ID of user project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a project to a CI/CD job token allowlist 
    # + return - Created 
    remote isolated function addJobTokenAllowlistProject(int id, AddJobTokenAllowlistProjectRequest payload, map<string|string[]> headers = {}) returns ProjectScopeLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope/allowlist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all groups in a CI/CD job token allowlist
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved job token allowlist groups 
    remote isolated function listJobTokenAllowlistGroups(IdOrPath id, map<string|string[]> headers = {}, *ListJobTokenAllowlistGroupsQueries queries) returns BasicGroupDetails[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope/groups_allowlist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a group to a CI/CD job token allowlist
    #
    # + id - ID of user project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a group to a CI/CD job token allowlist 
    # + return - Created 
    remote isolated function addJobTokenAllowlistGroup(int id, AddJobTokenAllowlistGroupRequest payload, map<string|string[]> headers = {}) returns GroupScopeLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope/groups_allowlist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a group from a CI/CD job token allowlist
    #
    # + id - ID of user project
    # + targetGroupId - ID of the group to be removed from the allowlist
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteJobTokenAllowlistGroup(int id, int targetGroupId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope/groups_allowlist/${getEncodedUri(targetGroupId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Delete a project from a CI/CD job token allowlist
    #
    # + id - ID of user project
    # + targetProjectId - ID of the project to be removed from the allowlist
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteJobTokenAllowlistProject(int id, int targetProjectId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/job_token_scope/allowlist/${getEncodedUri(targetProjectId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Workhorse authorize the project import upload
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function authorizeProjectImportUpload(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/import/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import a project from a local archive
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import a project from a local archive 
    # + return - Created 
    remote isolated function importProjectFromArchive(ImportProjectFromArchiveRequest payload, map<string|string[]> headers = {}) returns ProjectImportStatus|error {
        string resourcePath = string `/projects/import`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the status of a project import
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project import status 
    remote isolated function getProjectImportStatus(IdOrPath id, map<string|string[]> headers = {}) returns ProjectImportStatus|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/import`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Import a project from a Git URL
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import a project from a Git URL 
    # + return - Created 
    remote isolated function importProjectFromGitUrl(IdOrPath id, ImportProjectFromGitUrlRequest payload, map<string|string[]> headers = {}) returns ProjectImportStatus|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/import/git`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import a project from a remote archive
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import a project from a remote archive 
    # + return - Created 
    remote isolated function importProjectFromRemoteArchive(ImportProjectFromRemoteArchiveRequest payload, map<string|string[]> headers = {}) returns ProjectImportStatus|error {
        string resourcePath = string `/projects/remote-import`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Authorize project relation import
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function authorizeProjectRelationImport(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/import-relation/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import project resources
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import project resources 
    # + return - Created 
    remote isolated function importProjectRelation(ImportProjectRelationRequest payload, map<string|string[]> headers = {}) returns RelationImportTracker|error {
        string resourcePath = string `/projects/import-relation`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the status of a project resource import
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project relation import status 
    remote isolated function getProjectRelationImportStatus(IdOrPath id, map<string|string[]> headers = {}) returns ProjectImportStatus|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/relation-imports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Import a project from an AWS S3 bucket
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import a project from an AWS S3 bucket 
    # + return - Created 
    remote isolated function importProjectFromS3(ImportProjectFromS3Request payload, map<string|string[]> headers = {}) returns ProjectImportStatus|error {
        string resourcePath = string `/projects/remote-import-s3`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update a URL variable
    #
    # + hookId - The ID of the hook
    # + 'key - The key of the variable
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a URL variable 
    # + return - Success 
    remote isolated function updateProjectHookUrlVariable(int hookId, string 'key, IdOrPath id, UpdateHookUrlVariableRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/url_variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a URL variable
    #
    # + hookId - The ID of the hook
    # + 'key - The key of the variable
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectHookUrlVariable(int hookId, string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/url_variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a custom header
    #
    # + hookId - The ID of the hook
    # + 'key - The name of the custom header
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a custom header 
    # + return - Success 
    remote isolated function updateProjectHookCustomHeader(int hookId, string 'key, IdOrPath id, UpdateHookCustomHeaderRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/custom_headers/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a custom header
    #
    # + hookId - The ID of the hook
    # + 'key - The name of the custom header
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectHookCustomHeader(int hookId, string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/custom_headers/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all webhooks for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project webhooks 
    remote isolated function listProjectWebhooks(IdOrPath id, map<string|string[]> headers = {}, *ListProjectWebhooksQueries queries) returns ProjectHook[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a webhook to a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a webhook to a project 
    # + return - Created 
    remote isolated function addProjectWebhook(IdOrPath id, AddProjectWebhookRequest payload, map<string|string[]> headers = {}) returns ProjectHook|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a project webhook
    #
    # + id - The ID or URL-encoded path of the project
    # + hookId - The ID of a project hook
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project webhook 
    remote isolated function getProjectWebhook(IdOrPath id, int hookId, map<string|string[]> headers = {}) returns ProjectHook|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project webhook
    #
    # + id - The ID or URL-encoded path of the project
    # + hookId - The ID of the project hook
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project webhook 
    # + return - The updated project webhook 
    remote isolated function updateProjectWebhook(IdOrPath id, int hookId, UpdateProjectWebhookRequest payload, map<string|string[]> headers = {}) returns ProjectHook|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a project webhook
    #
    # + id - The ID or URL-encoded path of the project
    # + hookId - The ID of the project hook
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectWebhook(IdOrPath id, int hookId, map<string|string[]> headers = {}) returns ProjectHook|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all events
    #
    # + id - The ID or URL-encoded path of the project
    # + hookId - The ID of the hook
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listProjectHookEvents(IdOrPath id, int hookId, map<string|string[]> headers = {}, *ListProjectHookEventsQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"status": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Trigger a test webhook
    #
    # + hookId - The ID of the hook
    # + trigger - The type of trigger hook
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function testProjectHook(int hookId, "confidential_issues_events"|"confidential_note_events"|"deployment_events"|"emoji_events"|"feature_flag_events"|"issues_events"|"job_events"|"merge_requests_events"|"milestone_events"|"note_events"|"pipeline_events"|"push_events"|"releases_events"|"resource_access_token_events"|"resource_deploy_token_events"|"tag_push_events"|"wiki_page_events"|"vulnerability_events" trigger, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/test/${getEncodedUri(trigger)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Resend a webhook event
    #
    # + hookId - The ID of the hook
    # + hookLogId - The ID of the hook log entry
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resendProjectHookEvent(int hookId, int hookLogId, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/hooks/${getEncodedUri(hookId)}/events/${getEncodedUri(hookLogId)}/resend`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the status of a project export
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project export status 
    remote isolated function getProjectExportStatus(IdOrPath id, map<string|string[]> headers = {}) returns ProjectExportStatus|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/export`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Export a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to export a project 
    # + return - Accepted 
    remote isolated function exportProject(IdOrPath id, ExportProjectRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/export`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a project export
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectExport(IdOrPath id, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/export/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Schedule a relations export for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to schedule a relations export for a project 
    # + return - Accepted 
    remote isolated function scheduleProjectRelationsExport(IdOrPath id, ScheduleRelationsExportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/export_relations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a relations export for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectRelationsExport(IdOrPath id, map<string|string[]> headers = {}, *DownloadProjectRelationsExportQueries queries) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/export_relations/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the status of an relations export for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project relations export status 
    remote isolated function getProjectRelationsExportStatus(IdOrPath id, map<string|string[]> headers = {}, *GetProjectRelationsExportStatusQueries queries) returns BulkImportsExportStatus|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/export_relations/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all visible events for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project events 
    remote isolated function listProjectEvents(IdOrPath id, map<string|string[]> headers = {}, *ListProjectEventsQueries queries) returns Event[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Debian project distributions
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved debian project distributions 
    remote isolated function listDebianProjectDistributions(IdOrPath id, map<string|string[]> headers = {}, *ListDebianProjectDistributionsQueries queries) returns PackagesDebianDistribution[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/debian_distributions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"components": {style: FORM, explode: false}, "architectures": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a Debian project distribution
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Debian project distribution 
    # + return - Created 
    remote isolated function createDebianProjectDistribution(IdOrPath id, CreateDebianDistributionRequest payload, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/debian_distributions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a Debian project distribution
    #
    # + id - The ID or URL-encoded path of the project
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + return - The retrieved debian project distribution 
    remote isolated function getDebianProjectDistribution(IdOrPath id, string codename, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/debian_distributions/${getEncodedUri(codename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a Debian project distribution
    #
    # + id - The ID or URL-encoded path of the project
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Debian project distribution 
    # + return - The updated debian project distribution 
    remote isolated function updateDebianProjectDistribution(IdOrPath id, string codename, UpdateDebianDistributionRequest payload, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/debian_distributions/${getEncodedUri(codename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a Debian project distribution
    #
    # + id - The ID or URL-encoded path of the project
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Accepted 
    remote isolated function deleteDebianProjectDistribution(IdOrPath id, string codename, map<string|string[]> headers = {}, *DeleteDebianProjectDistributionQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/debian_distributions/${getEncodedUri(codename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"components": {style: FORM, explode: false}, "architectures": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a Debian project distribution key
    #
    # + id - The ID or URL-encoded path of the project
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + return - The retrieved debian project distribution key 
    remote isolated function getDebianProjectDistributionKey(IdOrPath id, string codename, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/debian_distributions/${getEncodedUri(codename)}/key.asc`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all container registry protection tag rules
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved container tag protection rules 
    remote isolated function listContainerTagProtectionRules(IdOrPath id, map<string|string[]> headers = {}) returns ProjectsContainerRegistryProtectionTagRule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/tag/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a container registry protection tag rule
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a container registry protection tag rule 
    # + return - Created 
    remote isolated function createContainerTagProtectionRule(IdOrPath id, CreateContainerTagProtectionRuleRequest payload, map<string|string[]> headers = {}) returns ProjectsContainerRegistryProtectionTagRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/tag/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a container registry protection tag rule
    #
    # + id - The ID or URL-encoded path of the project
    # + protectionRuleId - The ID of the container protection tag rule
    # + headers - Headers to be sent with the request 
    # + return - Delete a container protection tag rule 
    remote isolated function deleteContainerTagProtectionRule(IdOrPath id, int protectionRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/tag/rules/${getEncodedUri(protectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a container registry protection tag rule
    #
    # + id - The ID or URL-encoded path of the project
    # + protectionRuleId - The ID of the container protection tag rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a container registry protection tag rule 
    # + return - The updated container tag protection rule 
    remote isolated function updateContainerTagProtectionRule(IdOrPath id, int protectionRuleId, UpdateContainerTagProtectionRuleRequest payload, map<string|string[]> headers = {}) returns ProjectsContainerRegistryProtectionTagRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/tag/rules/${getEncodedUri(protectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all container repository protection rules
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved container repo protection rules 
    remote isolated function listContainerRepoProtectionRules(IdOrPath id, map<string|string[]> headers = {}) returns ProjectsContainerRegistryProtectionRule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/repository/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a container repository protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a container repository protection rule 
    # + return - Created 
    remote isolated function createContainerRepoProtectionRule(IdOrPath id, CreateContainerRepoProtectionRuleRequest payload, map<string|string[]> headers = {}) returns ProjectsContainerRegistryProtectionRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/repository/rules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a container repository protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + protectionRuleId - The ID of the container protection rule
    # + headers - Headers to be sent with the request 
    # + return - 204 No Content 
    remote isolated function deleteContainerRepoProtectionRule(IdOrPath id, int protectionRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/repository/rules/${getEncodedUri(protectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update a container repository protection rule
    #
    # + id - The ID or URL-encoded path of the project
    # + protectionRuleId - The ID of the container protection rule
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a container repository protection rule 
    # + return - The updated container repo protection rule 
    remote isolated function updateContainerRepoProtectionRule(IdOrPath id, int protectionRuleId, UpdateContainerRepoProtectionRuleRequest payload, map<string|string[]> headers = {}) returns ProjectsContainerRegistryProtectionRule|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/protection/repository/rules/${getEncodedUri(protectionRuleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all registry repositories for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project registry repositories 
    remote isolated function listProjectRegistryRepositories(IdOrPath id, map<string|string[]> headers = {}, *ListProjectRegistryRepositoriesQueries queries) returns ContainerRegistryRepository[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/repositories`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete registry repository
    #
    # + id - The ID or URL-encoded path of the project
    # + repositoryId - The ID of the repository
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRegistryRepository(IdOrPath id, int repositoryId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/repositories/${getEncodedUri(repositoryId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all registry repository tags for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + repositoryId - The ID of the repository
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved registry repository tags 
    remote isolated function listRegistryRepositoryTags(IdOrPath id, int repositoryId, map<string|string[]> headers = {}, *ListRegistryRepositoryTagsQueries queries) returns ContainerRegistryTag[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/repositories/${getEncodedUri(repositoryId)}/tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete multiple registry repository tags
    #
    # + id - The ID or URL-encoded path of the project
    # + repositoryId - The ID of the repository
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteRegistryRepositoryTags(IdOrPath id, int repositoryId, map<string|string[]> headers = {}, *DeleteRegistryRepositoryTagsQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/repositories/${getEncodedUri(repositoryId)}/tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve details of a registry repository tag
    #
    # + id - The ID or URL-encoded path of the project
    # + repositoryId - The ID of the repository
    # + tagName - The name of the tag
    # + headers - Headers to be sent with the request 
    # + return - The retrieved registry repository tag 
    remote isolated function getRegistryRepositoryTag(IdOrPath id, int repositoryId, string tagName, map<string|string[]> headers = {}) returns ContainerRegistryTagDetails|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/repositories/${getEncodedUri(repositoryId)}/tags/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a registry repository tag
    #
    # + id - The ID or URL-encoded path of the project
    # + repositoryId - The ID of the repository
    # + tagName - The name of the tag
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRegistryRepositoryTag(IdOrPath id, int repositoryId, string tagName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/registry/repositories/${getEncodedUri(repositoryId)}/tags/${getEncodedUri(tagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all clusters in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project clusters 
    remote isolated function listProjectClusters(IdOrPath id, map<string|string[]> headers = {}, *ListProjectClustersQueries queries) returns Cluster[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/clusters`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a cluster from a project
    #
    # + id - The ID or URL-encoded path of the project
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project cluster 
    remote isolated function getProjectCluster(IdOrPath id, int clusterId, map<string|string[]> headers = {}) returns ClusterProject|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a cluster in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a cluster in a project 
    # + return - The updated project cluster 
    remote isolated function updateProjectCluster(IdOrPath id, int clusterId, UpdateProjectClusterRequest payload, map<string|string[]> headers = {}) returns ClusterProject|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete cluster from a project
    #
    # + id - The ID or URL-encoded path of the project
    # + clusterId - The Cluster ID
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectCluster(IdOrPath id, int clusterId, map<string|string[]> headers = {}) returns ClusterProject|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Add a cluster to a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a cluster to a project 
    # + return - Created 
    remote isolated function addProjectCluster(IdOrPath id, AddClusterRequest payload, map<string|string[]> headers = {}) returns ClusterProject|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/clusters/user`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a project avatar
    #
    # + id - ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectAvatar(IdOrPath id, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/avatar`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Rotate a personal access token
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a personal access token 
    # + return - Success 
    remote isolated function rotateSelfPersonalAccessToken(RotateTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/personal_access_tokens/self/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a personal access token
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved self personal access token 
    remote isolated function getSelfPersonalAccessToken(map<string|string[]> headers = {}) returns PersonalAccessToken|error {
        string resourcePath = string `/personal_access_tokens/self`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a personal access token
    #
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokeSelfPersonalAccessToken(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/personal_access_tokens/self`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all token associations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved self token associations 
    remote isolated function listSelfTokenAssociations(map<string|string[]> headers = {}, *ListSelfTokenAssociationsQueries queries) returns PersonalAccessToken[]|error {
        string resourcePath = string `/personal_access_tokens/self/associations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all personal access tokens
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved personal access tokens 
    remote isolated function listPersonalAccessTokens(map<string|string[]> headers = {}, *ListPersonalAccessTokensQueries queries) returns PersonalAccessToken[]|error {
        string resourcePath = string `/personal_access_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a personal access token
    #
    # + id - The ID of a personal access token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved personal access token 
    remote isolated function getPersonalAccessToken(int id, map<string|string[]> headers = {}) returns PersonalAccessToken|error {
        string resourcePath = string `/personal_access_tokens/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke a personal access token
    #
    # + id - The ID of a personal access token
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function revokePersonalAccessToken(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/personal_access_tokens/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Rotate a personal access token
    #
    # + id - The ID of a personal access token
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rotate a personal access token 
    # + return - Created 
    remote isolated function rotatePersonalAccessToken(int id, RotateTokenRequest payload, map<string|string[]> headers = {}) returns PersonalAccessTokenWithToken|error {
        string resourcePath = string `/personal_access_tokens/${getEncodedUri(id)}/rotate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all Pages domains
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved pages domains 
    remote isolated function listPagesDomains(map<string|string[]> headers = {}, *ListPagesDomainsQueries queries) returns PagesDomainBasic[]|error {
        string resourcePath = string `/pages/domains`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Pages domains in a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project pages domains 
    remote isolated function listProjectPagesDomains(IdOrPath id, map<string|string[]> headers = {}, *ListProjectPagesDomainsQueries queries) returns PagesDomain[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages/domains`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create Pages domain
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create Pages domain 
    # + return - Created 
    remote isolated function createPagesDomain(IdOrPath id, CreatePagesDomainRequest payload, map<string|string[]> headers = {}) returns PagesDomain|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages/domains`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a Pages domain
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + domain - The domain
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pages domain 
    remote isolated function getPagesDomain(IdOrPath id, string domain, map<string|string[]> headers = {}) returns PagesDomain|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages/domains/${getEncodedUri(domain)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update Pages domain
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + domain - The domain
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update Pages domain 
    # + return - Success 
    remote isolated function updatePagesDomain(IdOrPath id, string domain, UpdatePagesDomainRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages/domains/${getEncodedUri(domain)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete Pages domain
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + domain - The domain
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deletePagesDomain(IdOrPath id, string domain, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages/domains/${getEncodedUri(domain)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Verify Pages domain
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + domain - The domain to verify
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function verifyPagesDomain(IdOrPath id, string domain, map<string|string[]> headers = {}) returns PagesDomain|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages/domains/${getEncodedUri(domain)}/verify`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve Pages settings for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getPagesSettings(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Unpublish Pages
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function unpublishPages(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Pages settings for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update Pages settings for a project 
    # + return - Success 
    remote isolated function updatePagesSettings(IdOrPath id, UpdatePagesSettingsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all package files
    #
    # + id - ID or URL-encoded path of the project
    # + packageId - ID of a package
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project package files 
    remote isolated function listProjectPackageFiles(IdOrPath id, int packageId, map<string|string[]> headers = {}, *ListProjectPackageFilesQueries queries) returns PackageFile[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/${getEncodedUri(packageId)}/package_files`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a package file
    #
    # + id - ID or URL-encoded path of the project
    # + packageId - ID of a package
    # + packageFileId - ID of a package file
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectPackageFile(IdOrPath id, int packageId, int packageFileId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/${getEncodedUri(packageId)}/package_files/${getEncodedUri(packageFileId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download a package file
    #
    # + id - ID or URL-encoded path of the project
    # + packageId - ID of a package
    # + packageFileId - ID of a package file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectPackageFile(IdOrPath id, int packageId, int packageFileId, map<string|string[]> headers = {}, *DownloadProjectPackageFileQueries queries) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/${getEncodedUri(packageId)}/package_files/${getEncodedUri(packageFileId)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an organization
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an organization 
    # + return - Created 
    remote isolated function createOrganization(CreateOrganizationRequest payload, map<string|string[]> headers = {}) returns OrganizationsOrganization|error {
        string resourcePath = string `/organizations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Soft-delete an organization
    #
    # + id - The ID of the organization
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function deleteOrganization(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/organizations/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all offline transfer exports
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listOfflineTransferExports(map<string|string[]> headers = {}, *ListOfflineTransferExportsQueries queries) returns error? {
        string resourcePath = string `/offline_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Start a new offline transfer export
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to start a new offline transfer export 
    # + return - Created 
    remote isolated function startOfflineTransferExport(StartOfflineTransferExportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/offline_exports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get offline transfer export details
    #
    # + id - The ID of user's offline transfer export
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getOfflineTransferExport(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/offline_exports/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Start a new offline transfer import
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to start a new offline transfer import 
    # + return - Created 
    remote isolated function startOfflineTransferImport(StartOfflineTransferImportRequest payload, map<string|string[]> headers = {}) returns BulkImport|error {
        string resourcePath = string `/offline_imports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # The NuGet V3 Feed Service Index
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project nu get service index 
    remote isolated function getProjectNuGetServiceIndex(IdOrPath id, map<string|string[]> headers = {}) returns NugetServiceIndex|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/index`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Symbol File Download Endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - The symbol file name
    # + signature - The symbol file signature
    # + sameFileName - The symbol file name. Must match the 'file_name' parameter
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectNuGetSymbolFile(IdOrPath id, string fileName, string signature, string sameFileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/symbolfiles/${getEncodedUri(fileName)}/${getEncodedUri(signature)}/${getEncodedUri(sameFileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet V2 Feed Service Index
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectNuGetV2ServiceIndex(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/v2`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a NuGet v2 package file for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a NuGet v2 package file for a project 
    # + return - Created 
    remote isolated function uploadNuGetV2Package(IdOrPath id, UploadNuGetPackageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/v2`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # The NuGet V2 Feed Package $metadata endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectNuGetV2Metadata(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/v2/$metadata`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Metadata Service - Package name level
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The NuGet package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project nu get package metadata 
    remote isolated function getProjectNuGetPackageMetadata(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NugetPackagesMetadata|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/metadata/${getEncodedUri(packageName)}/index`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Metadata Service - Package name and version level
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The NuGet package name
    # + packageVersion - The NuGet package version
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project nu get package version metadata 
    remote isolated function getProjectNuGetPackageVersionMetadata(IdOrPath id, string packageName, string packageVersion, map<string|string[]> headers = {}) returns NugetPackageMetadata|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/metadata/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Search Service
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchProjectNuGetPackages(IdOrPath id, map<string|string[]> headers = {}, *SearchProjectNuGetPackagesQueries queries) returns NugetSearchResults|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/query`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Content Service - index request
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The NuGet package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved nu get package versions index 
    remote isolated function getNuGetPackageVersionsIndex(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NugetPackagesVersions|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/download/${getEncodedUri(packageName)}/index`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Content Service - content request
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The NuGet package name
    # + packageVersion - The NuGet package version
    # + packageFilename - The NuGet package filename
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadNuGetPackageContent(IdOrPath id, string packageName, string packageVersion, string packageFilename, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/download/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageFilename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a NuGet v3 package file for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a NuGet v3 package file for a project 
    # + return - Created 
    remote isolated function uploadNuGetPackage(IdOrPath id, UploadNuGetPackageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # The NuGet Package Authorize endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeNuGetPackageUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Upload a NuGet symbol package file
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a NuGet symbol package file 
    # + return - Created 
    remote isolated function uploadNuGetSymbolPackage(IdOrPath id, UploadNuGetPackageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/symbolpackage`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # The NuGet Symbol Package Authorize endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeNuGetSymbolPackageUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/symbolpackage/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # The NuGet Package Delete endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The NuGet package name
    # + packageVersion - The NuGet package version
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteNuGetPackage(IdOrPath id, string packageName, string packageVersion, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # The NuGet V2 Feed Package Authorize endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeNuGetV2PackageUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/nuget/v2/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # The NuGet V2 Feed Find Packages by ID endpoint
    #
    # + projectId - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function findNuGetV2PackagesById(IdOrPath projectId, map<string|string[]> headers = {}, *FindNuGetV2PackagesByIdQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(projectId)}/packages/nuget/v2/FindPackagesById()`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet V2 Feed Enumerate Packages endpoint
    #
    # + projectId - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listNuGetV2Packages(IdOrPath projectId, map<string|string[]> headers = {}, *ListNuGetV2PackagesQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(projectId)}/packages/nuget/v2/Packages()`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet V2 Feed Single Package Metadata endpoint
    #
    # + projectId - The ID or URL-encoded path of the project
    # + packageName - The NuGet package name
    # + packageVersion - The NuGet package version
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getNuGetV2PackageMetadata(IdOrPath projectId, string packageName, string packageVersion, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(projectId)}/packages/nuget/v2/Packages(Id='${getEncodedUri(packageName)}',Version='${getEncodedUri(packageVersion)}')`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet V3 Feed Service Index
    #
    # + id - The group ID or full group path
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group nu get service index 
    remote isolated function getGroupNuGetServiceIndex(IdOrPath id, map<string|string[]> headers = {}) returns NugetServiceIndex|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/index`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Symbol File Download Endpoint
    #
    # + id - The group ID or full group path
    # + fileName - The symbol file name
    # + signature - The symbol file signature
    # + sameFileName - The symbol file name. Must match the 'file_name' parameter
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupNuGetSymbolFile(IdOrPath id, string fileName, string signature, string sameFileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/symbolfiles/${getEncodedUri(fileName)}/${getEncodedUri(signature)}/${getEncodedUri(sameFileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet V2 Feed Service Index
    #
    # + id - The group ID or full group path
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupNuGetV2ServiceIndex(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/v2`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet V2 Feed Package $metadata endpoint
    #
    # + id - The group ID or full group path
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupNuGetV2Metadata(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/v2/$metadata`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Metadata Service - Package name level
    #
    # + id - The group ID or full group path
    # + packageName - The NuGet package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group nu get package metadata 
    remote isolated function getGroupNuGetPackageMetadata(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NugetPackagesMetadata|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/metadata/${getEncodedUri(packageName)}/index`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Metadata Service - Package name and version level
    #
    # + id - The group ID or full group path
    # + packageName - The NuGet package name
    # + packageVersion - The NuGet package version
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group nu get package version metadata 
    remote isolated function getGroupNuGetPackageVersionMetadata(IdOrPath id, string packageName, string packageVersion, map<string|string[]> headers = {}) returns NugetPackageMetadata|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/metadata/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The NuGet Search Service
    #
    # + id - The group ID or full group path
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchGroupNuGetPackages(IdOrPath id, map<string|string[]> headers = {}, *SearchGroupNuGetPackagesQueries queries) returns NugetSearchResults|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/nuget/query`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get all tags for a given NPM package
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project npm package tags 
    remote isolated function listProjectNpmPackageTags(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NpmPackageTag[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or Update the given tag for the given NPM package and version
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + tag - Package dist-tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function setProjectNpmPackageTag(IdOrPath id, string packageName, string tag, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags/${getEncodedUri(tag)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Deletes the given tag
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + tag - Package dist-tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectNpmPackageTag(IdOrPath id, string packageName, string tag, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags/${getEncodedUri(tag)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # NPM registry bulk advisory endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function auditProjectNpmBulkAdvisories(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/-/npm/v1/security/advisories/bulk`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # NPM registry quick audit endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function auditProjectNpmQuick(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/-/npm/v1/security/audits/quick`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download the NPM tarball
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadNpmTarball(IdOrPath id, string packageName, string fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/${getEncodedUri(packageName)}/-/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # NPM registry metadata endpoint
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project npm package metadata 
    remote isolated function getProjectNpmPackageMetadata(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NpmPackage|error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or deprecate an NPM package
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or deprecate an NPM package 
    # + return - Success 
    remote isolated function createOrDeprecateNpmPackage(IdOrPath id, string packageName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/npm/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Get all tags for a given NPM package
    #
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved npm package tags 
    remote isolated function listNpmPackageTags(string packageName, map<string|string[]> headers = {}) returns NpmPackageTag[]|error {
        string resourcePath = string `/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or Update the given tag for the given NPM package and version
    #
    # + packageName - Package name
    # + tag - Package dist-tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function setNpmPackageTag(string packageName, string tag, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags/${getEncodedUri(tag)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Deletes the given tag
    #
    # + packageName - Package name
    # + tag - Package dist-tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteNpmPackageTag(string packageName, string tag, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags/${getEncodedUri(tag)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # NPM registry bulk advisory endpoint
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function auditNpmBulkAdvisories(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/npm/-/npm/v1/security/advisories/bulk`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # NPM registry quick audit endpoint
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function auditNpmQuick(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/npm/-/npm/v1/security/audits/quick`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # NPM registry metadata endpoint
    #
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved npm package metadata 
    remote isolated function getNpmPackageMetadata(string packageName, map<string|string[]> headers = {}) returns NpmPackage|error? {
        string resourcePath = string `/packages/npm/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get all tags for a given NPM package
    #
    # + id - The ID or URL-encoded path of the group
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group npm package tags 
    remote isolated function listGroupNpmPackageTags(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NpmPackageTag[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or Update the given tag for the given NPM package and version
    #
    # + id - The ID or URL-encoded path of the group
    # + packageName - Package name
    # + tag - Package dist-tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function setGroupNpmPackageTag(IdOrPath id, string packageName, string tag, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags/${getEncodedUri(tag)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Deletes the given tag
    #
    # + id - The ID or URL-encoded path of the group
    # + packageName - Package name
    # + tag - Package dist-tag
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupNpmPackageTag(IdOrPath id, string packageName, string tag, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/npm/-/package/${getEncodedUri(packageName)}/dist-tags/${getEncodedUri(tag)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # NPM registry bulk advisory endpoint
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function auditGroupNpmBulkAdvisories(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/npm/-/npm/v1/security/advisories/bulk`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # NPM registry quick audit endpoint
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function auditGroupNpmQuick(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/npm/-/npm/v1/security/audits/quick`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # NPM registry metadata endpoint
    #
    # + id - The ID or URL-encoded path of the group
    # + packageName - Package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group npm package metadata 
    remote isolated function getGroupNpmPackageMetadata(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns NpmPackage|error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/npm/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve namespace details
    #
    # + id - ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + return - The retrieved namespace 
    remote isolated function getNamespace(IdOrPath id, map<string|string[]> headers = {}) returns Namespace|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a namespace 
    # + return - The updated namespace 
    remote isolated function updateNamespace(IdOrPath id, UpdateNamespaceRequest payload, map<string|string[]> headers = {}) returns Namespace|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Creates a storage limit exclusion for a Namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a storage limit exclusion for a Namespace 
    # + return - Created 
    remote isolated function createNamespaceStorageLimitExclusion(IdOrPath id, CreateNamespaceStorageLimitExclusionRequest payload, map<string|string[]> headers = {}) returns NamespacesStorageLimitExclusion|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/storage/limit_exclusion`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Removes a storage limit exclusion for a Namespace
    #
    # + id - The ID or URL-encoded path of the namespace
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function removeNamespaceStorageLimitExclusion(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/storage/limit_exclusion`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve all limit exclusions
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved namespace limit exclusions 
    remote isolated function listNamespaceLimitExclusions(map<string|string[]> headers = {}, *ListNamespaceLimitExclusionsQueries queries) returns NamespacesStorageLimitExclusion[]|error {
        string resourcePath = string `/namespaces/storage/limit_exclusions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all namespaces
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved namespaces 
    remote isolated function listNamespaces(map<string|string[]> headers = {}, *ListNamespacesQueries queries) returns Namespace[]|error {
        string resourcePath = string `/namespaces`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Verify namespace availability
    #
    # + id - Namespace’s path
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function verifyNamespaceAvailability(string id, map<string|string[]> headers = {}, *VerifyNamespaceAvailabilityQueries queries) returns NamespaceExistence|error {
        string resourcePath = string `/namespaces/${getEncodedUri(id)}/exists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Register a mobile device for push notifications
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to register a mobile device for push notifications 
    # + return - Created 
    remote isolated function registerMobileDevice(RegisterMobileDeviceRequest payload, map<string|string[]> headers = {}) returns MobilePushSubscription|error {
        string resourcePath = string `/user/push_subscriptions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unregister a mobile device from push notifications
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function unregisterMobileDevice(map<string|string[]> headers = {}, *UnregisterMobileDeviceQueries queries) returns error? {
        string resourcePath = string `/user/push_subscriptions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Workhorse authorize model package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Name of the file
    # + modelVersionId - Model version id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for Workhorse to authorize model package file upload 
    # + return - Success 
    remote isolated function authorizeMlModelPackageFile(IdOrPath id, string fileName, string modelVersionId, AuthorizePackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/ml_models/${getEncodedUri(modelVersionId)}/files/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize model package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Name of the file
    # + path - File directory path
    # + modelVersionId - Model version id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for Workhorse to authorize model package file upload 
    # + return - Success 
    remote isolated function authorizeMlModelPackageFileInPath(IdOrPath id, string fileName, string path, string modelVersionId, AuthorizePackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/ml_models/${getEncodedUri(modelVersionId)}/files/${getEncodedUri(path)}/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Download an ml_model package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Name of the file
    # + modelVersionId - Model version id
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadMlModelPackageFile(IdOrPath id, string fileName, string modelVersionId, map<string|string[]> headers = {}, *DownloadMlModelPackageFileQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/ml_models/${getEncodedUri(modelVersionId)}/files/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Workhorse upload model package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Name of the file
    # + modelVersionId - Model version id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for Workhorse to upload model package file upload 
    # + return - Created 
    remote isolated function uploadMlModelPackageFile(IdOrPath id, string fileName, string modelVersionId, UploadMlModelPackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/ml_models/${getEncodedUri(modelVersionId)}/files/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Download an ml_model package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Name of the file
    # + path - File directory path
    # + modelVersionId - Model version id
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadMlModelPackageFileInPath(IdOrPath id, string fileName, string path, string modelVersionId, map<string|string[]> headers = {}, *DownloadMlModelPackageFileInPathQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/ml_models/${getEncodedUri(modelVersionId)}/files/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Workhorse upload model package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Name of the file
    # + path - File directory path
    # + modelVersionId - Model version id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for Workhorse to upload model package file upload 
    # + return - Created 
    remote isolated function uploadMlModelPackageFileInPath(IdOrPath id, string fileName, string path, string modelVersionId, UploadMlModelPackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/ml_models/${getEncodedUri(modelVersionId)}/files/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve metadata information for this GitLab instance
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved instance metadata 
    remote isolated function getInstanceMetadata(map<string|string[]> headers = {}) returns Metadata|error {
        string resourcePath = string `/metadata`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieves version information for the GitLab instance
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved version 
    remote isolated function getVersion(map<string|string[]> headers = {}) returns Metadata|error {
        string resourcePath = string `/version`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve merge request diff versions
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request diff versions 
    remote isolated function listMergeRequestDiffVersions(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestDiffVersionsQueries queries) returns MergeRequestDiff[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/versions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a merge request diff version
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + versionId - The ID of the merge request diff version
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request diff version 
    remote isolated function getMergeRequestDiffVersion(IdOrPath id, int mergeRequestIid, int versionId, map<string|string[]> headers = {}, *GetMergeRequestDiffVersionQueries queries) returns MergeRequestDiffFull|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/versions/${getEncodedUri(versionId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve approval state for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request approvals 
    remote isolated function getMergeRequestApprovals(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeRequestApprovals|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approvals`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Change approval-related configuration
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to change approval-related configuration 
    # + return - Created 
    # 
    # # Deprecated
    @deprecated
    remote isolated function updateMergeRequestApprovals(IdOrPath id, int mergeRequestIid, UpdateMergeRequestApprovalsRequest payload, map<string|string[]> headers = {}) returns ApprovalState|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approvals`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Approve merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to approve merge request 
    # + return - Created 
    remote isolated function approveMergeRequest(IdOrPath id, int mergeRequestIid, ApproveMergeRequestRequest payload, map<string|string[]> headers = {}) returns MergeRequestApprovals|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approve`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unapprove a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function unapproveMergeRequest(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeRequestApprovals|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/unapprove`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset approvals for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function resetMergeRequestApprovals(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/reset_approvals`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve approval details for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request approval state 
    remote isolated function getMergeRequestApprovalState(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeRequestApprovalState|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/approval_state`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a CI configuration merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createCiConfigMergeRequest(IdOrPath id, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/create_ci_config`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all merge requests
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge requests 
    remote isolated function listMergeRequests(map<string|string[]> headers = {}, *ListMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "labels": {style: FORM, explode: false}, "reviewer_id": {style: FORM, explode: true}, "not[assignee_id]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "approver_ids": {style: FORM, explode: true}, "approved_by_ids": {style: FORM, explode: true}, "approved_by_usernames": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group merge requests
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group merge requests 
    remote isolated function listGroupMergeRequests(string id, map<string|string[]> headers = {}, *ListGroupMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "labels": {style: FORM, explode: false}, "reviewer_id": {style: FORM, explode: true}, "not[assignee_id]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "approver_ids": {style: FORM, explode: true}, "approved_by_ids": {style: FORM, explode: true}, "approved_by_usernames": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Set the estimated time for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge_request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set the estimated time for a merge request 
    # + return - Created 
    remote isolated function setMergeRequestEstimatedTime(IdOrPath id, int mergeRequestIid, SetEstimatedTimeRequest payload, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/time_estimate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset the estimated time for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge_request
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetMergeRequestEstimatedTime(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/reset_time_estimate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Add spent time for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge_request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add spent time for a merge request 
    # + return - Created 
    remote isolated function addMergeRequestSpentTime(IdOrPath id, int mergeRequestIid, AddSpentTimeRequest payload, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/add_spent_time`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset spent time for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge_request
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetMergeRequestSpentTime(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/reset_spent_time`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve time tracking stats for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge_request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request time tracking stats 
    remote isolated function getMergeRequestTimeTrackingStats(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/time_stats`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project merge requests
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project merge requests 
    remote isolated function listProjectMergeRequests(IdOrPath id, map<string|string[]> headers = {}, *ListProjectMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "labels": {style: FORM, explode: false}, "reviewer_id": {style: FORM, explode: true}, "not[assignee_id]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "approver_ids": {style: FORM, explode: true}, "approved_by_ids": {style: FORM, explode: true}, "approved_by_usernames": {style: FORM, explode: true}, "iids": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a merge request 
    # + return - Created 
    remote isolated function createMergeRequest(IdOrPath id, CreateMergeRequestRequest payload, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request 
    remote isolated function getMergeRequest(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *GetMergeRequestQueries queries) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a merge request 
    # + return - The updated merge request 
    remote isolated function updateMergeRequest(IdOrPath id, int mergeRequestIid, UpdateMergeRequestRequest payload, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteMergeRequest(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve merge request participants
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request participants 
    remote isolated function listMergeRequestParticipants(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns UserBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/participants`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve merge request reviewers
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request reviewers 
    remote isolated function listMergeRequestReviewers(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeRequestReviewer[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/reviewers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve merge request commits
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request commits 
    remote isolated function listMergeRequestCommits(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestCommitsQueries queries) returns Commit[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all context commits for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request context commits 
    remote isolated function listMergeRequestContextCommits(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns Commit[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/context_commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create context commits for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create context commits for a merge request 
    # + return - Created 
    remote isolated function createMergeRequestContextCommits(IdOrPath id, int mergeRequestIid, CreateMergeRequestContextCommitsRequest payload, map<string|string[]> headers = {}) returns Commit|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/context_commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete context commits from a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function deleteMergeRequestContextCommits(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *DeleteMergeRequestContextCommitsQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/context_commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"commits": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve merge request changes
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request changes 
    remote isolated function getMergeRequestChanges(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *GetMergeRequestChangesQueries queries) returns MergeRequestChanges|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/changes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge request diffs
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request diffs 
    remote isolated function listMergeRequestDiffs(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestDiffsQueries queries) returns Diff[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/diffs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve merge request raw diffs
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getMergeRequestRawDiffs(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/raw_diffs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge request pipelines
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The internal ID of the merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request pipelines 
    remote isolated function listMergeRequestPipelines(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns CiPipelineBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/pipelines`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a merge request pipeline
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a merge request pipeline 
    # + return - Created 
    remote isolated function createMergeRequestPipeline(IdOrPath id, int mergeRequestIid, CreateMergeRequestPipelineRequest payload, map<string|string[]> headers = {}) returns CiPipeline|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/pipelines`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Merge a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to merge a merge request 
    # + return - Success 
    remote isolated function mergeMergeRequest(IdOrPath id, int mergeRequestIid, MergeMergeRequestRequest payload, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/merge`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Merge to default merge ref path
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getMergeRequestMergeRef(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/merge_ref`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Cancel merge when pipeline succeeds
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function cancelMergeWhenPipelineSucceeds(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns MergeRequest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/cancel_merge_when_pipeline_succeeds`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Rebase a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to rebase a merge request 
    # + return - Accepted 
    remote isolated function rebaseMergeRequest(IdOrPath id, int mergeRequestIid, RebaseMergeRequestRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/rebase`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all issues that close on merge
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request closes issues 
    remote isolated function listMergeRequestClosesIssues(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestClosesIssuesQueries queries) returns MRNote[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/closes_issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all issues related to the merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listMergeRequestRelatedIssues(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestRelatedIssuesQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/related_issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all direct members of a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group members 
    remote isolated function listGroupMembers(string id, map<string|string[]> headers = {}, *ListGroupMembersQueries queries) returns Member[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"user_ids": {style: FORM, explode: false}, "skip_users": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a member to a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a member to a group 
    # + return - Created 
    remote isolated function addGroupMember(string id, AddMemberRequest payload, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all members of a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all group members 
    remote isolated function listAllGroupMembers(string id, map<string|string[]> headers = {}, *ListAllGroupMembersQueries queries) returns Member[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/all`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"user_ids": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a direct group member
    #
    # + id - The group ID
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group member 
    remote isolated function getGroupMember(string id, int userId, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group member
    #
    # + id - The group ID
    # + userId - The user ID of the new member
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group member 
    # + return - The updated group member 
    remote isolated function updateGroupMember(string id, int userId, UpdateMemberRequest payload, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Remove a member from a group
    #
    # + id - The group ID
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Resource deleted 
    remote isolated function removeGroupMember(string id, int userId, map<string|string[]> headers = {}, *RemoveGroupMemberQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a group member
    #
    # + id - The group ID
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group member including inherited 
    remote isolated function getGroupMemberIncludingInherited(string id, int userId, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/all/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all direct members of a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project members 
    remote isolated function listProjectMembers(string id, map<string|string[]> headers = {}, *ListProjectMembersQueries queries) returns Member[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"user_ids": {style: FORM, explode: false}, "skip_users": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a member to a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a member to a project 
    # + return - Created 
    remote isolated function addProjectMember(string id, AddMemberRequest payload, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all members of a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all project members 
    remote isolated function listAllProjectMembers(string id, map<string|string[]> headers = {}, *ListAllProjectMembersQueries queries) returns Member[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members/all`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"user_ids": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a direct project member
    #
    # + id - The project ID
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project member 
    remote isolated function getProjectMember(string id, int userId, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a project member
    #
    # + id - The project ID
    # + userId - The user ID of the new member
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a project member 
    # + return - The updated project member 
    remote isolated function updateProjectMember(string id, int userId, UpdateMemberRequest payload, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Remove a member from a project
    #
    # + id - The project ID
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Resource deleted 
    remote isolated function removeProjectMember(string id, int userId, map<string|string[]> headers = {}, *RemoveProjectMemberQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a project member
    #
    # + id - The project ID
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project member including inherited 
    remote isolated function getProjectMemberIncludingInherited(string id, int userId, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/members/all/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Set override flag for a member of a group
    #
    # + id - The ID of a group
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function setGroupMemberOverride(string id, int userId, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(userId)}/override`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove an LDAP access level override
    #
    # + id - The ID of a group
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function removeGroupMemberOverride(string id, int userId, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(userId)}/override`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Approve a group member
    #
    # + id - The ID of a group
    # + memberId - The ID of the member requiring approval
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function approveGroupMember(string id, int memberId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(memberId)}/approve`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Approve all pending group members
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function approveAllPendingGroupMembers(string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/approve_all`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all pending group members
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function listPendingGroupMembers(string id, map<string|string[]> headers = {}, *ListPendingGroupMembersQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/pending_members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all billable group members
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved billable group members 
    remote isolated function listBillableGroupMembers(string id, map<string|string[]> headers = {}, *ListBillableGroupMembersQueries queries) returns Member[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/billable_members`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update group membership state for a user
    #
    # + id - The ID of a group
    # + userId - The user ID of the user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update group membership state for a user 
    # + return - Success 
    remote isolated function updateGroupMemberState(string id, int userId, UpdateGroupMemberStateRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/members/${getEncodedUri(userId)}/state`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all memberships for a billable group member
    #
    # + id - The ID of a group
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved billable member memberships 
    remote isolated function listBillableMemberMemberships(string id, int userId, map<string|string[]> headers = {}, *ListBillableMemberMembershipsQueries queries) returns BillableMembership[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/billable_members/${getEncodedUri(userId)}/memberships`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all indirect memberships for a billable group member
    #
    # + id - The ID of a group
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved billable member indirect memberships 
    remote isolated function listBillableMemberIndirectMemberships(string id, int userId, map<string|string[]> headers = {}, *ListBillableMemberIndirectMembershipsQueries queries) returns BillableMembership[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/billable_members/${getEncodedUri(userId)}/indirect`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Remove a billable member from a group
    #
    # + id - The ID of a group
    # + userId - The user ID of the member
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function removeBillableGroupMember(string id, int userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/billable_members/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download the maven package file for the instance
    #
    # + fileName - Package file name
    # + path - Package path
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadInstanceMavenPackageFile(string fileName, string path, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/maven/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download the maven package file for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + fileName - Package file name
    # + path - Package path
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupMavenPackageFile(IdOrPath id, string fileName, string path, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/maven/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download the maven package file for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - Package file name
    # + path - Package path
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectMavenPackageFile(IdOrPath id, string fileName, string path, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/maven/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload the maven package file
    #
    # + id - The ID or URL-encoded path of the project
    # + path - Package path
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload the maven package file 
    # + return - Success 
    remote isolated function uploadMavenPackageFile(IdOrPath id, string path, string fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/maven/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the maven package file upload
    #
    # + id - The ID or URL-encoded path of the project
    # + path - Package path
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeMavenPackageFileUpload(IdOrPath id, string path, string fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/maven/${getEncodedUri(path)}/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the file upload
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeProjectFileUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all uploads
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project uploads 
    remote isolated function listProjectUploads(IdOrPath id, map<string|string[]> headers = {}, *ListProjectUploadsQueries queries) returns MarkdownUploadAdmin[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an upload
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an upload 
    # + return - Created 
    remote isolated function uploadProjectFile(IdOrPath id, UploadProjectFileRequest payload, map<string|string[]> headers = {}) returns ProjectUpload|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download an uploaded file by ID
    #
    # + id - The ID or URL-encoded path of the project
    # + uploadId - The ID of a project upload
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectUpload(IdOrPath id, int uploadId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads/${getEncodedUri(uploadId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an uploaded file by ID
    #
    # + id - The ID or URL-encoded path of the project
    # + uploadId - The ID of a project upload
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectUpload(IdOrPath id, int uploadId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads/${getEncodedUri(uploadId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download an uploaded file by secret and filename
    #
    # + id - The ID or URL-encoded path of the project
    # + secret - The 32-character secret of a project upload
    # + filename - The filename of a project upload
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadProjectUploadBySecret(IdOrPath id, string secret, string filename, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads/${getEncodedUri(secret)}/${getEncodedUri(filename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an uploaded file by secret and filename
    #
    # + id - The ID or URL-encoded path of the project
    # + secret - The 32-character secret of a project upload
    # + filename - The filename of a project upload
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectUploadBySecret(IdOrPath id, string secret, string filename, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/uploads/${getEncodedUri(secret)}/${getEncodedUri(filename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Workhorse authorize the file upload
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeGroupFileUpload(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all uploads for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group uploads 
    remote isolated function listGroupUploads(IdOrPath id, map<string|string[]> headers = {}, *ListGroupUploadsQueries queries) returns MarkdownUploadAdmin[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a file to a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a file to a group 
    # + return - Created 
    remote isolated function uploadGroupFile(IdOrPath id, UploadGroupFileRequest payload, map<string|string[]> headers = {}) returns GroupUpload|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download an uploaded file by ID
    #
    # + id - The ID or URL-encoded path of the group
    # + uploadId - The ID of a group upload
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupUpload(IdOrPath id, int uploadId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads/${getEncodedUri(uploadId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an uploaded file by ID
    #
    # + id - The ID or URL-encoded path of the group
    # + uploadId - The ID of a group upload
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupUpload(IdOrPath id, int uploadId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads/${getEncodedUri(uploadId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download an uploaded file by secret and filename
    #
    # + id - The ID or URL-encoded path of the group
    # + secret - The 32-character secret of a group upload
    # + filename - The filename of a group upload
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupUploadBySecret(IdOrPath id, string secret, string filename, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads/${getEncodedUri(secret)}/${getEncodedUri(filename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an uploaded file by secret and filename
    #
    # + id - The ID or URL-encoded path of the group
    # + secret - The 32-character secret of a group upload
    # + filename - The filename of a group upload
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupUploadBySecret(IdOrPath id, string secret, string filename, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/uploads/${getEncodedUri(secret)}/${getEncodedUri(filename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Render Markdown content
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to render Markdown content 
    # + return - Created 
    remote isolated function renderMarkdownContent(RenderMarkdownContentRequest payload, map<string|string[]> headers = {}) returns Markdown|error {
        string resourcePath = string `/markdown`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Validate existing CI/CD configuration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function validateExistingCiConfig(IdOrPath id, map<string|string[]> headers = {}, *ValidateExistingCiConfigQueries queries) returns CiLintResult|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ci/lint`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Validate a CI/CD configuration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to validate a CI/CD configuration 
    # + return - Success 
    remote isolated function validateCiConfig(IdOrPath id, ValidateCiConfigRequest payload, map<string|string[]> headers = {}) returns CiLintResult|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ci/lint`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve user by SSH key ID
    #
    # + id - The ID of an SSH key
    # + headers - Headers to be sent with the request 
    # + return - The retrieved user by ssh key id 
    remote isolated function getUserBySshKeyId(IdOrPath id, map<string|string[]> headers = {}) returns SSHKeyWithUser|error {
        string resourcePath = string `/keys/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve user by SSH key fingerprint
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user by ssh key fingerprint 
    remote isolated function getUserBySshKeyFingerprint(map<string|string[]> headers = {}, *GetUserBySshKeyFingerprintQueries queries) returns UserWithAdmin|error {
        string resourcePath = string `/keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve issues statistics for the currently authenticated user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getCurrentUserIssuesStatistics(map<string|string[]> headers = {}, *GetCurrentUserIssuesStatisticsQueries queries) returns error? {
        string resourcePath = string `/issues_statistics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "iids": {style: FORM, explode: false}, "assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "not[iids]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[iteration_id]": {style: FORM, explode: true}, "weight": {style: FORM, explode: true}, "epic_id": {style: FORM, explode: true}, "iteration_id": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all issues for the currently authenticated user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved current user issues 
    remote isolated function listCurrentUserIssues(map<string|string[]> headers = {}, *ListCurrentUserIssuesQueries queries) returns Issue[]|error {
        string resourcePath = string `/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "iids": {style: FORM, explode: false}, "assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "not[iids]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[iteration_id]": {style: FORM, explode: true}, "weight": {style: FORM, explode: true}, "epic_id": {style: FORM, explode: true}, "iteration_id": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve an issue
    #
    # + id - The ID of the Issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue 
    remote isolated function getIssue(string id, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/issues/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all issues for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group issues 
    remote isolated function listGroupIssues(string id, map<string|string[]> headers = {}, *ListGroupIssuesQueries queries) returns Issue[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "iids": {style: FORM, explode: false}, "assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "not[iids]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[iteration_id]": {style: FORM, explode: true}, "weight": {style: FORM, explode: true}, "epic_id": {style: FORM, explode: true}, "iteration_id": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve issues statistics for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getGroupIssuesStatistics(string id, map<string|string[]> headers = {}, *GetGroupIssuesStatisticsQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/issues_statistics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "iids": {style: FORM, explode: false}, "assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "not[iids]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[iteration_id]": {style: FORM, explode: true}, "weight": {style: FORM, explode: true}, "epic_id": {style: FORM, explode: true}, "iteration_id": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Set the estimated time for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of the issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to set the estimated time for an issue 
    # + return - Created 
    remote isolated function setIssueEstimatedTime(IdOrPath id, int issueIid, SetEstimatedTimeRequest payload, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/time_estimate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset the estimated time for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of the issue
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetIssueEstimatedTime(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/reset_time_estimate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Add spent time for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of the issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add spent time for an issue 
    # + return - Created 
    remote isolated function addIssueSpentTime(IdOrPath id, int issueIid, AddSpentTimeRequest payload, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/add_spent_time`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset spent time for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of the issue
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetIssueSpentTime(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/reset_spent_time`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve time tracking stats for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of the issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue time tracking stats 
    remote isolated function getIssueTimeTrackingStats(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns IssuableTimeStats|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/time_stats`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project issues
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project issues 
    remote isolated function listProjectIssues(IdOrPath id, map<string|string[]> headers = {}, *ListProjectIssuesQueries queries) returns Issue[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "iids": {style: FORM, explode: false}, "assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "not[iids]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[iteration_id]": {style: FORM, explode: true}, "weight": {style: FORM, explode: true}, "epic_id": {style: FORM, explode: true}, "iteration_id": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an issue 
    # + return - Created 
    remote isolated function createIssue(IdOrPath id, CreateIssueRequest payload, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve issues statistics for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getProjectIssuesStatistics(IdOrPath id, map<string|string[]> headers = {}, *GetProjectIssuesStatisticsQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues_statistics`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"labels": {style: FORM, explode: false}, "iids": {style: FORM, explode: false}, "assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}, "not[iids]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[iteration_id]": {style: FORM, explode: true}, "weight": {style: FORM, explode: true}, "epic_id": {style: FORM, explode: true}, "iteration_id": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a project issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project issue 
    remote isolated function getProjectIssue(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an issue 
    # + return - The updated issue 
    remote isolated function updateIssue(IdOrPath id, int issueIid, UpdateIssueRequest payload, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteIssue(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the order of an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the order of an issue 
    # + return - Success 
    remote isolated function reorderIssue(IdOrPath id, int issueIid, ReorderIssueRequest payload, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/reorder`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Move an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to move an issue 
    # + return - Created 
    remote isolated function moveIssue(IdOrPath id, int issueIid, MoveIssueRequest payload, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/move`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Clone an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to clone an issue 
    # + return - Created 
    remote isolated function cloneIssue(IdOrPath id, int issueIid, CloneIssueRequest payload, map<string|string[]> headers = {}) returns Issue|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/clone`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all merge requests related to an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue related merge requests 
    remote isolated function listIssueRelatedMergeRequests(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns MergeRequestBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/related_merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge requests that close an issue on merge
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue closed by merge requests 
    remote isolated function listIssueClosedByMergeRequests(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns MergeRequestBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/closed_by`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all participants in an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue participants 
    remote isolated function listIssueParticipants(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns UserBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/participants`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve user agent details for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of a project issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue user agent 
    remote isolated function getIssueUserAgent(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns UserAgentDetail|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/user_agent_detail`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Authorize metric image upload
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function authorizeIncidentMetricImageUpload(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/metric_images/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all metric images for an incident
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved incident metric images 
    remote isolated function listIncidentMetricImages(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns IssuableMetricImage[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/metric_images`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a metric image for an incident
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a metric image for an incident 
    # + return - Created 
    remote isolated function uploadIncidentMetricImage(IdOrPath id, int issueIid, UploadIncidentMetricImageRequest payload, map<string|string[]> headers = {}) returns IssuableMetricImage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/metric_images`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update a metric image for an incident
    #
    # + id - The ID or URL-encoded path of the project
    # + metricImageId - The ID of metric image
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a metric image for an incident 
    # + return - The updated incident metric image 
    remote isolated function updateIncidentMetricImage(IdOrPath id, int metricImageId, int issueIid, UpdateIncidentMetricImageRequest payload, map<string|string[]> headers = {}) returns IssuableMetricImage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/metric_images/${getEncodedUri(metricImageId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a metric image from an incident
    #
    # + id - The ID or URL-encoded path of the project
    # + metricImageId - The ID of metric image
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteIncidentMetricImage(IdOrPath id, int metricImageId, int issueIid, map<string|string[]> headers = {}) returns IssuableMetricImage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/metric_images/${getEncodedUri(metricImageId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all issue links
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + issueIid - The internal ID of a project’s issue
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue links 
    remote isolated function listIssueLinks(IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns RelatedIssue[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an issue link
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + issueIid - The internal ID of a project’s issue
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an issue link 
    # + return - Created 
    remote isolated function createIssueLink(IdOrPath id, int issueIid, CreateIssueLinkRequest payload, map<string|string[]> headers = {}) returns IssueLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/links`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an issue link
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + issueIid - The internal ID of a project’s issue
    # + issueLinkId - ID of an issue relationship
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue link 
    remote isolated function getIssueLink(IdOrPath id, int issueIid, IdOrPath issueLinkId, map<string|string[]> headers = {}) returns IssueLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/links/${getEncodedUri(issueLinkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an issue link
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + issueIid - The internal ID of a project’s issue
    # + issueLinkId - The ID of an issue relationship
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteIssueLink(IdOrPath id, int issueIid, IdOrPath issueLinkId, map<string|string[]> headers = {}) returns IssueLink|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/links/${getEncodedUri(issueLinkId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all pending invitations for a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group pending invitations 
    remote isolated function listGroupPendingInvitations(string id, map<string|string[]> headers = {}, *ListGroupPendingInvitationsQueries queries) returns Invitation[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/invitations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a member to a group
    #
    # + id - The group ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a member to a group 
    # + return - Created 
    remote isolated function inviteGroupMember(string id, InviteMemberRequest payload, map<string|string[]> headers = {}) returns Invitation|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/invitations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update an invitation to a group
    #
    # + id - The group ID
    # + email - The email address of the invitation
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an invitation to a group 
    # + return - The updated group invitation 
    remote isolated function updateGroupInvitation(string id, string email, UpdateInvitationRequest payload, map<string|string[]> headers = {}) returns Invitation|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/invitations/${getEncodedUri(email)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an invitation to a group
    #
    # + id - The group ID
    # + email - The email address of the invitation
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupInvitation(string id, string email, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/invitations/${getEncodedUri(email)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all pending invitations for a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project pending invitations 
    remote isolated function listProjectPendingInvitations(string id, map<string|string[]> headers = {}, *ListProjectPendingInvitationsQueries queries) returns Invitation[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/invitations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a member to a project
    #
    # + id - The project ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a member to a project 
    # + return - Created 
    remote isolated function inviteProjectMember(string id, InviteMemberRequest payload, map<string|string[]> headers = {}) returns Invitation|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/invitations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update an invitation to a project
    #
    # + id - The project ID
    # + email - The email address of the invitation
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an invitation to a project 
    # + return - The updated project invitation 
    remote isolated function updateProjectInvitation(string id, string email, UpdateInvitationRequest payload, map<string|string[]> headers = {}) returns Invitation|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/invitations/${getEncodedUri(email)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an invitation to a project
    #
    # + id - The project ID
    # + email - The email address of the invitation
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteProjectInvitation(string id, string email, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/invitations/${getEncodedUri(email)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List GitLab for Jira (Forge) namespace subscriptions
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved jira forge subscriptions 
    remote isolated function listJiraForgeSubscriptions(map<string|string[]> headers = {}) returns JiraConnectSubscription[]|error {
        string resourcePath = string `/integrations/jira_forge/subscriptions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a GitLab for Jira (Forge) namespace subscription
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a GitLab for Jira (Forge) namespace subscription 
    # + return - Created 
    remote isolated function createJiraForgeSubscription(CreateJiraForgeSubscriptionRequest payload, map<string|string[]> headers = {}) returns BasicSuccess|error {
        string resourcePath = string `/integrations/jira_forge/subscriptions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a GitLab for Jira (Forge) namespace subscription
    #
    # + id - ID of the subscription to delete
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteJiraForgeSubscription(int id, map<string|string[]> headers = {}) returns BasicSuccess|error {
        string resourcePath = string `/integrations/jira_forge/subscriptions/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update the GitLab for Jira (Forge) installation instance URL
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update the GitLab for Jira (Forge) installation instance URL 
    # + return - The updated jira forge installation 
    remote isolated function updateJiraForgeInstallation(UpdateJiraForgeInstallationRequest payload, map<string|string[]> headers = {}) returns BasicSuccess|error {
        string resourcePath = string `/integrations/jira_forge/installation`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Register the GitLab for Jira (Forge) system token for direct dev-info sync
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function registerJiraForgeToken(map<string|string[]> headers = {}) returns BasicSuccess|error {
        string resourcePath = string `/integrations/jira_forge/installation/forge_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Subscribe a namespace to a JiraConnectInstallation
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to subscribe a namespace to a JiraConnectInstallation 
    # + return - Created 
    remote isolated function subscribeJiraConnectNamespace(SubscribeJiraConnectNamespaceRequest payload, map<string|string[]> headers = {}) returns BasicSuccess|error {
        string resourcePath = string `/integrations/jira_connect/subscriptions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get Slack interactive component options
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function getSlackInteractiveComponentOptions(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/integrations/slack/options`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Process Slack interaction events
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function processSlackInteractionEvents(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/integrations/slack/interactions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Receive Slack events
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to receive Slack events 
    # + return - Successfully processed event 
    remote isolated function receiveSlackEvents(ReceiveSlackEventsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/integrations/slack/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all active integrations
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project services 
    remote isolated function listProjectServices(IdOrPath id, map<string|string[]> headers = {}) returns IntegrationBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update the Apple App Store integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Apple App Store integration 
    # + return - Success 
    remote isolated function setProjectAppleAppStoreService(IdOrPath id, AppleAppStoreIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/apple-app-store`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Asana integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Asana integration 
    # + return - Success 
    remote isolated function setProjectAsanaService(IdOrPath id, AsanaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/asana`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Assembla integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Assembla integration 
    # + return - Success 
    remote isolated function setProjectAssemblaService(IdOrPath id, AssemblaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/assembla`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Bamboo integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Bamboo integration 
    # + return - Success 
    remote isolated function setProjectBambooService(IdOrPath id, BambooIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/bamboo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Bugzilla integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Bugzilla integration 
    # + return - Success 
    remote isolated function setProjectBugzillaService(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/bugzilla`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Buildkite integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Buildkite integration 
    # + return - Success 
    remote isolated function setProjectBuildkiteService(IdOrPath id, BuildkiteIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/buildkite`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Campfire integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Campfire integration 
    # + return - Success 
    remote isolated function setProjectCampfireService(IdOrPath id, CampfireIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/campfire`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Confluence integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Confluence integration 
    # + return - Success 
    remote isolated function setProjectConfluenceService(IdOrPath id, ConfluenceIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/confluence`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Custom Issue Tracker integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Custom Issue Tracker integration 
    # + return - Success 
    remote isolated function setProjectCustomTrackerService(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/custom-issue-tracker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Datadog integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Datadog integration 
    # + return - Success 
    remote isolated function setProjectDatadogService(IdOrPath id, DatadogIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/datadog`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Diffblue Cover integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Diffblue Cover integration 
    # + return - Success 
    remote isolated function setProjectDiffblueCoverService(IdOrPath id, DiffblueCoverIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/diffblue-cover`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Discord integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Discord integration 
    # + return - Success 
    remote isolated function setProjectDiscordService(IdOrPath id, DiscordIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/discord`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Drone Ci integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Drone Ci integration 
    # + return - Success 
    remote isolated function setProjectDroneCiService(IdOrPath id, DroneCiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/drone-ci`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Emails On Push integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Emails On Push integration 
    # + return - Success 
    remote isolated function setProjectEmailsOnPushService(IdOrPath id, EmailsOnPushIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/emails-on-push`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the External Wiki integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the External Wiki integration 
    # + return - Success 
    remote isolated function setProjectExternalWikiService(IdOrPath id, ExternalWikiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/external-wiki`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Gitlab Slack Application integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Gitlab Slack Application integration 
    # + return - Success 
    remote isolated function setProjectSlackAppService(IdOrPath id, SlackAppIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/gitlab-slack-application`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Play integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Play integration 
    # + return - Success 
    remote isolated function setProjectGooglePlayService(IdOrPath id, GooglePlayIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/google-play`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Hangouts Chat integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Hangouts Chat integration 
    # + return - Success 
    remote isolated function setProjectHangoutsChatService(IdOrPath id, HangoutsChatIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/hangouts-chat`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Harbor integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Harbor integration 
    # + return - Success 
    remote isolated function setProjectHarborService(IdOrPath id, HarborIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/harbor`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Irker integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Irker integration 
    # + return - Success 
    remote isolated function setProjectIrkerService(IdOrPath id, IrkerIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/irker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jenkins integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jenkins integration 
    # + return - Success 
    remote isolated function setProjectJenkinsService(IdOrPath id, JenkinsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/jenkins`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jira integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jira integration 
    # + return - Success 
    remote isolated function setProjectJiraService(IdOrPath id, JiraIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/jira`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jira Cloud App integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jira Cloud App integration 
    # + return - Success 
    remote isolated function setProjectJiraCloudAppService(IdOrPath id, JiraCloudAppIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/jira-cloud-app`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Linear integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Linear integration 
    # + return - Success 
    remote isolated function setProjectLinearService(IdOrPath id, LinearIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/linear`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Matrix integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Matrix integration 
    # + return - Success 
    remote isolated function setProjectMatrixService(IdOrPath id, MatrixIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/matrix`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mattermost Slash Commands integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mattermost Slash Commands integration 
    # + return - Success 
    remote isolated function setProjectMattermostSlashService(IdOrPath id, MattermostSlashIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/mattermost-slash-commands`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Packagist integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Packagist integration 
    # + return - Success 
    remote isolated function setProjectPackagistService(IdOrPath id, PackagistIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/packagist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Phorge integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Phorge integration 
    # + return - Success 
    remote isolated function setProjectPhorgeService(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/phorge`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pipelines Email integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pipelines Email integration 
    # + return - Success 
    remote isolated function setProjectPipelinesEmailService(IdOrPath id, PipelinesEmailIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/pipelines-email`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pivotaltracker integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pivotaltracker integration 
    # + return - Success 
    remote isolated function setProjectPivotaltrackerService(IdOrPath id, PivotaltrackerIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/pivotaltracker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pumble integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pumble integration 
    # + return - Success 
    remote isolated function setProjectPumbleService(IdOrPath id, PumbleIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/pumble`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pushover integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pushover integration 
    # + return - Success 
    remote isolated function setProjectPushoverService(IdOrPath id, PushoverIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/pushover`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Redmine integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Redmine integration 
    # + return - Success 
    remote isolated function setProjectRedmineService(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/redmine`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Ewm integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Ewm integration 
    # + return - Success 
    remote isolated function setProjectEwmService(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/ewm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Youtrack integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Youtrack integration 
    # + return - Success 
    remote isolated function setProjectYoutrackService(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/youtrack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Clickup integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Clickup integration 
    # + return - Success 
    remote isolated function setProjectClickupService(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/clickup`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Slack integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Slack integration 
    # + return - Success 
    remote isolated function setProjectSlackService(IdOrPath id, SlackIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/slack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Microsoft Teams integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Microsoft Teams integration 
    # + return - Success 
    remote isolated function setProjectMicrosoftTeamsService(IdOrPath id, MicrosoftTeamsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/microsoft-teams`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mattermost integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mattermost integration 
    # + return - Success 
    remote isolated function setProjectMattermostService(IdOrPath id, MattermostIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/mattermost`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Teamcity integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Teamcity integration 
    # + return - Success 
    remote isolated function setProjectTeamcityService(IdOrPath id, TeamcityIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/teamcity`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Telegram integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Telegram integration 
    # + return - Success 
    remote isolated function setProjectTelegramService(IdOrPath id, TelegramIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/telegram`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Unify Circuit integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Unify Circuit integration 
    # + return - Success 
    remote isolated function setProjectUnifyCircuitService(IdOrPath id, UnifyCircuitIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/unify-circuit`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Webex Teams integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Webex Teams integration 
    # + return - Success 
    remote isolated function setProjectWebexTeamsService(IdOrPath id, WebexTeamsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/webex-teams`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Zentao integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Zentao integration 
    # + return - Success 
    remote isolated function setProjectZentaoService(IdOrPath id, ZentaoIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/zentao`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Squash Tm integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Squash Tm integration 
    # + return - Success 
    remote isolated function setProjectSquashTmService(IdOrPath id, SquashTmIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/squash-tm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Github integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Github integration 
    # + return - Success 
    remote isolated function setProjectGithubService(IdOrPath id, GithubIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/github`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Git Guardian integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Git Guardian integration 
    # + return - Success 
    remote isolated function setProjectGitGuardianService(IdOrPath id, GitGuardianIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/git-guardian`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Cloud Platform Artifact Registry integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Cloud Platform Artifact Registry integration 
    # + return - Success 
    remote isolated function setProjectGcpArtifactRegistryService(IdOrPath id, GcpArtifactRegistryIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/google-cloud-platform-artifact-registry`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Cloud Platform Workload Identity Federation integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Cloud Workload Identity Federation integration 
    # + return - Success 
    remote isolated function setProjectGcpWorkloadIdentityService(IdOrPath id, GcpWorkloadIdentityIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/google-cloud-platform-workload-identity-federation`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mock Ci integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mock Ci integration 
    # + return - Success 
    remote isolated function setProjectMockCiService(IdOrPath id, MockCiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/mock-ci`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mock Monitoring integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mock Monitoring integration 
    # + return - Success 
    remote isolated function setProjectMockMonitoringService(IdOrPath id, MockMonitoringIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/mock-monitoring`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve integration settings
    #
    # + slug - The name of the integration
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project service 
    remote isolated function getProjectService("apple-app-store"|"asana"|"assembla"|"bamboo"|"bugzilla"|"buildkite"|"campfire"|"confluence"|"custom-issue-tracker"|"datadog"|"diffblue-cover"|"discord"|"drone-ci"|"emails-on-push"|"external-wiki"|"gitlab-slack-application"|"google-play"|"hangouts-chat"|"harbor"|"irker"|"jenkins"|"jira"|"jira-cloud-app"|"linear"|"matrix"|"mattermost-slash-commands"|"packagist"|"phorge"|"pipelines-email"|"pivotaltracker"|"pumble"|"pushover"|"redmine"|"ewm"|"youtrack"|"clickup"|"slack"|"microsoft-teams"|"mattermost"|"teamcity"|"telegram"|"unify-circuit"|"webex-teams"|"zentao"|"squash-tm"|"github"|"git-guardian"|"google-cloud-platform-artifact-registry"|"google-cloud-platform-workload-identity-federation"|"mock-ci"|"mock-monitoring" slug, IdOrPath id, map<string|string[]> headers = {}) returns Integration|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Disable an integration
    #
    # + slug - The name of the integration
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function disableProjectService("apple-app-store"|"asana"|"assembla"|"bamboo"|"bugzilla"|"buildkite"|"campfire"|"confluence"|"custom-issue-tracker"|"datadog"|"diffblue-cover"|"discord"|"drone-ci"|"emails-on-push"|"external-wiki"|"gitlab-slack-application"|"google-play"|"hangouts-chat"|"harbor"|"irker"|"jenkins"|"jira"|"jira-cloud-app"|"linear"|"matrix"|"mattermost-slash-commands"|"packagist"|"phorge"|"pipelines-email"|"pivotaltracker"|"pumble"|"pushover"|"redmine"|"ewm"|"youtrack"|"clickup"|"slack"|"microsoft-teams"|"mattermost"|"teamcity"|"telegram"|"unify-circuit"|"webex-teams"|"zentao"|"squash-tm"|"github"|"git-guardian"|"google-cloud-platform-artifact-registry"|"google-cloud-platform-workload-identity-federation"|"mock-ci"|"mock-monitoring" slug, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Trigger a slash command for mattermost-slash-commands
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to trigger a slash command for mattermost-slash-commands 
    # + return - Created 
    remote isolated function triggerMattermostServiceCommand(IdOrPath id, MattermostSlashCommandRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/services/mattermost_slash_commands/trigger`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all active integrations
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project integrations 
    remote isolated function listProjectIntegrations(IdOrPath id, map<string|string[]> headers = {}) returns IntegrationBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update the Apple App Store integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Apple App Store integration 
    # + return - Success 
    remote isolated function setProjectAppleAppStoreIntegration(IdOrPath id, AppleAppStoreIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/apple-app-store`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Asana integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Asana integration 
    # + return - Success 
    remote isolated function setProjectAsanaIntegration(IdOrPath id, AsanaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/asana`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Assembla integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Assembla integration 
    # + return - Success 
    remote isolated function setProjectAssemblaIntegration(IdOrPath id, AssemblaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/assembla`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Bamboo integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Bamboo integration 
    # + return - Success 
    remote isolated function setProjectBambooIntegration(IdOrPath id, BambooIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/bamboo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Bugzilla integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Bugzilla integration 
    # + return - Success 
    remote isolated function setProjectBugzillaIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/bugzilla`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Buildkite integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Buildkite integration 
    # + return - Success 
    remote isolated function setProjectBuildkiteIntegration(IdOrPath id, BuildkiteIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/buildkite`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Campfire integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Campfire integration 
    # + return - Success 
    remote isolated function setProjectCampfireIntegration(IdOrPath id, CampfireIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/campfire`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Confluence integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Confluence integration 
    # + return - Success 
    remote isolated function setProjectConfluenceIntegration(IdOrPath id, ConfluenceIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/confluence`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Custom Issue Tracker integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Custom Issue Tracker integration 
    # + return - Success 
    remote isolated function setProjectCustomTrackerIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/custom-issue-tracker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Datadog integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Datadog integration 
    # + return - Success 
    remote isolated function setProjectDatadogIntegration(IdOrPath id, DatadogIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/datadog`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Diffblue Cover integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Diffblue Cover integration 
    # + return - Success 
    remote isolated function setProjectDiffblueCoverIntegration(IdOrPath id, DiffblueCoverIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/diffblue-cover`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Discord integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Discord integration 
    # + return - Success 
    remote isolated function setProjectDiscordIntegration(IdOrPath id, DiscordIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/discord`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Drone Ci integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Drone Ci integration 
    # + return - Success 
    remote isolated function setProjectDroneCiIntegration(IdOrPath id, DroneCiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/drone-ci`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Emails On Push integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Emails On Push integration 
    # + return - Success 
    remote isolated function setProjectEmailsOnPushIntegration(IdOrPath id, EmailsOnPushIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/emails-on-push`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the External Wiki integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the External Wiki integration 
    # + return - Success 
    remote isolated function setProjectExternalWikiIntegration(IdOrPath id, ExternalWikiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/external-wiki`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Gitlab Slack Application integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Gitlab Slack Application integration 
    # + return - Success 
    remote isolated function setProjectSlackAppIntegration(IdOrPath id, SlackAppIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/gitlab-slack-application`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Play integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Play integration 
    # + return - Success 
    remote isolated function setProjectGooglePlayIntegration(IdOrPath id, GooglePlayIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/google-play`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Hangouts Chat integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Hangouts Chat integration 
    # + return - Success 
    remote isolated function setProjectHangoutsChatIntegration(IdOrPath id, HangoutsChatIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/hangouts-chat`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Harbor integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Harbor integration 
    # + return - Success 
    remote isolated function setProjectHarborIntegration(IdOrPath id, HarborIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/harbor`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Irker integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Irker integration 
    # + return - Success 
    remote isolated function setProjectIrkerIntegration(IdOrPath id, IrkerIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/irker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jenkins integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jenkins integration 
    # + return - Success 
    remote isolated function setProjectJenkinsIntegration(IdOrPath id, JenkinsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/jenkins`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jira integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jira integration 
    # + return - Success 
    remote isolated function setProjectJiraIntegration(IdOrPath id, JiraIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/jira`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jira Cloud App integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jira Cloud App integration 
    # + return - Success 
    remote isolated function setProjectJiraCloudAppIntegration(IdOrPath id, JiraCloudAppIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/jira-cloud-app`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Linear integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Linear integration 
    # + return - Success 
    remote isolated function setProjectLinearIntegration(IdOrPath id, LinearIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/linear`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Matrix integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Matrix integration 
    # + return - Success 
    remote isolated function setProjectMatrixIntegration(IdOrPath id, MatrixIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/matrix`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mattermost Slash Commands integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mattermost Slash Commands integration 
    # + return - Success 
    remote isolated function setProjectMattermostSlashIntegration(IdOrPath id, MattermostSlashIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/mattermost-slash-commands`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Packagist integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Packagist integration 
    # + return - Success 
    remote isolated function setProjectPackagistIntegration(IdOrPath id, PackagistIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/packagist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Phorge integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Phorge integration 
    # + return - Success 
    remote isolated function setProjectPhorgeIntegration(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/phorge`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pipelines Email integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pipelines Email integration 
    # + return - Success 
    remote isolated function setProjectPipelinesEmailIntegration(IdOrPath id, PipelinesEmailIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/pipelines-email`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pivotaltracker integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pivotaltracker integration 
    # + return - Success 
    remote isolated function setProjectPivotaltrackerIntegration(IdOrPath id, PivotaltrackerIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/pivotaltracker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pumble integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pumble integration 
    # + return - Success 
    remote isolated function setProjectPumbleIntegration(IdOrPath id, PumbleIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/pumble`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pushover integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pushover integration 
    # + return - Success 
    remote isolated function setProjectPushoverIntegration(IdOrPath id, PushoverIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/pushover`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Redmine integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Redmine integration 
    # + return - Success 
    remote isolated function setProjectRedmineIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/redmine`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Ewm integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Ewm integration 
    # + return - Success 
    remote isolated function setProjectEwmIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/ewm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Youtrack integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Youtrack integration 
    # + return - Success 
    remote isolated function setProjectYoutrackIntegration(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/youtrack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Clickup integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Clickup integration 
    # + return - Success 
    remote isolated function setProjectClickupIntegration(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/clickup`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Slack integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Slack integration 
    # + return - Success 
    remote isolated function setProjectSlackIntegration(IdOrPath id, SlackIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/slack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Microsoft Teams integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Microsoft Teams integration 
    # + return - Success 
    remote isolated function setProjectMicrosoftTeamsIntegration(IdOrPath id, MicrosoftTeamsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/microsoft-teams`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mattermost integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mattermost integration 
    # + return - Success 
    remote isolated function setProjectMattermostIntegration(IdOrPath id, MattermostIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/mattermost`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Teamcity integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Teamcity integration 
    # + return - Success 
    remote isolated function setProjectTeamcityIntegration(IdOrPath id, TeamcityIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/teamcity`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Telegram integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Telegram integration 
    # + return - Success 
    remote isolated function setProjectTelegramIntegration(IdOrPath id, TelegramIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/telegram`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Unify Circuit integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Unify Circuit integration 
    # + return - Success 
    remote isolated function setProjectUnifyCircuitIntegration(IdOrPath id, UnifyCircuitIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/unify-circuit`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Webex Teams integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Webex Teams integration 
    # + return - Success 
    remote isolated function setProjectWebexTeamsIntegration(IdOrPath id, WebexTeamsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/webex-teams`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Zentao integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Zentao integration 
    # + return - Success 
    remote isolated function setProjectZentaoIntegration(IdOrPath id, ZentaoIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/zentao`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Squash Tm integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Squash Tm integration 
    # + return - Success 
    remote isolated function setProjectSquashTmIntegration(IdOrPath id, SquashTmIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/squash-tm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Github integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Github integration 
    # + return - Success 
    remote isolated function setProjectGithubIntegration(IdOrPath id, GithubIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/github`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Git Guardian integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Git Guardian integration 
    # + return - Success 
    remote isolated function setProjectGitGuardianIntegration(IdOrPath id, GitGuardianIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/git-guardian`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Cloud Platform Artifact Registry integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Cloud Platform Artifact Registry integration 
    # + return - Success 
    remote isolated function setProjectGcpArtifactRegistry(IdOrPath id, GcpArtifactRegistryIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/google-cloud-platform-artifact-registry`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Cloud Platform Workload Identity Federation integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Cloud Workload Identity Federation integration 
    # + return - Success 
    remote isolated function setProjectGcpWorkloadIdentity(IdOrPath id, GcpWorkloadIdentityIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/google-cloud-platform-workload-identity-federation`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mock Ci integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mock Ci integration 
    # + return - Success 
    remote isolated function setProjectMockCiIntegration(IdOrPath id, MockCiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/mock-ci`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mock Monitoring integration
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mock Monitoring integration 
    # + return - Success 
    remote isolated function setProjectMockMonitoringIntegration(IdOrPath id, MockMonitoringIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/mock-monitoring`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve integration settings
    #
    # + slug - The name of the integration
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project integration 
    remote isolated function getProjectIntegration("apple-app-store"|"asana"|"assembla"|"bamboo"|"bugzilla"|"buildkite"|"campfire"|"confluence"|"custom-issue-tracker"|"datadog"|"diffblue-cover"|"discord"|"drone-ci"|"emails-on-push"|"external-wiki"|"gitlab-slack-application"|"google-play"|"hangouts-chat"|"harbor"|"irker"|"jenkins"|"jira"|"jira-cloud-app"|"linear"|"matrix"|"mattermost-slash-commands"|"packagist"|"phorge"|"pipelines-email"|"pivotaltracker"|"pumble"|"pushover"|"redmine"|"ewm"|"youtrack"|"clickup"|"slack"|"microsoft-teams"|"mattermost"|"teamcity"|"telegram"|"unify-circuit"|"webex-teams"|"zentao"|"squash-tm"|"github"|"git-guardian"|"google-cloud-platform-artifact-registry"|"google-cloud-platform-workload-identity-federation"|"mock-ci"|"mock-monitoring" slug, IdOrPath id, map<string|string[]> headers = {}) returns Integration|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Disable an integration
    #
    # + slug - The name of the integration
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function disableProjectIntegration("apple-app-store"|"asana"|"assembla"|"bamboo"|"bugzilla"|"buildkite"|"campfire"|"confluence"|"custom-issue-tracker"|"datadog"|"diffblue-cover"|"discord"|"drone-ci"|"emails-on-push"|"external-wiki"|"gitlab-slack-application"|"google-play"|"hangouts-chat"|"harbor"|"irker"|"jenkins"|"jira"|"jira-cloud-app"|"linear"|"matrix"|"mattermost-slash-commands"|"packagist"|"phorge"|"pipelines-email"|"pivotaltracker"|"pumble"|"pushover"|"redmine"|"ewm"|"youtrack"|"clickup"|"slack"|"microsoft-teams"|"mattermost"|"teamcity"|"telegram"|"unify-circuit"|"webex-teams"|"zentao"|"squash-tm"|"github"|"git-guardian"|"google-cloud-platform-artifact-registry"|"google-cloud-platform-workload-identity-federation"|"mock-ci"|"mock-monitoring" slug, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Trigger a slash command for mattermost-slash-commands
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to trigger a slash command for mattermost-slash-commands 
    # + return - Created 
    remote isolated function triggerMattermostSlashCommand(IdOrPath id, MattermostSlashCommandRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/integrations/mattermost_slash_commands/trigger`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all active integrations
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group integrations 
    remote isolated function listGroupIntegrations(IdOrPath id, map<string|string[]> headers = {}) returns IntegrationBasic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update the Apple App Store integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Apple App Store integration 
    # + return - Success 
    remote isolated function setGroupAppleAppStoreIntegration(IdOrPath id, AppleAppStoreIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/apple-app-store`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Asana integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Asana integration 
    # + return - Success 
    remote isolated function setGroupAsanaIntegration(IdOrPath id, AsanaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/asana`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Assembla integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Assembla integration 
    # + return - Success 
    remote isolated function setGroupAssemblaIntegration(IdOrPath id, AssemblaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/assembla`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Bamboo integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Bamboo integration 
    # + return - Success 
    remote isolated function setGroupBambooIntegration(IdOrPath id, BambooIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/bamboo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Bugzilla integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Bugzilla integration 
    # + return - Success 
    remote isolated function setGroupBugzillaIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/bugzilla`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Buildkite integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Buildkite integration 
    # + return - Success 
    remote isolated function setGroupBuildkiteIntegration(IdOrPath id, BuildkiteIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/buildkite`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Campfire integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Campfire integration 
    # + return - Success 
    remote isolated function setGroupCampfireIntegration(IdOrPath id, CampfireIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/campfire`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Confluence integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Confluence integration 
    # + return - Success 
    remote isolated function setGroupConfluenceIntegration(IdOrPath id, ConfluenceIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/confluence`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Custom Issue Tracker integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Custom Issue Tracker integration 
    # + return - Success 
    remote isolated function setGroupCustomTrackerIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/custom-issue-tracker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Datadog integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Datadog integration 
    # + return - Success 
    remote isolated function setGroupDatadogIntegration(IdOrPath id, DatadogIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/datadog`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Diffblue Cover integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Diffblue Cover integration 
    # + return - Success 
    remote isolated function setGroupDiffblueCoverIntegration(IdOrPath id, DiffblueCoverIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/diffblue-cover`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Discord integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Discord integration 
    # + return - Success 
    remote isolated function setGroupDiscordIntegration(IdOrPath id, DiscordIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/discord`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Drone Ci integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Drone Ci integration 
    # + return - Success 
    remote isolated function setGroupDroneCiIntegration(IdOrPath id, DroneCiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/drone-ci`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Emails On Push integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Emails On Push integration 
    # + return - Success 
    remote isolated function setGroupEmailsOnPushIntegration(IdOrPath id, EmailsOnPushIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/emails-on-push`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the External Wiki integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the External Wiki integration 
    # + return - Success 
    remote isolated function setGroupExternalWikiIntegration(IdOrPath id, ExternalWikiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/external-wiki`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Gitlab Slack Application integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Gitlab Slack Application integration 
    # + return - Success 
    remote isolated function setGroupSlackAppIntegration(IdOrPath id, SlackAppIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/gitlab-slack-application`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Play integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Play integration 
    # + return - Success 
    remote isolated function setGroupGooglePlayIntegration(IdOrPath id, GooglePlayIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/google-play`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Hangouts Chat integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Hangouts Chat integration 
    # + return - Success 
    remote isolated function setGroupHangoutsChatIntegration(IdOrPath id, HangoutsChatIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/hangouts-chat`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Harbor integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Harbor integration 
    # + return - Success 
    remote isolated function setGroupHarborIntegration(IdOrPath id, HarborIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/harbor`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Irker integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Irker integration 
    # + return - Success 
    remote isolated function setGroupIrkerIntegration(IdOrPath id, IrkerIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/irker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jenkins integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jenkins integration 
    # + return - Success 
    remote isolated function setGroupJenkinsIntegration(IdOrPath id, JenkinsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/jenkins`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jira integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jira integration 
    # + return - Success 
    remote isolated function setGroupJiraIntegration(IdOrPath id, JiraIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/jira`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Jira Cloud App integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Jira Cloud App integration 
    # + return - Success 
    remote isolated function setGroupJiraCloudAppIntegration(IdOrPath id, JiraCloudAppIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/jira-cloud-app`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Linear integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Linear integration 
    # + return - Success 
    remote isolated function setGroupLinearIntegration(IdOrPath id, LinearIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/linear`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Matrix integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Matrix integration 
    # + return - Success 
    remote isolated function setGroupMatrixIntegration(IdOrPath id, MatrixIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/matrix`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mattermost Slash Commands integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mattermost Slash Commands integration 
    # + return - Success 
    remote isolated function setGroupMattermostSlashIntegration(IdOrPath id, MattermostSlashIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/mattermost-slash-commands`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Packagist integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Packagist integration 
    # + return - Success 
    remote isolated function setGroupPackagistIntegration(IdOrPath id, PackagistIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/packagist`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Phorge integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Phorge integration 
    # + return - Success 
    remote isolated function setGroupPhorgeIntegration(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/phorge`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pipelines Email integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pipelines Email integration 
    # + return - Success 
    remote isolated function setGroupPipelinesEmailIntegration(IdOrPath id, PipelinesEmailIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/pipelines-email`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pivotaltracker integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pivotaltracker integration 
    # + return - Success 
    remote isolated function setGroupPivotaltrackerIntegration(IdOrPath id, PivotaltrackerIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/pivotaltracker`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pumble integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pumble integration 
    # + return - Success 
    remote isolated function setGroupPumbleIntegration(IdOrPath id, PumbleIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/pumble`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Pushover integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Pushover integration 
    # + return - Success 
    remote isolated function setGroupPushoverIntegration(IdOrPath id, PushoverIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/pushover`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Redmine integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Redmine integration 
    # + return - Success 
    remote isolated function setGroupRedmineIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/redmine`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Ewm integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Ewm integration 
    # + return - Success 
    remote isolated function setGroupEwmIntegration(IdOrPath id, BugzillaIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/ewm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Youtrack integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Youtrack integration 
    # + return - Success 
    remote isolated function setGroupYoutrackIntegration(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/youtrack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Clickup integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Clickup integration 
    # + return - Success 
    remote isolated function setGroupClickupIntegration(IdOrPath id, PhorgeIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/clickup`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Slack integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Slack integration 
    # + return - Success 
    remote isolated function setGroupSlackIntegration(IdOrPath id, SlackIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/slack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Microsoft Teams integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Microsoft Teams integration 
    # + return - Success 
    remote isolated function setGroupMicrosoftTeamsIntegration(IdOrPath id, MicrosoftTeamsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/microsoft-teams`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mattermost integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mattermost integration 
    # + return - Success 
    remote isolated function setGroupMattermostIntegration(IdOrPath id, MattermostIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/mattermost`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Teamcity integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Teamcity integration 
    # + return - Success 
    remote isolated function setGroupTeamcityIntegration(IdOrPath id, TeamcityIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/teamcity`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Telegram integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Telegram integration 
    # + return - Success 
    remote isolated function setGroupTelegramIntegration(IdOrPath id, TelegramIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/telegram`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Unify Circuit integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Unify Circuit integration 
    # + return - Success 
    remote isolated function setGroupUnifyCircuitIntegration(IdOrPath id, UnifyCircuitIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/unify-circuit`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Webex Teams integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Webex Teams integration 
    # + return - Success 
    remote isolated function setGroupWebexTeamsIntegration(IdOrPath id, WebexTeamsIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/webex-teams`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Zentao integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Zentao integration 
    # + return - Success 
    remote isolated function setGroupZentaoIntegration(IdOrPath id, ZentaoIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/zentao`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Squash Tm integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Squash Tm integration 
    # + return - Success 
    remote isolated function setGroupSquashTmIntegration(IdOrPath id, SquashTmIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/squash-tm`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Github integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Github integration 
    # + return - Success 
    remote isolated function setGroupGithubIntegration(IdOrPath id, GithubIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/github`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Git Guardian integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Git Guardian integration 
    # + return - Success 
    remote isolated function setGroupGitGuardianIntegration(IdOrPath id, GitGuardianIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/git-guardian`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Cloud Platform Artifact Registry integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Cloud Platform Artifact Registry integration 
    # + return - Success 
    remote isolated function setGroupGcpArtifactRegistry(IdOrPath id, GcpArtifactRegistryIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/google-cloud-platform-artifact-registry`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Google Cloud Platform Workload Identity Federation integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Google Cloud Workload Identity Federation integration 
    # + return - Success 
    remote isolated function setGroupGcpWorkloadIdentity(IdOrPath id, GcpWorkloadIdentityIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/google-cloud-platform-workload-identity-federation`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mock Ci integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mock Ci integration 
    # + return - Success 
    remote isolated function setGroupMockCiIntegration(IdOrPath id, MockCiIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/mock-ci`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create or update the Mock Monitoring integration
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update the Mock Monitoring integration 
    # + return - Success 
    remote isolated function setGroupMockMonitoringIntegration(IdOrPath id, MockMonitoringIntegrationRequest payload, map<string|string[]> headers = {}) returns IntegrationBasic|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/mock-monitoring`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve integration settings
    #
    # + slug - The name of the integration
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group integration 
    remote isolated function getGroupIntegration("apple-app-store"|"asana"|"assembla"|"bamboo"|"bugzilla"|"buildkite"|"campfire"|"confluence"|"custom-issue-tracker"|"datadog"|"diffblue-cover"|"discord"|"drone-ci"|"emails-on-push"|"external-wiki"|"gitlab-slack-application"|"google-play"|"hangouts-chat"|"harbor"|"irker"|"jenkins"|"jira"|"jira-cloud-app"|"linear"|"matrix"|"mattermost-slash-commands"|"packagist"|"phorge"|"pipelines-email"|"pivotaltracker"|"pumble"|"pushover"|"redmine"|"ewm"|"youtrack"|"clickup"|"slack"|"microsoft-teams"|"mattermost"|"teamcity"|"telegram"|"unify-circuit"|"webex-teams"|"zentao"|"squash-tm"|"github"|"git-guardian"|"google-cloud-platform-artifact-registry"|"google-cloud-platform-workload-identity-federation"|"mock-ci"|"mock-monitoring" slug, IdOrPath id, map<string|string[]> headers = {}) returns Integration|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Disable an integration
    #
    # + slug - The name of the integration
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function disableGroupIntegration("apple-app-store"|"asana"|"assembla"|"bamboo"|"bugzilla"|"buildkite"|"campfire"|"confluence"|"custom-issue-tracker"|"datadog"|"diffblue-cover"|"discord"|"drone-ci"|"emails-on-push"|"external-wiki"|"gitlab-slack-application"|"google-play"|"hangouts-chat"|"harbor"|"irker"|"jenkins"|"jira"|"jira-cloud-app"|"linear"|"matrix"|"mattermost-slash-commands"|"packagist"|"phorge"|"pipelines-email"|"pivotaltracker"|"pumble"|"pushover"|"redmine"|"ewm"|"youtrack"|"clickup"|"slack"|"microsoft-teams"|"mattermost"|"teamcity"|"telegram"|"unify-circuit"|"webex-teams"|"zentao"|"squash-tm"|"github"|"git-guardian"|"google-cloud-platform-artifact-registry"|"google-cloud-platform-workload-identity-federation"|"mock-ci"|"mock-monitoring" slug, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/integrations/${getEncodedUri(slug)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Trigger a global slack command
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to trigger a global slack command 
    # + return - Created 
    remote isolated function triggerGlobalSlackCommand(TriggerGlobalSlackCommandRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/slack/trigger`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import a repository from GitHub
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import a repository from GitHub 
    # + return - Created 
    remote isolated function importGitHubRepository(ImportGitHubRepositoryRequest payload, map<string|string[]> headers = {}) returns ProjectRef|error {
        string resourcePath = string `/import/github`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Cancel a GitHub project import
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to cancel a GitHub project import 
    # + return - Success 
    remote isolated function cancelGitHubProjectImport(CancelGitHubProjectImportRequest payload, map<string|string[]> headers = {}) returns ProjectImport|error {
        string resourcePath = string `/import/github/cancel`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import GitHub gists into GitLab snippets
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import GitHub gists into GitLab snippets 
    # + return - Accepted 
    remote isolated function importGitHubGists(ImportGitHubGistsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/import/github/gists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import repository from Bitbucket Server
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import repository from Bitbucket Server 
    # + return - Created 
    remote isolated function importBitbucketServerRepository(ImportBitbucketServerRepositoryRequest payload, map<string|string[]> headers = {}) returns ProjectRef|error {
        string resourcePath = string `/import/bitbucket_server`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Import repository from Bitbucket Cloud
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to import repository from Bitbucket Cloud 
    # + return - Created 
    remote isolated function importBitbucketCloudRepository(ImportBitbucketCloudRepositoryRequest payload, map<string|string[]> headers = {}) returns ProjectImport|error {
        string resourcePath = string `/import/bitbucket`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a chart index
    #
    # + id - The ID or full path of a project
    # + channel - Helm channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadHelmChartIndex(IdOrPath id, string channel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/helm/${getEncodedUri(channel)}/index.yaml`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a chart
    #
    # + id - The ID or full path of a project
    # + channel - Helm channel
    # + fileName - Helm package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadHelmChart(IdOrPath id, string channel, string fileName, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/helm/${getEncodedUri(channel)}/charts/${getEncodedUri(fileName)}.tgz`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Authorize a chart upload from workhorse
    #
    # + id - The ID or full path of a project
    # + channel - Helm channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeHelmChartUpload(IdOrPath id, string channel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/helm/api/${getEncodedUri(channel)}/charts/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Upload a chart
    #
    # + id - The ID or full path of a project
    # + channel - Helm channel
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a chart 
    # + return - Created 
    remote isolated function uploadHelmChart(IdOrPath id, string channel, UploadHelmChartRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/helm/api/${getEncodedUri(channel)}/charts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Execute a GLQL query
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to execute a GLQL query 
    # + return - Success 
    remote isolated function executeGlqlQuery(ExecuteGlqlQueryRequest payload, map<string|string[]> headers = {}) returns GlqlResult|error {
        string resourcePath = string `/glql`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the GLQL schema
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGlqlSchema(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/glql/schema`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group variables
    #
    # + id - The ID of a group or URL-encoded path of the group owned by the authenticated
    # user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group variables 
    remote isolated function listGroupVariables(string id, map<string|string[]> headers = {}, *ListGroupVariablesQueries queries) returns CiVariable[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group variable
    #
    # + id - The ID of a group or URL-encoded path of the group owned by the authenticated
    # user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group variable 
    # + return - Created 
    remote isolated function createGroupVariable(string id, CreateGroupVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve details of a group variable
    #
    # + id - The ID of a group or URL-encoded path of the group owned by the authenticated
    # user
    # + 'key - The key of the variable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group variable 
    remote isolated function getGroupVariable(string id, string 'key, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group variable
    #
    # + id - The ID of a group or URL-encoded path of the group owned by the authenticated
    # user
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group variable 
    # + return - The updated group variable 
    remote isolated function updateGroupVariable(string id, string 'key, UpdateGroupVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group variable
    #
    # + id - The ID of a group or URL-encoded path of the group owned by the authenticated
    # user
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupVariable(string id, string 'key, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve pending reassignments
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group placeholder reassignments 
    remote isolated function listGroupPlaceholderReassignments(string id, map<string|string[]> headers = {}) returns string|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/placeholder_reassignments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Reassign placeholders
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to reassign placeholders 
    # + return - Created 
    remote isolated function reassignGroupPlaceholders(string id, ReassignGroupPlaceholdersRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/placeholder_reassignments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Workhorse authorization for the reassignment CSV file
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function authorizeGroupPlaceholderReassignment(string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/placeholder_reassignments/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all packages for a group
    #
    # + id - ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group packages 
    remote isolated function listGroupPackages(IdOrPath id, map<string|string[]> headers = {}, *ListGroupPackagesQueries queries) returns Package[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Workhorse authorize the group import upload
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function authorizeGroupImportUpload(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/import/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a group import
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group import 
    # + return - Accepted 
    remote isolated function createGroupImport(CreateGroupImportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/import`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group export download
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupExport(string id, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/export/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group export
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function createGroupExport(string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/export`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Schedule a relations export for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to schedule a relations export for a group 
    # + return - Accepted 
    remote isolated function scheduleGroupRelationsExport(string id, ScheduleRelationsExportRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/export_relations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a relations export for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupRelationsExport(string id, map<string|string[]> headers = {}, *DownloadGroupRelationsExportQueries queries) returns byte[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/export_relations/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve the status of an relations export for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group relations export status 
    remote isolated function getGroupRelationsExportStatus(string id, map<string|string[]> headers = {}, *GetGroupRelationsExportStatusQueries queries) returns BulkImportsExportStatus|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/export_relations/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all Debian group distributions
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved debian group distributions 
    remote isolated function listDebianGroupDistributions(IdOrPath id, map<string|string[]> headers = {}, *ListDebianGroupDistributionsQueries queries) returns PackagesDebianDistribution[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/debian_distributions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"components": {style: FORM, explode: false}, "architectures": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a Debian group distribution
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a Debian group distribution 
    # + return - Created 
    remote isolated function createDebianGroupDistribution(IdOrPath id, CreateDebianDistributionRequest payload, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/debian_distributions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a Debian group distribution
    #
    # + id - The ID or URL-encoded path of the group
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + return - The retrieved debian group distribution 
    remote isolated function getDebianGroupDistribution(IdOrPath id, string codename, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/debian_distributions/${getEncodedUri(codename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a Debian group distribution
    #
    # + id - The ID or URL-encoded path of the group
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a Debian group distribution 
    # + return - The updated debian group distribution 
    remote isolated function updateDebianGroupDistribution(IdOrPath id, string codename, UpdateDebianDistributionRequest payload, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/debian_distributions/${getEncodedUri(codename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a Debian group distribution
    #
    # + id - The ID or URL-encoded path of the group
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Accepted 
    remote isolated function deleteDebianGroupDistribution(IdOrPath id, string codename, map<string|string[]> headers = {}, *DeleteDebianGroupDistributionQueries queries) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/debian_distributions/${getEncodedUri(codename)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"components": {style: FORM, explode: false}, "architectures": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a Debian group distribution key
    #
    # + id - The ID or URL-encoded path of the group
    # + codename - The Debian Codename
    # + headers - Headers to be sent with the request 
    # + return - The retrieved debian group distribution key 
    remote isolated function getDebianGroupDistributionKey(IdOrPath id, string codename, map<string|string[]> headers = {}) returns PackagesDebianDistribution|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/debian_distributions/${getEncodedUri(codename)}/key.asc`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all registry repositories for a group
    #
    # + id - The ID or URL-encoded path of the group accessible by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group registry repositories 
    remote isolated function listGroupRegistryRepositories(IdOrPath id, map<string|string[]> headers = {}, *ListGroupRegistryRepositoriesQueries queries) returns ContainerRegistryRepository[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/registry/repositories`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group clusters
    #
    # + id - The ID of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group clusters 
    remote isolated function listGroupClusters(string id, map<string|string[]> headers = {}, *ListGroupClustersQueries queries) returns Cluster[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/clusters`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a group cluster
    #
    # + id - The ID of the group
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group cluster 
    remote isolated function getGroupCluster(string id, int clusterId, map<string|string[]> headers = {}) returns ClusterGroup|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a group cluster
    #
    # + id - The ID of the group
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a group cluster 
    # + return - The updated group cluster 
    remote isolated function updateGroupCluster(string id, int clusterId, UpdateGroupClusterRequest payload, map<string|string[]> headers = {}) returns ClusterGroup|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a group cluster
    #
    # + id - The ID of the group
    # + clusterId - The Cluster ID
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteGroupCluster(string id, int clusterId, map<string|string[]> headers = {}) returns ClusterGroup|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Create a group cluster
    #
    # + id - The ID of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group cluster 
    # + return - Created 
    remote isolated function createGroupCluster(string id, CreateGroupClusterRequest payload, map<string|string[]> headers = {}) returns ClusterGroup|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/clusters/user`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download a group avatar
    #
    # + id - The ID of the group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupAvatar(string id, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/avatar`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List Go module versions
    #
    # + id - The project ID or full path of a project
    # + moduleName - The name of the Go module
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function listGoModuleVersions(IdOrPath id, string moduleName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/go/${getEncodedUri(moduleName)}/@v/list`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Version metadata
    #
    # + id - The project ID or full path of a project
    # + moduleName - The name of the Go module
    # + moduleVersion - The version of the Go module
    # + headers - Headers to be sent with the request 
    # + return - The retrieved go module version info 
    remote isolated function getGoModuleVersionInfo(IdOrPath id, string moduleName, string moduleVersion, map<string|string[]> headers = {}) returns GoModuleVersion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/go/${getEncodedUri(moduleName)}/@v/${getEncodedUri(moduleVersion)}.info`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download module file
    #
    # + id - The project ID or full path of a project
    # + moduleName - The name of the Go module
    # + moduleVersion - The version of the Go module
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGoModuleFile(IdOrPath id, string moduleName, string moduleVersion, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/go/${getEncodedUri(moduleName)}/@v/${getEncodedUri(moduleVersion)}.mod`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download module source
    #
    # + id - The project ID or full path of a project
    # + moduleName - The name of the Go module
    # + moduleVersion - The version of the Go module
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGoModuleSource(IdOrPath id, string moduleName, string moduleVersion, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/go/${getEncodedUri(moduleName)}/@v/${getEncodedUri(moduleVersion)}.zip`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Returns a Geo proxy response
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGeoProxyResponse(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/proxy`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Returns a replicable file from store (via CDN or sendfile)
    #
    # + replicableName - The replicable name of a replicator instance
    # + replicableId - The replicable ID of a replicable instance
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getReplicableFile(string replicableName, int replicableId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/retrieve/${getEncodedUri(replicableName)}/${getEncodedUri(replicableId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Returns the list of pipeline refs for the project
    #
    # + glRepository - The repository to check
    # + headers - Headers to be sent with the request 
    # + return - The retrieved geo pipeline refs 
    remote isolated function listGeoPipelineRefs(string glRepository, map<string|string[]> headers = {}) returns GeoPipelineRefs[]|error {
        string resourcePath = string `/geo/repositories/${getEncodedUri(glRepository)}/pipeline_refs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Posts the current node status to the primary site
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to post the current node status to the primary site 
    # + return - Success 
    remote isolated function reportGeoNodeStatus(ReportGeoNodeStatusRequest payload, map<string|string[]> headers = {}) returns GeoNodeStatus|error {
        string resourcePath = string `/geo/status`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reports resources that persistently fail verification on a secondary
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to report resources that persistently fail verification on a secondary 
    # + return - Accepted 
    remote isolated function reportGeoVerificationFailures(ReportGeoVerificationFailuresRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/failures`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Responsible for making HTTP GET /repo.git/info/refs?service=git-upload-pack
    #                   request from secondary gitlab-shell to primary
    #
    # + headers - Headers to be sent with the request 
    # + payload - Git HTTP transport payload for the POST info_refs_upload_pack request 
    # + return - Success 
    remote isolated function proxyInfoRefsUploadPack(ProxyInfoRefsUploadPackRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/proxy_git_ssh/info_refs_upload_pack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Responsible for making HTTP POST /repo.git/git-upload-pack
    #                   request from secondary gitlab-shell to primary
    #
    # + headers - Headers to be sent with the request 
    # + payload - Git HTTP transport payload for the POST upload_pack request 
    # + return - Success 
    remote isolated function proxyUploadPack(ProxyUploadPackRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/proxy_git_ssh/upload_pack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Responsible for making HTTP GET /repo.git/info/refs?service=git-receive-pack
    #                   request from secondary gitlab-shell to primary
    #
    # + headers - Headers to be sent with the request 
    # + payload - Git HTTP transport payload for the POST info_refs_receive_pack request 
    # + return - Success 
    remote isolated function proxyInfoRefsReceivePack(ProxyInfoRefsReceivePackRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/proxy_git_ssh/info_refs_receive_pack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Responsible for making HTTP POST /repo.git/info/refs?service=git-receive-pack
    #                   request from secondary gitlab-shell to primary
    #
    # + headers - Headers to be sent with the request 
    # + payload - Git HTTP transport payload for the POST receive_pack request 
    # + return - Success 
    remote isolated function proxyReceivePack(ProxyReceivePackRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/proxy_git_ssh/receive_pack`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Query the GraphQL endpoint of an existing Geo node
    #
    # + id - The ID of the Geo node
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function queryGeoNodeGraphql(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/geo/node_proxy/${getEncodedUri(id)}/graphql`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize generic package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + packageVersion - Package version
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for Workhorse to authorize generic package file upload 
    # + return - Success 
    remote isolated function authorizeGenericPackageFile(IdOrPath id, string packageName, string fileName, string packageVersion, AuthorizePackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/generic/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize generic package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + packageVersion - Package version
    # + path - Path of the package file directory
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for Workhorse to authorize generic package file upload 
    # + return - Success 
    remote isolated function authorizeGenericPackageFileInPath(IdOrPath id, string packageName, string fileName, string packageVersion, string path, AuthorizePackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/generic/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(path)}/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Download package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + packageVersion - Package version
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadGenericPackageFile(IdOrPath id, string packageName, string fileName, string packageVersion, map<string|string[]> headers = {}, *DownloadGenericPackageFileQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/generic/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + packageVersion - Package version
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload package file 
    # + return - Success 
    remote isolated function uploadGenericPackageFile(IdOrPath id, string packageName, string fileName, string packageVersion, UploadGenericPackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/generic/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Download package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + packageVersion - Package version
    # + path - File directory path
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadGenericPackageFileInPath(IdOrPath id, string packageName, string fileName, string packageVersion, string path, map<string|string[]> headers = {}, *DownloadGenericPackageFileInPathQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/generic/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + fileName - Package file name
    # + packageVersion - Package version
    # + path - File directory path
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload package file 
    # + return - Success 
    remote isolated function uploadGenericPackageFileInPath(IdOrPath id, string packageName, string fileName, string packageVersion, string path, UploadGenericPackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/generic/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(path)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all freeze periods
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved freeze periods 
    remote isolated function listFreezePeriods(IdOrPath id, map<string|string[]> headers = {}, *ListFreezePeriodsQueries queries) returns FreezePeriod[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/freeze_periods`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a freeze period
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a freeze period 
    # + return - Created 
    remote isolated function createFreezePeriod(IdOrPath id, CreateFreezePeriodRequest payload, map<string|string[]> headers = {}) returns FreezePeriod|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/freeze_periods`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a freeze period
    #
    # + id - The ID or URL-encoded path of the project
    # + freezePeriodId - The ID of the freeze period
    # + headers - Headers to be sent with the request 
    # + return - The retrieved freeze period 
    remote isolated function getFreezePeriod(IdOrPath id, int freezePeriodId, map<string|string[]> headers = {}) returns FreezePeriod|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/freeze_periods/${getEncodedUri(freezePeriodId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a freeze period
    #
    # + id - The ID or URL-encoded path of the project
    # + freezePeriodId - The ID of the freeze period
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a freeze period 
    # + return - The updated freeze period 
    remote isolated function updateFreezePeriod(IdOrPath id, int freezePeriodId, UpdateFreezePeriodRequest payload, map<string|string[]> headers = {}) returns FreezePeriod|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/freeze_periods/${getEncodedUri(freezePeriodId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a freeze period
    #
    # + id - The ID or URL-encoded path of the project
    # + freezePeriodId - The ID of the freeze period
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteFreezePeriod(IdOrPath id, int freezePeriodId, map<string|string[]> headers = {}) returns FreezePeriod|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/freeze_periods/${getEncodedUri(freezePeriodId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve file blame history from a repository
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository file blame 
    remote isolated function getRepositoryFileBlame(string id, string filePath, map<string|string[]> headers = {}, *GetRepositoryFileBlameQueries queries) returns BlameRange|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}/blame`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve file blame metadata
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getRepositoryFileBlameMetadata(string id, string filePath, map<string|string[]> headers = {}, *GetRepositoryFileBlameMetadataQueries queries) returns http:Response|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}/blame`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->head(resourcePath, httpHeaders);
    }

    # Retrieve a raw file from a repository
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved raw repository file 
    remote isolated function getRawRepositoryFile(string id, string filePath, map<string|string[]> headers = {}, *GetRawRepositoryFileQueries queries) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}/raw`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a file from a repository
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getRepositoryFile(string id, string filePath, map<string|string[]> headers = {}, *GetRepositoryFileQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a file in a repository
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a file in a repository 
    # + return - Success 
    remote isolated function updateRepositoryFile(string id, string filePath, UpdateRepositoryFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Create a file in a repository
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a file in a repository 
    # + return - Created 
    remote isolated function createRepositoryFile(string id, string filePath, CreateRepositoryFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a file in a repository
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - No Content 
    remote isolated function deleteRepositoryFile(string id, string filePath, map<string|string[]> headers = {}, *DeleteRepositoryFileQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve file metadata
    #
    # + id - The project ID
    # + filePath - The URL-encoded path to the file
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getRepositoryFileMetadata(string id, string filePath, map<string|string[]> headers = {}, *GetRepositoryFileMetadataQueries queries) returns http:Response|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/files/${getEncodedUri(filePath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->head(resourcePath, httpHeaders);
    }

    # List all feature flags
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved features 
    remote isolated function listFeatures(map<string|string[]> headers = {}) returns Feature[]|error {
        string resourcePath = string `/features`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all feature flag definitions
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved feature definitions 
    remote isolated function listFeatureDefinitions(map<string|string[]> headers = {}) returns FeatureDefinition[]|error {
        string resourcePath = string `/features/definitions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update a feature flag
    #
    # + name - The name of the feature flag
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update a feature flag 
    # + return - Created 
    remote isolated function setFeature(string name, SetFeatureRequest payload, map<string|string[]> headers = {}) returns Feature|error {
        string resourcePath = string `/features/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a feature
    #
    # + name - The name of the feature flag
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteFeature(string name, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/features/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all feature flag user lists for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved feature flag user lists 
    remote isolated function listFeatureFlagUserLists(IdOrPath id, map<string|string[]> headers = {}, *ListFeatureFlagUserListsQueries queries) returns FeatureFlagUserList[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_user_lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a feature flag user list
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a feature flag user list 
    # + return - Created 
    remote isolated function createFeatureFlagUserList(IdOrPath id, CreateFeatureFlagUserListRequest payload, map<string|string[]> headers = {}) returns FeatureFlagUserList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_user_lists`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a feature flag user list
    #
    # + id - The ID or URL-encoded path of the project
    # + iid - The internal ID of the project's feature flag user list
    # + headers - Headers to be sent with the request 
    # + return - The retrieved feature flag user list 
    remote isolated function getFeatureFlagUserList(IdOrPath id, IdOrPath iid, map<string|string[]> headers = {}) returns FeatureFlagUserList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_user_lists/${getEncodedUri(iid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a feature flag user list
    #
    # + id - The ID or URL-encoded path of the project
    # + iid - The internal ID of the project's feature flag user list
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a feature flag user list 
    # + return - The updated feature flag user list 
    remote isolated function updateFeatureFlagUserList(IdOrPath id, IdOrPath iid, UpdateFeatureFlagUserListRequest payload, map<string|string[]> headers = {}) returns FeatureFlagUserList|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_user_lists/${getEncodedUri(iid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete feature flag user list
    #
    # + id - The ID or URL-encoded path of the project
    # + iid - The internal ID of the project's feature flag user list
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteFeatureFlagUserList(IdOrPath id, IdOrPath iid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_user_lists/${getEncodedUri(iid)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all feature flags for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project feature flags 
    remote isolated function listProjectFeatureFlags(IdOrPath id, map<string|string[]> headers = {}, *ListProjectFeatureFlagsQueries queries) returns FeatureFlag[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a feature flag
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a feature flag 
    # + return - Created 
    remote isolated function createFeatureFlag(IdOrPath id, CreateFeatureFlagRequest payload, map<string|string[]> headers = {}) returns FeatureFlag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve feature flag settings
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved feature flag settings 
    remote isolated function getFeatureFlagSettings(IdOrPath id, map<string|string[]> headers = {}) returns FeatureFlagSettings|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update feature flag settings
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update feature flag settings 
    # + return - The updated feature flag settings 
    remote isolated function updateFeatureFlagSettings(IdOrPath id, UpdateFeatureFlagSettingsRequest payload, map<string|string[]> headers = {}) returns FeatureFlagSettings|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags_settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve a feature flag
    #
    # + id - The ID or URL-encoded path of the project
    # + featureFlagName - The name of the feature flag
    # + headers - Headers to be sent with the request 
    # + return - The retrieved feature flag 
    remote isolated function getFeatureFlag(IdOrPath id, string featureFlagName, map<string|string[]> headers = {}) returns FeatureFlag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags/${getEncodedUri(featureFlagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a feature flag
    #
    # + id - The ID or URL-encoded path of the project
    # + featureFlagName - The name of the feature flag
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a feature flag 
    # + return - The updated feature flag 
    remote isolated function updateFeatureFlag(IdOrPath id, string featureFlagName, UpdateFeatureFlagRequest payload, map<string|string[]> headers = {}) returns FeatureFlag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags/${getEncodedUri(featureFlagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a feature flag
    #
    # + id - The ID or URL-encoded path of the project
    # + featureFlagName - The name of the feature flag
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteFeatureFlag(IdOrPath id, string featureFlagName, map<string|string[]> headers = {}) returns FeatureFlag|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/feature_flags/${getEncodedUri(featureFlagName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all events
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved events 
    remote isolated function listEvents(map<string|string[]> headers = {}, *ListEventsQueries queries) returns Event[]|error {
        string resourcePath = string `/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve contribution events for a user
    #
    # + id - The ID or username of the user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved user contribution events 
    remote isolated function listUserContributionEvents(string id, map<string|string[]> headers = {}, *ListUserContributionEventsQueries queries) returns Event[]|error {
        string resourcePath = string `/users/${getEncodedUri(id)}/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve Error Tracking settings for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved error tracking settings 
    remote isolated function getErrorTrackingSettings(IdOrPath id, map<string|string[]> headers = {}) returns ErrorTrackingProjectSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/error_tracking/settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create Error Tracking settings for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create Error Tracking settings for a project 
    # + return - The created error tracking settings 
    remote isolated function createErrorTrackingSettings(IdOrPath id, CreateErrorTrackingSettingsRequest payload, map<string|string[]> headers = {}) returns ErrorTrackingProjectSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/error_tracking/settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Update Error Tracking settings for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update Error Tracking settings for a project 
    # + return - The updated error tracking settings 
    remote isolated function updateErrorTrackingSettings(IdOrPath id, UpdateErrorTrackingSettingsRequest payload, map<string|string[]> headers = {}) returns ErrorTrackingProjectSetting|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/error_tracking/settings`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List all project client keys
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project client keys 
    remote isolated function listProjectClientKeys(IdOrPath id, map<string|string[]> headers = {}) returns ErrorTrackingClientKey[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/error_tracking/client_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a client key
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function createClientKey(IdOrPath id, map<string|string[]> headers = {}) returns ErrorTrackingClientKey|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/error_tracking/client_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a client key
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + keyId - The ID of the client key
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteClientKey(IdOrPath id, int keyId, map<string|string[]> headers = {}) returns ErrorTrackingClientKey|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/error_tracking/client_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all environments
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved environments 
    remote isolated function listEnvironments(IdOrPath id, map<string|string[]> headers = {}, *ListEnvironmentsQueries queries) returns Environment[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an environment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an environment 
    # + return - Created 
    remote isolated function createEnvironment(IdOrPath id, CreateEnvironmentRequest payload, map<string|string[]> headers = {}) returns Environment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an environment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + environmentId - The ID of the environment
    # + headers - Headers to be sent with the request 
    # + return - The retrieved environment 
    remote isolated function getEnvironment(IdOrPath id, int environmentId, map<string|string[]> headers = {}) returns Environment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments/${getEncodedUri(environmentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an existing environment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + environmentId - The ID of the environment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an existing environment 
    # + return - The updated environment 
    remote isolated function updateEnvironment(IdOrPath id, int environmentId, UpdateEnvironmentRequest payload, map<string|string[]> headers = {}) returns Environment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments/${getEncodedUri(environmentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete an environment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + environmentId - The ID of the environment
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteEnvironment(IdOrPath id, int environmentId, map<string|string[]> headers = {}) returns Environment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments/${getEncodedUri(environmentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Schedule multiple stopped review apps for deletion
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteStoppedReviewApps(IdOrPath id, map<string|string[]> headers = {}, *DeleteStoppedReviewAppsQueries queries) returns EnvironmentBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments/review_apps`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Stop an environment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + environmentId - The ID of the environment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to stop an environment 
    # + return - Created 
    remote isolated function stopEnvironment(IdOrPath id, int environmentId, StopEnvironmentRequest payload, map<string|string[]> headers = {}) returns Environment|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments/${getEncodedUri(environmentId)}/stop`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Stop stale environments
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to stop stale environments 
    # + return - Created 
    remote isolated function stopStaleEnvironments(IdOrPath id, StopStaleEnvironmentsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/environments/stop_stale`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all merge request draft notes
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request draft notes 
    remote isolated function listMergeRequestDraftNotes(string id, int mergeRequestIid, map<string|string[]> headers = {}) returns DraftNote[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a draft note
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a draft note 
    # + return - Created 
    remote isolated function createDraftNote(string id, int mergeRequestIid, CreateDraftNoteRequest payload, map<string|string[]> headers = {}) returns DraftNote|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a draft note
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + draftNoteId - The ID of a draft note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved draft note 
    remote isolated function getDraftNote(string id, int mergeRequestIid, int draftNoteId, map<string|string[]> headers = {}) returns DraftNote|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes/${getEncodedUri(draftNoteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a draft note
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + draftNoteId - The ID of a draft note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a draft note 
    # + return - The updated draft note 
    remote isolated function updateDraftNote(string id, int mergeRequestIid, int draftNoteId, UpdateDraftNoteRequest payload, map<string|string[]> headers = {}) returns DraftNote|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes/${getEncodedUri(draftNoteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a draft note
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + draftNoteId - The ID of a draft note
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteDraftNote(string id, int mergeRequestIid, int draftNoteId, map<string|string[]> headers = {}) returns DraftNote|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes/${getEncodedUri(draftNoteId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Publish a draft note
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + draftNoteId - The ID of a draft note
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function publishDraftNote(string id, int mergeRequestIid, int draftNoteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes/${getEncodedUri(draftNoteId)}/publish`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Publish all pending draft notes
    #
    # + id - The ID of a project
    # + mergeRequestIid - The ID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to publish all pending draft notes 
    # + return - No Content 
    remote isolated function publishAllDraftNotes(string id, int mergeRequestIid, PublishAllDraftNotesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/draft_notes/bulk_publish`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project deployments
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project deployments 
    remote isolated function listProjectDeployments(IdOrPath id, map<string|string[]> headers = {}, *ListProjectDeploymentsQueries queries) returns Deployment[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a deployment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a deployment 
    # + return - Created 
    remote isolated function createDeployment(IdOrPath id, CreateDeploymentRequest payload, map<string|string[]> headers = {}) returns DeploymentExtended|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a deployment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + deploymentId - The ID of the deployment
    # + headers - Headers to be sent with the request 
    # + return - The retrieved deployment 
    remote isolated function getDeployment(IdOrPath id, int deploymentId, map<string|string[]> headers = {}) returns DeploymentExtended|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments/${getEncodedUri(deploymentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a deployment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + deploymentId - The ID of the deployment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a deployment 
    # + return - The updated deployment 
    remote isolated function updateDeployment(IdOrPath id, int deploymentId, UpdateDeploymentRequest payload, map<string|string[]> headers = {}) returns DeploymentExtended|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments/${getEncodedUri(deploymentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a deployment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + deploymentId - The ID of the deployment
    # + headers - Headers to be sent with the request 
    # + return - Deployment destroyed 
    remote isolated function deleteDeployment(IdOrPath id, int deploymentId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments/${getEncodedUri(deploymentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all merge requests associated with a deployment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + deploymentId - The ID of the deployment
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved deployment merge requests 
    remote isolated function listDeploymentMergeRequests(IdOrPath id, int deploymentId, map<string|string[]> headers = {}, *ListDeploymentMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments/${getEncodedUri(deploymentId)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"assignee_id": {style: FORM, explode: true}, "assignee_username": {style: FORM, explode: true}, "labels": {style: FORM, explode: false}, "reviewer_id": {style: FORM, explode: true}, "not[assignee_id]": {style: FORM, explode: true}, "not[assignee_username]": {style: FORM, explode: true}, "not[labels]": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Approve or reject a deployment
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + deploymentId - The ID of the deployment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to approve or reject a deployment 
    # + return - Created 
    remote isolated function approveOrRejectDeployment(IdOrPath id, int deploymentId, ApproveOrRejectDeploymentRequest payload, map<string|string[]> headers = {}) returns DeploymentsApproval|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deployments/${getEncodedUri(deploymentId)}/approval`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all deploy tokens
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved deploy tokens 
    remote isolated function listDeployTokens(map<string|string[]> headers = {}, *ListDeployTokensQueries queries) returns DeployToken[]|error {
        string resourcePath = string `/deploy_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project deploy tokens
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project deploy tokens 
    remote isolated function listProjectDeployTokens(IdOrPath id, map<string|string[]> headers = {}, *ListProjectDeployTokensQueries queries) returns DeployToken[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a project deploy token
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a project deploy token 
    # + return - Created 
    remote isolated function createProjectDeployToken(IdOrPath id, CreateProjectDeployTokenRequest payload, map<string|string[]> headers = {}) returns DeployTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a project deploy token
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + tokenId - The ID of the deploy token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project deploy token 
    remote isolated function getProjectDeployToken(IdOrPath id, int tokenId, map<string|string[]> headers = {}) returns DeployToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a project deploy token
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + tokenId - The ID of the deploy token
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteProjectDeployToken(IdOrPath id, int tokenId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all group deploy tokens
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group deploy tokens 
    remote isolated function listGroupDeployTokens(IdOrPath id, map<string|string[]> headers = {}, *ListGroupDeployTokensQueries queries) returns DeployToken[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/deploy_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group deploy token
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group deploy token 
    # + return - Created 
    remote isolated function createGroupDeployToken(IdOrPath id, CreateGroupDeployTokenRequest payload, map<string|string[]> headers = {}) returns DeployTokenWithToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/deploy_tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group deploy token
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + tokenId - The ID of the deploy token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group deploy token 
    remote isolated function getGroupDeployToken(IdOrPath id, int tokenId, map<string|string[]> headers = {}) returns DeployToken|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/deploy_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a group deploy token
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + tokenId - The ID of the deploy token
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteGroupDeployToken(IdOrPath id, int tokenId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/deploy_tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all deploy keys
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved deploy keys 
    remote isolated function listDeployKeys(map<string|string[]> headers = {}, *ListDeployKeysQueries queries) returns DeployKey[]|error {
        string resourcePath = string `/deploy_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a deploy key
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a deploy key 
    # + return - Created 
    remote isolated function createDeployKey(CreateDeployKeyRequest payload, map<string|string[]> headers = {}) returns DeployKey|error {
        string resourcePath = string `/deploy_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all deploy keys for project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project deploy keys 
    remote isolated function listProjectDeployKeys(IdOrPath id, map<string|string[]> headers = {}, *ListProjectDeployKeysQueries queries) returns DeployKeysProject[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a deploy key for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a deploy key for a project 
    # + return - Created 
    remote isolated function addProjectDeployKey(IdOrPath id, AddProjectDeployKeyRequest payload, map<string|string[]> headers = {}) returns DeployKeysProject|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_keys`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a deploy key
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + keyId - The ID of the deploy key
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project deploy key 
    remote isolated function getProjectDeployKey(IdOrPath id, int keyId, map<string|string[]> headers = {}) returns DeployKeysProject|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a deploy key
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + keyId - The ID of the deploy key
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a deploy key 
    # + return - The updated project deploy key 
    remote isolated function updateProjectDeployKey(IdOrPath id, int keyId, UpdateProjectDeployKeyRequest payload, map<string|string[]> headers = {}) returns DeployKey|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a deploy key
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + keyId - The ID of the deploy key
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteProjectDeployKey(IdOrPath id, int keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_keys/${getEncodedUri(keyId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Enable a deploy key
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + keyId - The ID of the deploy key
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function enableProjectDeployKey(IdOrPath id, int keyId, map<string|string[]> headers = {}) returns DeployKey|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/deploy_keys/${getEncodedUri(keyId)}/enable`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Purge the dependency proxy for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function purgeGroupDependencyProxy(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/dependency_proxy/cache`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # The Release file signature
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianReleaseSignature(IdOrPath id, string distribution, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/Release.gpg`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The unsigned Release file
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianRelease(IdOrPath id, string distribution, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/Release`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The signed Release file
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianSignedRelease(IdOrPath id, string distribution, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/InRelease`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The installer (udeb) binary files index
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianInstallerIndex(IdOrPath id, string distribution, string component, string architecture, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/debian-installer/binary-${getEncodedUri(architecture)}/Packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The installer (udeb) binary files index by hash
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + fileSha256 - The SHA256 checksum of the index file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianInstallerIndexByHash(IdOrPath id, string distribution, string component, string architecture, string fileSha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/debian-installer/binary-${getEncodedUri(architecture)}/by-hash/SHA256/${getEncodedUri(fileSha256)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The source files index
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianSourceIndex(IdOrPath id, string distribution, string component, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/source/Sources`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The source files index by hash
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + fileSha256 - The SHA256 checksum of the index file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianSourceIndexByHash(IdOrPath id, string distribution, string component, string fileSha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/source/by-hash/SHA256/${getEncodedUri(fileSha256)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The binary files index
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianBinaryIndex(IdOrPath id, string distribution, string component, string architecture, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/binary-${getEncodedUri(architecture)}/Packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The binary files index by hash
    #
    # + id - The group ID or full group path
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + fileSha256 - The SHA256 checksum of the index file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupDebianBinaryIndexByHash(IdOrPath id, string distribution, string component, string architecture, string fileSha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/binary-${getEncodedUri(architecture)}/by-hash/SHA256/${getEncodedUri(fileSha256)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download Debian package
    #
    # + id - The group ID or full group path
    # + projectId - The Project Id
    # + distribution - The Debian distribution name
    # + letter - The package name first letter
    # + packageName - The package name
    # + packageVersion - The package version
    # + fileName - The package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadGroupDebianPackage(IdOrPath id, int projectId, string distribution, string letter, string packageName, string packageVersion, string fileName, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/-/packages/debian/pool/${getEncodedUri(distribution)}/${getEncodedUri(projectId)}/${getEncodedUri(letter)}/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The Release file signature
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianReleaseSignature(IdOrPath id, string distribution, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/Release.gpg`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The unsigned Release file
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianRelease(IdOrPath id, string distribution, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/Release`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The signed Release file
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianSignedRelease(IdOrPath id, string distribution, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/InRelease`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The installer (udeb) binary files index
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianInstallerIndex(IdOrPath id, string distribution, string component, string architecture, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/debian-installer/binary-${getEncodedUri(architecture)}/Packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The installer (udeb) binary files index by hash
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + fileSha256 - The SHA256 checksum of the index file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianInstallerIndexByHash(IdOrPath id, string distribution, string component, string architecture, string fileSha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/debian-installer/binary-${getEncodedUri(architecture)}/by-hash/SHA256/${getEncodedUri(fileSha256)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The source files index
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianSourceIndex(IdOrPath id, string distribution, string component, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/source/Sources`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The source files index by hash
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + fileSha256 - The SHA256 checksum of the index file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianSourceIndexByHash(IdOrPath id, string distribution, string component, string fileSha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/source/by-hash/SHA256/${getEncodedUri(fileSha256)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The binary files index
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianBinaryIndex(IdOrPath id, string distribution, string component, string architecture, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/binary-${getEncodedUri(architecture)}/Packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # The binary files index by hash
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + component - The Debian Component
    # + architecture - The Debian Architecture
    # + fileSha256 - The SHA256 checksum of the index file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectDebianBinaryIndexByHash(IdOrPath id, string distribution, string component, string architecture, string fileSha256, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/dists/${getEncodedUri(distribution)}/${getEncodedUri(component)}/binary-${getEncodedUri(architecture)}/by-hash/SHA256/${getEncodedUri(fileSha256)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a Debian package file
    #
    # + id - The ID or URL-encoded path of the project
    # + distribution - The Debian Codename or Suite
    # + letter - The Debian Classification (first-letter or lib-first-letter)
    # + packageName - The Debian Source Package Name
    # + packageVersion - The Debian Source Package Version
    # + fileName - The Debian File Name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadDebianPackageFile(IdOrPath id, string distribution, string letter, string packageName, string packageVersion, string fileName, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/pool/${getEncodedUri(distribution)}/${getEncodedUri(letter)}/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a Debian package file
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - The filename
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a Debian package file 
    # + return - Created 
    remote isolated function uploadDebianPackageFile(IdOrPath id, string fileName, UploadDebianPackageFileRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Authorize Debian package upload
    #
    # + id - The ID or URL-encoded path of the project
    # + fileName - The filename
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to authorize Debian package upload 
    # + return - Success 
    remote isolated function authorizeDebianPackageUpload(IdOrPath id, string fileName, AuthorizeDebianPackageUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/debian/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List dictionary tables
    #
    # + databaseName - The database name
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved dictionary tables 
    remote isolated function listDictionaryTables("main"|"ci"|"sec"|"embedding"|"geo" databaseName, map<string|string[]> headers = {}, *ListDictionaryTablesQueries queries) returns DictionaryTable[]|error {
        string resourcePath = string `/databases/${getEncodedUri(databaseName)}/dictionary/tables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve details of a container registry repository
    #
    # + id - The ID of the repository
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved registry repository 
    remote isolated function getRegistryRepository(IdOrPath id, map<string|string[]> headers = {}, *GetRegistryRepositoryQueries queries) returns ContainerRegistryRepository|error {
        string resourcePath = string `/registry/repositories/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Receives notifications from the container registry when an operation occurs
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function receiveContainerRegistryEvents(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/container_registry_event/events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an authentication token
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanV2AuthToken(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/users/authenticate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Verify authentication credentials
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function checkConanV2Credentials(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/users/check_credentials`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search for a Conan package
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchConanV2Packages(IdOrPath id, map<string|string[]> headers = {}, *SearchConanV2PackagesQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve package references metadata
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The package name
    # + packageVersion - The package version
    # + packageUsername - The package username
    # + packageChannel - The package channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanV2PackageReferences(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve latest recipe revision
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan v2 latest recipe revision 
    remote isolated function getConanV2LatestRecipeRevision(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRevision|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/latest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all recipe revisions
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan v2 recipe revisions 
    remote isolated function listConanV2RecipeRevisions(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeRevisions[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete recipe revision
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteConanV2RecipeRevision(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all recipe files
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan v2 recipe files 
    remote isolated function listConanV2RecipeFiles(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, map<string|string[]> headers = {}) returns PackagesConanFilesList[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/files`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a recipe file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanV2RecipeFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/files/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a recipe file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a recipe file 
    # + return - Success 
    remote isolated function uploadConanV2RecipeFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/files/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the Conan recipe file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeConanV2RecipeFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/files/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve package references metadata by recipe revision
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanV2RevisionPackageReferences(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve latest package revision
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan v2 latest package revision 
    remote isolated function getConanV2LatestPackageRevision(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanRevision|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/latest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all package revisions
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan v2 package revisions 
    remote isolated function listConanV2PackageRevisions(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageRevisions[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/revisions`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a package revision
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + packageRevision - Package revision
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteConanV2PackageRevision(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/revisions/${getEncodedUri(packageRevision)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all package files
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + packageRevision - Package revision
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan v2 package files 
    remote isolated function listConanV2PackageFiles(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, map<string|string[]> headers = {}) returns PackagesConanFilesList[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/revisions/${getEncodedUri(packageRevision)}/files`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + packageRevision - Package revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanV2PackageFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/revisions/${getEncodedUri(packageRevision)}/files/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + packageRevision - Package revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a package file 
    # + return - Success 
    remote isolated function uploadConanV2PackageFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/revisions/${getEncodedUri(packageRevision)}/files/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the Conan package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Recipe revision
    # + conanPackageReference - Package reference
    # + packageRevision - Package revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeConanV2PackageFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v2/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/revisions/${getEncodedUri(recipeRevision)}/packages/${getEncodedUri(conanPackageReference)}/revisions/${getEncodedUri(packageRevision)}/files/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve an authentication token
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectConanAuthToken(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/users/authenticate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Verify authentication credentials
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function checkProjectConanCredentials(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/users/check_credentials`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search for a Conan package
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchProjectConanPackages(IdOrPath id, map<string|string[]> headers = {}, *SearchProjectConanPackagesQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve package references metadata
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The package name
    # + packageVersion - The package version
    # + packageUsername - The package username
    # + packageChannel - The package channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectConanPackageReferences(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Verify availability of a Conan repository
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function pingProjectConan(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/ping`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a package snapshot
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan package snapshot 
    remote isolated function getProjectConanPackageSnapshot(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageSnapshot|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a recipe snapshot
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan recipe snapshot 
    remote isolated function getProjectConanRecipeSnapshot(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeSnapshot|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a recipe and package
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectConanRecipe(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a package manifest
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan package manifest 
    remote isolated function getProjectConanPackageManifest(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageManifest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}/digest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a recipe manifest
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan recipe manifest 
    remote isolated function getProjectConanRecipeManifest(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeManifest|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/digest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all package download URLs
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan package download urls 
    remote isolated function listProjectConanPackageDownloadUrls(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageManifest[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}/download_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all recipe download URLs
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan recipe download urls 
    remote isolated function listProjectConanRecipeDownloadUrls(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeManifest[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/download_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all package upload URLs
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan package upload urls 
    remote isolated function listProjectConanPackageUploadUrls(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanUploadUrls|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}/upload_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all recipe upload URLs
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project conan recipe upload urls 
    remote isolated function listProjectConanRecipeUploadUrls(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanUploadUrls|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/upload_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a recipe file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectConanRecipeFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/export/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a recipe file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a recipe file 
    # + return - Success 
    remote isolated function uploadProjectConanRecipeFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/export/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the Conan recipe file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeProjectConanRecipeFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/export/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve a package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + conanPackageReference - Conan Package ID
    # + packageRevision - Conan Package Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getProjectConanPackageFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/package/${getEncodedUri(conanPackageReference)}/${getEncodedUri(packageRevision)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + conanPackageReference - Conan Package ID
    # + packageRevision - Conan Package Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a package file 
    # + return - Success 
    remote isolated function uploadProjectConanPackageFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/package/${getEncodedUri(conanPackageReference)}/${getEncodedUri(packageRevision)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the Conan package file
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + conanPackageReference - Conan Package ID
    # + packageRevision - Conan Package Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeProjectConanPackageFile(IdOrPath id, string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/package/${getEncodedUri(conanPackageReference)}/${getEncodedUri(packageRevision)}/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve an authentication token
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanAuthToken(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/users/authenticate`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Verify authentication credentials
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function checkConanCredentials(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/users/check_credentials`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Search for a Conan package
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function searchConanPackages(map<string|string[]> headers = {}, *SearchConanPackagesQueries queries) returns error? {
        string resourcePath = string `/packages/conan/v1/conans/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve package references metadata
    #
    # + packageName - The package name
    # + packageVersion - The package version
    # + packageUsername - The package username
    # + packageChannel - The package channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanPackageReferences(string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/search`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Verify availability of a Conan repository
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function pingConan(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/ping`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a package snapshot
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan package snapshot 
    remote isolated function getConanPackageSnapshot(string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageSnapshot|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a recipe snapshot
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan recipe snapshot 
    remote isolated function getConanRecipeSnapshot(string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeSnapshot|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a recipe and package
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteConanRecipe(string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve a package manifest
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan package manifest 
    remote isolated function getConanPackageManifest(string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageManifest|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}/digest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a recipe manifest
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan recipe manifest 
    remote isolated function getConanRecipeManifest(string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeManifest|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/digest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all package download URLs
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan package download urls 
    remote isolated function listConanPackageDownloadUrls(string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanPackageManifest[]|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}/download_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all recipe download URLs
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan recipe download urls 
    remote isolated function listConanRecipeDownloadUrls(string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanRecipeManifest[]|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/download_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all package upload URLs
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + conanPackageReference - Conan package ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan package upload urls 
    remote isolated function listConanPackageUploadUrls(string packageName, string packageVersion, string packageUsername, string packageChannel, string conanPackageReference, map<string|string[]> headers = {}) returns PackagesConanUploadUrls|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/packages/${getEncodedUri(conanPackageReference)}/upload_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all recipe upload URLs
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + headers - Headers to be sent with the request 
    # + return - The retrieved conan recipe upload urls 
    remote isolated function listConanRecipeUploadUrls(string packageName, string packageVersion, string packageUsername, string packageChannel, map<string|string[]> headers = {}) returns PackagesConanUploadUrls|error {
        string resourcePath = string `/packages/conan/v1/conans/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/upload_urls`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a recipe file
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanRecipeFile(string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/export/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a recipe file
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a recipe file 
    # + return - Success 
    remote isolated function uploadConanRecipeFile(string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/export/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the Conan recipe file
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeConanRecipeFile(string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/export/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve a package file
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + conanPackageReference - Conan Package ID
    # + packageRevision - Conan Package Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getConanPackageFile(string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/package/${getEncodedUri(conanPackageReference)}/${getEncodedUri(packageRevision)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a package file
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + conanPackageReference - Conan Package ID
    # + packageRevision - Conan Package Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a package file 
    # + return - Success 
    remote isolated function uploadConanPackageFile(string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, PackageFileUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/package/${getEncodedUri(conanPackageReference)}/${getEncodedUri(packageRevision)}/${getEncodedUri(fileName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize the Conan package file
    #
    # + packageName - Package name
    # + packageVersion - Package version
    # + packageUsername - Package username
    # + packageChannel - Package channel
    # + recipeRevision - Conan Recipe Revision
    # + conanPackageReference - Conan Package ID
    # + packageRevision - Conan Package Revision
    # + fileName - Package file name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeConanPackageFile(string packageName, string packageVersion, string packageUsername, string packageChannel, string recipeRevision, string conanPackageReference, string packageRevision, "conanfile.py"|"conanmanifest.txt"|"conan_sources.tgz"|"conan_export.tgz"|"conaninfo.txt"|"conan_package.tgz" fileName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/packages/conan/v1/files/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/${getEncodedUri(packageUsername)}/${getEncodedUri(packageChannel)}/${getEncodedUri(recipeRevision)}/package/${getEncodedUri(conanPackageReference)}/${getEncodedUri(packageRevision)}/${getEncodedUri(fileName)}/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve repository URL templates
    #
    # + id - The ID or URL-encoded path of a group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupComposerUrlTemplates(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/group/${getEncodedUri(id)}/-/packages/composer/packages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all packages for a group
    #
    # + id - The ID or URL-encoded path of a group
    # + sha - Shasum of current json
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupComposerPackagesBySha(IdOrPath id, string sha, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/group/${getEncodedUri(id)}/-/packages/composer/p/${getEncodedUri(sha)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Composer v2 packages p2 endpoint for a group's package versions metadata
    #
    # + id - The ID or URL-encoded path of a group
    # + packageName - The Composer package name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupComposerV2PackageVersions(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/group/${getEncodedUri(id)}/-/packages/composer/p2/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Composer packages endpoint for a group's package versions metadata
    #
    # + id - The ID or URL-encoded path of a group
    # + packageName - The Composer package name
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getGroupComposerPackageVersions(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/group/${getEncodedUri(id)}/-/packages/composer/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a package
    #
    # + id - The ID or URL-encoded path of a project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a package 
    # + return - Created 
    remote isolated function createComposerPackage(IdOrPath id, CreateComposerPackageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/composer`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Composer package endpoint to download a package archive
    #
    # + id - The ID or URL-encoded path of a project
    # + packageName - The Composer package name
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadComposerArchive(IdOrPath id, string packageName, map<string|string[]> headers = {}, *DownloadComposerArchiveQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/composer/archives/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all commit statuses
    #
    # + id - ID or URL-encoded path of the project
    # + sha - Hash of the commit
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit statuses 
    remote isolated function listCommitStatuses(IdOrPath id, string sha, map<string|string[]> headers = {}, *ListCommitStatusesQueries queries) returns CommitStatus[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/statuses`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update a commit pipeline status
    #
    # + id - ID or URL-encoded path of the project
    # + sha - The commit hash
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or update a commit pipeline status 
    # + return - Success 
    remote isolated function setCommitPipelineStatus(IdOrPath id, string sha, SetCommitPipelineStatusRequest payload, map<string|string[]> headers = {}) returns CommitStatus|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/statuses/${getEncodedUri(sha)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all repository commits
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository commits 
    remote isolated function listRepositoryCommits(IdOrPath id, map<string|string[]> headers = {}, *ListRepositoryCommitsQueries queries) returns Commit[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a commit
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a commit 
    # + return - The created commit 
    remote isolated function createCommit(IdOrPath id, CreateCommitRequest payload, map<string|string[]> headers = {}) returns CommitDetail|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a commit
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit 
    remote isolated function getCommit(IdOrPath id, string sha, map<string|string[]> headers = {}, *GetCommitQueries queries) returns CommitDetail|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a commit diff
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit diff 
    remote isolated function getCommitDiff(IdOrPath id, string sha, map<string|string[]> headers = {}, *GetCommitDiffQueries queries) returns Diff|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/diff`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all commit comments
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit comments 
    remote isolated function listCommitComments(IdOrPath id, string sha, map<string|string[]> headers = {}, *ListCommitCommentsQueries queries) returns CommitNote[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/comments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a comment on a commit
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag on which to post a comment
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a comment on a commit 
    # + return - The created commit comment 
    remote isolated function createCommitComment(IdOrPath id, string sha, CreateCommitCommentRequest payload, map<string|string[]> headers = {}) returns CommitNote|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/comments`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a commit sequence
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit SHA
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit sequence 
    remote isolated function getCommitSequence(IdOrPath id, string sha, map<string|string[]> headers = {}, *GetCommitSequenceQueries queries) returns CommitSequence|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/sequence`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Cherry-pick a commit
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag to be cherry-picked
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to cherry-pick a commit 
    # + return - Success 
    remote isolated function cherryPickCommit(IdOrPath id, string sha, CherryPickCommitRequest payload, map<string|string[]> headers = {}) returns Commit|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/cherry_pick`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Revert a commit
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - Commit SHA to revert
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to revert a commit 
    # + return - Success 
    remote isolated function revertCommit(IdOrPath id, string sha, RevertCommitRequest payload, map<string|string[]> headers = {}) returns Commit|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/revert`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all references a commit is pushed to
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit refs 
    remote isolated function listCommitRefs(IdOrPath id, string sha, map<string|string[]> headers = {}, *ListCommitRefsQueries queries) returns BasicRef[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/refs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all merge requests associated with a commit
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag on which to find Merge Requests
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved commit merge requests 
    remote isolated function listCommitMergeRequests(IdOrPath id, string sha, map<string|string[]> headers = {}, *ListCommitMergeRequestsQueries queries) returns MergeRequestBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/merge_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a commit signature
    #
    # + id - The ID or URL-encoded path of the project
    # + sha - A commit sha, or the name of a branch or tag
    # + headers - Headers to be sent with the request 
    # + return - The retrieved commit signature 
    remote isolated function getCommitSignature(IdOrPath id, string sha, map<string|string[]> headers = {}) returns CommitSignature|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/commits/${getEncodedUri(sha)}/signature`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get config.json
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getCargoConfig(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/cargo/config.json`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the sparse index for a Cargo crate (1-character name)
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The cargo package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved cargo index one char 
    remote isolated function getCargoIndexOneChar(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns string|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/cargo/1/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the sparse index for a Cargo crate (2-character name)
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The cargo package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved cargo index two char 
    remote isolated function getCargoIndexTwoChar(IdOrPath id, string packageName, map<string|string[]> headers = {}) returns string|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/cargo/2/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the sparse index for a Cargo crate (3-character name)
    #
    # + id - The ID or URL-encoded path of the project
    # + firstChar - First character of the cargo package name
    # + packageName - The cargo package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved cargo index three char 
    remote isolated function getCargoIndexThreeChar(IdOrPath id, string firstChar, string packageName, map<string|string[]> headers = {}) returns string|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/cargo/3/${getEncodedUri(firstChar)}/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the sparse index for a Cargo crate (4+ character name)
    #
    # + id - The ID or URL-encoded path of the project
    # + prefix1 - First two characters of the cargo package name
    # + prefix2 - Next two characters of the cargo package name
    # + packageName - The cargo package name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved cargo index 
    remote isolated function getCargoIndex(IdOrPath id, string prefix1, string prefix2, string packageName, map<string|string[]> headers = {}) returns string|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/cargo/${getEncodedUri(prefix1)}/${getEncodedUri(prefix2)}/${getEncodedUri(packageName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a Cargo crate
    #
    # + id - The ID or URL-encoded path of the project
    # + packageName - The cargo package name
    # + packageVersion - The cargo package version
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadCargoCrate(IdOrPath id, string packageName, string packageVersion, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/packages/cargo/${getEncodedUri(packageName)}/${getEncodedUri(packageVersion)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all agents
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved agents 
    remote isolated function listAgents(IdOrPath id, map<string|string[]> headers = {}, *ListAgentsQueries queries) returns ClustersAgent[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an agent
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an agent 
    # + return - Created 
    remote isolated function createAgent(IdOrPath id, CreateAgentRequest payload, map<string|string[]> headers = {}) returns ClustersAgent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve details on an agent
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + headers - Headers to be sent with the request 
    # + return - The retrieved agent 
    remote isolated function getAgent(IdOrPath id, int agentId, map<string|string[]> headers = {}) returns ClustersAgent|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an agent
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteAgent(IdOrPath id, int agentId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all agent tokens
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved agent tokens 
    remote isolated function listAgentTokens(IdOrPath id, int agentId, map<string|string[]> headers = {}, *ListAgentTokensQueries queries) returns ClustersAgentTokenBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create an agent token
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an agent token 
    # + return - Created 
    remote isolated function createAgentToken(IdOrPath id, int agentId, CreateAgentTokenRequest payload, map<string|string[]> headers = {}) returns ClustersAgentTokenWithToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/tokens`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an agent token
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + tokenId - The ID of the agent token
    # + headers - Headers to be sent with the request 
    # + return - The retrieved agent token 
    remote isolated function getAgentToken(IdOrPath id, int agentId, int tokenId, map<string|string[]> headers = {}) returns ClustersAgentToken|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Revoke an agent token
    #
    # + id - The ID or URL-encoded path of the project
    # + agentId - The ID of an agent
    # + tokenId - The ID of the agent token
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function revokeAgentToken(IdOrPath id, int agentId, int tokenId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/cluster_agents/${getEncodedUri(agentId)}/tokens/${getEncodedUri(tokenId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all certificate-based clusters
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved certificate based clusters 
    remote isolated function listCertificateBasedClusters(map<string|string[]> headers = {}, *ListCertificateBasedClustersQueries queries) returns DiscoveredClusters[]|error {
        string resourcePath = string `/discover-cert-based-clusters`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all project variables
    #
    # + id - The ID of a project or URL-encoded NAMESPACE/PROJECT_NAME of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project variables 
    remote isolated function listProjectVariables(IdOrPath id, map<string|string[]> headers = {}, *ListProjectVariablesQueries queries) returns CiVariable[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a variable
    #
    # + id - The ID of a project or URL-encoded NAMESPACE/PROJECT_NAME of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a variable 
    # + return - Created 
    remote isolated function createProjectVariable(IdOrPath id, CreateProjectVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a single variable
    #
    # + id - The ID of a project or URL-encoded NAMESPACE/PROJECT_NAME of the project owned by the authenticated user
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project variable 
    remote isolated function getProjectVariable(IdOrPath id, string 'key, map<string|string[]> headers = {}, *GetProjectVariableQueries queries) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a variable
    #
    # + id - The ID of a project or URL-encoded NAMESPACE/PROJECT_NAME of the project owned by the authenticated user
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a variable 
    # + return - The updated project variable 
    remote isolated function updateProjectVariable(IdOrPath id, string 'key, UpdateProjectVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a variable
    #
    # + id - The ID of a project or URL-encoded NAMESPACE/PROJECT_NAME of the project owned by the authenticated user
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function deleteProjectVariable(IdOrPath id, string 'key, map<string|string[]> headers = {}, *DeleteProjectVariableQueries queries) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Trigger a pipeline with a token
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to trigger a pipeline with a token 
    # + return - Created 
    remote isolated function triggerPipeline(IdOrPath id, TriggerPipelineRequest payload, map<string|string[]> headers = {}) returns CiPipeline|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/trigger/pipeline`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Trigger a pipeline with a token
    #
    # + id - The ID or URL-encoded path of the project
    # + ref - The commit sha or name of a branch or tag
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to trigger a pipeline with a token 
    # + return - Created 
    remote isolated function triggerPipelineForRef(IdOrPath id, string ref, TriggerPipelineRequest payload, map<string|string[]> headers = {}) returns CiPipeline|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/ref/${getEncodedUri(ref)}/trigger/pipeline`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all project trigger tokens
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved trigger tokens 
    remote isolated function listTriggerTokens(IdOrPath id, map<string|string[]> headers = {}, *ListTriggerTokensQueries queries) returns Trigger[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/triggers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a trigger token
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a trigger token 
    # + return - Created 
    remote isolated function createTriggerToken(IdOrPath id, CreateTriggerTokenRequest payload, map<string|string[]> headers = {}) returns Trigger|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/triggers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve trigger token details
    #
    # + id - The ID or URL-encoded path of the project
    # + triggerId - The trigger token ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved trigger token 
    remote isolated function getTriggerToken(IdOrPath id, int triggerId, map<string|string[]> headers = {}) returns Trigger|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/triggers/${getEncodedUri(triggerId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a pipeline trigger token
    #
    # + id - The ID or URL-encoded path of the project
    # + triggerId - The trigger token ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a pipeline trigger token 
    # + return - The updated trigger token 
    remote isolated function updateTriggerToken(IdOrPath id, int triggerId, UpdateTriggerTokenRequest payload, map<string|string[]> headers = {}) returns Trigger|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/triggers/${getEncodedUri(triggerId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a pipeline trigger token
    #
    # + id - The ID or URL-encoded path of the project
    # + triggerId - The trigger token ID
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteTriggerToken(IdOrPath id, int triggerId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/triggers/${getEncodedUri(triggerId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List pipelines triggered by the authenticated user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved current user pipelines 
    remote isolated function listCurrentUserPipelines(map<string|string[]> headers = {}, *ListCurrentUserPipelinesQueries queries) returns CiUserPipeline[]|error {
        string resourcePath = string `/pipelines`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all pipeline schedules
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved pipeline schedules 
    remote isolated function listPipelineSchedules(IdOrPath id, map<string|string[]> headers = {}, *ListPipelineSchedulesQueries queries) returns CiPipelineSchedule[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a pipeline schedule 
    # + return - Created 
    remote isolated function createPipelineSchedule(IdOrPath id, CreatePipelineScheduleRequest payload, map<string|string[]> headers = {}) returns CiPipelineScheduleDetails|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pipeline schedule 
    remote isolated function getPipelineSchedule(IdOrPath id, int pipelineScheduleId, map<string|string[]> headers = {}) returns CiPipelineScheduleDetails|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a pipeline schedule 
    # + return - The updated pipeline schedule 
    remote isolated function updatePipelineSchedule(IdOrPath id, int pipelineScheduleId, UpdatePipelineScheduleRequest payload, map<string|string[]> headers = {}) returns CiPipelineScheduleDetails|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deletePipelineSchedule(IdOrPath id, int pipelineScheduleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all pipelines triggered by a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved pipeline schedule pipelines 
    remote isolated function listPipelineSchedulePipelines(IdOrPath id, int pipelineScheduleId, map<string|string[]> headers = {}, *ListPipelineSchedulePipelinesQueries queries) returns CiPipelineBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/pipelines`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create or update ownership of a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function takePipelineScheduleOwnership(IdOrPath id, int pipelineScheduleId, map<string|string[]> headers = {}) returns CiPipelineScheduleDetails|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/take_ownership`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Run a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function runPipelineSchedule(IdOrPath id, int pipelineScheduleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/play`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Create a variable for a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a variable for a pipeline schedule 
    # + return - Created 
    remote isolated function createPipelineScheduleVariable(IdOrPath id, int pipelineScheduleId, CreatePipelineScheduleVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a variable for a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + 'key - The key of the variable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pipeline schedule variable 
    remote isolated function getPipelineScheduleVariable(IdOrPath id, int pipelineScheduleId, string 'key, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a variable for a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + 'key - The key of the variable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a variable for a pipeline schedule 
    # + return - The updated pipeline schedule variable 
    remote isolated function updatePipelineScheduleVariable(IdOrPath id, int pipelineScheduleId, string 'key, UpdatePipelineScheduleVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a variable for a pipeline schedule
    #
    # + id - The ID or URL-encoded path of the project
    # + pipelineScheduleId - The pipeline schedule id
    # + 'key - The key of the variable
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function deletePipelineScheduleVariable(IdOrPath id, int pipelineScheduleId, string 'key, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline_schedules/${getEncodedUri(pipelineScheduleId)}/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all project pipelines
    #
    # + id - The project ID or URL-encoded path
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project pipelines 
    remote isolated function listProjectPipelines(string id, map<string|string[]> headers = {}, *ListProjectPipelinesQueries queries) returns CiPipelineBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a pipeline 
    # + return - Created 
    remote isolated function createPipeline(string id, CreatePipelineRequest payload, map<string|string[]> headers = {}) returns CiPipeline|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipeline`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve the latest pipeline
    #
    # + id - The project ID or URL-encoded path
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved latest pipeline 
    remote isolated function getLatestPipeline(string id, map<string|string[]> headers = {}, *GetLatestPipelineQueries queries) returns CiPipelineWithMetadata|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/latest`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pipeline 
    remote isolated function getPipeline(string id, int pipelineId, map<string|string[]> headers = {}) returns CiPipelineWithMetadata|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - Pipeline was deleted 
    remote isolated function deletePipeline(string id, int pipelineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all jobs by pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved pipeline jobs 
    remote isolated function listPipelineJobs(string id, int pipelineId, map<string|string[]> headers = {}, *ListPipelineJobsQueries queries) returns CiJob[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/jobs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"scope": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all bridge jobs by pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved pipeline bridges 
    # 
    # # Deprecated
    @deprecated
    remote isolated function listPipelineBridges(string id, int pipelineId, map<string|string[]> headers = {}, *ListPipelineBridgesQueries queries) returns CiBridge[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/bridges`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"scope": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all trigger jobs by pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved pipeline trigger jobs 
    remote isolated function listPipelineTriggerJobs(string id, int pipelineId, map<string|string[]> headers = {}, *ListPipelineTriggerJobsQueries queries) returns CiBridge[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/trigger_jobs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"scope": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all pipeline variables
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pipeline variables 
    remote isolated function listPipelineVariables(string id, int pipelineId, map<string|string[]> headers = {}) returns CiVariable[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a test report for a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pipeline test report 
    remote isolated function getPipelineTestReport(string id, int pipelineId, map<string|string[]> headers = {}) returns TestReport|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/test_report`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a test report summary for a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved pipeline test report summary 
    remote isolated function getPipelineTestReportSummary(string id, int pipelineId, map<string|string[]> headers = {}) returns TestReportSummary|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/test_report_summary`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update pipeline metadata
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update pipeline metadata 
    # + return - The updated pipeline metadata 
    remote isolated function updatePipelineMetadata(string id, int pipelineId, UpdatePipelineMetadataRequest payload, map<string|string[]> headers = {}) returns CiPipelineWithMetadata|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/metadata`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retry jobs in a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function retryPipeline(string id, int pipelineId, map<string|string[]> headers = {}) returns CiPipeline|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/retry`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Cancel all jobs for a pipeline
    #
    # + id - The project ID or URL-encoded path
    # + pipelineId - The pipeline ID
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function cancelPipeline(string id, int pipelineId, map<string|string[]> headers = {}) returns CiPipeline|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/pipelines/${getEncodedUri(pipelineId)}/cancel`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all secure files for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the
    # authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved secure files 
    remote isolated function listSecureFiles(IdOrPath id, map<string|string[]> headers = {}, *ListSecureFilesQueries queries) returns CiSecureFile[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/secure_files`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a secure file
    #
    # + id - The ID or URL-encoded path of the project owned by the
    # authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a secure file 
    # + return - Created 
    remote isolated function createSecureFile(IdOrPath id, CreateSecureFileRequest payload, map<string|string[]> headers = {}) returns CiSecureFile|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/secure_files`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve details of a secure file
    #
    # + id - The ID or URL-encoded path of the project owned by the
    # authenticated user
    # + secureFileId - The ID of a secure file
    # + headers - Headers to be sent with the request 
    # + return - The retrieved secure file 
    remote isolated function getSecureFile(IdOrPath id, int secureFileId, map<string|string[]> headers = {}) returns CiSecureFile|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/secure_files/${getEncodedUri(secureFileId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a secure file
    #
    # + id - The ID or URL-encoded path of the project owned by the
    # authenticated user
    # + secureFileId - The ID of a secure file
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteSecureFile(IdOrPath id, int secureFileId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/secure_files/${getEncodedUri(secureFileId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Download a secure file
    #
    # + id - The ID or URL-encoded path of the project owned by the
    # authenticated user
    # + secureFileId - The ID of a secure file
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function downloadSecureFile(IdOrPath id, int secureFileId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/secure_files/${getEncodedUri(secureFileId)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all available runners
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved runners 
    remote isolated function listRunners(map<string|string[]> headers = {}, *ListRunnersQueries queries) returns CiRunner[]|error {
        string resourcePath = string `/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"tag_list": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a runner
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a runner 
    # + return - Created 
    remote isolated function createRunner(CreateRunnerRequest payload, map<string|string[]> headers = {}) returns CiRunnerRegistrationDetails|error {
        string resourcePath = string `/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a registered runner
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteRunnerByToken(map<string|string[]> headers = {}, *DeleteRunnerByTokenQueries queries) returns error? {
        string resourcePath = string `/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all runners
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all runners 
    remote isolated function listAllRunners(map<string|string[]> headers = {}, *ListAllRunnersQueries queries) returns CiRunner[]|error {
        string resourcePath = string `/runners/all`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"tag_list": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve details on a runner
    #
    # + id - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved runner 
    remote isolated function getRunner(int id, map<string|string[]> headers = {}, *GetRunnerQueries queries) returns CiRunnerDetails|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a runner
    #
    # + id - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a runner 
    # + return - The updated runner 
    remote isolated function updateRunner(int id, UpdateRunnerRequest payload, map<string|string[]> headers = {}) returns CiRunnerDetails|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a runner
    #
    # + id - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRunner(int id, map<string|string[]> headers = {}) returns CiRunner|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all managers for a runner
    #
    # + id - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + return - The retrieved runner managers 
    remote isolated function listRunnerManagers(int id, map<string|string[]> headers = {}) returns CiRunnerManager[]|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}/managers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get projects associated with a runner
    #
    # + id - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved runner projects 
    remote isolated function listRunnerProjects(int id, map<string|string[]> headers = {}, *ListRunnerProjectsQueries queries) returns BasicProjectDetails[]|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}/projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all jobs processed by a runner
    #
    # + id - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved runner jobs 
    remote isolated function listRunnerJobs(int id, map<string|string[]> headers = {}, *ListRunnerJobsQueries queries) returns CiJobBasicWithProject[]|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}/jobs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Reset an authentication token for a runner
    #
    # + id - The ID of the runner
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetRunnerAuthToken(int id, map<string|string[]> headers = {}) returns CiResetTokenResult|error {
        string resourcePath = string `/runners/${getEncodedUri(id)}/reset_authentication_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all runners for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project runners 
    remote isolated function listProjectRunners(IdOrPath id, map<string|string[]> headers = {}, *ListProjectRunnersQueries queries) returns CiRunner[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"tag_list": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Assign a runner to a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to assign a runner to a project 
    # + return - Created 
    remote isolated function assignProjectRunner(IdOrPath id, AssignProjectRunnerRequest payload, map<string|string[]> headers = {}) returns CiRunner|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unassign a runner from a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + runnerId - The ID of a runner
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unassignProjectRunner(IdOrPath id, int runnerId, map<string|string[]> headers = {}) returns CiRunner|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/runners/${getEncodedUri(runnerId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all runners in a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group runners 
    remote isolated function listGroupRunners(string id, map<string|string[]> headers = {}, *ListGroupRunnersQueries queries) returns CiRunner[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/runners`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"tag_list": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Reset the runner registration token for the instance
    #
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetInstanceRunnerRegistrationToken(map<string|string[]> headers = {}) returns CiResetTokenResult|error {
        string resourcePath = string `/runners/reset_registration_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset the runner registration token for a project
    #
    # + id - The ID of a project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetProjectRunnerRegistrationToken(string id, map<string|string[]> headers = {}) returns CiResetTokenResult|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/runners/reset_registration_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset the runner registration token for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function resetGroupRunnerRegistrationToken(string id, map<string|string[]> headers = {}) returns CiResetTokenResult|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/runners/reset_registration_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a registered runner manager
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Runner manager was deleted 
    remote isolated function deleteRunnerManager(map<string|string[]> headers = {}, *DeleteRunnerManagerQueries queries) returns error? {
        string resourcePath = string `/runners/managers`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Verify authentication for a registered runner
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to verify authentication for a registered runner 
    # + return - Credentials are valid 
    remote isolated function verifyRunner(VerifyRunnerRequest payload, map<string|string[]> headers = {}) returns CiRunnerRegistrationDetails|error {
        string resourcePath = string `/runners/verify`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Reset a runner authentication token with the current token
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to reset a runner authentication token with the current token 
    # + return - Created 
    remote isolated function resetRunnerAuthTokenByToken(ResetRunnerAuthTokenByTokenRequest payload, map<string|string[]> headers = {}) returns CiResetTokenResult|error {
        string resourcePath = string `/runners/reset_authentication_token`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Discover Job Router information
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function discoverJobRouter(map<string|string[]> headers = {}) returns CiJobRouterDiscoveryInformation|error {
        string resourcePath = string `/runners/router/discovery`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Request a job
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload for request a job 
    # + return - Job was scheduled 
    remote isolated function requestJob(RequestJobRequest payload, map<string|string[]> headers = {}) returns CiJobRequestResponse|error? {
        string resourcePath = string `/jobs/request`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update a job
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a job 
    # + return - Job was updated 
    remote isolated function updateJob(int id, UpdateJobRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/jobs/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Get the runtime environment key for a job
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved job runtime environment key 
    remote isolated function getJobRuntimeEnvironmentKey(int id, map<string|string[]> headers = {}, *GetJobRuntimeEnvironmentKeyQueries queries) returns CiRuntimeEnvironmentKey|error {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/runtime_environment_key`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Append a patch to the job trace
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to append a patch to the job trace 
    # + return - Trace was patched 
    remote isolated function appendJobTrace(int id, AppendJobTraceRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/trace`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Authorize artifacts upload
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to authorize artifacts upload 
    # + return - Upload allowed 
    remote isolated function authorizeRunnerArtifactsUpload(int id, AuthorizeRunnerArtifactsUploadRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/artifacts/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Download job artifacts
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Download allowed 
    remote isolated function downloadRunnerJobArtifacts(int id, map<string|string[]> headers = {}, *DownloadRunnerJobArtifactsQueries queries) returns byte[]|error {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/artifacts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload job artifacts
    #
    # + id - ID of the job
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload job artifacts 
    # + return - Created 
    remote isolated function uploadRunnerJobArtifacts(int id, UploadRunnerJobArtifactsRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/jobs/${getEncodedUri(id)}/artifacts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all resource groups
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved resource groups 
    remote isolated function listResourceGroups(IdOrPath id, map<string|string[]> headers = {}, *ListResourceGroupsQueries queries) returns CiResourceGroup[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/resource_groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a resource group
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + 'key - The key of the resource group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved resource group 
    remote isolated function getResourceGroup(IdOrPath id, string 'key, map<string|string[]> headers = {}) returns CiResourceGroup|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/resource_groups/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a resource group
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + 'key - The key of the resource group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a resource group 
    # + return - The updated resource group 
    remote isolated function updateResourceGroup(IdOrPath id, string 'key, UpdateResourceGroupRequest payload, map<string|string[]> headers = {}) returns CiResourceGroup|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/resource_groups/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve current job for a resource group
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + 'key - The key of the resource group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved resource group current job 
    remote isolated function getResourceGroupCurrentJob(IdOrPath id, string 'key, map<string|string[]> headers = {}) returns CiJobBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/resource_groups/${getEncodedUri('key)}/current_job`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all upcoming jobs for a resource group
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + 'key - The key of the resource group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved resource group upcoming jobs 
    remote isolated function listResourceGroupUpcomingJobs(IdOrPath id, string 'key, map<string|string[]> headers = {}, *ListResourceGroupUpcomingJobsQueries queries) returns CiJobBasic[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/resource_groups/${getEncodedUri('key)}/upcoming_jobs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all jobs for a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project jobs 
    remote isolated function listProjectJobs(IdOrPath id, map<string|string[]> headers = {}, *ListProjectJobsQueries queries) returns CiJob[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"scope": {style: FORM, explode: true}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a job
    #
    # + jobId - The ID of a job
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - The retrieved job 
    remote isolated function getJob(int jobId, IdOrPath id, map<string|string[]> headers = {}) returns CiJob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get a trace of a specific job of a project
    #
    # + id - The ID or URL-encoded path of the project
    # + jobId - The ID of a job
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved job trace 
    remote isolated function getJobTrace(IdOrPath id, int jobId, map<string|string[]> headers = {}, *GetJobTraceQueries queries) returns CiJob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/trace`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Cancel a job
    #
    # + jobId - The ID of a job
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to cancel a job 
    # + return - Created 
    remote isolated function cancelJob(int jobId, IdOrPath id, CancelJobRequest payload, map<string|string[]> headers = {}) returns CiJob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/cancel`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retry a job
    #
    # + jobId - The ID of a job
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to retry a job 
    # + return - Created 
    remote isolated function retryJob(int jobId, IdOrPath id, RetryJobRequest payload, map<string|string[]> headers = {}) returns CiJob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/retry`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Erase a job
    #
    # + jobId - The ID of a build
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function eraseJob(int jobId, IdOrPath id, map<string|string[]> headers = {}) returns CiJob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/erase`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Run a manual job
    #
    # + jobId - The ID of a Job
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to run a job 
    # + return - Success 
    remote isolated function runManualJob(int jobId, IdOrPath id, RunManualJobRequest payload, map<string|string[]> headers = {}) returns CiJobBasic|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/play`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a job by job token
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved job by token 
    remote isolated function getJobByToken(map<string|string[]> headers = {}) returns CiJob|error {
        string resourcePath = string `/job`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all GitLab agents for Kubernetes by job token
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved job allowed agents 
    remote isolated function listJobAllowedAgents(map<string|string[]> headers = {}) returns CiJob[]|error {
        string resourcePath = string `/job/allowed_agents`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all custom attributes for a group
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group custom attributes 
    remote isolated function listGroupCustomAttributes(IdOrPath id, map<string|string[]> headers = {}) returns CustomAttribute[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/custom_attributes`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a custom attribute for a group
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group custom attribute 
    remote isolated function getGroupCustomAttribute(string 'key, IdOrPath id, map<string|string[]> headers = {}) returns CustomAttribute|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Creates or updates a custom attribute for a group
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create or updates a custom attribute for a group 
    # + return - Success 
    remote isolated function setGroupCustomAttribute(string 'key, IdOrPath id, SetCustomAttributeRequest payload, map<string|string[]> headers = {}) returns CustomAttribute|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a custom attribute for a group
    #
    # + 'key - The key of the custom attribute
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupCustomAttribute(string 'key, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/custom_attributes/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all groups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved groups 
    remote isolated function listGroups(map<string|string[]> headers = {}, *ListGroupsQueries queries) returns Group[]|error {
        string resourcePath = string `/groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"skip_groups": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a group
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a group 
    # + return - Created 
    remote isolated function createGroup(CreateGroupRequest payload, map<string|string[]> headers = {}) returns Group|error {
        string resourcePath = string `/groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group 
    remote isolated function getGroup(string id, map<string|string[]> headers = {}, *GetGroupQueries queries) returns GroupDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update group attributes
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update group attributes 
    # + return - The updated group 
    remote isolated function updateGroup(string id, UpdateGroupRequest payload, map<string|string[]> headers = {}) returns Group|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Schedule a group for deletion
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function deleteGroup(string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Archive a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function archiveGroup(string id, map<string|string[]> headers = {}) returns Group|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/archive`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Unarchive a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unarchiveGroup(string id, map<string|string[]> headers = {}) returns Group|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/unarchive`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Restore a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function restoreGroup(string id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/restore`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all shared groups
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group shared groups 
    remote isolated function listGroupSharedGroups(string id, map<string|string[]> headers = {}, *ListGroupSharedGroupsQueries queries) returns Group[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/groups/shared`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"skip_groups": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all invited groups
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved invited groups 
    remote isolated function listInvitedGroups(string id, map<string|string[]> headers = {}, *ListInvitedGroupsQueries queries) returns Group[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/invited_groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"relation": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all projects in a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group projects 
    remote isolated function listGroupProjects(string id, map<string|string[]> headers = {}, *ListGroupProjectsQueries queries) returns Project[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/projects`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all shared projects
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved shared projects 
    remote isolated function listSharedProjects(string id, map<string|string[]> headers = {}, *ListSharedProjectsQueries queries) returns Project[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/projects/shared`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all subgroups
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group subgroups 
    remote isolated function listGroupSubgroups(string id, map<string|string[]> headers = {}, *ListGroupSubgroupsQueries queries) returns Group[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/subgroups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"skip_groups": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all descendant groups
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved descendant groups 
    remote isolated function listDescendantGroups(string id, map<string|string[]> headers = {}, *ListDescendantGroupsQueries queries) returns Group[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/descendant_groups`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<Encoding> queryParamEncoding = {"skip_groups": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Transfer a project to a group
    #
    # + id - The ID of a group
    # + projectId - The ID or path of the project
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function transferProjectToGroup(string id, string projectId, map<string|string[]> headers = {}) returns GroupDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/projects/${getEncodedUri(projectId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all transfer locations for a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group transfer locations 
    remote isolated function listGroupTransferLocations(string id, map<string|string[]> headers = {}, *ListGroupTransferLocationsQueries queries) returns Group[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/transfer_locations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Transfer a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to transfer a group 
    # + return - Created 
    remote isolated function transferGroup(string id, TransferGroupRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/transfer`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Transfer a group to an organization
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to transfer a group to an organization 
    # + return - Created 
    remote isolated function transferGroupToOrganization(string id, TransferGroupToOrganizationRequest payload, map<string|string[]> headers = {}) returns GroupDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/transfer_to_organization`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Add a group to a group
    #
    # + id - The ID of a group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a group to a group 
    # + return - Created 
    remote isolated function shareGroupWithGroup(string id, ShareGroupWithGroupRequest payload, map<string|string[]> headers = {}) returns GroupDetail|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/share`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Remove a group from a group
    #
    # + id - The ID of a group
    # + groupId - The ID of the shared group
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function unshareGroupWithGroup(string id, int groupId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/share/${getEncodedUri(groupId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Remove a shared project from a group
    #
    # + id - The ID of a group
    # + projectId - The ID of the shared project
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function removeGroupSharedProject(string id, int projectId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/shared_projects/${getEncodedUri(projectId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Sync a group with LDAP
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function syncGroupLdap(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ldap_sync`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group audit events
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group audit events 
    remote isolated function listGroupAuditEvents(IdOrPath id, map<string|string[]> headers = {}, *ListGroupAuditEventsQueries queries) returns AuditEvent[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/audit_events`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a group audit event
    #
    # + auditEventId - The ID of the audit event
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group audit event 
    remote isolated function getGroupAuditEvent(int auditEventId, IdOrPath id, map<string|string[]> headers = {}) returns AuditEvent|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/audit_events/${getEncodedUri(auditEventId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all SAML users
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved saml users 
    remote isolated function listSamlUsers(IdOrPath id, map<string|string[]> headers = {}, *ListSamlUsersQueries queries) returns UserPublic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/saml_users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all provisioned users
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved provisioned users 
    remote isolated function listProvisionedUsers(IdOrPath id, map<string|string[]> headers = {}, *ListProvisionedUsersQueries queries) returns UserPublic[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/provisioned_users`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group SSH certificates
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group ssh certificates 
    remote isolated function listGroupSshCertificates(IdOrPath id, map<string|string[]> headers = {}, *ListGroupSshCertificatesQueries queries) returns SshCertificate[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ssh_certificates`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add a group SSH certificate
    #
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add a group SSH certificate 
    # + return - Created 
    remote isolated function addGroupSshCertificate(IdOrPath id, AddGroupSshCertificateRequest payload, map<string|string[]> headers = {}) returns SshCertificate|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ssh_certificates`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete a group SSH certificate
    #
    # + sshCertificatesId - The ID of a group SSH certificate
    # + id - The ID or URL-encoded path of the group
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteGroupSshCertificate(int sshCertificatesId, IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/ssh_certificates/${getEncodedUri(sshCertificatesId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve job artifacts
    #
    # + id - The ID or URL-encoded path of the project
    # + refName - Branch or tag name in repository. `HEAD` or `SHA` references are not supported
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadJobArtifactsByRef(IdOrPath id, string refName, map<string|string[]> headers = {}, *DownloadJobArtifactsByRefQueries queries) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/artifacts/${getEncodedUri(refName)}/download`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a specific file from artifacts archive from a ref
    #
    # + id - The ID or URL-encoded path of the project
    # + refName - Branch or tag name in repository. `HEAD` or `SHA` references are not supported
    # + artifactPath - Path to a file inside the artifacts archive
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadJobArtifactFileByRef(IdOrPath id, string refName, string artifactPath, map<string|string[]> headers = {}, *DownloadJobArtifactFileByRefQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/artifacts/${getEncodedUri(refName)}/raw/${getEncodedUri(artifactPath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download an artifact from a job
    #
    # + id - The ID or URL-encoded path of the project
    # + jobId - The ID of a job
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadJobArtifacts(IdOrPath id, int jobId, map<string|string[]> headers = {}, *DownloadJobArtifactsQueries queries) returns byte[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/artifacts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete job artifacts
    #
    # + id - The ID or URL-encoded path of the project
    # + jobId - The ID of a job
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteJobArtifacts(IdOrPath id, int jobId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/artifacts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all files in an artifacts archive
    #
    # + id - The ID or URL-encoded path of the project
    # + jobId - ID of a job
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved job artifact files 
    remote isolated function listJobArtifactFiles(IdOrPath id, int jobId, map<string|string[]> headers = {}, *ListJobArtifactFilesQueries queries) returns CiJobArtifactEntry[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/artifacts/tree`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Download a specific file from artifacts archive
    #
    # + id - The ID or URL-encoded path of the project
    # + jobId - The ID of a job
    # + artifactPath - Path to a file inside the artifacts archive
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function downloadJobArtifactFile(IdOrPath id, int jobId, string artifactPath, map<string|string[]> headers = {}, *DownloadJobArtifactFileQueries queries) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/artifacts/${getEncodedUri(artifactPath)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retain job artifacts
    #
    # + id - The ID or URL-encoded path of the project
    # + jobId - The ID of a job
    # + headers - Headers to be sent with the request 
    # + return - Created 
    remote isolated function keepJobArtifacts(IdOrPath id, int jobId, map<string|string[]> headers = {}) returns CiJob|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/jobs/${getEncodedUri(jobId)}/artifacts/keep`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete all job artifacts in a project
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - Accepted 
    remote isolated function deleteProjectJobArtifacts(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/artifacts`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Publish a new component project release as version to the CI/CD catalog
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to publish a new component project release as version to the CI/CD catalog 
    # + return - Created 
    remote isolated function publishCatalogRelease(IdOrPath id, PublishCatalogReleaseRequest payload, map<string|string[]> headers = {}) returns CiCatalogResourcesVersion|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/catalog/publish`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group or project migrations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group or project migrations 
    remote isolated function listGroupOrProjectMigrations(map<string|string[]> headers = {}, *ListGroupOrProjectMigrationsQueries queries) returns BulkImport[]|error {
        string resourcePath = string `/bulk_imports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Start a group or project migration
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to start a group or project migration 
    # + return - Created 
    remote isolated function startGroupOrProjectMigration(StartGroupOrProjectMigrationRequest payload, map<string|string[]> headers = {}) returns BulkImport|error {
        string resourcePath = string `/bulk_imports`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        string encodedRequestBody = createFormURLEncodedRequestBody(check jsondata:toJson(payload).ensureType());
        request.setPayload(encodedRequestBody, "application/x-www-form-urlencoded");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all group or project migration entities
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved all migration entities 
    remote isolated function listAllMigrationEntities(map<string|string[]> headers = {}, *ListAllMigrationEntitiesQueries queries) returns BulkImportsEntity[]|error {
        string resourcePath = string `/bulk_imports/entities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a group or project migration
    #
    # + importId - The ID of user's GitLab Migration
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group or project migration 
    remote isolated function getGroupOrProjectMigration(int importId, map<string|string[]> headers = {}) returns BulkImport|error {
        string resourcePath = string `/bulk_imports/${getEncodedUri(importId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all group or project migration entities
    #
    # + importId - The ID of user's GitLab Migration
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved migration entities 
    remote isolated function listMigrationEntities(int importId, map<string|string[]> headers = {}, *ListMigrationEntitiesQueries queries) returns BulkImportsEntity[]|error {
        string resourcePath = string `/bulk_imports/${getEncodedUri(importId)}/entities`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a group or project migration entity
    #
    # + importId - The ID of user's GitLab Migration
    # + entityId - The ID of GitLab Migration entity
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group or project migration entity 
    remote isolated function getGroupOrProjectMigrationEntity(int importId, int entityId, map<string|string[]> headers = {}) returns BulkImportsEntity|error {
        string resourcePath = string `/bulk_imports/${getEncodedUri(importId)}/entities/${getEncodedUri(entityId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all failed import records for a migration entity
    #
    # + importId - The ID of user's GitLab Migration
    # + entityId - The ID of GitLab Migration entity
    # + headers - Headers to be sent with the request 
    # + return - The retrieved migration entity failures 
    remote isolated function listMigrationEntityFailures(int importId, int entityId, map<string|string[]> headers = {}) returns BulkImportsEntityFailure[]|error {
        string resourcePath = string `/bulk_imports/${getEncodedUri(importId)}/entities/${getEncodedUri(entityId)}/failures`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Cancel a migration
    #
    # + importId - The ID of user's GitLab Migration
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function cancelMigration(int importId, map<string|string[]> headers = {}) returns BulkImport|error {
        string resourcePath = string `/bulk_imports/${getEncodedUri(importId)}/cancel`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all repository branches
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved repository branches 
    remote isolated function listRepositoryBranches(IdOrPath id, map<string|string[]> headers = {}, *ListRepositoryBranchesQueries queries) returns Branch[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a repository branch
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a repository branch 
    # + return - Created 
    remote isolated function createRepositoryBranch(IdOrPath id, CreateRepositoryBranchRequest payload, map<string|string[]> headers = {}) returns Branch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a repository branch
    #
    # + id - The ID or URL-encoded path of the project
    # + branch - The name of the branch
    # + headers - Headers to be sent with the request 
    # + return - The retrieved repository branch 
    remote isolated function getRepositoryBranch(IdOrPath id, string branch, map<string|string[]> headers = {}) returns Branch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches/${getEncodedUri(branch)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete a repository branch
    #
    # + id - The ID or URL-encoded path of the project
    # + branch - The name of the branch
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteRepositoryBranch(IdOrPath id, string branch, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches/${getEncodedUri(branch)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Check if a branch exists
    #
    # + id - The ID or URL-encoded path of the project
    # + branch - The name of the branch
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function checkRepositoryBranch(IdOrPath id, string branch, map<string|string[]> headers = {}) returns http:Response|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches/${getEncodedUri(branch)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->head(resourcePath, httpHeaders);
    }

    # Protect a single branch
    #
    # + id - The ID or URL-encoded path of the project
    # + branch - The name of the branch
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to protect a single branch 
    # + return - Success 
    remote isolated function protectRepositoryBranch(IdOrPath id, string branch, ProtectRepositoryBranchRequest payload, map<string|string[]> headers = {}) returns Branch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches/${getEncodedUri(branch)}/protect`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Unprotect a single branch
    #
    # + id - The ID or URL-encoded path of the project
    # + branch - The name of the branch
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unprotectRepositoryBranch(IdOrPath id, string branch, map<string|string[]> headers = {}) returns Branch|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/branches/${getEncodedUri(branch)}/unprotect`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete all merged branches
    #
    # + id - The ID or URL-encoded path of the project
    # + headers - Headers to be sent with the request 
    # + return - 202 Accepted 
    remote isolated function deleteMergedBranches(IdOrPath id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/repository/merged_branches`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all badges for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group badges 
    remote isolated function listGroupBadges(string id, map<string|string[]> headers = {}, *ListGroupBadgesQueries queries) returns Badge[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/badges`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a badge for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a badge for a group 
    # + return - Created 
    remote isolated function createGroupBadge(string id, CreateBadgeRequest payload, map<string|string[]> headers = {}) returns Badge|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/badges`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a badge preview for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function previewGroupBadge(string id, map<string|string[]> headers = {}, *PreviewGroupBadgeQueries queries) returns BasicBadgeDetails|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/badges/render`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a badge for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + badgeId - The badge ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved group badge 
    remote isolated function getGroupBadge(string id, int badgeId, map<string|string[]> headers = {}) returns Badge|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/badges/${getEncodedUri(badgeId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a badge for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + badgeId - The badge ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a badge for a group 
    # + return - The updated group badge 
    remote isolated function updateGroupBadge(string id, int badgeId, UpdateBadgeRequest payload, map<string|string[]> headers = {}) returns Badge|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/badges/${getEncodedUri(badgeId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a badge from a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + badgeId - The badge ID
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteGroupBadge(string id, int badgeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/badges/${getEncodedUri(badgeId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all badges for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project badges 
    remote isolated function listProjectBadges(string id, map<string|string[]> headers = {}, *ListProjectBadgesQueries queries) returns Badge[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/badges`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a badge for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a badge for a project 
    # + return - Created 
    remote isolated function createProjectBadge(string id, CreateBadgeRequest payload, map<string|string[]> headers = {}) returns Badge|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/badges`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a badge preview for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function previewProjectBadge(string id, map<string|string[]> headers = {}, *PreviewProjectBadgeQueries queries) returns BasicBadgeDetails|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/badges/render`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a badge for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + badgeId - The badge ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved project badge 
    remote isolated function getProjectBadge(string id, int badgeId, map<string|string[]> headers = {}) returns Badge|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/badges/${getEncodedUri(badgeId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a badge for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + badgeId - The badge ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a badge for a project 
    # + return - The updated project badge 
    remote isolated function updateProjectBadge(string id, int badgeId, UpdateBadgeRequest payload, map<string|string[]> headers = {}) returns Badge|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/badges/${getEncodedUri(badgeId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a badge from a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + badgeId - The badge ID
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function deleteProjectBadge(string id, int badgeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/badges/${getEncodedUri(badgeId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - ID (`iid` for merge requests/issues/epics, `id` for snippets) of an awardable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue emoji reactions 
    remote isolated function listIssueEmojiReactions(IdOrPath id, int issueIid, map<string|string[]> headers = {}, *ListIssueEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to an issue
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to an issue 
    # + return - Created 
    remote isolated function addIssueEmojiReaction(IdOrPath id, int issueIid, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from an issue
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue emoji reaction 
    remote isolated function getIssueEmojiReaction(int awardId, IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from an issue
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteIssueEmojiReaction(int awardId, IdOrPath id, int issueIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for an issue comment
    #
    # + noteId - The ID of a note
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved issue note emoji reactions 
    remote isolated function listIssueNoteEmojiReactions(int noteId, IdOrPath id, int issueIid, map<string|string[]> headers = {}, *ListIssueNoteEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to an issue comment
    #
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to an issue comment 
    # + return - Created 
    remote isolated function addIssueNoteEmojiReaction(IdOrPath id, int issueIid, int noteId, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from an issue comment
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved issue note emoji reaction 
    remote isolated function getIssueNoteEmojiReaction(int awardId, IdOrPath id, int issueIid, int noteId, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from an issue comment
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + issueIid - The internal ID of an issuable
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteIssueNoteEmojiReaction(int awardId, IdOrPath id, int issueIid, int noteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/issues/${getEncodedUri(issueIid)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - ID (`iid` for merge requests/issues/epics, `id` for snippets) of an awardable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request emoji reactions 
    remote isolated function listMergeRequestEmojiReactions(IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to a merge request
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to a merge request 
    # + return - Created 
    remote isolated function addMergeRequestEmojiReaction(IdOrPath id, int mergeRequestIid, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from a merge request
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request emoji reaction 
    remote isolated function getMergeRequestEmojiReaction(int awardId, IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from a merge request
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteMergeRequestEmojiReaction(int awardId, IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for a merge request comment
    #
    # + noteId - The ID of a note
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved merge request note emoji reactions 
    remote isolated function listMergeRequestNoteEmojiReactions(int noteId, IdOrPath id, int mergeRequestIid, map<string|string[]> headers = {}, *ListMergeRequestNoteEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to a merge request comment
    #
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to a merge request comment 
    # + return - Created 
    remote isolated function addMergeRequestNoteEmojiReaction(IdOrPath id, int mergeRequestIid, int noteId, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from a merge request comment
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved merge request note emoji reaction 
    remote isolated function getMergeRequestNoteEmojiReaction(int awardId, IdOrPath id, int mergeRequestIid, int noteId, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from a merge request comment
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + mergeRequestIid - The IID of a merge request
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteMergeRequestNoteEmojiReaction(int awardId, IdOrPath id, int mergeRequestIid, int noteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/merge_requests/${getEncodedUri(mergeRequestIid)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for a snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - ID (`iid` for merge requests/issues/epics, `id` for snippets) of an awardable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved snippet emoji reactions 
    remote isolated function listSnippetEmojiReactions(IdOrPath id, int snippetId, map<string|string[]> headers = {}, *ListSnippetEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to a snippet
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to a snippet 
    # + return - Created 
    remote isolated function addSnippetEmojiReaction(IdOrPath id, int snippetId, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from a snippet
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet emoji reaction 
    remote isolated function getSnippetEmojiReaction(int awardId, IdOrPath id, int snippetId, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from a snippet
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteSnippetEmojiReaction(int awardId, IdOrPath id, int snippetId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for a snippet comment
    #
    # + noteId - The ID of a note
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved snippet note emoji reactions 
    remote isolated function listSnippetNoteEmojiReactions(int noteId, IdOrPath id, int snippetId, map<string|string[]> headers = {}, *ListSnippetNoteEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to a snippet comment
    #
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to a snippet comment 
    # + return - Created 
    remote isolated function addSnippetNoteEmojiReaction(IdOrPath id, int snippetId, int noteId, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from a snippet comment
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved snippet note emoji reaction 
    remote isolated function getSnippetNoteEmojiReaction(int awardId, IdOrPath id, int snippetId, int noteId, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from a snippet comment
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the project
    # + snippetId - The ID of a project snippet
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteSnippetNoteEmojiReaction(int awardId, IdOrPath id, int snippetId, int noteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/snippets/${getEncodedUri(snippetId)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for an epic
    #
    # + id - The ID or URL-encoded path of the group
    # + epicIid - ID (`iid` for merge requests/issues/epics, `id` for snippets) of an awardable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved epic emoji reactions 
    remote isolated function listEpicEmojiReactions(IdOrPath id, int epicIid, map<string|string[]> headers = {}, *ListEpicEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to an epic
    #
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to an epic 
    # + return - Created 
    remote isolated function addEpicEmojiReaction(IdOrPath id, int epicIid, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from an epic
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic emoji reaction 
    remote isolated function getEpicEmojiReaction(int awardId, IdOrPath id, int epicIid, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from an epic
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteEpicEmojiReaction(int awardId, IdOrPath id, int epicIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all emoji reactions for an epic comment
    #
    # + noteId - The ID of a note
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved epic note emoji reactions 
    remote isolated function listEpicNoteEmojiReactions(int noteId, IdOrPath id, int epicIid, map<string|string[]> headers = {}, *ListEpicNoteEmojiReactionsQueries queries) returns AwardEmoji[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Add an emoji reaction to an epic comment
    #
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to add an emoji reaction to an epic comment 
    # + return - Created 
    remote isolated function addEpicNoteEmojiReaction(IdOrPath id, int epicIid, int noteId, AddEmojiReactionRequest payload, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/notes/${getEncodedUri(noteId)}/award_emoji`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve an emoji reaction from an epic comment
    #
    # + awardId - ID of the emoji reaction
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - The retrieved epic note emoji reaction 
    remote isolated function getEpicNoteEmojiReaction(int awardId, IdOrPath id, int epicIid, int noteId, map<string|string[]> headers = {}) returns AwardEmoji|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete an emoji reaction from an epic comment
    #
    # + awardId - ID of an emoji reaction
    # + id - The ID or URL-encoded path of the group
    # + epicIid - The internal ID of a group epic
    # + noteId - The ID of a note
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteEpicNoteEmojiReaction(int awardId, IdOrPath id, int epicIid, int noteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/epics/${getEncodedUri(epicIid)}/notes/${getEncodedUri(noteId)}/award_emoji/${getEncodedUri(awardId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Return avatar url for a user
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved avatar url 
    remote isolated function getAvatarUrl(map<string|string[]> headers = {}, *GetAvatarUrlQueries queries) returns Avatar|error {
        string resourcePath = string `/avatar`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Return the OIDC ID-token claims for the caller identified by an IAM OAuth token
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getOidcIdTokenClaims(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/iam/userinfo`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get applications
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved applications 
    remote isolated function listApplications(map<string|string[]> headers = {}) returns Application[]|error {
        string resourcePath = string `/applications`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a new application
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a new application 
    # + return - The created application 
    remote isolated function createApplication(CreateApplicationRequest payload, map<string|string[]> headers = {}) returns ApplicationWithSecret|error {
        string resourcePath = string `/applications`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Delete an application
    #
    # + id - The ID of the application (not the application_id)
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteApplication(int id, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/applications/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Renew an application secret
    #
    # + id - The ID of the application (not the application_id)
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function renewApplicationSecret(int id, map<string|string[]> headers = {}) returns ApplicationWithSecret|error {
        string resourcePath = string `/applications/${getEncodedUri(id)}/renew-secret`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get the current appearance
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved appearance 
    remote isolated function getAppearance(map<string|string[]> headers = {}) returns Appearance|error {
        string resourcePath = string `/application/appearance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Modify appearance
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to modify appearance 
    # + return - The updated appearance 
    remote isolated function updateAppearance(UpdateAppearanceRequest payload, map<string|string[]> headers = {}) returns Appearance|error {
        string resourcePath = string `/application/appearance`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Workhorse authorize metric image file upload
    #
    # + id - The ID or URL-encoded path of the project
    # + alertIid - The IID of the Alert
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function authorizeAlertMetricImageUpload(IdOrPath id, int alertIid, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/alert_management_alerts/${getEncodedUri(alertIid)}/metric_images/authorize`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all metric images
    #
    # + id - The ID or URL-encoded path of the project
    # + alertIid - The IID of the Alert
    # + headers - Headers to be sent with the request 
    # + return - The retrieved alert metric images 
    remote isolated function listAlertMetricImages(IdOrPath id, int alertIid, map<string|string[]> headers = {}) returns MetricImage[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/alert_management_alerts/${getEncodedUri(alertIid)}/metric_images`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Upload a metric image
    #
    # + id - The ID or URL-encoded path of the project
    # + alertIid - The IID of the Alert
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to upload a metric image 
    # + return - Success 
    remote isolated function uploadAlertMetricImage(IdOrPath id, int alertIid, UploadAlertMetricImageRequest payload, map<string|string[]> headers = {}) returns MetricImage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/alert_management_alerts/${getEncodedUri(alertIid)}/metric_images`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Update a metric image
    #
    # + id - The ID or URL-encoded path of the project
    # + alertIid - The IID of the Alert
    # + metricImageId - The ID of metric image
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a metric image 
    # + return - The updated alert metric image 
    remote isolated function updateAlertMetricImage(IdOrPath id, int alertIid, int metricImageId, UpdateAlertMetricImageRequest payload, map<string|string[]> headers = {}) returns MetricImage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/alert_management_alerts/${getEncodedUri(alertIid)}/metric_images/${getEncodedUri(metricImageId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a metric image
    #
    # + id - The ID or URL-encoded path of the project
    # + alertIid - The IID of the Alert
    # + metricImageId - The ID of metric image
    # + headers - Headers to be sent with the request 
    # + return - No Content 
    remote isolated function deleteAlertMetricImage(IdOrPath id, int alertIid, int metricImageId, map<string|string[]> headers = {}) returns MetricImage|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/alert_management_alerts/${getEncodedUri(alertIid)}/metric_images/${getEncodedUri(metricImageId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get current plan limits
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved plan limits 
    remote isolated function getPlanLimits(map<string|string[]> headers = {}, *GetPlanLimitsQueries queries) returns PlanLimit|error {
        string resourcePath = string `/application/plan_limits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Change plan limits
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to change plan limits 
    # + return - The updated plan limits 
    remote isolated function updatePlanLimits(UpdatePlanLimitsRequest payload, map<string|string[]> headers = {}) returns PlanLimit|error {
        string resourcePath = string `/application/plan_limits`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all pending migrations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - 200 OK 
    remote isolated function listPendingMigrations(map<string|string[]> headers = {}, *ListPendingMigrationsQueries queries) returns error? {
        string resourcePath = string `/admin/migrations/pending`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update status of a migration
    #
    # + timestamp - The migration version timestamp
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update status of a migration 
    # + return - 201 Created 
    remote isolated function markMigration(int timestamp, DatabaseOperationRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/admin/migrations/${getEncodedUri(timestamp)}/mark`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # List all instance clusters
    #
    # + headers - Headers to be sent with the request 
    # + return - The retrieved instance clusters 
    remote isolated function listInstanceClusters(map<string|string[]> headers = {}) returns Cluster[]|error {
        string resourcePath = string `/admin/clusters`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a single instance cluster
    #
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved instance cluster 
    remote isolated function getInstanceCluster(int clusterId, map<string|string[]> headers = {}) returns Cluster|error {
        string resourcePath = string `/admin/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an instance cluster
    #
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an instance cluster 
    # + return - The updated instance cluster 
    remote isolated function updateInstanceCluster(int clusterId, UpdateInstanceClusterRequest payload, map<string|string[]> headers = {}) returns Cluster|error {
        string resourcePath = string `/admin/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete instance cluster
    #
    # + clusterId - The cluster ID
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteInstanceCluster(int clusterId, map<string|string[]> headers = {}) returns Cluster|error {
        string resourcePath = string `/admin/clusters/${getEncodedUri(clusterId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Create an instance cluster
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create an instance cluster 
    # + return - Created 
    remote isolated function createInstanceCluster(AddClusterRequest payload, map<string|string[]> headers = {}) returns Cluster|error {
        string resourcePath = string `/admin/clusters/add`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve dictionary details
    #
    # + databaseName - The database name
    # + tableName - The table name
    # + headers - Headers to be sent with the request 
    # + return - The retrieved dictionary table 
    remote isolated function getDictionaryTable("main"|"ci" databaseName, string tableName, map<string|string[]> headers = {}) returns DictionaryTable|error {
        string resourcePath = string `/admin/databases/${getEncodedUri(databaseName)}/dictionary/tables/${getEncodedUri(tableName)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # List all instance variables
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved instance variables 
    remote isolated function listInstanceVariables(map<string|string[]> headers = {}, *ListInstanceVariablesQueries queries) returns CiVariable[]|error {
        string resourcePath = string `/admin/ci/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create instance variable
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create instance variable 
    # + return - Created 
    remote isolated function createInstanceVariable(CreateInstanceVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/admin/ci/variables`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve instance variable details
    #
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved instance variable 
    remote isolated function getInstanceVariable(string 'key, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/admin/ci/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update an instance variable
    #
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update an instance variable 
    # + return - The updated instance variable 
    remote isolated function updateInstanceVariable(string 'key, UpdateInstanceVariableRequest payload, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/admin/ci/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete instance variable
    #
    # + 'key - The key of a variable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteInstanceVariable(string 'key, map<string|string[]> headers = {}) returns CiVariable|error {
        string resourcePath = string `/admin/ci/variables/${getEncodedUri('key)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all broadcast messages
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved broadcast messages 
    remote isolated function listBroadcastMessages(map<string|string[]> headers = {}, *ListBroadcastMessagesQueries queries) returns SystemBroadcastMessage[]|error {
        string resourcePath = string `/broadcast_messages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create a broadcast message
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to create a broadcast message 
    # + return - Created 
    remote isolated function createBroadcastMessage(CreateBroadcastMessageRequest payload, map<string|string[]> headers = {}) returns SystemBroadcastMessage|error {
        string resourcePath = string `/broadcast_messages`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Retrieve a broadcast message
    #
    # + id - Broadcast message ID
    # + headers - Headers to be sent with the request 
    # + return - The retrieved broadcast message 
    remote isolated function getBroadcastMessage(int id, map<string|string[]> headers = {}) returns SystemBroadcastMessage|error {
        string resourcePath = string `/broadcast_messages/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update a broadcast message
    #
    # + id - Broadcast message ID
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to update a broadcast message 
    # + return - The updated broadcast message 
    remote isolated function updateBroadcastMessage(int id, UpdateBroadcastMessageRequest payload, map<string|string[]> headers = {}) returns SystemBroadcastMessage|error {
        string resourcePath = string `/broadcast_messages/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Delete a broadcast message
    #
    # + id - Broadcast message ID
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteBroadcastMessage(int id, map<string|string[]> headers = {}) returns SystemBroadcastMessage|error {
        string resourcePath = string `/broadcast_messages/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Get the list of batched background operations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved batched background operations 
    remote isolated function listBatchedBackgroundOperations(map<string|string[]> headers = {}, *ListBatchedBackgroundOperationsQueries queries) returns BatchedBackgroundOperation[]|error {
        string resourcePath = string `/admin/batched_background_operations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Retrieve a batched background operation
    #
    # + id - The batched background operation id
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved batched background operation 
    remote isolated function getBatchedBackgroundOperation(int id, map<string|string[]> headers = {}, *GetBatchedBackgroundOperationQueries queries) returns BatchedBackgroundOperation|error {
        string resourcePath = string `/admin/batched_background_operations/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Stop a batched background operation
    #
    # + id - The batched background operation id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to stop a batched background operation 
    # + return - Success 
    remote isolated function stopBatchedBackgroundOperation(int id, DatabaseOperationRequest payload, map<string|string[]> headers = {}) returns BatchedBackgroundOperation|error {
        string resourcePath = string `/admin/batched_background_operations/${getEncodedUri(id)}/stop`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Restart a batched background operation
    #
    # + id - The batched background operation id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to restart a batched background operation 
    # + return - Success 
    remote isolated function restartBatchedBackgroundOperation(int id, DatabaseOperationRequest payload, map<string|string[]> headers = {}) returns BatchedBackgroundOperation|error {
        string resourcePath = string `/admin/batched_background_operations/${getEncodedUri(id)}/restart`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Retrieve a batched background migration
    #
    # + id - The batched background migration id
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved batched background migration 
    remote isolated function getBatchedBackgroundMigration(int id, map<string|string[]> headers = {}, *GetBatchedBackgroundMigrationQueries queries) returns BatchedBackgroundMigration|error {
        string resourcePath = string `/admin/batched_background_migrations/${getEncodedUri(id)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the list of batched background migrations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved batched background migrations 
    remote isolated function listBatchedBackgroundMigrations(map<string|string[]> headers = {}, *ListBatchedBackgroundMigrationsQueries queries) returns BatchedBackgroundMigration[]|error {
        string resourcePath = string `/admin/batched_background_migrations`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Resume a batched background migration
    #
    # + id - The batched background migration id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to resume a batched background migration 
    # + return - Success 
    remote isolated function resumeBatchedBackgroundMigration(int id, DatabaseOperationRequest payload, map<string|string[]> headers = {}) returns BatchedBackgroundMigration|error {
        string resourcePath = string `/admin/batched_background_migrations/${getEncodedUri(id)}/resume`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Pause a batched background migration
    #
    # + id - The batched background migration id
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to pause a batched background migration 
    # + return - Success 
    remote isolated function pauseBatchedBackgroundMigration(int id, DatabaseOperationRequest payload, map<string|string[]> headers = {}) returns BatchedBackgroundMigration|error {
        string resourcePath = string `/admin/batched_background_migrations/${getEncodedUri(id)}/pause`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # List all access requests for a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved group access requests 
    remote isolated function listGroupAccessRequests(string id, map<string|string[]> headers = {}, *ListGroupAccessRequestsQueries queries) returns AccessRequester[]|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Request access to a group
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function requestGroupAccess(string id, map<string|string[]> headers = {}) returns AccessRequester|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Approve an access request
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + userId - The user ID of the access requester
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to approve an access request 
    # + return - Created 
    remote isolated function approveGroupAccessRequest(string id, int userId, AccessRequestApproval payload, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_requests/${getEncodedUri(userId)}/approve`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Deny an access request
    #
    # + id - The ID or URL-encoded path of the group owned by the authenticated user
    # + userId - The user ID of the access requester
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function denyGroupAccessRequest(string id, int userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/groups/${getEncodedUri(id)}/access_requests/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # List all access requests for a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved project access requests 
    remote isolated function listProjectAccessRequests(string id, map<string|string[]> headers = {}, *ListProjectAccessRequestsQueries queries) returns AccessRequester[]|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Request access to a project
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function requestProjectAccess(string id, map<string|string[]> headers = {}) returns AccessRequester|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_requests`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Approve an access request
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + userId - The user ID of the access requester
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to approve an access request 
    # + return - Created 
    remote isolated function approveProjectAccessRequest(string id, int userId, AccessRequestApproval payload, map<string|string[]> headers = {}) returns Member|error {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_requests/${getEncodedUri(userId)}/approve`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, httpHeaders);
    }

    # Deny an access request
    #
    # + id - The ID or URL-encoded path of the project owned by the authenticated user
    # + userId - The user ID of the access requester
    # + headers - Headers to be sent with the request 
    # + return - Resource deleted 
    remote isolated function denyProjectAccessRequest(string id, int userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/projects/${getEncodedUri(id)}/access_requests/${getEncodedUri(userId)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Retrieve Service Ping payload
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getServicePingPayload(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/service_ping`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Track an internal GitLab event
    #
    # + headers - Headers to be sent with the request 
    # + payload - Request payload to track an internal GitLab event 
    # + return - Success 
    remote isolated function trackInternalGitLabEvent(TrackInternalGitLabEventRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/usage_data/track_event`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Swagger compatible API description
    #
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function getSwaggerDoc(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/swagger_doc`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Swagger compatible API description for specific API
    #
    # + name - Resource name of mounted API
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Success 
    remote isolated function getSwaggerDocByName(string name, map<string|string[]> headers = {}, *GetSwaggerDocByNameQueries queries) returns error? {
        string resourcePath = string `/swagger_doc/${getEncodedUri(name)}`;
        map<anydata> headerValues = {...headers};
        if self.apiKeyConfig is ApiKeysConfig {
            headerValues["Private-Token"] = self.apiKeyConfig?.privateToken;
        }
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headerValues);
        return self.clientEp->get(resourcePath, httpHeaders);
    }
}
